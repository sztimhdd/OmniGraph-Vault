# databricks-deploy/requirements.txt — read-only KB search/article API only.
# kb/api.py at import time uses ONLY fastapi + pydantic + markdown + stdlib.
# kg_synthesize / lightrag are deferred-imported inside synthesize handler;
# the synthesize endpoint will return 500 on first hit unless those are added
# back later. /health, /api/articles, /api/search (fts mode) all work without them.
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
jinja2>=3.1.0
markdown>=3.6
pygments>=2.18.0
# _db_bootstrap.py uses w.files.download() to hydrate KB_DB_PATH from UC
# volume at boot (volumes are not auto-FUSE-mounted in Databricks Apps).
# SDK is pre-installed in the Apps runtime; pinned here defensively.
databricks-sdk>=0.30.0
