---
phase: 19-generic-scraper-schema-kol-hotfix
plan: 03
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - .planning/STATE.md
  - .planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md
autonomous: false
requirements: [SCH-02]
must_haves:
  truths:
    - "Full pytest suite passes with zero regressions vs pre-Phase-19 baseline"
    - "19-DEPLOY.md exists with the 1-line Hermes post-pull operator runbook for SHA-256 migration"
    - ".planning/STATE.md records Phase 19 as complete with the Day-1 unblock note"
    - "User has SSH-verified Hermes KOL path works via site_hint=wechat (manual checkpoint)"
  artifacts:
    - path: ".planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md"
      provides: "Operator runbook for SCH-02 hash migration on Hermes"
      contains: "python scripts/checkpoint_reset.py --all --confirm"
    - path: ".planning/STATE.md"
      provides: "Updated milestone state with phase 19 complete"
      contains: "Phase 19"
  key_links:
    - from: "Full test suite"
      to: "Phase 19 regression gate"
      via: "venv/Scripts/python -m pytest tests/ -x"
      pattern: "pytest tests/"
---

<objective>
Close Phase 19 with three operations:

1. **Regression gate** — run the full `tests/` suite, confirm zero regressions (all 8 Phase-19 tests pass + the ~200 pre-existing tests stay green).
2. **Deploy runbook** — write `19-DEPLOY.md` with the single line Hermes operator command to clean up legacy 10-char MD5 checkpoint directories (Pitfall 2 from 19-RESEARCH.md).
3. **Manual Hermes spot-check** — user SSHes into Hermes (per `memory/hermes_ssh.md`), pulls, runs `checkpoint_reset.py --all --confirm`, installs the new deps, and runs the one-shot dry-run to confirm the KOL path uses `apify` / `cdp` / `mcp` / `ua` (NOT ua-only) via `scrape_url`. Updates STATE.md with verdict.

Purpose: Ship Phase 19 safely — ensure both local automated verification AND remote live verification pass before STATE marks the phase complete.

Output: 1 commit with the deploy runbook + STATE.md update; Hermes post-deploy verification recorded in SUMMARY.

**Note on `autonomous: false`:** This plan contains a manual Hermes SSH checkpoint. The executor runs the automated regression gate + writes the deploy doc autonomously, but MUST pause before STATE.md commit for the user to complete the SSH verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-generic-scraper-schema-kol-hotfix/19-RESEARCH.md
@.planning/phases/19-generic-scraper-schema-kol-hotfix/19-VALIDATION.md
@.planning/phases/19-generic-scraper-schema-kol-hotfix/19-01-SUMMARY.md
@.planning/phases/19-generic-scraper-schema-kol-hotfix/19-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Hermes post-pull operator flow (from 19-RESEARCH.md lines 521-525, Pitfall 2):

    # On Hermes (SSH target per memory/hermes_ssh.md):
    cd ~/OmniGraph-Vault
    git pull --ff-only
    source venv/bin/activate
    pip install -r requirements.txt
    python scripts/checkpoint_reset.py --all --confirm   # <- cleans mixed MD5+SHA256 residue
    python -m pytest tests/ -x                             # <- confirm green
    # Then dry-run a single KOL article to confirm scrape_url is wired:
    python batch_ingest_from_spider.py --from-db --topic-filter AI --min-depth 2 --max-articles 1 --dry-run

Expected method in scrape log: `apify` or `cdp` or `mcp` (NOT `ua` — UA is the last fallback).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Run full regression suite — confirm zero regressions</name>
  <files></files>
  <read_first>
    - .planning/phases/19-generic-scraper-schema-kol-hotfix/19-01-SUMMARY.md
    - .planning/phases/19-generic-scraper-schema-kol-hotfix/19-02-SUMMARY.md
    - .planning/phases/19-generic-scraper-schema-kol-hotfix/19-VALIDATION.md  (full suite command)
  </read_first>
  <action>
    Run the full test suite exactly as specified in 19-VALIDATION.md `Full suite command`:

        venv/Scripts/python -m pytest tests/ -x

    Capture the pass/fail count from the final pytest summary line. Expected form: `NNN passed in MMs` (zero failures, zero errors).

    If ANY test fails, STOP the plan. Do NOT proceed to Task 3.2 or Task 3.3. Report the failing test + stack trace and escalate to the user. This is the hard gate: Phase 19 does not ship with any regression.

    If all pass, record the pass count for the SUMMARY.md.

    Also run the targeted Phase-19 subset to print a cleaner mapping to requirements:

        venv/Scripts/python -m pytest tests/unit/test_scraper.py tests/unit/test_batch_ingest_hash.py tests/unit/test_rss_schema_migration.py -v

    Expected: 8 passed (5 scraper + 2 hash + 1 schema).

    Addresses: full regression gate (SCR-01..07, SCH-01, SCH-02 all present in their respective tests).
  </action>
  <verify>
    <automated>venv/Scripts/python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `venv/Scripts/python -m pytest tests/ -x -q` exits 0 (no failing tests)
    - The pytest summary line shows zero failures
    - `venv/Scripts/python -m pytest tests/unit/test_scraper.py tests/unit/test_batch_ingest_hash.py tests/unit/test_rss_schema_migration.py -q` reports exactly 8 tests, 8 passed
    - Total test count is GREATER THAN OR EQUAL to the pre-Phase-19 baseline + 8 new tests (no tests were deleted)
  </acceptance_criteria>
  <done>
    Full regression suite green; 8 new Phase-19 tests all passing; test count has grown by exactly 8.
  </done>
</task>

<task type="auto">
  <name>Task 3.2: Write 19-DEPLOY.md operator runbook</name>
  <files>.planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md</files>
  <read_first>
    - .planning/phases/19-generic-scraper-schema-kol-hotfix/19-RESEARCH.md  (Pitfall 2 lines 521-525)
    - CLAUDE.md  (Remote Hermes Deployment section — how to reach the box)
    - scripts/checkpoint_reset.py  (confirm --all --confirm flags work; do NOT modify, read-only verification)
  </read_first>
  <action>
    Create `.planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md` with the following content verbatim:

        # Phase 19 Deploy Runbook — Hermes Operator Steps

        **When to run:** After Phase 19 PR is merged to `main` and the commit is pushed. Before the next cron cycle executes `batch_ingest_from_spider.py`.

        **Why this is needed:** SCH-02 migrates the article-hash function from MD5 first-10-hex to SHA-256 first-16-hex. Existing `checkpoints/<10-char-MD5>` directories on Hermes will be orphaned (`checkpoint_status.py` will still list them, but new ingest runs will not find them by the new hash). The safest cleanup is to wipe the entire checkpoints/ directory — WeChat re-scrape of articles is cheap, and the checkpoint cache is a performance optimization, not a data source.

        ## One-time post-deploy steps on Hermes

        SSH target: see `~/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/hermes_ssh.md`

        Run these commands in order on the Hermes box:

        ```bash
        cd ~/OmniGraph-Vault
        git pull --ff-only

        # 1. Install the new trafilatura + lxml dependencies
        source venv/bin/activate
        pip install -r requirements.txt

        # 2. Verify installs
        python -c "import trafilatura; print(trafilatura.__version__)"
        # Expected: a 2.x version string

        # 3. Wipe legacy MD5-10 checkpoint dirs (SCH-02 migration)
        python scripts/checkpoint_reset.py --all --confirm

        # 4. Run the full test suite against the production venv
        python -m pytest tests/ -x
        # Expected: all tests pass, 0 failed

        # 5. Smoke-test the KOL path: 1 article dry-run, --min-depth 2 to minimize work
        python batch_ingest_from_spider.py --from-db --topic-filter AI --min-depth 2 --max-articles 1 --dry-run
        ```

        ## Verifying the SCR-06 hotfix landed

        When step 5 above runs a real (non-dry-run) ingest, the scrape log line MUST show one of:

        - `method: apify` — primary path worked
        - `method: cdp` — local CDP fallback worked
        - `method: mcp` — remote MCP fallback worked
        - `method: ua` — last-resort fallback worked

        It MUST NOT be the case that the pipeline fails with a UA-only error and never tries apify / cdp / mcp. That was the Day-1 06:00 ADT regression the SCR-06 hotfix closes.

        If the log shows `method: ua` for more than ~20% of articles in a normal batch, that means APIFY_TOKEN / CDP_URL are misconfigured (environment issue, not code issue). Check `~/.hermes/.env`.

        ## Checkpoint directory sanity check

        After step 3 (checkpoint_reset), verify no legacy dirs remain:

        ```bash
        ls ~/.hermes/omonigraph-vault/checkpoints/ 2>/dev/null | awk '{print length($0)}' | sort -u
        ```

        Expected: either no output (directory empty or absent) or only `16`. If `10` appears in the output, re-run step 3.

        ## Rollback

        If Phase 19 must be reverted:

        ```bash
        cd ~/OmniGraph-Vault
        git revert <phase-19-merge-commit>
        pip install -r requirements.txt   # reverts to pre-trafilatura deps
        python scripts/checkpoint_reset.py --all --confirm   # wipe both old + new hash dirs
        ```

        No data loss — checkpoints are a performance cache, not a source of truth.

        ## Phase 19 success criteria (operator checklist)

        - [ ] `git pull` brought in Phase 19 commits
        - [ ] `pip install -r requirements.txt` exits 0
        - [ ] `python -c "import trafilatura"` exits 0 and prints 2.x version
        - [ ] `python scripts/checkpoint_reset.py --all --confirm` exits 0
        - [ ] `ls ~/.hermes/omonigraph-vault/checkpoints/` shows only 16-char names (or empty)
        - [ ] `python -m pytest tests/ -x` exits 0
        - [ ] Dry-run of 1 KOL article in step 5 parses without crash
        - [ ] Next scheduled cron run's log shows `method: apify` / `cdp` / `mcp` (not pure `ua`) for at least the first 3 articles

        ---

        *Written 2026-05-03 as part of Phase 19 plan 19-03.*

    Addresses: deploy documentation for SCH-02 post-merge migration + SCR-06 smoke verification.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md && grep -c "checkpoint_reset.py --all --confirm" .planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md` → exists
    - `grep -c "checkpoint_reset.py --all --confirm" 19-DEPLOY.md` → 2 or more occurrences (main step + sanity check / rollback)
    - `grep -c "method: apify\|method: cdp\|method: mcp\|method: ua" 19-DEPLOY.md` → 4 or more lines
    - `grep "pip install -r requirements.txt" .planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md` → match
    - `grep "trafilatura" .planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md` → match
    - File contains Phase 19 success criteria checklist with at least 6 checkboxes
  </acceptance_criteria>
  <done>
    `19-DEPLOY.md` file exists with the complete Hermes post-pull operator runbook. Operator can execute steps 1-5 without needing to cross-reference other docs.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3.3: USER — SSH into Hermes, run the 19-DEPLOY.md post-pull steps, report verdict</name>
  <files>(remote) ~/OmniGraph-Vault on Hermes — no local files modified by this task</files>
  <action>User manually SSHes into Hermes per memory/hermes_ssh.md and runs the steps listed in &lt;how-to-verify&gt; below. Executor cannot automate this — the Hermes box is on a separate network reachable only via SSH, and live KOL ingest verification requires Apify / CDP that are unreachable from the dev box. Executor's job: pause execution, surface the &lt;how-to-verify&gt; checklist to the user, wait for the user's verdict (&quot;approved&quot; or &quot;issues: ...&quot;), and record the verdict in the SUMMARY before proceeding to Task 3.4.</action>
  <verify>
    <automated>echo "Manual SSH verification required — see how-to-verify block"</automated>
  </verify>
  <what-built>
    - `lib/scraper.py` with 4-layer WeChat cascade + generic trafilatura cascade + 429 backoff + login-wall quality gate
    - `batch_ingest_from_spider.py:275` hash unified to `get_article_hash(url)` (SHA-256 first 16)
    - `batch_ingest_from_spider.py:940` KOL classify now routes through `scrape_url(url, site_hint="wechat")` — closes Day-1 06:00 ADT regression
    - `enrichment/rss_schema.py` gained `_ensure_rss_columns` adding 5 nullable columns to `rss_articles`
    - 8 new passing unit tests (mock-only, no live HTTPS)
  </what-built>
  <how-to-verify>
    1. Merge Phase 19 to `main` (or confirm the direct-push commit landed per project YOLO convention).

    2. SSH into Hermes using the connection details in `~/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/hermes_ssh.md`.

    3. Follow `.planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md` exactly:
       - `cd ~/OmniGraph-Vault && git pull --ff-only`
       - `source venv/bin/activate`
       - `pip install -r requirements.txt`
       - `python -c "import trafilatura; print(trafilatura.__version__)"` (expect 2.x)
       - `python scripts/checkpoint_reset.py --all --confirm`
       - `python -m pytest tests/ -x` (expect all passing, including the 8 new Phase-19 tests)
       - `python batch_ingest_from_spider.py --from-db --topic-filter AI --min-depth 2 --max-articles 1 --dry-run` (expect parse without crash)

    4. After the 06:00 ADT cron fires (or manually trigger a 1-article non-dry-run if the cron hasn't fired yet), check the scrape log: the `method:` line MUST show `apify`, `cdp`, `mcp`, or `ua` — and ideally NOT `ua` for the first 3 articles (unless APIFY_TOKEN / CDP_URL are misconfigured, which is an environment issue not a code issue).

    5. Report back with one of:
       - **approved** — all 6 steps pass, SCR-06 hotfix verified live
       - **issues: <description>** — any step failed; describe the failure so we can open a follow-up
  </how-to-verify>
  <resume-signal>Type `approved` or `issues: <description>`</resume-signal>
  <done>
    User has reported verdict. If approved, proceed to Task 3.4 (STATE.md update). If issues, create a follow-up `/gsd:quick` or revision plan and DO NOT update STATE.md to "complete" yet.
  </done>
</task>

<task type="auto">
  <name>Task 3.4: Update .planning/STATE.md — mark Phase 19 complete, record v3.4 progress</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md  (current frontmatter + "Current Position" section)
    - .planning/phases/19-generic-scraper-schema-kol-hotfix/19-00-SUMMARY.md (if exists)
    - .planning/phases/19-generic-scraper-schema-kol-hotfix/19-01-SUMMARY.md
    - .planning/phases/19-generic-scraper-schema-kol-hotfix/19-02-SUMMARY.md
  </read_first>
  <action>
    Update `.planning/STATE.md` in-place with THREE minimal edits. Preserve everything else — this is a surgical update (CLAUDE.md Principle 3).

    **Edit 1 — Frontmatter fields:**

    Change:
      - `current_plan: 19` → stays as `19`
      - `status: planning-complete` → `status: phase-complete`
      - `stopped_at: Roadmap created — phases 19-22 defined; execute BLOCKED until Day-1/2/3 KOL baseline complete (~2026-05-06 ADT)` → `stopped_at: Phase 19 shipped — lib/scraper.py + KOL line-940 hotfix + SHA-256 hash + rss_articles ALTER. Hermes operator runbook in 19-DEPLOY.md.`
    - `last_updated: "2026-05-03T20:30:00.000Z"` → update to the current ISO timestamp at time of execution
    - `last_activity: 2026-05-03 — Milestone v3.4 roadmap created (phases 19-22, 31 REQs mapped)` → `last_activity: <YYYY-MM-DD> — Phase 19 complete: scraper + hotfix + schema + hash unified; 8/8 new unit tests GREEN; Hermes operator verified`
    - `progress.completed_phases: 0` → `1`
    - `progress.total_plans: 0` → `4`
    - `progress.completed_plans: 0` → `4`

    **Edit 2 — "Current Position" section (around lines 27-43):**

    Update the "Phase:" line:
      - `Phase: 19 (Generic Scraper + Schema + KOL Hotfix) — planned, not started`
        becomes
      - `Phase: 20 (RSS Full-Body Classify + Multimodal Ingest + Cognee Fix) — NEXT; Phase 19 complete`

    Update the "Plan:" line:
      - `Plan: — (no plans written yet; use /gsd:plan-phase 19 to start)`
        becomes
      - `Plan: — (Phase 19 shipped 4 plans; next is /gsd:plan-phase 20)`

    Update the "Status:" line:
      - `Status: Planning complete — execute BLOCKED`
        becomes
      - `Status: Phase 19 shipped. Phase 20 execute BLOCKED until Day-1/2/3 KOL baseline complete (~2026-05-06 ADT)`

    Update the "Last activity:" line with current date + Phase 19 summary.

    Update "Immediate next step" block to:

        Wait for Day-1/2/3 KOL baseline window to complete (~2026-05-06 ADT).
        After baseline confirmed stable, resume with `/gsd:plan-phase 20`.

    **Edit 3 — Add a new bullet under "Accumulated Context / Decisions" (around line 156):**

    Insert after the `- **[v3.4] D-RSS-SCRAPER-SCOPE = Option A**` line:

        - **[Phase 19 complete, 2026-05-DD]** lib/scraper.py shipped with 4-layer WeChat cascade (apify → cdp → mcp → ua) + generic trafilatura cascade + 429 backoff + login-wall gate. batch_ingest_from_spider.py:940 SCR-06 hotfix landed (scrape_url, site_hint="wechat"). batch_ingest_from_spider.py:275 hash unified to get_article_hash (SHA-256 first 16). rss_articles ALTER added 5 nullable columns for Phase 20. 8 new unit tests GREEN. Hermes post-pull: `checkpoint_reset.py --all --confirm` wipes legacy MD5-10 dirs (see 19-DEPLOY.md).

    Replace `<YYYY-MM-DD>` / `<DD>` with the current UTC date in YYYY-MM-DD format at execution time.

    Do NOT remove or reorder other STATE.md sections — this is an additive update.

    Addresses: STATE.md as project ground truth; records Phase 19 closure so subsequent sessions resume from the correct position.
  </action>
  <verify>
    <automated>grep -c "Phase 19 complete\|Phase 19 shipped" .planning/STATE.md && grep -c "completed_phases: 1" .planning/STATE.md && grep -c "Phase 20" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "status: phase-complete" .planning/STATE.md` → match (frontmatter status updated)
    - `grep "completed_phases: 1" .planning/STATE.md` → match
    - `grep "completed_plans: 4" .planning/STATE.md` → match
    - `grep "total_plans: 4" .planning/STATE.md` → match
    - `grep "Phase 19 complete\|Phase 19 shipped" .planning/STATE.md` → at least 2 matches
    - `grep "Phase 20" .planning/STATE.md` → at least 1 match (next phase reference)
    - `grep "/gsd:plan-phase 20" .planning/STATE.md` → match
    - `grep "lib/scraper.py shipped" .planning/STATE.md` → match (the new Decisions bullet)
    - `grep "checkpoint_reset.py --all --confirm" .planning/STATE.md` → match (in new bullet)
    - No pre-existing sections of STATE.md are deleted — verify by line-count check: wc -l of new STATE.md should be GREATER THAN original minus 10 (i.e., we added more than we removed)
    - `head -15 .planning/STATE.md` still shows valid YAML frontmatter (opens and closes with `---`)
  </acceptance_criteria>
  <done>
    STATE.md reflects Phase 19 closure; downstream session can resume from `/gsd:plan-phase 20` without manual archaeology.
  </done>
</task>

</tasks>

<verification>
Phase 19 close-out verification (all must pass):

Automated:

    venv/Scripts/python -m pytest tests/ -x -q

Expected: all tests pass.

Manual (Task 3.3 output):

    User SSH-verified on Hermes:
    - pip install of trafilatura/lxml OK
    - checkpoint_reset wiped legacy MD5 dirs (16-char names only remaining)
    - pytest green on Hermes
    - batch_ingest dry-run parses
    - Live cron shows method: apify / cdp / mcp (NOT pure ua) for first 3 articles

File checks:

    ls lib/scraper.py
    ls .planning/phases/19-generic-scraper-schema-kol-hotfix/19-DEPLOY.md
    grep "status: phase-complete" .planning/STATE.md
</verification>

<success_criteria>
- [ ] Full pytest suite green (~200+ tests, 0 failures)
- [ ] 19-DEPLOY.md exists with Hermes post-pull runbook
- [ ] User has SSH-confirmed Hermes works (Task 3.3 approved)
- [ ] STATE.md frontmatter shows `status: phase-complete`, `completed_phases: 1`, `completed_plans: 4`
- [ ] STATE.md Current Position points to Phase 20 as next
- [ ] New Decisions bullet records Phase 19 closure summary
- [ ] All changes committed per project YOLO flow (`--no-verify` allowed; push origin/main)
</success_criteria>

<output>
After completion, create `.planning/phases/19-generic-scraper-schema-kol-hotfix/19-03-SUMMARY.md` with:
- Full test suite result (count passed, count of new tests, any flaky or skipped)
- Hermes verification verdict (approved / issues + description)
- Path to `19-DEPLOY.md` for any future operator
- STATE.md diff summary (which frontmatter fields changed, what new bullet was added)
- Phase 19 closure verdict: complete / conditional (with follow-up list)

Also ensure the phase-level close-out work commits cleanly to origin/main per project convention (`--no-verify`).
</output>
