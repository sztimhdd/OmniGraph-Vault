---
quick_id: 260504-x9l
description: 本地 5-article batch ingest pilot — new LightRAG config cold-graph throughput
mode: quick
created: 2026-05-05
status: planned
---

# Quick Task 260504-x9l — Local 5-Article Pilot

## Goal

One clean multi-article cold-graph run on `.dev-runtime/` with the new 5-knob LightRAG config (commit `e833206`). Capture per-article timing + graph growth so the new config has a multi-article data point to be evaluated against. Single article cold-run (`b47c29c`) was structurally inconclusive because none of the 5 knobs activate at N=1.

## Hard Constraints (HARD NOs)

- No code changes (`ingest_wechat.py`, `lib/lightrag_embedding.py`, `batch_ingest_from_spider.py` — read-only)
- No SSH to Hermes (Hermes is running production 10-article batch in parallel)
- No `git pull` (local b47c29c == origin + spec/cold-run docs already merged via f42d78a)
- No more than 5 articles
- No clearing `.dev-runtime/data/` (DB has 563 articles + 209 ingestions rows + history)
- No commit of `.dev-runtime/` content (gitignored) or log file
- No retry on failure — write ABORTED report instead
- No "X× faster / config works / config broken" claims — numbers only, interpretation is user's

## Tasks

### Task 1: Pre-flight verification + reset

**Files (read-only):** `ingest_wechat.py`, `.dev-runtime/.env`, `.dev-runtime/data/kol_scan.db`

**Action:**
1. Confirm HEAD is `b47c29c` or later
2. Confirm `ingest_wechat.py:218-222` has new 5-knob config (already verified: async=4, batch=64, llm=4, parallel_insert=3, insert_batch_size=100)
3. Reset `.dev-runtime/lightrag_storage/*`, `checkpoints/*`, `images/*` (do NOT touch `data/`)
4. Run `scripts/local_dev_start.ps1` for env bootstrap + image server (port 8765)

**Verify:** `.dev-runtime/lightrag_storage/` is empty; image server responds on `localhost:8765`; DB row count unchanged.

**Done:** Empty graph, env loaded, image server running.

### Task 2: 5-article ingest with timer + log capture

**Action:**
```powershell
$start = Get-Date
.venv\Scripts\python .dev-runtime\run_local.py batch_ingest_from_spider.py `
  --from-db --topic-filter openclaw,hermes,agent,harness `
  --min-depth 2 --max-articles 5 `
  2>&1 | Tee-Object -FilePath .dev-runtime\logs\pilot-5art-<timestamp>.log
$elapsed = (Get-Date) - $start
```

**Verify:** Process exits cleanly, log file exists, total wall-clock recorded.

**Done:** Log captured, exit code recorded, total elapsed seconds known.

**Failure handling:** If single article > 30 min OR total > 2h hard cap, Ctrl+C, write partial-data report. Do NOT retry.

### Task 3: Metric extraction

**Action:**
1. Parse log for per-article: article_id, body length, start/end timestamp, status (ok/skip/fail)
2. Grep for: `pre-scrape skip`, `429`, `API error`, `RuntimeError`
3. After completion, snapshot `.dev-runtime/lightrag_storage/`:
   - `nx.read_graphml('graph_chunk_entity_relation.graphml')` → nodes / edges
   - `os.stat()` for `vdb_entities.json`, `vdb_relationships.json`, `graphml`
4. Per-article file size growth: log shows `vdb size after each article` if available; otherwise capture only end-state
5. Read internal metric `data/batch_timeout_metrics_<timestamp>.json` for cross-check

**Verify:** All 9 required deliverable numbers present (per task brief).

**Done:** Numbers in hand, ready to write report.

### Task 4: Write benchmark report

**File (new):** `docs/benchmarks/local_5article_pilot_2026-05-04.md`

**Sections:**
- Result Summary (total wall-clock, ok/skip/fail, graph terminus, file sizes)
- Per-Article Timing table (5 rows: #, article_id, wall-clock, nodes after, edges after, status)
- Per-Article Trend (1-2 sentences: does article-N wall-clock rise with N at this scale?)
- Anomalies (pre-scrape hits, 429 / errors)
- Comparison Anchors (g7a/lt2 single-article numbers; this pilot's article 1)
- Raw log path

**Verify:** File ≤1.5 pages; no rollback recommendation; no "X× faster" claims; numbers presented neutrally.

**Done:** Report written.

### Task 5: SUMMARY.md + STATE.md + atomic commit

**Files:** `.planning/quick/260504-x9l-.../260504-x9l-SUMMARY.md`, `.planning/STATE.md`, `docs/benchmarks/local_5article_pilot_2026-05-04.md`

**Action:**
1. Write SUMMARY.md (status, key findings, files committed, hard-NOs respected)
2. Append row to STATE.md "Quick Tasks Completed" table
3. Single atomic commit:
   ```
   docs(benchmarks): local 5-article pilot — new config cold-graph throughput
   ```
4. Do NOT push (user push policy)

**Verify:** `git status` clean for new files; commit hash short-form recorded.

**Done:** Quick task complete, single commit on main.

## Iteration Budget

1 attempt. Completion or clean abort. No retry.

## Time Budget

~2h total (1.5h ingest + 30 min setup/parse/report).
