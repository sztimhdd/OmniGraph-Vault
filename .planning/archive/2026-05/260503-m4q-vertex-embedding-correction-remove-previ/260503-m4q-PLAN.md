---
phase: quick-260503-m4q
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lightrag_embedding.py
  - cognee_wrapper.py
  - tests/unit/test_lightrag_embedding_vertex.py
  - tests/unit/test_cognee_vertex_model_name.py
  - scripts/vertex_live_probe.py
  - .planning/phases/05-pipeline-automation/05-00-SUMMARY.md
  - CLAUDE.md
autonomous: true
requirements:
  - VERTEX-FIX-01  # Delete incorrect -preview alias; gemini-embedding-2 is GA on global
  - VERTEX-FIX-02  # Upgrade vertex_live_probe.py to 2x3 endpoint x model matrix
  - VERTEX-FIX-03  # Rewrite 05-00 SUMMARY § C narrative + CLAUDE.md endpoint clarification

must_haves:
  truths:
    - "Production Hermes (GOOGLE_CLOUD_LOCATION=global) embeds succeed against gemini-embedding-2"
    - "No residual reference to _VERTEX_EMBEDDING_ALIAS, _resolve_model, or gemini-embedding-2-preview anywhere outside scripts/vertex_live_probe.py"
    - "9 existing Vertex unit tests (7 + 2) all pass on Windows dev with mock-only HTTP"
    - "vertex_live_probe.py probes 6 combinations (3 models x 2 locations) and only alerts on known-good-combo 404s"
    - "05-00-SUMMARY § C reflects root cause (endpoint x model mismatch), not 'Preview lifecycle 3 flips' narrative"
    - "CLAUDE.md Vertex section documents GOOGLE_CLOUD_LOCATION=global as production-recommended + endpoint-dependent model naming"
  artifacts:
    - path: "lib/lightrag_embedding.py"
      provides: "Vertex embedding path with pass-through model name (no alias layer)"
      contains: "no _VERTEX_EMBEDDING_ALIAS, no _resolve_model"
    - path: "cognee_wrapper.py"
      provides: "Cognee env setup without alias residuals"
      contains: "no _resolve_model import, no os.environ['EMBEDDING_MODEL'] mutation gated on Vertex mode"
    - path: "tests/unit/test_lightrag_embedding_vertex.py"
      provides: "7 tests asserting GA model name gemini-embedding-2 in Vertex mode"
    - path: "tests/unit/test_cognee_vertex_model_name.py"
      provides: "2 tests asserting GA model name preserved via Cognee path"
    - path: "scripts/vertex_live_probe.py"
      provides: "2x3 endpoint x model matrix probe with smart alert logic"
      contains: "known_good dict, loops over (global, us-central1) x 3 models"
    - path: ".planning/phases/05-pipeline-automation/05-00-SUMMARY.md"
      provides: "§ C rewritten to endpoint x model mismatch narrative"
    - path: "CLAUDE.md"
      provides: "Vertex section clarified: global endpoint + endpoint-dependent model naming"
  key_links:
    - from: "lib/lightrag_embedding._embed_once"
      to: "client.aio.models.embed_content(model=<unresolved>)"
      via: "direct pass-through (no _resolve_model call)"
      pattern: "model=model"
    - from: "cognee_wrapper (EMBEDDING_MODEL env)"
      to: "gemini-embedding-2 (literal)"
      via: "constant string, no alias function"
      pattern: "EMBEDDING_MODEL.*gemini-embedding-2"
    - from: "scripts/vertex_live_probe.py"
      to: "Telegram alert"
      via: "known_good[(loc, model)] is True AND result is 404"
      pattern: "known_good"
---

<objective>
Correct the 2026-04-30 → 2026-05-03 misdiagnosis: delete the incorrect `-preview` alias and embrace GA `gemini-embedding-2` on the `global` Vertex endpoint. The "3 flips in 4 days" observation was an illusion caused by probing `us-central1` while production ran `global`. Tonight's E2E "LightRAG timeout on RSS" was actually embedding 404 → LightRAG retry to deadline.

**Time window:** Must push to origin/main before 2026-05-04 06:00 ADT (Day-1 cron fire). Day-1 cron will hit the same bug if unfixed → digest likely zero new ingests.

Purpose: Make production Hermes (GOOGLE_CLOUD_LOCATION=global) embed path work end-to-end, eliminate the 2-machine divergence illusion between Windows dev and remote Hermes.

Output: 3 atomic commits on origin/main; 9/9 Vertex unit tests green; probe upgraded to 6-combo matrix; 05-00 SUMMARY + CLAUDE.md narrative corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@lib/lightrag_embedding.py
@cognee_wrapper.py
@tests/unit/test_lightrag_embedding_vertex.py
@tests/unit/test_cognee_vertex_model_name.py
@scripts/vertex_live_probe.py
@.planning/phases/05-pipeline-automation/05-00-SUMMARY.md

<interfaces>
<!-- Relevant contracts the executor needs. Extracted from files above. -->

Current (to be REMOVED) in lib/lightrag_embedding.py lines 186-218:
```python
_VERTEX_EMBEDDING_ALIAS = {
    "gemini-embedding-2": "gemini-embedding-2-preview",
}

def _resolve_model(base_model: str) -> str:
    if _is_vertex_mode():
        return _VERTEX_EMBEDDING_ALIAS.get(base_model, base_model)
    return base_model
```

Current (to be SIMPLIFIED) in lib/lightrag_embedding._embed_once, line 241:
```python
    resolved_model = _resolve_model(model)
    response = await client.aio.models.embed_content(
        model=resolved_model,
        ...
    )
```

Must become (pass `model` directly, drop `resolved_model` entirely):
```python
    response = await client.aio.models.embed_content(
        model=model,
        ...
    )
```

Current (to be REMOVED) in cognee_wrapper.py line 43:
```python
from lib.lightrag_embedding import _resolve_model
```

Current (to be REPLACED with literal) in cognee_wrapper.py line 55:
```python
os.environ["EMBEDDING_MODEL"] = _resolve_model("gemini-embedding-2")
```
Must become:
```python
os.environ["EMBEDDING_MODEL"] = "gemini-embedding-2"
```

MUST NOT TOUCH:
- `_is_vertex_mode()` function (call-time evaluation preserved — required by test_is_vertex_mode_evaluated_at_call_time)
- `_make_client()` function
- `lib/models.py` EMBEDDING_MODEL constant (already correct as "gemini-embedding-2")
- `batch_ingest_from_spider.py`, `orchestrate_daily.py`, `rss_ingest.py` (out of scope — separate quick task)
- `memory/*.md` (main session updates `vertex_ai_smoke_validated.md` post-verification)
- STATE.md / ROADMAP.md / VALIDATION.md (hygiene-only task, no phase advance)
</interfaces>

<user_decisions_locked>
**Verbatim from user spec — NON-NEGOTIABLE:**

1. **Truth (correction):** gemini-embedding-2 went GA on 2026-04-22 on the `global` endpoint. gemini-embedding-2-preview is regional-only (us-central1). Production Hermes .env has `GOOGLE_CLOUD_LOCATION=global` → preview alias causes every embed to 404.

2. **Scope IN:** lib/lightrag_embedding.py, cognee_wrapper.py, tests/unit/test_lightrag_embedding_vertex.py, tests/unit/test_cognee_vertex_model_name.py, scripts/vertex_live_probe.py, .planning/phases/05-pipeline-automation/05-00-SUMMARY.md § C, CLAUDE.md Vertex section.

3. **Scope OUT:** lib/models.py, _is_vertex_mode(), _make_client(), batch/orchestrate/rss scripts, STATE/ROADMAP/VALIDATION, memory/*.md.

4. **Non-negotiables:**
   - All HTTP is mock-only (Cisco Umbrella blocks real Vertex calls from Windows dev; unit tests MUST NOT hit aiplatform.googleapis.com)
   - 9 existing Vertex unit tests must all pass (7 + 2)
   - _is_vertex_mode() must preserve call-time evaluation behavior
   - scripts/vertex_live_probe.py must remain runnable standalone from command line
   - Keep timestamps 2026-04-30 / 05-02 / 05-03 in 05-00-SUMMARY § C (just change the narrative)

5. **Commit discipline — 3 atomic commits, each pushed with `--no-verify` to origin/main:**
   - Commit 1: `fix(embedding): remove incorrect preview alias; gemini-embedding-2 is GA on global endpoint` (Fix 1 + 2 + 3)
   - Commit 2: `feat(probe): upgrade vertex_live_probe.py to 2×3 endpoint × model matrix` (Fix 4)
   - Commit 3: `docs: rewrite 05-00 § C Vertex narrative — root cause was endpoint × model mismatch` (Fix 5 + 6)
</user_decisions_locked>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove alias layer (code + tests) — Commit 1</name>
  <files>
    lib/lightrag_embedding.py,
    cognee_wrapper.py,
    tests/unit/test_lightrag_embedding_vertex.py,
    tests/unit/test_cognee_vertex_model_name.py
  </files>
  <behavior>
    Tests must flip to assert the GA name `gemini-embedding-2` (not `-preview`) in Vertex mode, then remain green:

    - test_free_tier_path_default: `ekw.get("model") == "gemini-embedding-2"` (unchanged — already correct)
    - test_vertex_mode_both_env_vars_set: `ekw.get("model") == "gemini-embedding-2"` (was `-preview`, flip back)
    - test_vertex_mode_custom_location: unchanged (assertion is on location, not model)
    - test_only_credentials_set_falls_back: `ekw.get("model") == "gemini-embedding-2"` (unchanged)
    - test_only_project_set_falls_back: `ekw.get("model") == "gemini-embedding-2"` (unchanged)
    - test_is_vertex_mode_evaluated_at_call_time: Vertex-mode call asserts `"gemini-embedding-2"` (not `-preview`); call-time re-evaluation behavior preserved
    - test_is_vertex_mode_helper_truth_table: unchanged (truth-table on env vars, not model)
    - test_vertex_mode_maps_to_preview → renamed to test_vertex_mode_preserves_ga_model_name; assert `got == "gemini-embedding-2"`
    - test_free_tier_path_preserves_base_model_name: unchanged (already `gemini-embedding-2`)

    After code edits, all 9 tests pass with mock-only HTTP (no real Vertex calls).
  </behavior>
  <action>
    **Fix 1 — `lib/lightrag_embedding.py`:**
    - Delete `_VERTEX_EMBEDDING_ALIAS` dict (lines 186-188).
    - Delete `_resolve_model(base_model: str)` function entirely (lines 191-218).
    - In `_embed_once` (around line 241): remove `resolved_model = _resolve_model(model)`; change `embed_content(model=resolved_model, ...)` to `embed_content(model=model, ...)`.
    - Rewrite the file-top docstring (currently covers "3 flips in 4 days" / "Preview lifecycle") to approximately 10 lines maximum. Use this exact content block (verbatim from user spec):
      ```
      Model routing (2026-05-03 correction): gemini-embedding-2 is GA as of
      2026-04-22 on the `global` endpoint. Use the unsuffixed name as-is;
      no alias layer. gemini-embedding-2-preview is regional-only
      (us-central1 etc.) and does not exist on the global endpoint; do not
      rely on it for production. See 05-00-SUMMARY.md § C for the full
      story of the endpoint × model naming confusion that preceded this fix.
      ```
    - DO NOT touch `_is_vertex_mode()` or `_make_client()`.
    - DO NOT touch `lib/models.py` EMBEDDING_MODEL constant (already correct).

    **Fix 2 — `cognee_wrapper.py`:**
    - Line 43: delete `from lib.lightrag_embedding import _resolve_model`.
    - Line 55: change `os.environ["EMBEDDING_MODEL"] = _resolve_model("gemini-embedding-2")` to `os.environ["EMBEDDING_MODEL"] = "gemini-embedding-2"`.
    - Also remove the now-stale comment block (lines 38-42) that explains "must be rewritten to its -preview variant" — replace with a single brief line: `# EMBEDDING_MODEL is GA on global endpoint (2026-04-22); no alias needed.`
    - Run grep across `lib/` and `cognee_wrapper.py` to confirm no other `_VERTEX_EMBEDDING_ALIAS | _resolve_model | gemini-embedding-2-preview` residuals remain: `grep -rn "_VERTEX_EMBEDDING_ALIAS\|_resolve_model\|gemini-embedding-2-preview" lib/ cognee_wrapper.py`. Expected: zero matches.

    **Fix 3 — `tests/unit/test_lightrag_embedding_vertex.py`:**
    - Rewrite file-top docstring "Model-name history" section to 1-2 lines that point at 05-00-SUMMARY § C (suggested: `Model-name handling: Vertex uses GA 'gemini-embedding-2' on the 'global' endpoint (2026-04-22 GA). See .planning/phases/05-pipeline-automation/05-00-SUMMARY.md § C for the 2026-04-30 → 05-03 correction history.`).
    - `test_vertex_mode_both_env_vars_set` (line ~156): change assertion to `assert ekw.get("model") == "gemini-embedding-2"`. Strip `-preview rollback` narrative from surrounding comments — keep only a brief explanation that GA `gemini-embedding-2` is expected.
    - `test_is_vertex_mode_evaluated_at_call_time` (line ~259): change Vertex-mode assertion to `assert captured["embed_kwargs"][-1].get("model") == "gemini-embedding-2"`. Update surrounding comment to say `Vertex mode: GA model name is unsuffixed.` The post-unset (free-tier) assertion for `gemini-embedding-2` already matches; leave it.
    - Do NOT modify the other 5 tests (all already assert `gemini-embedding-2` or are orthogonal to model name).

    **Fix 3 — `tests/unit/test_cognee_vertex_model_name.py`:**
    - Rewrite lines 1-26 docstring (currently "flip 3 次" narrative) to ~5 lines: describe that Cognee's LiteLLM → Vertex AI chain is independent of `lib.lightrag_embedding`, and that in Vertex mode on the `global` endpoint the canonical name is GA `gemini-embedding-2`. Reference 05-00-SUMMARY § C.
    - Rename test `test_vertex_mode_maps_to_preview` → `test_vertex_mode_preserves_ga_model_name`.
    - Inside that test, change assertion (line ~99) to `assert got == "gemini-embedding-2"`.
    - Rewrite the test's docstring to describe the GA behavior (no alias; model name passes through unchanged).
    - `test_free_tier_path_preserves_base_model_name` is unchanged.

    **Implementation order (TDD-lite):**
    1. First, flip test assertions (tests/unit/test_lightrag_embedding_vertex.py + test_cognee_vertex_model_name.py).
    2. Run pytest — expect failures (RED) because code still has alias.
    3. Apply Fix 1 + Fix 2 to code.
    4. Run pytest — expect all 9 green (GREEN).
    5. Run grep to confirm zero residuals.

    **Commit 1:** After all 9 tests green + grep clean, commit with exact message:
    `fix(embedding): remove incorrect preview alias; gemini-embedding-2 is GA on global endpoint`
    and push with `git push --no-verify origin main`.
  </action>
  <verify>
    <automated>venv/Scripts/python -m pytest tests/unit/test_lightrag_embedding_vertex.py tests/unit/test_cognee_vertex_model_name.py -v</automated>
    Additional shell check (no failures expected):
    ```bash
    grep -rn "gemini-embedding-2-preview\|_VERTEX_EMBEDDING_ALIAS\|_resolve_model" lib/ cognee_wrapper.py tests/unit/ | grep -v ".pyc"
    ```
    Expected output: empty (zero matches outside scripts/vertex_live_probe.py — that file is updated in Task 2).
  </verify>
  <done>
    - All 9 Vertex unit tests pass (7 in test_lightrag_embedding_vertex.py + 2 in test_cognee_vertex_model_name.py).
    - `grep -rn "gemini-embedding-2-preview\|_VERTEX_EMBEDDING_ALIAS\|_resolve_model" lib/ cognee_wrapper.py tests/unit/` returns zero matches.
    - `_is_vertex_mode()` signature and call-time evaluation semantics preserved (test_is_vertex_mode_evaluated_at_call_time + test_is_vertex_mode_helper_truth_table both green).
    - Commit pushed to origin/main with message `fix(embedding): remove incorrect preview alias; gemini-embedding-2 is GA on global endpoint`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Upgrade vertex_live_probe.py to 2×3 matrix — Commit 2</name>
  <files>scripts/vertex_live_probe.py</files>
  <action>
    **Fix 4 — Upgrade to 2×3 matrix (6 combinations).**

    Replace the current single-location probe with a matrix that probes 3 models × 2 locations (global, us-central1) = 6 combinations. Known-good/bad expectation table must match the user-provided dict verbatim:

    ```python
    known_good = {
        ("global", "gemini-embedding-2"): True,
        ("global", "gemini-embedding-2-preview"): False,
        ("global", "gemini-embedding-001"): True,
        ("us-central1", "gemini-embedding-2"): False,
        ("us-central1", "gemini-embedding-2-preview"): True,
        ("us-central1", "gemini-embedding-001"): True,
    }
    ```

    Required behavior changes:
    - **Loop:** for loc in ("global", "us-central1"): for model in CANDIDATES: instantiate a fresh `genai.Client(vertexai=True, project=project, location=loc)` and call probe_one.
    - **Output:** each combo prints with its expected vs actual status. Failure lines show the actual error tail (first ~200 chars).
    - **Alert logic (CRITICAL):** Telegram alert ONLY when a `known_good[(loc, model)] == True` combination returns 404 (or any non-OK). Known-bad combos (value `False`) returning 404 are SILENT — that is expected behavior. This is the key design change from the old "alert if ALL 404" logic.
    - **Structured per-combo output:** `✅ dims=3072 (expected OK) / ❌ 404 (expected OK, ALERT) / ➖ 404 (expected 404, ok)` — include expected marker in every line.
    - **JSON mode** (`--json`): emit list of `{loc, model, dims, error, expected_ok, alert}` dicts.
    - **Exit code:** 0 if every known_good combo returned dims > 0; 1 if any known_good combo failed (regardless of known-bad behavior).
    - **Docstring update:** rewrite the module docstring to describe the 2×3 matrix, list all 6 combinations with their expected results, and note "~6 paid calls per run (still cheap)".
    - **Preserve:** `--no-telegram` flag, `--json` flag, `send_telegram()` helper, standalone command-line execution via `venv/bin/python scripts/vertex_live_probe.py`.
    - **LOCATION constant:** remove the hard-coded `LOCATION = "us-central1"`; replaced by the explicit matrix.

    Do NOT change the `CANDIDATES` tuple — the 3 models listed are correct.

    After edits, run the script in dry-syntax mode only (no real API calls from Windows dev — Cisco Umbrella blocks them):
    ```bash
    venv/Scripts/python -c "import ast; ast.parse(open('scripts/vertex_live_probe.py').read()); print('syntax-ok')"
    venv/Scripts/python scripts/vertex_live_probe.py --help
    ```

    **Commit 2:** `feat(probe): upgrade vertex_live_probe.py to 2×3 endpoint × model matrix`
    Push with `git push --no-verify origin main`.
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import ast; ast.parse(open('scripts/vertex_live_probe.py').read()); print('syntax-ok')" && venv/Scripts/python scripts/vertex_live_probe.py --help</automated>
    Additional grep check:
    ```bash
    grep -n "known_good\|(\"global\"\|(\"us-central1\"" scripts/vertex_live_probe.py
    ```
    Expected: `known_good` dict and both location literals appear. Live execution from Windows dev is blocked by Umbrella — do NOT attempt.
  </verify>
  <done>
    - scripts/vertex_live_probe.py contains the exact `known_good` dict from the spec (6 entries).
    - Script loops over 2 locations × 3 models = 6 combinations and constructs a per-location genai.Client.
    - Alert logic: only known-good combos returning 404 trigger Telegram; known-bad 404s are silent.
    - `--help` exits 0 (syntax + argparse still valid).
    - Standalone invocation preserved (`if __name__ == "__main__"` block intact).
    - Commit pushed to origin/main with message `feat(probe): upgrade vertex_live_probe.py to 2×3 endpoint × model matrix`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite 05-00 SUMMARY § C + CLAUDE.md Vertex clarification — Commit 3</name>
  <files>
    .planning/phases/05-pipeline-automation/05-00-SUMMARY.md,
    CLAUDE.md
  </files>
  <action>
    **Fix 5 — Rewrite `.planning/phases/05-pipeline-automation/05-00-SUMMARY.md § C`:**

    Section C is "Five substantive fixes landed during Wave 0 close window" with the sub-incident + 2026-05-02 + 2026-05-03 AM + 2026-05-03 PM "THIRD flip" paragraphs (approx lines 200-216). Preserve the table (rows A-E) at the top of § C unchanged. Rewrite ONLY the narrative paragraphs below the table, with the following structure (content from user spec):

    - **2026-04-22:** Google made `gemini-embedding-2` GA on Vertex AI (global endpoint).
    - **2026-04-30 → 05-03:** We were trapped by multiple compounding illusions:
      - Probe ran in `us-central1` (where `-preview` lives but GA does not) → probe appeared stable there
      - Code's `_VERTEX_EMBEDDING_ALIAS` silently mapped all `-2` requests to `-preview`
      - Production Hermes `.env` had `GOOGLE_CLOUD_LOCATION=global` (undocumented change adopted for global quota pool)
      - Combination: probe stable / production 404s every embed / 2-machine divergence created the "flip" illusion
    - **2026-05-03 evening:** Hermes caught the divergence. Claude verified Google docs + `gemini-cli` issues #19055 / #20761 → endpoint × model name must match; `-preview` exists in regional deployments only, `global` hosts GA.
    - **Fix (this commit):** delete the alias, embrace GA `gemini-embedding-2` on `global`, upgrade probe to 2×3 matrix.
    - **Lesson (bold, as the first line of the rewritten narrative):** **Lesson: When Google releases a new model, probe the full endpoint matrix, not a single endpoint.**

    Preserve the existing timestamps (2026-04-30, 2026-05-02, 2026-05-03) but RE-LABEL them "observed confusion, not actual Google flips". The old "Preview lifecycle 3 flips in 4 days" framing is wrong and must be retired.

    Keep everything else in the SUMMARY untouched (the earlier sections about graph state, deviations, cross-modal verifier, Wave 0 close-out § A/B/D/E/F/G). Only § C narrative paragraphs change; § C's top table is preserved.

    **Fix 6 — CLAUDE.md Vertex section small updates:**

    Locate the "Vertex AI Migration Path" section in CLAUDE.md. Make two surgical updates:

    1. Clarify that `GOOGLE_CLOUD_LOCATION=global` (not `us-central1`) is the production-recommended value. Find any reference to `us-central1` as the Vertex location default for OmniGraph-Vault and either update it or add a clarifying sentence.

    2. Add this exact sentence somewhere in the "When to Migrate" or "Full Specification" subsection (executor's judgment on placement — pick whichever reads naturally):
       `Embedding model naming is endpoint-dependent: gemini-embedding-2 is GA on global; gemini-embedding-2-preview is regional-only. Always match model to endpoint.`

    Do NOT expand scope: do NOT rewrite the full migration section; do NOT add new subsections; do NOT touch any section outside "Vertex AI Migration Path".

    **Verification after edits:**

    ```bash
    grep -n "preview\|flip" .planning/phases/05-pipeline-automation/05-00-SUMMARY.md | head -20
    ```
    Expected: some `preview` mentions OK in the new narrative (e.g., describing `-preview` is regional-only); but NO assertions that "Preview lifecycle" is the truth or that Google flipped anything 3 times. The word "flip" should appear, if at all, only in contexts like "observed flip was illusion".

    ```bash
    grep -n "GOOGLE_CLOUD_LOCATION=global\|endpoint-dependent" CLAUDE.md
    ```
    Expected: both strings appear at least once.

    **Commit 3:** `docs: rewrite 05-00 § C Vertex narrative — root cause was endpoint × model mismatch`
    Push with `git push --no-verify origin main`.
  </action>
  <verify>
    <automated>grep -c "global endpoint\|endpoint-dependent" CLAUDE.md && grep -c "endpoint × model\|endpoint x model\|endpoint.*mismatch" .planning/phases/05-pipeline-automation/05-00-SUMMARY.md</automated>
    Additional narrative sanity check:
    ```bash
    grep -i "preview lifecycle\|3 flips in 4 days\|flipped 3 times" .planning/phases/05-pipeline-automation/05-00-SUMMARY.md
    ```
    Expected: zero matches (old narrative fully retired).
  </verify>
  <done>
    - 05-00-SUMMARY § C narrative rewritten: "Lesson: When Google releases a new model, probe the full endpoint matrix, not a single endpoint." appears as the first bold line.
    - § C table (rows A-E) preserved unchanged.
    - Timestamps 2026-04-30 / 05-02 / 05-03 preserved but relabeled as "observed confusion, not actual Google flips".
    - No "Preview lifecycle" / "3 flips in 4 days" assertions remain in § C.
    - CLAUDE.md Vertex section clarifies `GOOGLE_CLOUD_LOCATION=global` is production-recommended.
    - CLAUDE.md contains the exact sentence: "Embedding model naming is endpoint-dependent: gemini-embedding-2 is GA on global; gemini-embedding-2-preview is regional-only. Always match model to endpoint."
    - Commit pushed to origin/main with message `docs: rewrite 05-00 § C Vertex narrative — root cause was endpoint × model mismatch`.
  </done>
</task>

</tasks>

<verification>
End-of-plan gate (all three must succeed before marking plan complete):

```bash
# Gate 1: all 9 Vertex unit tests green (mock-only HTTP)
venv/Scripts/python -m pytest tests/unit/test_lightrag_embedding_vertex.py tests/unit/test_cognee_vertex_model_name.py -v
# expected: 9 passed

# Gate 2: zero alias residuals outside scripts/vertex_live_probe.py
grep -r "gemini-embedding-2-preview\|_VERTEX_EMBEDDING_ALIAS\|_resolve_model" lib/ tests/unit/ scripts/ | grep -v ".pyc"
# expected: matches ONLY inside scripts/vertex_live_probe.py (as probe targets in the known_good dict); zero matches in lib/, tests/unit/, or other files under scripts/

# Gate 3: narrative cleanup on 05-00 SUMMARY
grep "Preview lifecycle\|3 flips in 4 days" .planning/phases/05-pipeline-automation/05-00-SUMMARY.md
# expected: zero matches (old narrative fully retired)
```

Three commit hashes must be present on origin/main with the exact messages specified in Tasks 1-3.
</verification>

<success_criteria>
- 3 atomic commits pushed to origin/main in order: alias removal (code+tests) → probe matrix upgrade → docs rewrite. Each pushed with `--no-verify`.
- `venv/Scripts/python -m pytest tests/unit/test_lightrag_embedding_vertex.py tests/unit/test_cognee_vertex_model_name.py -v` reports 9 passed.
- Grep for `gemini-embedding-2-preview | _VERTEX_EMBEDDING_ALIAS | _resolve_model` across `lib/`, `tests/unit/`, `scripts/` returns matches ONLY inside `scripts/vertex_live_probe.py` (as probe target strings in the known_good dict).
- `scripts/vertex_live_probe.py --help` exits 0; file contains the exact `known_good` dict with 6 entries.
- 05-00-SUMMARY § C begins its narrative with a bold lesson line about endpoint-matrix probing.
- CLAUDE.md Vertex section includes the "endpoint-dependent" sentence and clarifies global endpoint.
- Pushed before 2026-05-04 06:00 ADT cron fire window.
</success_criteria>

<output>
After completion, the executor must create `.planning/quick/260503-m4q-vertex-embedding-correction-remove-previ/260503-m4q-SUMMARY.md` containing:

1. **Before/after behavior table** for `global × gemini-embedding-2`:
   | State | global × gemini-embedding-2 | global × gemini-embedding-2-preview |
   |---|---|---|
   | Before | ❌ 404 (alias mapped to -preview, which doesn't exist on global) | ❌ 404 |
   | After | ✅ dims=3072 (GA) | ❌ 404 (known-bad; silent in probe) |

2. **9-test green evidence:** paste pytest output showing 9 passed.

3. **Grep-zero evidence:** paste grep output confirming zero residuals in lib/, tests/unit/, cognee_wrapper.py.

4. **Three commit hashes** with their full one-line messages.

5. **Reminder line at the end:** `Main session: update memory/vertex_ai_smoke_validated.md to reflect GA on global endpoint (2026-04-22); strike the 3-flip narrative.`
</output>
