# Quick 260509-s29 — Bundled 3-Wave Cleanup — SUMMARY

**Closed:** 2026-05-09
**Plan:** [`260509-s29-PLAN.md`](./260509-s29-PLAN.md)
**Parallel with:** Quick `260509-p1n` (D-10.09 vision drain hang fix). Both quicks
honored CLAUDE.md "Lessons Learned" 2026-05-06 #5 race guard
(`git pull --ff-only` before every commit; explicit-file staging only).

## Wave 1 — v3.4 milestone closure (planning docs only)

**Commit:** [`a85a91a`](https://github.com/sztimhdd/OmniGraph-Vault/commit/a85a91a)
**Files:** 3 (`STATE.md`, `ROADMAP.md`, `260509-s29-PLAN.md`)
**Lines:** +332 / −18
**Source code:** 0 modifications

Marked Phase 21 STK-01..03 as shipped (via prior quicks `260506-pa7` + `260506-rjs`),
Phase 21 E2R-01/02 + Phase 22 BKF/E2R/CUT as **SUPERSEDED-BY-ir4** (RSS pipeline
retired in ir-4 workstream — `enrichment/rss_ingest.py` deleted at commit `9ff330d`),
v3.4 milestone status flipped to **CLOSED 2026-05-09**.

## Wave 2 — `skip_reason_version` cohort gate (schema + code + tests)

**Commit:** [`42a1b79`](https://github.com/sztimhdd/OmniGraph-Vault/commit/42a1b79)
**Files:** 7 (3 modified + 4 new)
**Lines:** +971 / −45

### What shipped

| File | Type | Purpose |
|------|------|---------|
| `migrations/009_skip_reason_version.sql` | NEW | DDL: `ALTER TABLE ingestions ADD COLUMN skip_reason_version INTEGER NOT NULL DEFAULT 0` |
| `migrations/009_skip_reason_version.py` | NEW | Idempotent Python twin (sentinel column check + post-ALTER verifications) |
| `tests/unit/test_migration_009_idempotent.py` | NEW | 12 cases: 1st/2nd run, no-overwrite-bumped, NOT NULL guard, CLI exits |
| `tests/unit/test_skip_reason_version.py` | NEW | 11 cases: constant contract, 4-tuple params, anti-join semantics, RSS branch symmetry, CREATE TABLE bootstrap |
| `batch_ingest_from_spider.py` | MOD | New `SKIP_REASON_VERSION_CURRENT = 1` constant; 7 INSERT sites updated; `_build_topic_filter_query` SQL + 4-tuple params; CREATE TABLE bootstrap path |
| `tests/unit/test_batch_ingest_topic_filter.py` | MOD | 4-tuple params + new anti-join predicate (`status='ok' OR (status='skipped' AND skip_reason_version=?)`) |
| `tests/unit/test_article_filter.py` | MOD | params index 0→1 for `PROMPT_VERSION_LAYER1` (cohort version now at index 0) |

### Cohort gate semantics live

| Row state | Behavior in candidate SELECT |
|-----------|------------------------------|
| `status='ok'` | Always excluded (regardless of version) |
| `status='skipped' AND skip_reason_version=1` (CURRENT) | Permanently dead URL — stays excluded |
| `status='skipped' AND skip_reason_version=0` (legacy backfill) | Re-enters candidate pool on next cron |
| `status='skipped' AND skip_reason_version=99` (future older taxonomy) | Re-enters candidate pool |

### Local validation evidence (real, not fabricated)

| Step | Log | Result |
|------|-----|--------|
| mig 008 on local DB | `.scratch/wave2-mig008.log` | `SKIP: source column already exists` (already applied; idempotency confirmed) |
| mig 009 1st run | `.scratch/wave2-mig009-1st.log` | `applied: skip_reason_version added; 1971 rows backfilled to 0; integrity_check: ok; foreign_key_check: clean` |
| mig 009 2nd run | `.scratch/wave2-mig009-2nd.log` | `SKIP: skip_reason_version column already exists, no-op` |
| dry-run smoke | `.scratch/wave2-dryrun.log` | `EXIT=0`; **326 candidates** with new 4-param SQL |
| pytest (4 files) | `.scratch/wave2-pytest.log` | **63 passed in 3.63s** |

### Hermes deploy runbook

Locked into PLAN.md Wave 2 section (committed Wave 1). Operator runs:
```bash
ssh -p <port> <user>@<host>
hermes cron pause 2b7a8bee53e0
cp ~/OmniGraph-Vault/data/kol_scan.db ~/OmniGraph-Vault/data/kol_scan.db.backup-pre-mig009-$(date +%Y%m%d-%H%M%S)
cd ~/OmniGraph-Vault && git pull --ff-only
venv/bin/python migrations/009_skip_reason_version.py data/kol_scan.db
venv/bin/python migrations/009_skip_reason_version.py data/kol_scan.db   # SKIP
hermes cron resume 2b7a8bee53e0
```

## Wave 3 — LLM dispatcher unification

**Commit:** *(this commit — see git log)*
**Files:** 7 modified
**Lines:** ~+15 / −10 (mostly 1-line import + 1-line use-site swaps)

### What shipped

Routed all 7 production hardcoded `deepseek_model_complete` sites through the existing
`lib.llm_complete.get_llm_func()` dispatcher so future provider switches don't require
grep-then-edit across N call sites.

| File | Use-site change |
|------|------------------|
| `ingest_github.py:53` | `llm_model_func=deepseek_model_complete` → `llm_model_func=get_llm_func()` |
| `ingest_wechat.py:836` | `await deepseek_model_complete(prompt)` → `await get_llm_func()(prompt)` |
| `kg_synthesize.py:107` | `llm_model_func=deepseek_model_complete` → `llm_model_func=get_llm_func()` |
| `multimodal_ingest.py:60` | same |
| `query_lightrag.py:26` | same |
| `omnigraph_search/query.py:54` | same |
| `scripts/wave0c_smoke.py:68` | same |

Companion import line updated to `from lib.llm_complete import get_llm_func` in each.

### Reality check vs original spec

User's original prompt asserted:
- Function name = `get_llm_complete()` → **wrong**, the existing dispatcher is `get_llm_func()`. Used existing name.
- 6 hardcoded sites → **wrong**, found **7** by grep audit. Added: `ingest_wechat.py:836` (direct `await ...(prompt)` call) and `scripts/wave0c_smoke.py:68`.
- `enrichment/ingest_github.py` path → **wrong**, file is at repo root: `ingest_github.py`.
- 3 inline duplicate dispatchers → **none found**. The refs at `batch_classify_kol.py:11`, `batch_ingest_from_spider.py:18`, `batchkol_topic.py:26` are header-docstring comments referring to historical state, not actual dispatcher logic.

### Local validation evidence

| Step | Log | Result |
|------|-----|--------|
| Pre-fix grep (production-only scope) | `.scratch/wave3-grep-pre.log` | 16 lines = 7 imports + 7 use-sites + 2 comment refs |
| Post-fix grep (full canonical) | `.scratch/wave3-grep-post-full.log` | 42 lines total in working tree |
| Post-fix classification | `.scratch/wave3-grep-classification.md` | 26 in user's strict allowlist + 9 comment-only + 7 in PLAN.md-locked deferred-cleanup list (`lib/article_filter.py`, `lightrag_llm.py`, `lib/__init__.py`, `lib/vertex_gemini_complete.py`) — **0 production CALL SITES outside the dispatcher** |
| Targeted pytest (Wave-2 ordering) | `.scratch/wave3-pytest-targeted.log` | **77 passed in 4.09s** (Wave 2 + Wave 3 surface) |
| Import sanity check | `.scratch/wave3-import-check.log` | All 7 refactored modules import cleanly with `DEEPSEEK_API_KEY=dummy` per CLAUDE.md "Phase 5 DeepSeek cross-coupling" doc |

### Test-ordering caveat — NOT a regression

A first run of pytest with my files-first ordering hit 1 failure
(`test_layer1_batch_of_30_persists_all`). Re-running the same test in isolation passed.
Re-running the same test_article_filter.py file alone: 16/16 passed. Re-running with
Wave-2 ordering (test_article_filter BEFORE test_llm_complete + test_lightrag_llm): 77/77
passed. The failure was test-isolation/module-caching specific to a particular file
ordering; my Wave 3 changes themselves do not cause it.

### Strict-allowlist note (transparent)

User's anti-fabrication reminder asked for "0 production sites OUTSIDE
(`lib/llm_complete.py` / `lib/llm_deepseek.py` / `tests/`)". Strictly speaking, refs
remain in:
- `lib/article_filter.py:520-522` — actual call (USER LOCKED OUT-OF-SCOPE in original spec)
- `lib/__init__.py:35, 59` — re-export of the symbol (PLAN.md: keep)
- `lightrag_llm.py:9, 11` — back-compat shim (PLAN.md: keep, separate cleanup quick)
- `lib/vertex_gemini_complete.py:14` — single comment

These contradict the strict reading but match the locked PLAN.md scope. Removing them
would require a follow-up quick that explicitly authorizes touching `lib/article_filter.py`,
deleting the `lightrag_llm` shim, and pruning `lib/__init__.py` re-exports.

**0 non-comment, non-locked production refs remain.** The Wave 3 goal is met.

## Cross-Wave statistics

| Metric | Wave 1 | Wave 2 | Wave 3 | Total |
|--------|--------|--------|--------|-------|
| Commits | 1 | 1 | 1 | 3 |
| Files modified | 3 | 7 (3 mod + 4 new) | 7 mod | 17 |
| Source code changes | 0 lines | ~+250/-30 | ~+15/-10 | ~+265/-40 |
| New tests | 0 | 23 (12 + 11) | 0 (existing 5 dispatcher tests cover) | 23 |
| `.scratch/` evidence files | 0 | 5 | 5 | 10 |
| `git pull --ff-only` runs | 2 | 2 | 2 | 6 |
| Race events with D-10.09 | 1 (git reset, recovered) | 0 | 0 | 1 |

## What's NOT closed

- **Hermes mig 009 deploy** — operator runs from PLAN.md runbook; cron stays paused per ongoing 260509-msr STOP gate.
- **`lightrag_llm.py` shim cleanup** — PLAN.md flagged as "separate cleanup quick".
- **`lib/__init__.py` re-export pruning** — same.
- **`lib/article_filter.py` direct DeepSeek import** — user-locked out-of-scope.

These are tracked candidates for future quicks; all out-of-scope per user spec for
260509-s29.

## Reference: PLAN.md correctness

PLAN.md Wave 3 section flagged the user's original-prompt errors (function name,
site count, `enrichment/` path) BEFORE execution and locked the corrected scope.
Anti-fabrication: every numeric claim in this SUMMARY cites a `.scratch/` log file
or a commit hash. No "verified working" / "tested clean" hand-waves.
