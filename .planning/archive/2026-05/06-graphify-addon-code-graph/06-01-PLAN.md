---
phase: 06-graphify-addon-code-graph
plan: 01
type: execute
wave: 2
depends_on:
  - "06-00"
files_modified:
  - AGENTS.md
autonomous: false
requirements:
  - REQ-01
  - REQ-02

must_haves:
  truths:
    - "Running `hermes skills list` on the remote PC shows `graphify | enabled`"
    - "T1 repos (openclaw/openclaw and anthropics/claude-code) are cloned on the remote under `~/.hermes/omonigraph-vault/graphify/repos/`"
    - "If D-S10 scope is hermes-and-claw: `claw skills list` shows graphify enabled. Otherwise: REQ-02 marked partially deferred in SUMMARY."
    - "Repo-root `AGENTS.md` contains a `## graphify` section appended by the installer"
  artifacts:
    - path: "AGENTS.md"
      provides: "Top-level AGENTS.md file registering graphify skill per graphifyy installer marker"
      contains: "## graphify"
    - path: "~/.hermes/skills/graphify/SKILL.md (remote only)"
      provides: "Hermes-side graphify skill file written by `graphify install --platform hermes`"
    - path: "~/.hermes/omonigraph-vault/graphify/repos/openclaw/openclaw/.git (remote only)"
      provides: "Cloned T1 repo — openclaw"
    - path: "~/.hermes/omonigraph-vault/graphify/repos/anthropics/claude-code/.git (remote only)"
      provides: "Cloned T1 repo — claude-code"
  key_links:
    - from: "graphify install --platform hermes"
      to: "~/.hermes/skills/graphify/SKILL.md"
      via: "installer writes file + .graphify_version stamp"
      pattern: "\\.graphify_version"
    - from: "graphify hermes install (in repo root)"
      to: "AGENTS.md"
      via: "appends section with _AGENTS_MD_MARKER"
      pattern: "## graphify"
---

<objective>
Install the `graphify` skill on the remote PC — Hermes unconditionally; OpenClaw conditionally based on the D-S10 scope decision from Plan 00. Clone both T1 repos (openclaw/openclaw and anthropics/claude-code) into the canonical vault storage path. Register `graphify` in the repo-root `AGENTS.md` so the skill is always-on for this project.

Purpose: REQ-01 + REQ-02. `graphify install --platform hermes` (and, conditionally, `--platform claw`) writes the skill file; the companion `graphify hermes install` (run inside the repo) appends the `## graphify` section to `AGENTS.md` — both are needed. D-G04 locks T1-only scope: NO T2/T3 repos beyond openclaw + claude-code.

Output: Skill files on remote at `~/.hermes/skills/graphify/` (+ `~/.openclaw/skills/graphify/` if scope = hermes-and-claw), T1 repos cloned under `~/.hermes/omonigraph-vault/graphify/repos/<owner>/<repo>/`, and committed `AGENTS.md` with graphify registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-graphify-addon-code-graph/06-CONTEXT.md
@.planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md
@.planning/phases/06-graphify-addon-code-graph/06-VALIDATION.md
@.planning/phases/06-graphify-addon-code-graph/06-00-SUMMARY.md
@docs/testing/06-remote-probe-result.md

<interfaces>
<!-- Commands this plan uses. Exact flags verified in research §Code Examples §1+§2. -->

graphify install --platform <hermes|claw>
  → writes ~/.<platform>/skills/graphify/SKILL.md + .graphify_version stamp

graphify <hermes|claw> install
  → run inside a repo; appends "## graphify" marker section to ./AGENTS.md

graphify clone <git-url> --out <path>
  → git clones to <path>; without --out defaults to ~/.graphify/repos/<owner>/<repo>

Probe-result file contract (from Plan 00):
  docs/testing/06-remote-probe-result.md contains `scope: hermes-and-claw` or `scope: hermes-only`
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1.1: Install graphifyy and graphify skill on REMOTE (Hermes always; claw conditional)</name>
  <files>(remote filesystem — not in this repo)</files>
  <read_first>
    - docs/testing/06-remote-probe-result.md (to read the `scope:` line)
    - .planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md §Code Examples §1 (exact command sequence)
    - .planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md §Pitfall 4 (two-command install pattern)
    - ~/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/hermes_ssh.md (SSH details — do NOT commit)
  </read_first>
  <what-built>
    Orchestrator SSHes to the remote PC and executes, via SSH session:

    ```
    cd ~/OmniGraph-Vault
    source venv/bin/activate
    pip install graphifyy==0.5.3

    # Verify
    graphify --version
    python -c "import graphify; print(graphify.__version__)"

    # Hermes skill install (unconditional)
    graphify install --platform hermes

    # Register in repo AGENTS.md (so graphify is always-on for this repo)
    cd ~/OmniGraph-Vault
    graphify hermes install

    # Conditional on scope from Plan 00:
    # IF scope = hermes-and-claw:
    graphify install --platform claw
    cd ~/OmniGraph-Vault
    graphify claw install
    # ELSE (scope = hermes-only):
    #   skip claw commands entirely; note in SUMMARY that REQ-02 is partially deferred.

    # Defensive: ask Hermes gateway to pick up the new skill (no-op if auto-reloads)
    pkill -HUP hermes-gateway 2>/dev/null || systemctl --user restart hermes 2>/dev/null || true

    # Verify
    ls -la ~/.hermes/skills/graphify/
    hermes skills list 2>/dev/null | grep -i graphify
    # If scope includes claw:
    ls -la ~/.openclaw/skills/graphify/ 2>/dev/null
    command -v claw && claw skills list 2>/dev/null | grep -i graphify || true
    ```

    Then `scp` the updated `AGENTS.md` from remote back to the local workspace (since the installer modifies it on disk at the repo root — remote is where the install runs, local needs the committed change).
  </what-built>
  <how-to-verify>
    1. On remote: `ls -la ~/.hermes/skills/graphify/SKILL.md` — file must exist
    2. On remote: `hermes skills list 2>/dev/null | grep -i graphify` — output must include `graphify` and some enabled indicator
    3. On remote: verify `.graphify_version` stamp exists: `test -f ~/.hermes/skills/graphify/.graphify_version`
    4. If scope = hermes-and-claw: also confirm `~/.openclaw/skills/graphify/SKILL.md` exists and `claw skills list | grep graphify` returns a match
    5. On remote: `test -f ~/OmniGraph-Vault/AGENTS.md && grep -q "## graphify" ~/OmniGraph-Vault/AGENTS.md` — must exit 0
    6. scp the modified `AGENTS.md` back to the local workspace: `scp <remote>:~/OmniGraph-Vault/AGENTS.md ./AGENTS.md`
    7. Locally: `grep -q "## graphify" AGENTS.md` must exit 0
    8. Orchestrator reports to user: "Graphify skill installed on {hermes | hermes+claw}. AGENTS.md synced locally. Ready to commit."
  </how-to-verify>
  <resume-signal>Type "approved" after verification, or describe failure mode.</resume-signal>
  <acceptance_criteria>
    - Remote `~/.hermes/skills/graphify/SKILL.md` exists (SSH check)
    - Remote `hermes skills list 2>/dev/null | grep -qi graphify` exits 0
    - Local `./AGENTS.md` exists and `grep -q "^## graphify" AGENTS.md` exits 0
    - If scope = hermes-and-claw: remote `test -f ~/.openclaw/skills/graphify/SKILL.md` exits 0 AND `claw skills list | grep -qi graphify` exits 0
    - No credentials, hostnames, or ports from `hermes_ssh.md` appear in any committed file in this plan
  </acceptance_criteria>
  <action>See <what-built> above. This is a checkpoint task: the orchestrator performs the steps described in <what-built>, verifies per <how-to-verify>, and waits for <resume-signal> before continuing. Acceptance is gated by <acceptance_criteria>.</action>
  <verify>See <how-to-verify> above; final pass condition is <acceptance_criteria>.</verify>
  <done>User types the <resume-signal> token AND every bullet in <acceptance_criteria> is satisfied.</done>

</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1.2: Clone T1 repos on REMOTE into canonical vault paths</name>
  <files>(remote filesystem only)</files>
  <read_first>
    - .planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md §Code Examples §2 (exact command sequence with --out flag)
    - .planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md §Pitfall 5 (why --out is mandatory)
    - .planning/phases/06-graphify-addon-code-graph/06-CONTEXT.md (D-G03 + D-G04 — separate storage, T1 only)
  </read_first>
  <what-built>
    Orchestrator runs on remote:

    ```
    source ~/OmniGraph-Vault/venv/bin/activate

    GRAPHIFY_ROOT="$HOME/.hermes/omonigraph-vault/graphify"
    mkdir -p "$GRAPHIFY_ROOT/repos"

    # T1 repo 1: openclaw/openclaw
    graphify clone https://github.com/openclaw/openclaw \
      --out "$GRAPHIFY_ROOT/repos/openclaw/openclaw"

    # T1 repo 2: anthropics/claude-code
    graphify clone https://github.com/anthropics/claude-code \
      --out "$GRAPHIFY_ROOT/repos/anthropics/claude-code"

    # Verify
    ls -la "$GRAPHIFY_ROOT/repos/openclaw/openclaw/.git" \
           "$GRAPHIFY_ROOT/repos/anthropics/claude-code/.git"
    du -sh "$GRAPHIFY_ROOT/repos/"*
    ```

    The `--out` flag is required because `graphify clone` defaults to `~/.graphify/repos/<owner>/<repo>`, which violates D-G03 (separate storage under the vault root).

    Per D-G04: clone ONLY these two repos. Do NOT clone Hermes, LightRAG, Cognee, or any T2/T3 repo, even if a helpful suggestion arises.
  </what-built>
  <how-to-verify>
    1. On remote: `test -d ~/.hermes/omonigraph-vault/graphify/repos/openclaw/openclaw/.git`
    2. On remote: `test -d ~/.hermes/omonigraph-vault/graphify/repos/anthropics/claude-code/.git`
    3. On remote: `ls ~/.hermes/omonigraph-vault/graphify/repos/` should return exactly two directories (`openclaw` and `anthropics`) — not 3+
    4. If a third dir exists, a T2/T3 repo was cloned by mistake: remove it and document in SUMMARY
    5. Orchestrator reports clone size (`du -sh`) and repo count, notes if anything looks unexpected (e.g., one repo failed mid-clone)
  </how-to-verify>
  <resume-signal>Type "approved" once both repos exist and no extras are present.</resume-signal>
  <acceptance_criteria>
    - SSH check: `test -d ~/.hermes/omonigraph-vault/graphify/repos/openclaw/openclaw/.git` exits 0
    - SSH check: `test -d ~/.hermes/omonigraph-vault/graphify/repos/anthropics/claude-code/.git` exits 0
    - SSH check: `ls ~/.hermes/omonigraph-vault/graphify/repos/ | wc -l` returns exactly `2`
    - Remote `~/.graphify/repos/` is NOT used (if it exists, it's from a prior different tool — not ours)
  </acceptance_criteria>
  <action>See <what-built> above. This is a checkpoint task: the orchestrator performs the steps described in <what-built>, verifies per <how-to-verify>, and waits for <resume-signal> before continuing. Acceptance is gated by <acceptance_criteria>.</action>
  <verify>See <how-to-verify> above; final pass condition is <acceptance_criteria>.</verify>
  <done>User types the <resume-signal> token AND every bullet in <acceptance_criteria> is satisfied.</done>

</task>

<task type="auto">
  <name>Task 1.3: Commit AGENTS.md to local git</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (the file just synced from remote in Task 1.1 — confirm it has the "## graphify" section)
  </read_first>
  <action>
    The `AGENTS.md` file was modified by the remote installer and synced back in Task 1.1. Commit it locally using the project's standard commit invocation:

    ```
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "chore(graphify): register graphify skill in AGENTS.md (hermes+claw platforms)" --files AGENTS.md
    ```

    If scope = hermes-only, adjust the commit message to `"chore(graphify): register graphify skill in AGENTS.md (hermes only — claw deferred)"`.

    Do NOT edit `AGENTS.md` content by hand — it must stay byte-identical to the file on remote (so reinstalling `graphify hermes install` is idempotent and won't produce a diff next time).
  </action>
  <verify>
    <automated>grep -q "^## graphify" AGENTS.md && git log -1 --name-only | grep -q AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## graphify" AGENTS.md` exits 0
    - `git log -1 --name-only` output includes `AGENTS.md`
    - `git log -1 --format=%s` output contains `graphify` and `AGENTS.md` (commit message)
  </acceptance_criteria>
  <done>AGENTS.md committed with the installer-generated graphify section.</done>
</task>

</tasks>

<verification>
- REQ-01: `ssh remote "test -f ~/.hermes/skills/graphify/SKILL.md && hermes skills list 2>/dev/null | grep -qi graphify"` exits 0
- REQ-02: Either (scope=hermes-and-claw) `ssh remote "test -f ~/.openclaw/skills/graphify/SKILL.md"` exits 0, OR (scope=hermes-only) SUMMARY records REQ-02 as partially deferred with rationale from probe
- Both T1 repos cloned under `~/.hermes/omonigraph-vault/graphify/repos/` (NOT under `~/.graphify/`)
- Local git has one commit modifying only `AGENTS.md` with the graphify section
</verification>

<success_criteria>
- `hermes skills list` on remote shows graphify as an available, enabled skill
- (Conditional) `claw skills list` on remote shows graphify as an available, enabled skill
- Two T1 repos exist under the canonical path; no T2/T3 repos were cloned
- `AGENTS.md` committed with the graphify marker section
</success_criteria>

<output>
After completion, create `.planning/phases/06-graphify-addon-code-graph/06-01-SUMMARY.md` with:
- Outcome of `hermes skills list` (verbatim output line for graphify)
- D-S10 scope actually realized (hermes-and-claw or hermes-only) and, if hermes-only, explicit statement "REQ-02 is partially deferred — skill files provisioned on filesystem but not verified via CLI because `claw` CLI is not deployed on remote"
- Actual clone sizes (du -sh output) and branch/commit hashes of the two T1 repos
- Any warnings from the install CLI
</output>
