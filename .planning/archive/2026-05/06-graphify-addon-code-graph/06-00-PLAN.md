---
phase: 06-graphify-addon-code-graph
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/omnigraph_search/SKILL.md
  - skills/omnigraph_search/scripts/query.sh
  - skills/omnigraph_search/references/api-surface.md
  - omnigraph_search/__init__.py
  - omnigraph_search/query.py
  - scripts/graphify-refresh.sh
  - tests/skills/test_omnigraph_search.json
  - docs/testing/06-demo1-transcript.md
  - docs/testing/06-demo2-transcript.md
  - docs/testing/06-graph-seed-runbook.md
  - docs/testing/06-remote-probe-result.md
  - requirements.txt
autonomous: false
requirements:
  - REQ-03
  - REQ-04
  - REQ-08
user_setup: []

must_haves:
  truths:
    - "Every file a downstream plan needs exists as a stub or placeholder"
    - "graphifyy package is installed in local venv and the binary `graphify` is on PATH"
    - "The D-S10 scope decision is recorded (Hermes+OpenClaw or Hermes-only) based on remote probe"
  artifacts:
    - path: "skills/omnigraph_search/SKILL.md"
      provides: "Stub SKILL.md that skill_runner.py --validate will reject until filled (Plan 03)"
      contains: "name: omnigraph_search"
    - path: "skills/omnigraph_search/scripts/query.sh"
      provides: "Empty bash stub with shebang"
      contains: "#!/usr/bin/env bash"
    - path: "skills/omnigraph_search/references/api-surface.md"
      provides: "Placeholder markdown file"
    - path: "omnigraph_search/__init__.py"
      provides: "Python package marker"
    - path: "omnigraph_search/query.py"
      provides: "Python stub raising NotImplementedError"
      contains: "NotImplementedError"
    - path: "scripts/graphify-refresh.sh"
      provides: "Shell script stub with shebang"
      contains: "#!/bin/bash"
    - path: "tests/skills/test_omnigraph_search.json"
      provides: "Valid JSON array (may be empty)"
      contains: "["
    - path: "docs/testing/06-demo1-transcript.md"
      provides: "Placeholder for Demo 1 transcript (filled Wave 5)"
    - path: "docs/testing/06-demo2-transcript.md"
      provides: "Placeholder for Demo 2 transcript (filled Wave 5)"
    - path: "docs/testing/06-graph-seed-runbook.md"
      provides: "Placeholder for the graph-seed runbook (filled Wave 3)"
    - path: "docs/testing/06-remote-probe-result.md"
      provides: "Records result of probing remote for hermes/claw binaries and the D-S10 scope decision"
      contains: "hermes:"
    - path: "requirements.txt"
      provides: "Includes graphifyy pin"
      contains: "graphifyy"
  key_links:
    - from: "requirements.txt"
      to: "local venv"
      via: "pip install -r requirements.txt"
      pattern: "graphifyy==0\\.5\\.3"
    - from: "docs/testing/06-remote-probe-result.md"
      to: "Plan 01"
      via: "Sets `claw_present: true|false` which Plan 01 reads"
      pattern: "claw_present:"
---

<objective>
Create the file scaffold for the entire phase so downstream plans drop implementations into known slots, install `graphifyy` locally, and probe the remote PC to determine whether OpenClaw (`claw` CLI) is actually deployed. The probe result gates the D-S10 scope for Plan 01.

Purpose: Downstream plans (01, 02, 03, 04, 05) assume these files exist. Creating the scaffold up front lets them be planned and executed without discovery scavenger-hunts. The `claw` probe is a branching decision that cannot be deferred — if `claw` is absent, Plan 01's OpenClaw tasks change from "install + verify" to "install skill file best-effort, mark REQ-02 as partially deferred."

Output: All 12 new files (11 committed to git + 1 local-only `requirements.txt` change), `graphifyy==0.5.3` installed in local venv, and `docs/testing/06-remote-probe-result.md` with recorded probe output and explicit D-S10 scope decision.

Note: All 12 files are stubs (<20 lines each); real implementations drop in Wave 2+ plans. The file count reflects scaffolding breadth — not task complexity — so the 2-task body stays within the scope-sanity budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-graphify-addon-code-graph/06-CONTEXT.md
@.planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md
@.planning/phases/06-graphify-addon-code-graph/06-VALIDATION.md
@skills/omnigraph_query/SKILL.md
@skills/omnigraph_query/scripts/query.sh
@skills/omnigraph_query/references/api-surface.md
@tests/skills/test_omnigraph_query.json
@config.py
@query_lightrag.py
@requirements.txt

<interfaces>
<!-- The stub files created here set up these contracts. Downstream plans fill them in. -->

omnigraph_search/query.py (Python API — defined in Plan 03):
```python
async def search(query_text: str, mode: str = "hybrid") -> str: ...
def main() -> None: ...  # CLI entry point
```

skills/omnigraph_search/SKILL.md (frontmatter required keys — filled in Plan 03):
  name, description, compatibility, metadata.openclaw.os, metadata.openclaw.requires.bins, metadata.openclaw.requires.config

scripts/graphify-refresh.sh (shell contract — filled in Plan 04):
  Env: GRAPHIFY_ROOT, LOG_FILE, GRAPH_JSON
  Exit 0 on success; appends to $LOG_FILE on failure.

docs/testing/06-remote-probe-result.md (Plan 01 reads this):
```
hermes: present|absent
claw:   present|absent
d-s10_scope: hermes-only | hermes-and-claw
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Install graphifyy locally and pin in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current pins so new entry is appended, not duplicating an existing line)
    - .planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md §Standard Stack (the pinned version is 0.5.3, note the double-y spelling)
  </read_first>
  <action>
    Append exactly this line to the end of `requirements.txt` (do NOT modify other pins):
    ```
    graphifyy==0.5.3
    ```
    Then run (use the project's local venv):
    ```
    venv/Scripts/pip install graphifyy==0.5.3
    ```
    (On Linux the path is `venv/bin/pip`; use whichever matches the dev machine's OS — the executor is running on Windows per env, so use `venv/Scripts/pip`.)

    After install, verify:
    ```
    venv/Scripts/python -c "import graphify; print(graphify.__version__)"
    venv/Scripts/graphify --version
    ```
    Both must succeed. Note the common pitfall from research §Pitfall 1: `pip install graphify` (single-y) installs the wrong package. Use `graphifyy` only.
  </action>
  <verify>
    <automated>grep -F "graphifyy==0.5.3" requirements.txt && venv/Scripts/python -c "import graphify; print(graphify.__version__)" | grep -F "0.5.3" && venv/Scripts/graphify --help | grep -q "install" && venv/Scripts/graphify --help | grep -q "update" && venv/Scripts/graphify --help | grep -q "clone"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "graphifyy==0.5.3" requirements.txt` returns a match
    - `venv/Scripts/python -c "import graphify; print(graphify.__version__)"` prints a line starting with `0.5.3`
    - `venv/Scripts/graphify --help` output contains the substrings `install`, `update`, and `clone`
    - `venv/Scripts/graphify install --help` output contains `--platform`
    - No other lines in requirements.txt have changed (git diff shows only one line added)
  </acceptance_criteria>
  <done>Package installed, importable, binary exposes the expected subcommands, and the pin is committed.</done>
</task>

<task type="auto">
  <name>Task 0.2: Create stub files for omnigraph_search skill, Python module, cron script, and test JSON</name>
  <files>
    skills/omnigraph_search/SKILL.md,
    skills/omnigraph_search/scripts/query.sh,
    skills/omnigraph_search/references/api-surface.md,
    omnigraph_search/__init__.py,
    omnigraph_search/query.py,
    scripts/graphify-refresh.sh,
    tests/skills/test_omnigraph_search.json,
    docs/testing/06-demo1-transcript.md,
    docs/testing/06-demo2-transcript.md,
    docs/testing/06-graph-seed-runbook.md
  </files>
  <read_first>
    - skills/omnigraph_query/SKILL.md (frontmatter format — Plan 03 will populate the new SKILL.md to match this shape)
    - skills/omnigraph_query/scripts/query.sh (bash pattern — Plan 03 will fill in)
    - .planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md §Architecture Patterns §Recommended Project Structure (shows target directory layout)
    - .planning/phases/06-graphify-addon-code-graph/06-VALIDATION.md §Wave 0 Requirements (authoritative list of files needed)
  </read_first>
  <action>
    Create each of the following files with exactly the given content. Create missing directories as needed (`skills/omnigraph_search/scripts/`, `skills/omnigraph_search/references/`, `omnigraph_search/`, `docs/testing/` already exists).

    **File 1 — `skills/omnigraph_search/SKILL.md`** (minimal valid frontmatter, body is a TODO note that Plan 03 will replace):
    ```markdown
    ---
    name: omnigraph_search
    description: |
      STUB — filled in Plan 03. This skill will wrap LightRAG hybrid-mode query for
      design-rationale/best-practices retrieval.
    compatibility: |
      STUB — see Plan 03.
    metadata:
      openclaw:
        os: ["darwin", "linux", "win32"]
        requires:
          bins: ["bash", "python"]
          config: ["GEMINI_API_KEY"]
    ---

    # omnigraph_search

    Stub created in Phase 6 Plan 00. Plan 03 populates the full skill body.
    ```

    **File 2 — `skills/omnigraph_search/scripts/query.sh`** (executable stub, marked chmod +x):
    ```bash
    #!/usr/bin/env bash
    # STUB — filled in Plan 03 by mirroring skills/omnigraph_query/scripts/query.sh.
    set -euo pipefail
    echo "⚠️ omnigraph_search not yet implemented (Phase 6 Plan 03)" >&2
    exit 1
    ```
    After writing, run `chmod +x skills/omnigraph_search/scripts/query.sh`.

    **File 3 — `skills/omnigraph_search/references/api-surface.md`** (placeholder):
    ```markdown
    # omnigraph_search — API Surface Reference

    STUB — filled in Plan 03.
    ```

    **File 4 — `omnigraph_search/__init__.py`** (empty package marker):
    ```python
    ```
    (zero-byte file — just create it)

    **File 5 — `omnigraph_search/query.py`** (module stub that will be overwritten in Plan 03):
    ```python
    """LightRAG hybrid-mode wrapper for the omnigraph_search skill.

    STUB — filled in Phase 6 Plan 03 by mirroring query_lightrag.py.
    """

    def search(*_args, **_kwargs):
        raise NotImplementedError("omnigraph_search.query.search will be implemented in Plan 03")


    def main() -> None:
        raise NotImplementedError("omnigraph_search.query.main will be implemented in Plan 03")


    if __name__ == "__main__":
        main()
    ```

    **File 6 — `scripts/graphify-refresh.sh`** (stub, chmod +x):
    ```bash
    #!/bin/bash
    # STUB — filled in Phase 6 Plan 04 (weekly cron, remote only).
    set -euo pipefail
    echo "⚠️ graphify-refresh.sh not yet implemented (Phase 6 Plan 04)" >&2
    exit 1
    ```
    After writing, run `chmod +x scripts/graphify-refresh.sh`.

    **File 7 — `tests/skills/test_omnigraph_search.json`** (empty array — Plan 03 populates):
    ```json
    []
    ```

    **File 8 — `docs/testing/06-demo1-transcript.md`**:
    ```markdown
    # Phase 6 — Demo 1 Transcript (Streaming Tool Output)

    _Placeholder — filled in Phase 6 Plan 05 after live Hermes session on remote._

    ## Prompt

    TODO (Plan 05)

    ## Tool-use log

    TODO (Plan 05)

    ## Assertion: both `graphify` AND `omnigraph_search` appear in tool-use log

    TODO (Plan 05)

    ## Qualitative sign-off

    TODO (Plan 05)
    ```

    **File 9 — `docs/testing/06-demo2-transcript.md`**:
    ```markdown
    # Phase 6 — Demo 2 Transcript (Self-Evolution)

    _Placeholder — filled in Phase 6 Plan 05 after live Hermes session on remote._

    ## Prompt

    TODO (Plan 05)

    ## Tool-use log

    TODO (Plan 05)

    ## Assertion: both `graphify` AND `omnigraph_search` appear in tool-use log

    TODO (Plan 05)

    ## Qualitative sign-off

    TODO (Plan 05)
    ```

    **File 10 — `docs/testing/06-graph-seed-runbook.md`**:
    ```markdown
    # Phase 6 — Graph Seed Runbook (One-Shot, Human-in-Loop)

    _Placeholder — filled in Phase 6 Plan 02 with the 7-step procedure for running `/graphify` inside a live Hermes session on the remote PC._
    ```
  </action>
  <verify>
    <automated>test -f skills/omnigraph_search/SKILL.md && test -f skills/omnigraph_search/scripts/query.sh && test -x skills/omnigraph_search/scripts/query.sh && test -f skills/omnigraph_search/references/api-surface.md && test -f omnigraph_search/__init__.py && test -f omnigraph_search/query.py && test -f scripts/graphify-refresh.sh && test -x scripts/graphify-refresh.sh && test -f tests/skills/test_omnigraph_search.json && python -c "import json; json.load(open('tests/skills/test_omnigraph_search.json'))" && test -f docs/testing/06-demo1-transcript.md && test -f docs/testing/06-demo2-transcript.md && test -f docs/testing/06-graph-seed-runbook.md && bash -n skills/omnigraph_search/scripts/query.sh && bash -n scripts/graphify-refresh.sh && python -c "import omnigraph_search.query; omnigraph_search.query.search() if False else None"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 files exist on disk
    - `bash -n skills/omnigraph_search/scripts/query.sh` exits 0 (shell syntax valid)
    - `bash -n scripts/graphify-refresh.sh` exits 0 (shell syntax valid)
    - `python -c "import json; json.load(open('tests/skills/test_omnigraph_search.json'))"` exits 0 (valid JSON)
    - `python -c "import omnigraph_search.query"` exits 0 (module importable)
    - `calling omnigraph_search.query.search()` raises `NotImplementedError` (verify by `python -c "import omnigraph_search.query; omnigraph_search.query.search()"` — should exit non-zero with NotImplementedError in stderr)
    - `skills/omnigraph_search/SKILL.md` contains the line `name: omnigraph_search`
    - `skills/omnigraph_search/SKILL.md` contains the line `config: ["GEMINI_API_KEY"]`
    - Both shell scripts have the executable bit set (`test -x` succeeds)
  </acceptance_criteria>
  <done>All stub files in place; shell scripts syntactically valid and executable; Python module importable and raises NotImplementedError as expected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0.3: Probe remote PC and record D-S10 scope decision</name>
  <files>docs/testing/06-remote-probe-result.md</files>
  <read_first>
    - ~/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/hermes_ssh.md (SSH connection details — do NOT echo contents into the file or any logs; read mentally only)
    - .planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md §Environment Availability and §Common Pitfalls §Pitfall 3 (explicit guidance on the claw probe)
  </read_first>
  <what-built>
    Orchestrator runs this command set on the remote PC via SSH (using credentials from the memory file):

    ```
    ssh <remote> "command -v hermes || echo 'hermes:absent'; command -v claw || echo 'claw:absent'; test -d ~/.hermes/skills && echo 'hermes_skills_dir:present' || echo 'hermes_skills_dir:absent'; test -d ~/.openclaw/skills && echo 'openclaw_skills_dir:present' || echo 'openclaw_skills_dir:absent'; which python3 python 2>/dev/null | head -2; git -C ~/OmniGraph-Vault log --oneline -1 2>/dev/null || echo 'repo:missing'"
    ```

    Then writes the results to `docs/testing/06-remote-probe-result.md`. Note: NEVER include hostname, port, username, or any credential in the result file — write only the probe OUTPUT (binary paths, which may contain `/home/<username>/`; replace the username with `<user>` if it appears).

    Expected output format for `docs/testing/06-remote-probe-result.md`:
    ```markdown
    # Phase 6 — Remote Probe Result

    **Probed:** 2026-MM-DD

    ## Binary availability (from `command -v` on remote)

    hermes: present | absent
    claw:   present | absent

    ## Directory availability

    ~/.hermes/skills:   present | absent
    ~/.openclaw/skills: present | absent

    ## Python

    <interpreter paths>

    ## Repo state

    <commit hash + subject>, OR "repo:missing"

    ## D-S10 Scope Decision

    scope: hermes-and-claw | hermes-only

    **Rationale:**
    - If `claw: present` AND `~/.openclaw/skills: present` → scope = hermes-and-claw; Plan 01 installs on BOTH platforms
    - Otherwise → scope = hermes-only; Plan 01 skips the `graphify install --platform claw` + `graphify claw install` tasks, and marks REQ-02 as partially deferred (skill file can still be written to ~/.openclaw/skills/ best-effort, but no CLI verification is possible)
    ```
  </what-built>
  <how-to-verify>
    1. SSH to remote using credentials from the memory file
    2. Run the probe command set above
    3. Paste the output into `docs/testing/06-remote-probe-result.md` using the template
    4. Sanitize the output: replace any username that appears in `/home/<user>/...` paths with the literal string `<user>`
    5. Fill in the `scope: hermes-and-claw | hermes-only` line based on the decision rule
    6. Confirm no hostname, port, or credential appears anywhere in the file (grep the file for the remote hostname substring — should return zero matches)
    7. Orchestrator reports to user: "Probe done. D-S10 scope is X. Plan 01 will install on Y platforms."
  </how-to-verify>
  <resume-signal>Type "approved" after the file is written and D-S10 scope is confirmed. Or describe issues (e.g., "SSH failed, need retry" or "user wants to override scope to hermes-only regardless").</resume-signal>
  <acceptance_criteria>
    - `docs/testing/06-remote-probe-result.md` exists
    - Contains the literal string `hermes:` (with either `present` or `absent`)
    - Contains the literal string `claw:` (with either `present` or `absent`)
    - Contains the literal string `scope:` followed by `hermes-and-claw` or `hermes-only`
    - Does NOT contain the remote hostname substring from `hermes_ssh.md` (grep-verify returns no matches)
    - Does NOT contain a plaintext port number from `hermes_ssh.md` (grep-verify returns no matches)
  </acceptance_criteria>
  <action>See <what-built> above. This is a checkpoint task: the orchestrator performs the steps described in <what-built>, verifies per <how-to-verify>, and waits for <resume-signal> before continuing. Acceptance is gated by <acceptance_criteria>.</action>
  <verify>See <how-to-verify> above; final pass condition is <acceptance_criteria>.</verify>
  <done>User types the <resume-signal> token AND every bullet in <acceptance_criteria> is satisfied.</done>

</task>

</tasks>

<verification>
After all three tasks complete:
- `git status` shows 11 new tracked files (10 from Task 0.2 + 1 requirements.txt edit from Task 0.1 + 1 probe result from Task 0.3 = 12 total — one of which is requirements.txt modification)
- `venv/Scripts/graphify --version` prints a version string starting with `0.5.3`
- `python skill_runner.py skills/omnigraph_search --validate` EXPECTED to FAIL at this stage (SKILL.md is a stub without a real description) — this is fine; Plan 03 makes it pass
- `docs/testing/06-remote-probe-result.md` records the D-S10 scope, and downstream Plan 01 will read this file to branch
</verification>

<success_criteria>
- graphifyy 0.5.3 installed and importable in local venv
- All 10 scaffold files exist with stub content matching the contracts documented in `<interfaces>`
- D-S10 scope decision recorded in `docs/testing/06-remote-probe-result.md` (hermes-and-claw OR hermes-only)
- No credentials leaked to committed files
</success_criteria>

<output>
After completion, create `.planning/phases/06-graphify-addon-code-graph/06-00-SUMMARY.md` with:
- What was installed (graphifyy version)
- D-S10 scope decision (hermes-and-claw or hermes-only) and rationale
- Any deviations from the stub templates (e.g., if a path differed)
</output>
