---
quick_id: 260530-gf1
slug: 260530-doc-housekeeping-4-issues-cleanup
description: 4 doc-only issues from .planning/ISSUES.md — #19 + #10 + #20 + #12
created: 2026-05-30
mode: quick
---

# Quick 260530-gf1 — Doc Housekeeping (4 issues)

## Goal

Clear 4 file-disjoint doc-tier issues from `.planning/ISSUES.md` in a single quick. All targets are documentation / comments / memory — zero behavioral change. File-disjoint with in-flight P2-3 T6 working tree.

## Constraints (from user prompt)

- A. No edits to `.py` / `.yaml` / `.json` / `kb/static` / `kb/templates` substantive content
- B. Atomic commits: 4 separate commits (one per issue) for review clarity
- C. No push (origin/main HEAD = `cc692e7`, P2-3 in flight 9 commits ahead — wait for P2-3)
- D. No edits inside P2-3 working tree (kb/api.py / kg_synthesize.py / kb/services/synthesize.py / kb/api_routers/search.py / requirements.txt / databricks-deploy/requirements.txt / pyproject.toml / tests/eval/* / tests/integration/kb/test_p2_p3_*)
- E. Chinese in conversation, simple language

## Tasks (atomic, sequential, one commit each)

### T1 — Issue #19 — CLAUDE.md PRINCIPLE #3 wiki cross-link exception

**File:** `CLAUDE.md`
**Action:** append a "例外 — wiki cross-link 反链" paragraph at the end of PRINCIPLE #3 block (before "4. Goal-Driven Execution")
**Verify:** `grep -A2 "wiki cross-link" CLAUDE.md` returns the new paragraph
**Done:** commit `docs(claude.md): PRINCIPLE #3 — wiki cross-link reverse-link exception`

### T2 — Issue #10 — KB_BASE_PATH asymmetry comments

**Files:**

- `databricks-deploy/deploy.sh` — comment after `set -euo pipefail` warning against copying KB_BASE_PATH=/kb here
- `kb/scripts/daily_rebuild.sh` — comment near the existing `KB_BASE_PATH:=/kb` line warning against copying empty default

**Verify:** `grep -n "ISSUES.md #10" databricks-deploy/deploy.sh kb/scripts/daily_rebuild.sh` returns hits in both
**Done:** commit `docs(deploy): KB_BASE_PATH asymmetry warnings (Databricks root vs Aliyun /kb)`

### T3 — Issue #20 — databricks-deploy/_ssg/.README.md build-artifact notice

**File:** `databricks-deploy/_ssg/.README.md` (new file, leading dot — hidden from `ls`)
**Action:** write the README.md per user-supplied content (DO NOT EDIT warning, regen mechanism, hot-patch path, upstream sources)
**Constraint:** `_ssg/` is gitignored at top-level via root .gitignore; this single file gets explicitly tracked via `git add -f`
**Verify:** file exists; `git check-ignore` confirms gitignored, `git status` after `add -f` shows it staged
**Done:** commit `docs(databricks-deploy): _ssg/.README.md — build artifact warning`

### T4 — Issue #12 — Memory v3+v4 merge

**File:** `~/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/aliyun_drift_recovery_260528_lessons.md`
**Action:** in Lesson 1, replace separate v3 + v4 sub-sections with single combined "Lesson 1 v3+v4: systemd cascade traps (bidirectional)" sub-section. Keep v1 + v2 untouched. Single paragraph explains Requires= bidirectional cascade. Lean unit template + corrected playbook + diagnostic checklist + observation timelines all preserved.
**Constraint:** memory file is OUTSIDE repo (under `~/.claude/projects/...`) — no commit needed; it's a separate workspace. But user wants atomic commit boundary, so this T4 will commit a marker / no-op since memory is out-of-tree. Actually re-reading user prompt: "atomic commits — 4 件事各 1 commit (4 commits 总), OR 合 2 commit". Memory is out-of-tree so cannot be in repo commit. Will document T4 in SUMMARY.md but the memory edit is filesystem-only.
**Verify:** `grep "v3+v4" ~/.claude/projects/.../memory/aliyun_drift_recovery_260528_lessons.md`
**Done:** memory file updated; no repo commit (out-of-tree); annotate in SUMMARY

### T5 — STATE.md + SUMMARY.md + final commit

**Action:** write SUMMARY.md, append row to STATE.md "Quick Tasks Completed" table, commit as 5th commit (or roll into T1 commit if 2-commit consolidation chosen — see commit-strategy below).

## Commit strategy

User offered "4 commits OR 合 2 commit (CLAUDE.md + 3 个 deploy/memory docs)". Going with **4 commits** (one per repo edit) plus a 5th `docs(quick-260530-gf1)` commit for STATE.md + SUMMARY.md + PLAN.md (standard quick close-out). Memory edit (T4) is out-of-tree, no commit.

So total = 5 commits. T4 has zero repo footprint.

## Files touched (final list, repo)

- `CLAUDE.md` (T1)
- `databricks-deploy/deploy.sh` (T2a)
- `kb/scripts/daily_rebuild.sh` (T2b)
- `databricks-deploy/_ssg/.README.md` (T3, new file, force-add)
- `.planning/STATE.md` (T5)
- `.planning/quick/260530-gf1-.../260530-gf1-PLAN.md` (T5, this file)
- `.planning/quick/260530-gf1-.../260530-gf1-SUMMARY.md` (T5)

## File-disjointness with P2-3 T6 working tree

P2-3 T6 in-flight files (per user prompt constraint D):

- kb/api.py
- kg_synthesize.py
- kb/services/synthesize.py
- kb/api_routers/search.py
- requirements.txt
- databricks-deploy/requirements.txt
- pyproject.toml
- tests/eval/*
- tests/integration/kb/test_p2_p3_*

This quick touches: CLAUDE.md, databricks-deploy/deploy.sh, kb/scripts/daily_rebuild.sh, databricks-deploy/_ssg/.README.md, .planning/STATE.md, .planning/quick/260530-gf1-.../*

→ **Zero overlap.** Safe.
