---
quick_id: 260529-d3p
description: Promote .scratch/deploy_inline_260528.sh to databricks-deploy/deploy.sh, Makefile delegates
mode: quick
date: 2026-05-29
---

# Quick Task 260529-d3p — Plan

## Task Description

Promote the verified `.scratch/deploy_inline_260528.sh` to a permanent `databricks-deploy/deploy.sh` and shrink the `Makefile` `deploy:` recipe (94 lines of bash) to a single-line bash delegation. This eliminates the recurring drift problem where transient `.scratch/deploy_inline_*.sh` copies fall behind the Makefile (cause of the 2026-05-27 arx-3 singleton REGRESSION: missing 3 critical `--include` flags).

## Background

| Fact | Reference |
| --- | --- |
| GNU `make` not available on Windows dev box | User statement, OmniGraph-Vault CLAUDE.md doesn't list `make` as a dependency |
| `.scratch/deploy_inline_260528.sh` GREEN-verified | Deploy log `.scratch/deploy-inline-20260528-195659.log` shows deployment ID `01f15aeb208f1cb09400bd1ea9ef4957` (matches user-cited `01f15aeb`) |
| Last `.scratch/` inline drift caused arx-3 REGRESSION | Header of `.scratch/deploy_inline_260528.sh` lines 8-13 documents this |
| HEAD = `e698cd4` (P5 closed clean) | `git log --oneline` |
| `.scratch/` is in `.gitignore` | Confirmed earlier in this session |

## Constraints

- A. **Inline byte content unchanged** — promote is path + comment-header rewrite only, not behavior change
- B. **Atomic commit** — `databricks-deploy/deploy.sh` + `databricks-deploy/Makefile` in one commit
- C. **No push** — wait for user review
- D. **Chinese explanations** — interactive language is Chinese
- E. **No `kb/static/` or `kb/templates/` changes** — Principle #9 doesn't apply but discipline preserved
- F. **`deploy-clean:` target untouched** — its existing `$(MAKE) deploy` recursion still works because the target name is preserved

## must_haves

| ID | Truth | Verify |
| --- | --- | --- |
| MH-1 | `databricks-deploy/deploy.sh` exists and is executable | `ls -la databricks-deploy/deploy.sh` shows `x` bit |
| MH-2 | `bash -n databricks-deploy/deploy.sh` exits 0 | Syntax sanity |
| MH-3 | `Makefile` `deploy:` target is single-line `bash` delegation | `grep -A 2 '^deploy:' Makefile` shows ≤3 lines |
| MH-4 | Other Makefile targets (deploy-clean / logs / logs-tail / stop / smoke / sp-grants) preserved | `grep -E '^(deploy-clean|logs|logs-tail|stop|smoke|sp-grants):' Makefile` shows all 6 |
| MH-5 | `.scratch/deploy_inline_260528.sh` removed | `ls .scratch/deploy_inline_260528.sh` returns ENOENT |
| MH-6 | `cd "$(dirname "$0")/.."` resolves to repo root from `databricks-deploy/` | `databricks-deploy/X.sh` + `..` = repo root, same as `.scratch/X.sh` + `..` = repo root |
| MH-7 | All 4 critical `--include` flags preserved (`_ssg/**`, `kg_synthesize.py`, `config.py`, `lib/**`) | grep on new `deploy.sh` |
| MH-8 | Atomic commit lands on `main` (no push) | `git log --oneline -1` shows commit; `git status -sb` shows `## main...origin/main [ahead 1]` |

## Tasks

### Task 1 — Promote inline to `databricks-deploy/deploy.sh`

- **files**: `databricks-deploy/deploy.sh` (new, copy of `.scratch/deploy_inline_260528.sh` with header rewrite)
- **action**:
  1. `cp .scratch/deploy_inline_260528.sh databricks-deploy/deploy.sh`
  2. `chmod +x databricks-deploy/deploy.sh`
  3. Edit header (lines 1-22) to reflect new SoT status — drop `.scratch` / "Transient inline" / "Used because make not installed" framing, replace with: `databricks-deploy/deploy.sh — primary deploy entry point. Makefile deploy: target delegates to this script.` Preserve the 4-include CRITICAL comment (lines 68-73 of original) verbatim — that's the regression-prevention.
  4. Verify `cd "$(dirname "$0")/.."` still resolves to repo root: `databricks-deploy/X.sh` → `dirname` = `databricks-deploy/` → `..` = repo root. Identical to `.scratch/X.sh` → `dirname` = `.scratch/` → `..` = repo root. **No code change to the cd line needed.**
- **verify**:
  - `bash -n databricks-deploy/deploy.sh && echo OK` exits 0
  - `ls -la databricks-deploy/deploy.sh` shows executable
  - `grep -c '\-\-include' databricks-deploy/deploy.sh` returns `4`
- **done**: New file exists, syntactically clean, 4 `--include` flags present, header reframed.

### Task 2 — Rewrite Makefile `deploy:` target as single-line delegation

- **files**: `databricks-deploy/Makefile`
- **action**: Replace `deploy:` target body (current lines 35-128, 94 lines of bash recipe) with:
  ```makefile
  deploy:
  	@echo ">>> Delegating to databricks-deploy/deploy.sh (primary entry point)"
  	bash $(CURDIR)/deploy.sh
  ```
  Preserve `.PHONY: deploy ...` line and ALL other targets (`deploy-clean`, `logs`, `logs-tail`, `stop`, `smoke`, `sp-grants`) byte-for-byte.
  Note on `$(CURDIR)`: when GNU make is invoked from `databricks-deploy/` (the typical pattern with `cd databricks-deploy && make deploy` or via `make -C databricks-deploy deploy`), `$(CURDIR)` = `databricks-deploy/`, so `$(CURDIR)/deploy.sh` = `databricks-deploy/deploy.sh`. The script's `cd "$(dirname "$0")/.."` then takes care of switching to repo root.
- **verify**:
  - `grep -A 2 '^deploy:' databricks-deploy/Makefile | head -5` shows the delegation, not the old recipe
  - `grep -E '^(deploy-clean|logs|logs-tail|stop|smoke|sp-grants):' databricks-deploy/Makefile` returns all 6 target lines
  - `wc -l databricks-deploy/Makefile` shows ~80 lines (was 170; -90 from deploy: collapse)
- **done**: Makefile shrunk to delegation, other targets untouched.

### Task 3 — Remove `.scratch/deploy_inline_260528.sh`

- **files**: `.scratch/deploy_inline_260528.sh` (delete)
- **action**: `.scratch/` is in `.gitignore` so `git rm` will fail with "did not match any files". Use plain `rm`:
  ```
  rm .scratch/deploy_inline_260528.sh
  ```
- **verify**: `ls .scratch/deploy_inline_260528.sh 2>&1 | grep -q "No such"` exits 0
- **done**: File gone. Not staged (was untracked).

### Task 4 — Atomic commit

- **files**: `databricks-deploy/deploy.sh` (new) + `databricks-deploy/Makefile` (modified)
- **action**:
  ```
  git add databricks-deploy/deploy.sh databricks-deploy/Makefile
  git commit -m "<message per task prompt>"
  ```
  Do NOT add `requirements.txt` (modified by unrelated work) or the .planning artifacts (separate commit at end of quick).
- **verify**:
  - `git log --oneline -1` shows new commit
  - `git status -sb` shows `## main...origin/main [ahead 1]` plus the unrelated dirty paths from before
  - `git diff HEAD~1 HEAD --stat` shows 2 files changed, ~95 deletions, ~95 insertions (net ~zero on file count, large delta on Makefile)
- **done**: One atomic commit on `main`, no push.

## Halt Triggers

- `bash -n` syntax check fails on new `deploy.sh` → STOP, fix syntax
- Makefile after rewrite has line count > 200 → STOP (likely accidentally deleted other targets)
- `git diff HEAD --stat` shows 3+ files in commit → STOP (committed too much)
- Header rewrite accidentally drops the 4-include CRITICAL comment → STOP (this is the regression-prevention guarantee)

## Out of Scope

- `deploy-clean:` cleanup — keep as-is, already works via `$(MAKE) deploy` recursion
- `logs` / `smoke` etc. — not touched
- Push — wait for user review

## key_links

- `.scratch/deploy_inline_260528.sh` (source, will be removed)
- `databricks-deploy/Makefile` (target of rewrite)
- `databricks-deploy/deploy.sh` (new file)
- `.scratch/deploy-inline-20260528-195659.log` (verification trail of GREEN P5 deploy)

## artifacts (post-execution)

- `databricks-deploy/deploy.sh` — primary deploy entry point
- `databricks-deploy/Makefile` — slim delegation target
- `.planning/quick/260529-d3p-.../260529-d3p-SUMMARY.md` — quick-task summary
