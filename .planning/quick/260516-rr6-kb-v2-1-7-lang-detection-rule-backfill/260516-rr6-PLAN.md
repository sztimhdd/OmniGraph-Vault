---
phase: 260516-rr6-kb-v2-1-7-lang-detection-rule-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb/data/lang_detect.py
  - kb/scripts/detect_article_lang.py
  - tests/unit/kb/test_lang_detect.py
  - tests/integration/kb/test_lang_backfill.py
  - .planning/quick/260516-rr6-kb-v2-1-7-lang-detection-rule-backfill/260516-rr6-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - kb-v2.1-7-LANG-RULE
  - kb-v2.1-7-BACKFILL
  - kb-v2.1-7-FTS-REBUILD
  - kb-v2.1-7-UAT
must_haves:
  truths:
    - "kb/data/lang_detect.py implements title-first CJK rule (title CJK -> zh-CN; else body CJK -> zh-CN; else short body -> unknown; else en)"
    - "Japanese kana-only and Korean Hangul-only titles do NOT classify as zh-CN (only Han/CJK Unified Ideographs count)"
    - "kb/scripts/detect_article_lang.py calls kb.data.lang_detect.detect_lang (no duplicated rule logic)"
    - ">=9 unit tests in tests/unit/kb/test_lang_detect.py covering all spec cases (CN-title-EN-body, EN-title-CN-body, all-EN, short-body, kana-title, empty, hangul, mixed-single-CJK, ext-A range)"
    - ">=2 integration tests in tests/integration/kb/test_lang_backfill.py covering script idempotency and no-op on already-correct rows"
    - "Full kb pytest suite passes (no regression in existing tests)"
    - ".dev-runtime/data/kol_scan.db backfill executed; before/after lang counts captured in SUMMARY"
    - "articles_fts virtual table rebuilt via kb.scripts.rebuild_fts after backfill"
    - "KB re-exported via kb/export_knowledge_base.py with fresh data-lang values on /articles/ cards"
    - "Local UAT confirms: English filter no longer surfaces Chinese-titled articles in first 10 cards"
    - "Local UAT confirms: 中文 filter correctly lists zh-CN articles"
    - "SUMMARY documents Hermes drift caveat (option a — accept short-term drift, Hermes NOT in scope)"
    - "STATE.md edit limited to v2.1-7 phase line (no concurrent v2.1-6 / kdb-1.5 lines touched)"
  artifacts:
    - path: "kb/data/lang_detect.py"
      provides: "Pure functional CJK-title-first language detection"
      contains: "_CJK_PATTERN"
      contains_alt: "detect_lang"
    - path: "tests/unit/kb/test_lang_detect.py"
      provides: ">=9 unit cases for new rule"
      min_test_count: 9
    - path: "tests/integration/kb/test_lang_backfill.py"
      provides: ">=2 integration cases for backfill driver"
      min_test_count: 2
    - path: ".planning/quick/260516-rr6-kb-v2-1-7-lang-detection-rule-backfill/260516-rr6-SUMMARY.md"
      provides: "Closure doc with before/after counts + UAT screenshots + Hermes drift caveat"
  key_links:
    - from: "kb/scripts/detect_article_lang.py"
      to: "kb/data/lang_detect.py"
      via: "from kb.data.lang_detect import detect_lang"
      pattern: "from kb\\.data\\.lang_detect import detect_lang"
    - from: "tests/unit/kb/test_lang_detect.py"
      to: "kb/data/lang_detect.py"
      via: "import detect_lang + has_cjk"
      pattern: "from kb\\.data\\.lang_detect import"
    - from: "kb/scripts/rebuild_fts.py"
      to: "articles.lang"
      via: "list_articles -> rec.lang -> articles_fts INSERT"
      pattern: "rec\\.lang"
---

<objective>
Replace `kb/data/lang_detect.py` "Chinese char ratio > 30%" rule with a title-first CJK detection rule that fixes the Aliyun production /kb/articles/ "English" filter mis-classification of Chinese-titled, English-term-heavy articles. Backfill `.dev-runtime/data/kol_scan.db`, rebuild FTS, re-export the static KB, and verify end-to-end with a Playwright browser UAT.

**Why this matters now (P1):** Aliyun production `/kb/articles/` filter has user-visible breakage. Chinese-titled articles (e.g., "如何用 LightRAG 构建知识图谱") are surfaced under the "English" filter because the body contains many English technical terms (LightRAG, embedding, vector). The new rule prioritizes title CJK presence, eliminating the failure class.

**Output:**
- Rewritten `kb/data/lang_detect.py` (pure functional, type-annotated, module docstring per spec)
- ≥9 unit tests + ≥2 integration tests (all green)
- Backfilled `.dev-runtime/data/kol_scan.db` with new lang values
- Rebuilt `articles_fts` and re-exported `/kb/articles/` cards with new `data-lang` attributes
- Browser UAT evidence (screenshots) confirming filter correctness
- SUMMARY closing with Hermes drift caveat (option a — out of scope)
</objective>

<concurrent_caveats>
**CRITICAL — concurrent agents may be running in parallel on this checkout:**

- **v2.1-6** may be editing: `kb/data/article_query.py`, `tests/integration/kb/test_image_rendering.py`, `kb/export_knowledge_base.py` — DO NOT TOUCH
- **kdb-1.5** may be editing: `databricks-deploy/` — DO NOT TOUCH
- **STATE.md edit must be surgical:** edit ONLY the v2.1-7 phase line. If push collision, `git pull --ff-only`. NEVER `git commit --amend`, `git reset --soft`, or `git rebase` (per `feedback_no_amend_in_concurrent_quicks.md` and `feedback_git_add_explicit_in_parallel_quicks.md`)
- **Use `git add <explicit-files>` always** — never `git add -A`/`git add .`
- **UAT port 8766 is shared** — acquire `.scratch/.uat-port-8766.lock` before starting `local_serve.py`. If lockfile exists (held by v2.1-6 UAT), wait or back off. Release lockfile after UAT.
- **Hermes drift:** option (a) — accept short-term drift between Aliyun (this fix) and Hermes prod. Hermes NOT in scope for this quick. Document in SUMMARY.
</concurrent_caveats>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@kb/data/lang_detect.py
@kb/scripts/detect_article_lang.py
@kb/scripts/rebuild_fts.py
</context>

<interfaces>
<!-- Current kb/data/lang_detect.py exports (to be REPLACED): -->
<!-- LangCode = Literal["zh-CN", "en", "unknown"] -->
<!-- chinese_char_ratio(text: str) -> float    [REMOVE — old rule] -->
<!-- detect_lang(text: str) -> LangCode        [REPLACE — new signature: detect_lang(title, body)] -->

<!-- New kb/data/lang_detect.py exports (per spec): -->
<!-- has_cjk(text: str | None) -> bool -->
<!-- detect_lang(title: str | None, body: str | None) -> Literal["zh-CN", "en", "unknown"] -->

<!-- BREAKING CHANGE: detect_lang signature changes from (text: str) to (title, body). -->
<!-- Caller in kb/scripts/detect_article_lang.py:62 currently calls detect_lang(body or "") — -->
<!-- MUST be updated to detect_lang(title or "", body or "") and SELECT must include title column. -->

<!-- kb/scripts/detect_article_lang.py current SELECT (line 58-60): -->
<!-- "SELECT id, body FROM {table} WHERE lang IS NULL" -->
<!-- MUST change to: -->
<!-- "SELECT id, title, body FROM {table} WHERE lang IS NULL" -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite lang_detect.py + update detect_article_lang.py caller + write unit tests</name>
  <files>kb/data/lang_detect.py, kb/scripts/detect_article_lang.py, tests/unit/kb/test_lang_detect.py</files>
  <behavior>
    Unit tests in tests/unit/kb/test_lang_detect.py (≥9 cases):
    1. test_chinese_title_english_body_returns_zh_cn — title="如何用 LightRAG 构建知识图谱", body="Use embedding + vector store..." (English-heavy) → 'zh-CN'
    2. test_english_title_chinese_body_returns_zh_cn — title="LightRAG Tutorial", body=200+ Chinese chars → 'zh-CN'
    3. test_all_english_returns_en — title="How to build a KB", body=200+ chars all English → 'en'
    4. test_short_english_body_returns_unknown — title="Hi", body="short" (<50 chars) → 'unknown'
    5. test_japanese_kana_title_returns_en_not_zh_cn — title="カタカナのテスト" (pure katakana), body=200 chars all English → 'en' (must NOT be zh-CN)
    6. test_empty_title_and_body_returns_unknown — title="", body="" → 'unknown'
    7. test_korean_hangul_title_returns_en_not_zh_cn — title="한국어 테스트" (pure Hangul), body=200 chars all English → 'en' (must NOT be zh-CN)
    8. test_mixed_title_single_cjk_returns_zh_cn — title="Build a 知识 graph", body=200 chars all English → 'zh-CN' (single Han char in title triggers zh-CN)
    9. test_extension_a_cjk_in_title_returns_zh_cn — title="Test 㐀 char" (U+3400, Extension A), body=any → 'zh-CN'

    Implementation in kb/data/lang_detect.py (per spec — pure-functional, type-annotated):
    - Module docstring describing the rule (in priority order)
    - `_CJK_PATTERN = re.compile(r"[一-鿿㐀-䶿]")` — covers U+4E00-U+9FFF (CJK Unified Ideographs) AND U+3400-U+4DBF (Extension A); explicitly EXCLUDES kana (U+3040-U+30FF) and Hangul (U+AC00-U+D7AF)
    - `has_cjk(text: str | None) -> bool` — empty / None safe; uses _CJK_PATTERN.search
    - `detect_lang(title: str | None, body: str | None) -> Literal["zh-CN", "en", "unknown"]` — implements 4-rule priority order
    - Use `from __future__ import annotations` and `from typing import Literal`
    - Keep `LangCode` type alias if it improves clarity, OR use inline `Literal` per spec — match spec exactly
    - REMOVE old `chinese_char_ratio` function (no longer needed; not exported anywhere outside this module per grep)

    Caller update in kb/scripts/detect_article_lang.py:
    - Line 58-60: `SELECT id, body FROM {table} WHERE lang IS NULL` → `SELECT id, title, body FROM {table} WHERE lang IS NULL`
    - Line 61-64: unpack 3-tuple `(row_id, title, body)`; call `detect_lang(title or "", body or "")` instead of `detect_lang(body or "")`
    - Update module docstring at top to reflect new rule (title-first)
    - DO NOT change `_TABLES`, `_ensure_lang_column`, `coverage_for_table`, or `main()` — these are unchanged
  </behavior>
  <action>
    Skill(skill="python-patterns", args="Pure-functional language detection module: PEP 8, type annotations, `from __future__ import annotations`, compiled regex at module load (not per-call), docstring per Google style, no I/O, no dependencies beyond stdlib `re` + `typing`. Match the reference implementation in the blueprint EXACTLY — no improvising signatures, no extra functions.")

    Skill(skill="writing-tests", args="pytest unit tests against pure functions: parametrize where it improves clarity (cases 1-4 and 5-9 are good parametrize candidates), assert_eq style (`assert detect_lang(...) == 'zh-CN'`), one assertion per test, descriptive test names matching the spec list. NO mocks — pure functions. Tests live in tests/unit/kb/test_lang_detect.py. Verify >=9 distinct cases.")

    Step 1 — Rewrite kb/data/lang_detect.py per spec:
    - Replace entire file with the reference implementation from blueprint
    - CJK regex MUST be exactly `[一-鿿㐀-䶿]` (covers U+4E00-U+9FFF + U+3400-U+4DBF; excludes kana and Hangul)
    - Module docstring MUST list rules in priority order with explicit kana/Hangul exclusion note

    Step 2 — Update kb/scripts/detect_article_lang.py caller:
    - Use Edit tool to change SELECT from `SELECT id, body` to `SELECT id, title, body`
    - Update the row unpack and `detect_lang(...)` call to pass `(title or "", body or "")`
    - Update top-of-file docstring to reflect new rule (title-first; include U+4E00-U+9FFF + U+3400-U+4DBF range note)
    - NO duplicated rule logic — must call `lang_detect.detect_lang` directly

    Step 3 — Write tests/unit/kb/test_lang_detect.py:
    - Create new file (or extend if exists — check first via Read)
    - Implement all 9 cases above
    - For body-length tests, generate body with explicit length: `body_en = "a " * 200` (>50 chars), `body_zh = "中" * 200`
    - Run tests: `venv/Scripts/python.exe -m pytest tests/unit/kb/test_lang_detect.py -v`
    - All ≥9 tests must PASS

    Step 4 — Verify caller is the only consumer:
    - Run grep for any other importer of `chinese_char_ratio` or the old `detect_lang(text)` signature elsewhere in kb/ — if found, surface in SUMMARY and consult user; do not silently break
    - Expected: only `kb/scripts/detect_article_lang.py` and tests import from `kb.data.lang_detect`
  </action>
  <verify>
    <automated>venv/Scripts/python.exe -m pytest tests/unit/kb/test_lang_detect.py -v --tb=short</automated>
  </verify>
  <done>
    - kb/data/lang_detect.py rewritten per spec (CJK regex 一-鿿 + 㐀-䶿 only; module docstring lists 4-rule priority; has_cjk + detect_lang exported)
    - kb/scripts/detect_article_lang.py SELECT includes title; calls detect_lang(title, body); no duplicated rule
    - tests/unit/kb/test_lang_detect.py has ≥9 cases, all PASS
    - grep confirms no other consumers of old chinese_char_ratio or 1-arg detect_lang signature
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests + DB backfill + FTS rebuild</name>
  <files>tests/integration/kb/test_lang_backfill.py</files>
  <behavior>
    Integration tests in tests/integration/kb/test_lang_backfill.py (≥2 cases):
    1. test_detect_article_lang_script_idempotent_on_dev_db — Use a tmp SQLite DB with `articles` (and optionally `rss_articles`) table seeded with 3-5 rows mixing Chinese-title, English-title, kana-title; invoke `detect_for_table(conn, "articles")`; assert returned Counter shows expected lang split; invoke a second time → returned Counter is empty (idempotent — WHERE lang IS NULL filters all already-classified rows)
    2. test_backfill_does_not_change_already_correct_lang — Seed row with `lang='zh-CN'` already set on a Chinese-title article; invoke `detect_for_table`; assert the row's lang stays 'zh-CN' (UPDATE skipped because WHERE lang IS NULL)

    Both tests use real SQLite (no mocks). Reuse `kb.scripts.detect_article_lang.detect_for_table` directly. Tests must be isolated (tmp_path fixture) so they do not touch `.dev-runtime/data/kol_scan.db`.
  </behavior>
  <action>
    Skill(skill="writing-tests", args="Integration tests against real SQLite via tmp_path: create DB schema matching kb production (id, title, body, lang nullable INTEGER PK with title TEXT, body TEXT, lang TEXT NULL), seed with explicit fixtures, invoke detect_for_table directly. Both tests assert on Counter return + post-state via SELECT. NO mocks. Match writing-tests Testing Trophy: integration here because the caller updates SQL state.")

    Step 1 — Write tests/integration/kb/test_lang_backfill.py:
    - Use `pytest` `tmp_path` fixture
    - Create minimal `articles` table with columns: `id INTEGER PRIMARY KEY, title TEXT, body TEXT, lang TEXT`
    - Seed fixtures (mix CN-title / EN-title / kana-title / pre-classified row)
    - Invoke `detect_for_table(conn, "articles")` from `kb.scripts.detect_article_lang`
    - Assert Counter return + `SELECT id, lang FROM articles` post-state
    - Run tests: `venv/Scripts/python.exe -m pytest tests/integration/kb/test_lang_backfill.py -v`
    - Both ≥2 tests must PASS

    Step 2 — Backup .dev-runtime DB:
    - `cp .dev-runtime/data/kol_scan.db .dev-runtime/data/kol_scan.db.backup-pre-rr6-$(date +%Y%m%d-%H%M%S)`
    - Verify backup file size > 0

    Step 3 — Capture BEFORE counts:
    - `venv/Scripts/python.exe -c "import sqlite3; c = sqlite3.connect('.dev-runtime/data/kol_scan.db'); print('articles before:', dict(c.execute('SELECT lang, COUNT(*) FROM articles GROUP BY lang').fetchall())); print('rss_articles before:', dict(c.execute('SELECT lang, COUNT(*) FROM rss_articles GROUP BY lang').fetchall()))"`
    - Save output for SUMMARY

    Step 4 — Reset lang to NULL so the new rule re-classifies all rows:
    - **NOTE on idempotency:** the script as-written only re-classifies WHERE lang IS NULL. To apply the new rule to already-classified rows, we must `UPDATE articles SET lang = NULL` (and same for rss_articles) BEFORE invoking the script. This is INTENTIONAL — the new rule changes assignments and the script is correct in its idempotent design.
    - `venv/Scripts/python.exe -c "import sqlite3; c = sqlite3.connect('.dev-runtime/data/kol_scan.db'); c.execute('UPDATE articles SET lang = NULL'); c.execute('UPDATE rss_articles SET lang = NULL'); c.commit(); print('reset rows:', c.execute('SELECT COUNT(*) FROM articles WHERE lang IS NULL').fetchone()[0] + c.execute('SELECT COUNT(*) FROM rss_articles WHERE lang IS NULL').fetchone()[0])"`

    Step 5 — Run backfill:
    - `venv/Scripts/python.exe -m kb.scripts.detect_article_lang`
    - Capture stdout — should print per-table updated dict + total_coverage dict

    Step 6 — Capture AFTER counts:
    - Same SQL as Step 3, save for SUMMARY

    Step 7 — Rebuild FTS:
    - `venv/Scripts/python.exe -m kb.scripts.rebuild_fts`
    - Expect single-line summary `[rebuild_fts] indexed N rows in X.XXs`

    Step 8 — Run full kb pytest to confirm no regression:
    - `venv/Scripts/python.exe -m pytest tests/unit/kb tests/integration/kb -v --tb=short`
    - All tests must PASS (any pre-existing failures should be flagged in SUMMARY but not block — note them separately)
  </action>
  <verify>
    <automated>venv/Scripts/python.exe -m pytest tests/integration/kb/test_lang_backfill.py tests/unit/kb -v --tb=short</automated>
  </verify>
  <done>
    - tests/integration/kb/test_lang_backfill.py has ≥2 cases, all PASS
    - .dev-runtime/data/kol_scan.db backed up with timestamped filename
    - BEFORE / AFTER lang count distributions captured (will be cited in SUMMARY in Task 3)
    - kb.scripts.detect_article_lang ran successfully, lang populated for both tables
    - kb.scripts.rebuild_fts ran successfully, indexed N rows
    - Full kb pytest suite (unit + integration) shows no regression
  </done>
</task>

<task type="auto">
  <name>Task 3: KB re-export + Local UAT (Playwright) + SUMMARY + STATE.md update + commit</name>
  <files>.planning/quick/260516-rr6-kb-v2-1-7-lang-detection-rule-backfill/260516-rr6-SUMMARY.md, .planning/STATE.md</files>
  <action>
    Step 1 — Acquire UAT port 8766 lockfile:
    - Check `.scratch/.uat-port-8766.lock` — if exists, wait/poll up to ~5 min (concurrent v2.1-6 may be using it). If still held, surface in SUMMARY and skip browser UAT (run a degraded curl-based smoke instead).
    - When clear: `echo "$$ rr6 $(date)" > .scratch/.uat-port-8766.lock`

    Step 2 — Re-export KB:
    - `KB_BASE_PATH=/kb venv/Scripts/python.exe kb/export_knowledge_base.py`
    - Capture stdout for SUMMARY (export row counts, output dir)

    Step 3 — Start local_serve.py in background:
    - `venv/Scripts/python.exe .scratch/local_serve.py &` (or via `nohup`/`Start-Process` per Windows convention)
    - Wait for port 8766 to be listening (`curl -s http://127.0.0.1:8766/health` — retry up to 10s)

    Step 4 — Browser UAT via Playwright MCP:
    - `mcp__playwright__browser_navigate` to `http://127.0.0.1:8766/articles/`
    - `mcp__playwright__browser_take_screenshot` → save to `.playwright-mcp/rr6-uat-articles-default-{ts}.png`
    - Click "English" filter (use `browser_snapshot` first to get refs)
    - `browser_take_screenshot` → `.playwright-mcp/rr6-uat-articles-english-{ts}.png`
    - Verify first 10 cards have NO obvious Chinese-titled articles (visual + check `data-lang` attributes via `browser_evaluate` → `Array.from(document.querySelectorAll('article[data-lang]')).slice(0,10).map(el => ({title: el.querySelector('h2,h3,a').textContent.trim(), lang: el.dataset.lang}))`)
    - Click "中文" filter
    - `browser_take_screenshot` → `.playwright-mcp/rr6-uat-articles-zh-{ts}.png`
    - Verify cards shown have `data-lang="zh-CN"` and titles contain CJK
    - Capture all evidence (screenshot paths + JS-evaluate results) for SUMMARY

    Step 5 — Stop local_serve.py + release lockfile:
    - Kill the background process (capture PID at start)
    - `rm .scratch/.uat-port-8766.lock`

    Step 6 — Smoke curl tests (corroborate browser UAT):
    - `curl -s http://127.0.0.1:8766/api/articles?lang=en | head -200` (if server still up — otherwise restart briefly)
    - Note: `/articles/` is static SSG output; if `/api/*` path requires server, run a brief restart for the curl tests, then stop.

    Step 7 — Write SUMMARY at .planning/quick/260516-rr6-kb-v2-1-7-lang-detection-rule-backfill/260516-rr6-SUMMARY.md:
    Required sections:
    - **Mission** — one-paragraph restatement
    - **Files Changed** — table of every file with one-line diff intent
    - **Test Evidence** — pytest output (unit + integration), exact test counts, all-green confirmation
    - **Backfill Evidence** — BEFORE/AFTER lang count tables for `articles` and `rss_articles`, total rows reclassified, rebuild_fts row count
    - **Local UAT** — port lockfile acquisition timestamp, screenshot paths under `.playwright-mcp/`, JS-evaluate results showing first-10 cards under English filter (titles + lang), 中文 filter sample
    - **Hermes Drift Caveat** — explicit option (a) statement: Aliyun production now applies new rule; Hermes prod still on old rule until separate deploy quick. Out of scope for rr6.
    - **Concurrent Safety Confirmation** — STATE.md edit limited to rr6 line; no v2.1-6 / kdb-1.5 file touched; explicit `git add` file list
    - **Acceptance Criteria Checklist** — every must_have item from frontmatter, marked ✅ with evidence pointer

    Step 8 — Update STATE.md:
    - Find or create the "Quick Tasks Completed" section (search for "Quick Tasks Completed" or similar header)
    - Add ONE row: `- 260516-rr6: kb-v2.1-7 lang detection rule + backfill (Aliyun prod fix; Hermes deferred — option a)` with current date
    - Edit ONLY this single row. DO NOT touch concurrent v2.1-6 / kdb-1.5 lines.
    - If section does not exist, create it as a leaf section (do not restructure adjacent sections).

    Step 9 — Commit (forward-only, explicit files):
    - `git add kb/data/lang_detect.py kb/scripts/detect_article_lang.py tests/unit/kb/test_lang_detect.py tests/integration/kb/test_lang_backfill.py`
    - `git add .planning/quick/260516-rr6-kb-v2-1-7-lang-detection-rule-backfill/260516-rr6-PLAN.md .planning/quick/260516-rr6-kb-v2-1-7-lang-detection-rule-backfill/260516-rr6-SUMMARY.md`
    - `git add .planning/STATE.md`
    - **DO NOT add re-exported KB SSG output** (kb/_site/ or wherever export writes) UNLESS the project convention is to commit it — check existing git status; if untracked but pre-existing tracked siblings exist for old export, follow same pattern. Surface decision in SUMMARY.
    - **DO NOT add `.playwright-mcp/*.png` UNLESS** project convention commits UAT screenshots — by default, screenshots are gitignored evidence (check .gitignore).
    - **DO NOT add `.dev-runtime/data/kol_scan.db*`** — runtime DB and backup never committed.
    - Commit message: `feat(kb-v2.1-7): title-first CJK lang detection + .dev-runtime backfill + FTS rebuild` (conventional commits format)
    - Commit body: brief 2-3 sentence summary, cite PLAN/SUMMARY paths, note Hermes drift caveat
    - **NEVER `git commit --amend`, `git reset`, `git rebase`** — forward-only
    - `git status` after commit to verify clean working tree (or only untracked screenshots/runtime files remain)

    Step 10 — If push attempted and collision detected:
    - `git pull --ff-only origin main` (or current branch)
    - If non-ff: surface in SUMMARY, ask user; do NOT force push, do NOT amend
  </action>
  <verify>
    <automated>git log -1 --oneline && git status --short && ls -la .planning/quick/260516-rr6-kb-v2-1-7-lang-detection-rule-backfill/260516-rr6-SUMMARY.md</automated>
  </verify>
  <done>
    - KB re-exported successfully with new data-lang values
    - Local UAT executed: ≥3 screenshots in .playwright-mcp/ (default + English + 中文 filters)
    - English filter first-10 cards confirmed free of obvious Chinese-titled articles (evidence cited in SUMMARY)
    - 中文 filter confirmed listing zh-CN articles only (evidence cited in SUMMARY)
    - Port 8766 lockfile released cleanly
    - SUMMARY written with all required sections (Mission, Files, Tests, Backfill, UAT, Hermes Drift, Concurrent Safety, Acceptance Checklist)
    - STATE.md updated with one new line for rr6 only
    - Commit landed via forward-only `git add <explicit-files>` + `git commit` (no amend/reset/rebase)
    - `git status` clean (or only expected untracked files: runtime DB backups, gitignored screenshots)
  </done>
</task>

</tasks>

<verification>
**Phase-level verification (re-run after all 3 tasks):**

1. Unit tests pass:
   `venv/Scripts/python.exe -m pytest tests/unit/kb/test_lang_detect.py -v` → ≥9 PASSED

2. Integration tests pass:
   `venv/Scripts/python.exe -m pytest tests/integration/kb/test_lang_backfill.py -v` → ≥2 PASSED

3. Full kb suite no regression:
   `venv/Scripts/python.exe -m pytest tests/unit/kb tests/integration/kb -v --tb=short` → all green (or pre-existing failures unrelated to rr6, documented)

4. Backfill applied:
   `venv/Scripts/python.exe -c "import sqlite3; c = sqlite3.connect('.dev-runtime/data/kol_scan.db'); print(dict(c.execute('SELECT lang, COUNT(*) FROM articles GROUP BY lang').fetchall()))"` → no NULL rows; lang split differs from BEFORE snapshot

5. FTS rebuilt:
   `venv/Scripts/python.exe -c "import sqlite3; c = sqlite3.connect('.dev-runtime/data/kol_scan.db'); print(c.execute('SELECT COUNT(*) FROM articles_fts').fetchone()[0])"` → row count > 0 and matches list_articles output

6. SSG re-export contains new data-lang attributes:
   `grep -rE 'data-lang="(en|zh-CN)"' <export-output-dir>/articles/ | head -5` → matches present

7. Browser UAT screenshots exist:
   `ls .playwright-mcp/rr6-uat-articles-*.png` → ≥3 files

8. Caller dedupe — no duplicated rule logic:
   `grep -rn "chinese_char_ratio\|ZH_THRESHOLD\|MIN_TEXT_LEN" kb/ tests/` → only references inside `kb/data/lang_detect.py` (if any retained for back-compat) — ideally zero in callers

9. Port lockfile released:
   `ls .scratch/.uat-port-8766.lock` → not exists (or held by a different concurrent agent, never us)

10. STATE.md surgical:
    `git diff HEAD~1 .planning/STATE.md` → only +1 line for rr6, no other changes

11. Commit attribution clean:
    `git log -1 --stat` → only rr6-scope files in the commit (no v2.1-6 / kdb-1.5 files swept in)
</verification>

<success_criteria>
- ≥9 unit tests + ≥2 integration tests, all green
- Full kb pytest suite no regression
- `.dev-runtime/data/kol_scan.db` backfilled — no NULL `lang` rows; before/after counts cited in SUMMARY
- `articles_fts` rebuilt successfully
- KB re-exported with new `data-lang` values; static `/articles/` shows correct cards under English / 中文 filters
- Local UAT (Playwright) executed with screenshot evidence; English filter does not surface obvious Chinese-titled articles in first 10 cards; 中文 filter shows zh-CN articles correctly
- SUMMARY documents Hermes drift caveat (option a — out of scope, accept short-term drift)
- STATE.md edit limited to rr6 phase line
- Commit made via forward-only `git add <explicit-files>` + `git commit`; no amend/reset/rebase
- Port 8766 lockfile acquired and released cleanly
</success_criteria>

<output>
After completion, ensure `.planning/quick/260516-rr6-kb-v2-1-7-lang-detection-rule-backfill/260516-rr6-SUMMARY.md` exists with:
- Mission, Files Changed, Test Evidence, Backfill Evidence (BEFORE/AFTER tables), Local UAT (screenshot paths), Hermes Drift Caveat, Concurrent Safety Confirmation, Acceptance Criteria Checklist
- All entries traceable to acceptance criteria in plan frontmatter `must_haves`
</output>
