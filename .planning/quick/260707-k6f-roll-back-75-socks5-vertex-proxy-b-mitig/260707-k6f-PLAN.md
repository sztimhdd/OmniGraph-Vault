---
quick_id: 260707-k6f
title: "Roll back #75 SOCKS5 vertex-proxy B-mitigation — Phase 1 (retire proxy, keep code + units)"
date: 2026-07-07
mode: quick
---

# Quick Task 260707-k6f — Roll back #75 SOCKS5 vertex-proxy (Phase 1)

## Trigger (met 2026-07-07)

#76 rollback conditions empirically confirmed on Aliyun (all read-only, IPv6 trap excluded):

| Criterion | Evidence |
|---|---|
| WG handshake < 30s | `wg show wg-gcp-sg` → `latest handshake: 10s ago` after tunnel traffic; transfer 857KiB recv / 1.44MiB sent growing |
| TCP:443 direct (non-timeout) | `curl -4 https://oauth2.googleapis.com/token` → `404 0.57s remote=74.125.20.95` |
| Google IP routes THROUGH tunnel | `ip route get 74.125.195.95` → `dev wg-gcp-sg src 10.0.0.2` |
| App-layer direct embedding | `OMNIGRAPH_EMBED_PROXY= python -c embedding_func(...)` → `EMBED OK dim=3072` (repeatable ×2) |

**IPv6 trap noted:** oauth2/aiplatform resolve to IPv6 by default but host has NO IPv6 egress (`curl -6` → `000`). Bare `curl` 404 comes from IPv4 happy-eyeballs fallback — always force `curl -4` + check `ip route`. (Recorded in memory `wg_gcp_sg_repaired_260707`.)

## Scope — Phase 1 ONLY (conservative phased rollback, user-approved 2026-07-07)

Retire the proxy at the runtime/ops layer. Leave code proxy-injection + systemd `Wants=` in place for a 1-week observation window (they auto-no-op when `OMNIGRAPH_EMBED_PROXY` is unset).

**DEFERRED to a follow-up quick after 1 week stable:**
- Delete `_make_client()` proxy-injection in `lib/lightrag_embedding.py` + `lib/vertex_gemini_complete.py`
- Remove `Wants=/After=omnigraph-vertex-proxy.service` from the 5 unit files
- Optionally delete the `omnigraph-vertex-proxy.service` unit file

## Reference

Full rollback procedure: `.planning/quick/260630-jgx-260630-hermes-vertex-egress-proxy/260630-jgx-DECISION.md` (lines 122-147).

## Tasks

### Task 1 — Retire the proxy on Aliyun (prod ops via `ssh aliyun-vitaclaw`)

**Steps (in order, each verified before next):**
1. **Backup** `.env`: `cp /root/.hermes/.env /root/.hermes/.env.bak-pre-rollback-260707` (verify byte-count matches original)
2. **Remove** the `OMNIGRAPH_EMBED_PROXY=` line: `sed -i '/^OMNIGRAPH_EMBED_PROXY=/d' /root/.hermes/.env`. **Keep** `NO_PROXY=` (harmless; DeepSeek/SiliconFlow bypass list). Verify `OMNIGRAPH_EMBED_PROXY` no longer greps.
3. **Disable + stop** the tunnel: `systemctl disable --now omnigraph-vertex-proxy.service`. Do **NOT** `rm` the unit file. Verify `is-active=inactive`, `is-enabled=disabled`, and port `18080` no longer LISTEN.
4. **Restart** the 5 Vertex-dependent services so they re-read the clean env:
   `omnigraph-daily-ingest`, `omnigraph-afternoon-ingest`, `omnigraph-evening-ingest`, `omnigraph-kol-classify`, `omnigraph-rss-layer2-classify`.
   (These are `.service` units triggered by timers — a manual `restart` on a `oneshot`/`simple` unit that isn't currently running just re-runs it; if any is a template not meant for manual start, note it and rely on the next timer fire instead. Do NOT force-fail the quick on a non-running oneshot.)

**Verify (`verify`):**
- `.env` no longer contains `OMNIGRAPH_EMBED_PROXY`, backup exists, `NO_PROXY` still present
- `omnigraph-vertex-proxy.service` → `inactive` + `disabled`, port 18080 not bound
- Unit file `/etc/systemd/system/omnigraph-vertex-proxy.service` still exists on disk

**Done (`done`):** proxy is stopped/disabled, env is clean, unit file preserved for observation window.

### Task 2 — Post-rollback embedding smoke via direct WG (prod verify)

**Steps:**
- Run embedding smoke WITH the live `.env` (proxy now absent — this is the real post-rollback path, not an inline-unset simulation):
  ```
  ssh aliyun-vitaclaw 'cd /root/OmniGraph-Vault && set -a && source /root/.hermes/.env && set +a && \
    timeout 90 /root/OmniGraph-Vault/venv-aim1/bin/python3 -c "
  import asyncio, numpy as np
  from lib.lightrag_embedding import embedding_func
  async def probe():
      try:
          r = await embedding_func([\"post-rollback direct WG verify\"])
          print(f\"EMBED OK dim={r.shape[-1]}\" if isinstance(r, np.ndarray) else f\"UNEXPECTED {type(r)}\")
      except Exception as e:
          print(f\"EMBED FAIL: {type(e).__name__}: {e}\")
  asyncio.run(probe())"'
  ```

**Verify (`verify`):** output is `EMBED OK dim=3072` — proving embedding works via direct WG tunnel with NO proxy env var set.

**Done (`done`):** direct-WG embedding confirmed working post-rollback; if FAIL, STOP and restore `.env` from backup + re-enable proxy (rollback-the-rollback), report to user.

### Task 3 — Docs: ISSUES.md (#76 Phase-1 done, #75 WG repaired) + STATE.md

**Steps:**
- ISSUES.md **#76**: annotate `Phase-1 DONE 2026-07-07 (260707-k6f)` — proxy disabled + env cleaned; code + `Wants=` cleanup DEFERRED to follow-up quick after 1-week observation. Keep row OPEN (cleanup pending).
- ISSUES.md **#75**: annotate `WG wg-gcp-sg REPAIRED (empirically confirmed 2026-07-07) — B-mitigation being retired via 260707-k6f Phase-1`. Downgrade severity note. Keep OPEN until code cleanup lands + 1 week sustained.
- Update ISSUES.md `Last updated:` line.
- STATE.md: add row to "Quick Tasks Completed" table + update "Last activity".

**Verify (`verify`):** ISSUES.md #76 + #75 rows show Phase-1 annotations; STATE.md has 260707-k6f row.

**Done (`done`):** tracker + state reflect Phase-1 rollback; deferred cleanup explicitly recorded.

## must_haves

- **truths:**
  - WG wg-gcp-sg is repaired (handshake<30s, curl -4 oauth2 non-timeout routed dev wg-gcp-sg, EMBED OK dim=3072 no-proxy)
  - Post-rollback embedding works via direct WG with OMNIGRAPH_EMBED_PROXY absent
- **artifacts:**
  - `/root/.hermes/.env.bak-pre-rollback-260707` (Aliyun backup)
  - `omnigraph-vertex-proxy.service` → inactive+disabled, unit file preserved
  - ISSUES.md #76/#75 Phase-1 annotations; STATE.md quick row
- **key_links:**
  - `.planning/quick/260630-jgx-260630-hermes-vertex-egress-proxy/260630-jgx-DECISION.md`
  - memory `wg_gcp_sg_repaired_260707`

## Rollback-the-rollback (if Task 2 FAILS)

`ssh aliyun-vitaclaw 'cp /root/.hermes/.env.bak-pre-rollback-260707 /root/.hermes/.env && systemctl enable --now omnigraph-vertex-proxy.service'` → re-verify EMBED OK → report failure to user (WG not actually stable). No local commit in that case.
