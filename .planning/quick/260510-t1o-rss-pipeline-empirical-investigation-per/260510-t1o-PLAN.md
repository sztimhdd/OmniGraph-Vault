---
phase: quick-260510-t1o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md
autonomous: true
requirements:
  - INV-RSS-01  # determine whether the "0 RSS ingestions=ok despite 546 bodies persisted" gap is ar-1 milestone scope or quick-fix scope

must_haves:
  truths:
    - "RSS-INVESTIGATION.md exists and contains the 5 required sections (call-site grep, source-branch grep, skip_reason query, layer1_verdict query, local dry-run)"
    - "The verdict line at the bottom of the file is one of: 'ar-1 milestone scope' / '~50 LOC quick scope' / 'middle ground' (with 1-2 sentence justification)"
    - "Every code claim cites a specific file:line; every DB claim cites the SQL run + raw output table"
    - "Production code, production DB, and the article scrape table are unchanged after the investigation runs"
  artifacts:
    - path: ".planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md"
      provides: "Empirical investigation findings with verdict on scope of fix"
      contains: "Layer 2 persistence call-sites, source-branch analysis, skip_reason histogram, layer1_verdict histogram, dry-run trace, verdict"
  key_links:
    - from: "investigation report"
      to: "Hermes production DB (read-only SELECT)"
      via: "ssh + sqlite3"
      pattern: "SELECT.*FROM (ingestions|rss_articles).*GROUP BY"
    - from: "investigation report"
      to: "production source code"
      via: "Grep tool with file:line citations"
      pattern: "(persist_layer2_verdicts|layer2_full_body_score|source.*=.*.rss.)"
---

<objective>
Empirically determine whether the "0 RSS ingestions=ok despite 546 RSS articles with body persisted" gap in production is a code-level milestone (ar-1 scope) or a small fixable bug (~50 LOC quick scope).

Purpose: STATE.md shows Phase 22 RSS work was SUPERSEDED-BY-ir4 (RSS pipeline retired), but production data still shows scrape succeeds and ingest never completes. We need ground-truth evidence — code call-sites + DB histograms + one local dry-run trace — before committing to either a milestone-scoped rewrite or a small fix.

Output: Single Markdown file at `.planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md` containing 5 sections + a one-line verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

# Hermes SSH connection details (host/port/user/auth)
@$HOME/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/hermes_ssh.md

# Source files the investigation will grep (read-only)
@batch_ingest_from_spider.py
@enrichment/rss_ingest.py
@lib/article_filter.py

<interfaces>
<!-- Key facts from STATE.md / CLAUDE.md the executor needs upfront — no spelunking required -->

Production reality (from STATE.md + task brief):
- v3.4 closed 2026-05-09; Phase 22 RSS work SUPERSEDED-BY-ir4 ("RSS pipeline retired")
- ir-4 deleted `enrichment/rss_ingest.py` and removed the step_7 RSS branch (commit `9ff330d`)
- BUT production DB still shows: 0 RSS ingestions with status='ok' AND 546 rss_articles with body IS NOT NULL
- This is the gap the investigation must explain

Corp-network constraints (CLAUDE.md):
- DeepSeek (`api.deepseek.com`) blocked locally → Layer 2 + LightRAG entity extraction don't run locally
- Vertex AI Gemini reachable → Layer 1 works locally
- Local DB at `.dev-runtime/data/kol_scan.db` is the production schema mirror but may not have RSS rows with body
- ALWAYS use `scripts/local_e2e.sh` for local invocations — NOT raw `python` (the harness handles env vars, TLS bundle, OMNIGRAPH_BASE_DIR)

Read-only Hermes pattern (CLAUDE.md "Remote Hermes Deployment"):
- ssh details in memory file `hermes_ssh.md`
- Repo path: `~/OmniGraph-Vault`; runtime data: `~/.hermes/omonigraph-vault/`
- Production DB path: `~/.hermes/omonigraph-vault/data/kol_scan.db` (or whatever the deployed path is — verify via env var or config)
- Run SELECT-only queries via `sqlite3 <path> "<query>"` over SSH; NEVER UPDATE/DELETE/INSERT
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 5 investigation steps and write 260510-t1o-RSS-INVESTIGATION.md</name>
  <files>.planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md</files>
  <action>
**This is a READ-ONLY investigation. Do NOT modify production code, DB rows, env files, or trigger any new article ingestion. Every command must be SELECT-only / grep / dry-run.**

Execute the 5 sub-steps in order, capture raw output for each, then write the final report.

---

### Step 1 — Layer 2 verdict persistence call-site grep

Use the Grep tool against the working tree:

```
Grep pattern: "persist_layer2_verdicts|layer2_full_body_score"
output_mode: content
-n: true
context: 5
```

For each match, identify:
- Caller file + line number
- Whether the call writes to the `articles` table or the `rss_articles` table (look at the SQL string nearby, or the function that wraps it)
- If the call site does NOT branch on `source` (i.e. blindly writes to one table), record that as a candidate root cause

Save raw grep output (truncate at 100 lines) for embedding in the report.

---

### Step 2 — `batch_ingest_from_spider.py` source-branch grep

Use the Grep tool:

```
Grep pattern: "source\\s*==\\s*['\"]rss['\"]|source\\s*=\\s*['\"]rss['\"]|args\\.source|row\\[['\"]source['\"]\\]"
path: batch_ingest_from_spider.py
output_mode: content
-n: true
context: 3
```

Also grep for the main loop entrypoint:

```
Grep pattern: "def main|if __name__"
path: batch_ingest_from_spider.py
output_mode: content
-n: true
context: 2
```

Identify:
- Whether the main loop branches on `source='rss'` at scrape stage / Layer 2 stage / ainsert stage
- If branches exist: do the RSS branches actually call into the same Layer 2 persistence path as KOL? (cross-reference Step 1 results)
- If no branches: does the script even handle RSS rows, or is RSS entirely outside its scope?

---

### Step 3 — Hermes production DB: skip_reason histogram (READ-ONLY SSH)

First read SSH details from `~/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/hermes_ssh.md` (host, port, user, key-or-auth). Then verify git state and find the production DB path:

```bash
ssh -p <port> <user>@<host> "cd ~/OmniGraph-Vault && git rev-parse --short HEAD && git status -sb"
ssh -p <port> <user>@<host> "ls -la ~/.hermes/omonigraph-vault/data/*.db 2>/dev/null || find ~/.hermes/omonigraph-vault -name '*.db' -type f 2>/dev/null | head -5"
```

Then run the SELECT (READ-ONLY — `sqlite3` opens read-write by default; pass `file:...?mode=ro` URI to be defensive):

```bash
ssh -p <port> <user>@<host> "sqlite3 'file:/home/<user>/.hermes/omonigraph-vault/data/kol_scan.db?mode=ro' -cmd '.timeout 5000' \"SELECT skip_reason, COUNT(*) AS n FROM ingestions WHERE source='rss' AND status='skipped' GROUP BY skip_reason ORDER BY n DESC;\""
```

If the `?mode=ro` URI form is rejected by the local sqlite3 build, fall back to the plain path BUT verify with `.databases` (no `.tables` mutation) and never run anything other than `SELECT`. Capture full raw output.

Edge cases to record explicitly:
- If `ingestions` table has no rows where `source='rss'`: that's a finding (RSS never reached ingest stage)
- If `skip_reason` is mostly NULL: re-query without the `WHERE status='skipped'` filter and show the status distribution instead

---

### Step 4 — Hermes production DB: layer1_verdict histogram (READ-ONLY SSH)

Same SSH connection, same DB path:

```bash
ssh -p <port> <user>@<host> "sqlite3 'file:/home/<user>/.hermes/omonigraph-vault/data/kol_scan.db?mode=ro' \"SELECT layer1_verdict, COUNT(*) AS n FROM rss_articles WHERE body IS NOT NULL AND layer2_verdict IS NULL GROUP BY layer1_verdict ORDER BY n DESC;\""
```

Capture full raw output. Edge cases:
- If `layer1_verdict` column does not exist on `rss_articles`: that itself is the finding — record `PRAGMA table_info(rss_articles);` output instead
- If the count is much smaller than 546: explain the gap (some bodies have layer2_verdict set already, etc.)

---

### Step 5 — Local dry-run on one RSS article

Per CLAUDE.md: use `scripts/local_e2e.sh`, NOT raw `python`. First check whether the local DB has any RSS row with `body IS NOT NULL`:

```bash
sqlite3 'file:.dev-runtime/data/kol_scan.db?mode=ro' "SELECT COUNT(*) FROM rss_articles WHERE body IS NOT NULL LIMIT 1;"
```

**Decision branch:**
- If count == 0: skip Step 5 with an explicit note in the report ("local DB has no RSS rows with body — Step 5 skipped, see report Section 5"). Document what local mode would have exercised, and why local can't reproduce (per CLAUDE.md: DeepSeek blocked → Layer 2 unreachable locally; Layer 1 works → Vertex Gemini reachable).
- If count > 0: pick one RSS URL from a body-bearing row and run a dry-run via the harness:

  ```bash
  ./scripts/local_e2e.sh help                      # confirm available modes
  # If `rss --dry-run` exists:
  ./scripts/local_e2e.sh rss --dry-run --max-articles 1 2>&1 | tee .scratch/t1o-step5-dryrun.log
  # OR if a single-URL mode exists:
  ./scripts/local_e2e.sh wechat <url> 2>&1 | tee .scratch/t1o-step5-dryrun.log   # NOTE: wechat mode goes through KOL path, NOT RSS path — only useful as comparator
  ```

  Trace where the article "dies" (which stage prints the last log line before exit / blocks). Cite the file:line of the gate. The expected death points (per CLAUDE.md):
  - Stage ② if `rss_articles.depth IS NULL` (corp-DeepSeek blocks classify)
  - Stage ⑥ on LightRAG entity extraction (DeepSeek hardcoded at `enrichment/rss_ingest.py:367` per CLAUDE.md — but that file may have been deleted in ir-4; verify with `ls enrichment/rss_ingest.py`)

  If `enrichment/rss_ingest.py` does NOT exist (deleted in ir-4), record that as the headline finding: "RSS ingest entrypoint deleted in ir-4; no production code path exists to consume the 546 body-bearing rows."

---

### Write the report

Path: `.planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md`

Required sections (in order):

```markdown
# RSS Pipeline Empirical Investigation (260510-t1o)

**Date:** 2026-05-10
**Scope:** Read-only. No production code, DB, or env changes.
**Question:** Is the "0 RSS ingestions=ok despite 546 bodies persisted" gap an ar-1 milestone or a ~50 LOC quick fix?

## 1. Layer 2 verdict persistence call-sites

[Raw grep output, then bullet list of: file:line, target table (`articles` or `rss_articles`), source-aware (Y/N)]

## 2. batch_ingest_from_spider.py source-branch analysis

[Raw grep output, then 1-paragraph summary: where does it branch on source, where does it not, does it handle RSS at all]

## 3. Production DB — `ingestions` skip_reason histogram (source='rss')

[Raw query + raw output table; 1-paragraph interpretation]

## 4. Production DB — `rss_articles` layer1_verdict histogram (body present, layer2 missing)

[Raw query + raw output table; 1-paragraph interpretation]

## 5. Local dry-run trace

[Either: full death-point trace with file:line citation, OR explicit skip-with-reason note per CLAUDE.md corp-network constraints]

## Verdict

**One line:** [`ar-1 milestone scope` / `~50 LOC quick scope` / `middle ground`]

**Justification (1-2 sentences):** [Why the evidence points to that scope. Reference the specific finding(s) from sections 1-5 that drove the call.]

## Appendix: SSH commands run

[Verbatim ssh commands for Steps 3 + 4 — for reproducibility]
```

**Verdict heuristic** (use this to pick the one-line answer):
- **`ar-1 milestone scope`** if: RSS ingest entrypoint deleted (ir-4) AND no source-branch in `batch_ingest_from_spider.py` AND Layer 2 persistence is KOL-only — i.e. consuming the 546 bodies requires building a new RSS ingest path.
- **`~50 LOC quick scope`** if: persistence call-sites just need an `if source=='rss'` branch added to write to `rss_articles.layer2_verdict` instead of `articles.layer2_verdict`, AND the rest of the pipeline already handles RSS rows.
- **`middle ground`** if: some plumbing exists (e.g. branches in scrape but not Layer 2 persistence), so it's bigger than ~50 LOC but smaller than a milestone — call out the rough effort estimate.

**Anti-patterns to avoid:**
- DO NOT execute any DB UPDATE / INSERT / DELETE — even "harmless" test rows.
- DO NOT run a real ingest (`./scripts/local_e2e.sh rss --max-articles N` without `--dry-run` triggers actual scrape + Apify cost). The Step 5 invocation MUST include `--dry-run`.
- DO NOT modify `~/.hermes/.env` on Hermes or any local env file.
- DO NOT speculate. If the evidence is ambiguous, say so in the verdict ("middle ground") rather than picking a side.
- Cite file:line for every code claim. Cite the raw query + output for every DB claim.
  </action>
  <verify>
    <automated>test -f .planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md && grep -E "^## (1\\.|2\\.|3\\.|4\\.|5\\.|Verdict)" .planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md | wc -l | grep -q "^6$" && grep -E "^\\*\\*One line:\\*\\* \\[?(ar-1 milestone scope|~50 LOC quick scope|middle ground)" .planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md</automated>
  </verify>
  <done>
    RSS-INVESTIGATION.md exists with all 6 required sections (1-5 + Verdict), the verdict line uses one of the three allowed values, every code claim cites file:line, every DB claim cites raw query + output, and Step 5 either has a death-point trace or an explicit corp-network skip note. No production code, DB rows, or env files were modified.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:
1. `cat .planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md | head -100` — confirm structure
2. Spot-check: pick 2 random code claims in the report, follow the file:line citation, confirm it matches
3. Spot-check: confirm SSH commands in the appendix are SELECT-only (no UPDATE/INSERT/DELETE keywords)
4. Confirm Hermes git state was unchanged: `ssh -p <port> <user>@<host> "cd ~/OmniGraph-Vault && git status -sb"` should match the pre-investigation snapshot taken in Step 3
</verification>

<success_criteria>
- File exists at `.planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/260510-t1o-RSS-INVESTIGATION.md`
- All 5 investigation sections + Verdict section present
- Verdict line is exactly one of: `ar-1 milestone scope` / `~50 LOC quick scope` / `middle ground`
- Every code claim has a file:line citation; every DB claim has raw query + raw output
- Zero production-side mutations: Hermes git status unchanged, production DB row counts unchanged for `ingestions` and `rss_articles`, no new article scrapes or ingests triggered
- Total wall-clock time 15-30 min
</success_criteria>

<output>
After completion, the deliverable IS the SUMMARY — `260510-t1o-RSS-INVESTIGATION.md` is both the artifact and the report. No separate SUMMARY.md needed for this read-only quick.

Commit at end:
```bash
git add .planning/quick/260510-t1o-rss-pipeline-empirical-investigation-per/
git commit -m "docs(quick-260510-t1o): RSS pipeline empirical investigation"
```
</output>
