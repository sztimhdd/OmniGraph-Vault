---
phase: quick-260505-s1h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/research/scraper_layer_shape_audit_2026_05_05.md
autonomous: true
requirements:
  - AUDIT-01  # Consumer-expectation table for lib/scraper.py::_scrape_wechat
  - AUDIT-02  # Per-layer return-shape tables (apify/cdp/mcp/ua)
  - AUDIT-03  # Cross-mismatch findings + severity classification
  - AUDIT-04  # Recommendations (no code changes)

must_haves:
  truths:
    - "Report file exists at docs/research/scraper_layer_shape_audit_2026_05_05.md"
    - "Report contains a Consumer Expectations table extracted from lib/scraper.py:_scrape_wechat (lines ~160-213)"
    - "Report contains 4 per-layer return-shape tables: Apify (reference only), CDP, MCP, UA"
    - "Report contains a Mismatch Findings table with severity icons (🔴/🟡/⚪)"
    - "Report contains a TL;DR (≤3 lines) and a Recommendations section"
    - "No source code under lib/ or ingest_wechat.py is modified"
    - "Exactly one commit is created with the mandated message"
    - "Commit is pushed to origin/main"
  artifacts:
    - path: "docs/research/scraper_layer_shape_audit_2026_05_05.md"
      provides: "Read-only audit report of scraper return-shape mismatches"
      contains: "Consumer Expectations"
  key_links:
    - from: "docs/research/scraper_layer_shape_audit_2026_05_05.md"
      to: "lib/scraper.py:_scrape_wechat (lines 160-213)"
      via: "Consumer Expectations table cross-referenced by line number"
      pattern: "lib/scraper\\.py.*160|_scrape_wechat"
    - from: "docs/research/scraper_layer_shape_audit_2026_05_05.md"
      to: "ingest_wechat.scrape_wechat_{cdp,mcp,ua}"
      via: "Per-layer return-shape tables cross-referenced by function name"
      pattern: "scrape_wechat_(cdp|mcp|ua)"
---

<objective>
Read-only source-code audit of the four scraper fallback layers (apify, cdp,
mcp, ua) versus the consumer in lib/scraper.py::_scrape_wechat. Find return-
shape mismatches in the same class as the SCR-06 bug fixed earlier today
(consumer reads "content_html" but Apify returns "markdown" → silent data
loss on 121 articles), classify each mismatch by severity, and write ONE
markdown report.

Purpose: De-risk tomorrow's 06:00 ADT cron by surfacing latent bugs in the
CDP/MCP/UA fallbacks BEFORE Apify failures cascade through them in
production. Audit-only — no fixes, no runtime calls, no Hermes SSH.

Output: docs/research/scraper_layer_shape_audit_2026_05_05.md +
one atomic commit pushed to origin/main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@lib/scraper.py
@ingest_wechat.py

<interfaces>
<!-- Consumer site to audit. Read these line ranges first. -->

lib/scraper.py:_scrape_wechat (approx lines 155-213) — the consumer.
Key behaviors observed in the file:
  - Iterates fn_name in ("scrape_wechat_apify", "scrape_wechat_cdp",
    "scrape_wechat_mcp", "scrape_wechat_ua")
  - Reads result.get("content_html") and result.get("markdown")
  - Reads result.get("images"), result.get("title"),
    result.get("publish_time"), result.get("method")
  - SCR-06 short-circuit: if "markdown" present and "content_html" absent,
    use markdown directly + result.get("images")
  - Else calls ingest_wechat.process_content(content_html) → (markdown, imgs)
  - Returns ScrapeResult(...) on first non-None layer
  - Falls back to summary_only=True if all 4 layers return None/empty

ScrapeResult dataclass fields (read from lib/scraper.py top of file):
  markdown: str
  images: list[...] (default empty)
  metadata: dict
  method: str
  summary_only: bool
  content_html: str | None

Layers to audit in ingest_wechat.py:
  - scrape_wechat_apify  (reference only — already fixed via SCR-06)
  - scrape_wechat_cdp    (local Edge CDP path; production fallback)
  - scrape_wechat_mcp    (remote Playwright MCP path; testing fallback)
  - scrape_wechat_ua     (raw UA HTTP path; last resort)

For each: trace ALL return paths and document the dict shape (key names,
value types, defaults, presence conditions). Note whether the layer calls
ingest_wechat.process_content() itself or returns raw content_html /
markdown for the consumer to process.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit source + draft report</name>
  <files>docs/research/scraper_layer_shape_audit_2026_05_05.md</files>
  <action>
Read-only source audit. DO NOT modify lib/scraper.py or ingest_wechat.py.
DO NOT call any scrape_* function. DO NOT SSH to Hermes. DO NOT git pull
or git fetch — audit current local working tree.

Step 1 — Consumer expectations:
  Read lib/scraper.py around lines 155-213 (the _scrape_wechat function +
  ScrapeResult dataclass). Build a Consumer Expectations table with
  columns: Key | Type | Required? | Used for | Source line. Include every
  result.get(...) call. Note the SCR-06 short-circuit branch explicitly.

Step 2 — Per-layer return shapes:
  For each of scrape_wechat_apify / scrape_wechat_cdp / scrape_wechat_mcp /
  scrape_wechat_ua in ingest_wechat.py:
    - Locate the function definition.
    - Trace EVERY return path: success, partial-success, exception caught,
      empty-content, fallback. Record line numbers.
    - For each return path, document the dict literal: key names, value
      types, possible None/empty, whether process_content() was called
      pre-return.
    - Note any side effects (writes to disk, checkpoint markers) — only
      relevant if they affect the return shape.

Step 3 — Cross-compare:
  For each layer, compare its return-shape rows against the consumer
  expectations table. Tag each mismatch with severity:
    - 🔴 silent data loss (e.g., Apify returns "markdown" but consumer
      only checked "content_html" pre-SCR-06 — the bug pattern)
    - 🟡 incorrect fallback path / needs runtime verification
    - ⚪ harmless quirk (e.g., extra debug key the consumer ignores)

Step 4 — Write the report at
docs/research/scraper_layer_shape_audit_2026_05_05.md with this structure:

  # Scraper Layer Return-Shape Audit (2026-05-05)

  ## TL;DR
  (≤3 lines: how many mismatches found, worst severity, primary
  recommendation.)

  ## Audit Scope
  (Files audited, audit method, what is OUT of scope: Apify deep dive,
  any code changes, any runtime calls.)

  ## Consumer Expectations — lib/scraper.py:_scrape_wechat
  (Table from Step 1.)

  ## Per-Layer Return Shapes
  ### Apify (reference only — fixed via SCR-06)
  ### CDP (scrape_wechat_cdp)
  ### MCP (scrape_wechat_mcp)
  ### UA (scrape_wechat_ua)
  (Each section: function location, return-paths table, notes.)

  ## Mismatch Findings
  (Single table: # | Layer | Consumer expects | Layer returns | Severity |
  Notes. Sort by severity descending.)

  ## Recommendations
  (Bullet list. NO code changes — only verification suggestions, runtime
  probes, or queue-for-future-phase items. Mark any 1-line fixes as
  "DEFER — see plan-phase, not this audit".)

  ## Limitations
  (Anything marked INCONCLUSIVE due to source complexity within budget.)

Failure handling:
  - If a layer's source is too tangled (sync wrapper around async, multi-
    layer protocol abstraction like _MCPClient), spend ≤30 min on it then
    write "INCONCLUSIVE: source complexity beyond audit budget" in that
    layer's section + describe approximate behavior + move on.
  - Mismatch with uncertain severity → mark 🟡 + "needs runtime verification".
  - 1-line-fixable mismatch found → DO NOT fix; capture in Recommendations.

Total time budget: 90-120 min. Time-box per layer: 30 min max.
  </action>
  <verify>
    <automated>test -f docs/research/scraper_layer_shape_audit_2026_05_05.md && grep -E "^## (TL;DR|Consumer Expectations|Per-Layer Return Shapes|Mismatch Findings|Recommendations)" docs/research/scraper_layer_shape_audit_2026_05_05.md | wc -l | grep -E "^\s*5\s*$"</automated>
  </verify>
  <done>
Report file exists at the expected path. All five required H2 sections
present (TL;DR, Consumer Expectations, Per-Layer Return Shapes, Mismatch
Findings, Recommendations). Each of the 4 layers has its own subsection
under "Per-Layer Return Shapes". Mismatch Findings table contains at least
one row OR explicitly states "No mismatches found". No source files
under lib/ or ingest_wechat.py are modified (verify with `git status` —
only the new docs file should be untracked).
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit + push + stdout summary</name>
  <files>docs/research/scraper_layer_shape_audit_2026_05_05.md</files>
  <action>
Sanity check before committing:
  - Run `git status` — confirm ONLY docs/research/scraper_layer_shape_audit_2026_05_05.md
    is new/modified. If anything under lib/ or ingest_wechat.py is dirty,
    STOP and surface the discrepancy to the user.
  - Run `git diff --stat` to confirm zero changes to source code.

Stage the single file:
  git add docs/research/scraper_layer_shape_audit_2026_05_05.md

Commit with the EXACT mandated message (no co-author trailer, no body):
  git commit -m "docs(research): scraper layer return shape audit — find SCR-06-class latent bugs"

Push to origin/main:
  git push origin main

Print to stdout:
  - Total mismatches found, broken down by severity (🔴/🟡/⚪ counts)
  - One-line summary of the highest-severity finding (or "no mismatches" if
    none)
  - The commit SHA
  - The path to the report file
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -F "docs(research): scraper layer return shape audit — find SCR-06-class latent bugs" && git status --porcelain | grep -v "^??" | wc -l | grep -E "^\s*0\s*$"</automated>
  </verify>
  <done>
Exactly one new commit on main with the mandated message. Working tree
clean (no unstaged source changes). Push to origin/main succeeded. Stdout
summary printed with mismatch counts + worst finding + commit SHA + report
path.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run after both tasks complete):
  1. `git log -1 --pretty=fuller` shows the audit commit on main.
  2. `git diff HEAD~1 HEAD --stat` shows ONLY one file changed:
     docs/research/scraper_layer_shape_audit_2026_05_05.md.
  3. `git diff HEAD~1 HEAD -- lib/ ingest_wechat.py` returns empty (no
     source changes).
  4. The report's TL;DR is ≤3 lines.
  5. The report's Mismatch Findings section uses 🔴/🟡/⚪ severity icons
     consistently.
</verification>

<success_criteria>
  - Single new file at docs/research/scraper_layer_shape_audit_2026_05_05.md
  - Report has all 5 required H2 sections + 4 per-layer subsections
  - Zero modifications to lib/scraper.py or ingest_wechat.py
  - Exactly 1 commit, exact mandated message, pushed to origin/main
  - Stdout summary printed (mismatch count by severity + worst finding)
  - Audit completed within 90-120 min wall clock; any layer marked
    INCONCLUSIVE if it exceeded its 30-min budget
</success_criteria>

<output>
After completion, no SUMMARY.md is required (this is a quick task, not a
phase). The audit report itself IS the deliverable. Stdout summary
satisfies the operator-facing reporting need.
</output>
