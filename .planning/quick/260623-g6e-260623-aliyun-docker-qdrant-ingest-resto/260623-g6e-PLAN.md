---
phase: quick-260623-g6e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "REMOTE aliyun:/etc/systemd/system/omnigraph-daily-ingest.service (start/stop only, no edit)"
  - "REMOTE aliyun: docker-ce engine packages (apt install)"
  - "REMOTE aliyun:/var/lib/qdrant (mount into qdrant container — NO wipe, NO edit)"
  - "REMOTE aliyun: docker image qdrant/qdrant:v1.11.0 (pull)"
autonomous: false
requirements: [DOCKER-RESTORE, QDRANT-UP, INGEST-VERIFY, DISK-PRUNE]
operator_note: "Prod-ops recovery — orchestrator executes all steps directly via SSH alias `aliyun-vitaclaw` (Principle #5 agent-is-operator). ZERO local repo edits. This plan is the authoritative ordered runbook. All timestamps CST (UTC+8)."

must_haves:
  truths:
    - "Docker engine is installed, active, and survives reboot (systemctl is-active docker = active)"
    - "Qdrant container is Up with restart policy unless-stopped, reusing the existing 6/16 data volume"
    - "Qdrant serves the 3 existing collections with non-zero point counts (no data loss, no reingest)"
    - "A real ingest run reaches LightRAG ainsert with NO 'Connection refused' and lands >=1 article (chunks count increases)"
    - "The 10-minute restart-loop on omnigraph-daily-ingest has stopped (restart counter no longer growing)"
    - "long_form RAG retrieval returns real graph-grounded content (graph is queryable, not a stub/fallback)"
    - "Disk usage on / drops after pruning orphan containerd image layers (85% -> target <70%)"
  artifacts:
    - path: "REMOTE: docker-ce / docker-ce-cli / docker-buildx-plugin / docker-compose-plugin (dpkg -l = ii)"
      provides: "Container runtime engine layered on the already-running containerd.io"
      contains: "docker-ce"
    - path: "REMOTE: docker container named 'qdrant' (docker ps)"
      provides: "Running Qdrant vector DB on :6333/:6334 with /var/lib/qdrant mounted"
      contains: "qdrant/qdrant:v1.11.0"
    - path: "REMOTE:/var/lib/qdrant/collections/ (3 collections, suffix _gemini_embedding_2_3072d)"
      provides: "Pre-existing 6/16 vector data (chunks/entities/relationships) — reused, not rebuilt"
      contains: "lightrag_vdb_chunks_gemini_embedding_2_3072d"
  key_links:
    - from: "omnigraph-daily-ingest (batch_ingest_from_spider.py --from-db --max-articles 5)"
      to: "Qdrant http://127.0.0.1:6333 (LightRAG ainsert / qdrant vector storage)"
      via: "QDRANT_URL env + httpx client during ainsert"
      pattern: "no ConnectError: Connection refused in journalctl; ainsert commits"
    - from: "Qdrant container"
      to: "/var/lib/qdrant on host"
      via: "docker -v /var/lib/qdrant:/qdrant/storage mount"
      pattern: "collections served from existing data, RestartPolicy=unless-stopped"
---

<objective>
Recover the Aliyun ECS instance `iZj1imk39yc55iZ` (EIP 47.117.244.253, cn-shanghai) from a 7-day knowledge-graph ingest restart-loop.

**Root cause (diagnosed, LOCKED in CONTEXT.md):** the 6/17 instance rebuild left Docker engine half-installed — `containerd.io` is installed and running, but `docker-ce`/`docker-ce-cli` were never installed. Without a Docker daemon, the Qdrant vector DB container never started, so every ingest fails at the LightRAG `ainsert` stage with `httpx.ConnectError: [Errno 111] Connection refused`. The `omnigraph-daily-ingest` unit (`Restart=on-failure RestartSec=10min`) has burned ~351 restarts since 6/17.

**Purpose:** restore the full scan -> classify -> LightRAG ingest -> synthesis pipeline by completing the Docker install and bringing Qdrant up on the existing data, then prove it end-to-end and reclaim disk.

**Output:** docker-ce installed + enabled; Qdrant container Up (`--restart=unless-stopped`, reusing `/var/lib/qdrant`); restart-loop stopped; one verification ingest landed; long_form RAG smoke passes; orphan containerd layers pruned.

**Execution model:** prod-ops only. The orchestrator runs every command itself via SSH (`aliyun-vitaclaw`). NO local repo edits, NO git code commits for the fix. This file is the copy-exact runbook.
</objective>

<execution_context>
Operator channel: SSH alias `aliyun-vitaclaw` (already points to new EIP). Run all commands as root on the Aliyun box.

Env loading for any MANUAL python invocation (NOT needed for `systemctl start`, which uses EnvironmentFile): prefix with
`set -a; source /root/.hermes/.env; set +a`
(memory `aliyun_ssh_manual_trigger_env` — else `DEEPSEEK_API_KEY=dummy` -> silent 401).

Timezone: all timestamps CST (UTC+8). Aliyun system tz is CST; user local is ADT (UTC-3), 11h delta (memory `timezone_drift_adt_vs_cst`).

Key env on box: `OMNIGRAPH_VECTOR_STORAGE=qdrant`, `QDRANT_URL=http://127.0.0.1:6333`, `EMBEDDING_MODEL=gemini-embedding-2`.

SSH banner-throttle note: if `:22` returns banner-throttle/connection-refused intermittently, it self-heals in 1-5h; use a `until ssh ...; sleep 180` background poll with a 60-min ceiling rather than foreground retry-storms (memory `feedback_ssh_throttle_poll_loop`). Do NOT confuse SSH banner-throttle with the Qdrant `Connection refused` — they are unrelated.
</execution_context>

<context>
@.planning/quick/260623-g6e-260623-aliyun-docker-qdrant-ingest-resto/260623-g6e-CONTEXT.md

REMOTE authoritative recipe: /root/OmniGraph-Vault/INVENTORY.md
  - lines 179-182: docker run recipe (qdrant/qdrant:v1.11.0, --restart=unless-stopped, -v /var/lib/qdrant:/qdrant/storage)
  - line 350: image tag caveat (v1.11.0 default; confirm via collection config.json version)
  - line 312: registry-unreachable fallback (docker save/load offline; ask user if no offline tar)
  - line 337: --restart=unless-stopped is mandatory (6/7 outage 35h root cause)

REMOTE rollback ref: /root/.hermes/omonigraph-vault/README.qdrant-migration.txt (L1 env-flip nanovectordb rollback if Qdrant cannot be brought up at all)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop the restart-loop (FIRST — before any other change)</name>
  <files>REMOTE aliyun:/etc/systemd/system/omnigraph-daily-ingest.service (stop only, no file edit)</files>
  <action>
    Stop the burning restart-loop before touching anything else (CONTEXT.md "restart-loop 处理(动手前第一步)").

    ssh aliyun-vitaclaw, then:
    1. Capture current restart count + state for the before/after evidence:
       `systemctl show omnigraph-daily-ingest -p NRestarts -p ActiveState -p SubState`
       (expect SubState=auto-restart, NRestarts a high number ~351)
    2. Stop the unit:
       `systemctl stop omnigraph-daily-ingest`
    3. Confirm the failing timers won't fire mid-window. afternoon-ingest (14:00 CST) and evening-ingest (20:00 CST) are timer-triggered and currently dead with Conflicts= mutex. Check current CST time; if the maintenance window risks crossing 14:00 or 20:00 CST, temporarily mask the timers:
       `systemctl stop omnigraph-afternoon-ingest.timer omnigraph-evening-ingest.timer 2>/dev/null || true`
       (re-arm in Task 5). If well clear of those times, leave them.
    DO NOT `disable` the daily-ingest unit — only `stop`. It is re-started for verification in Task 5.
  </action>
  <verify>
    `ssh aliyun-vitaclaw "systemctl show omnigraph-daily-ingest -p ActiveState -p SubState"` shows ActiveState=inactive (or failed), NOT activating/auto-restart. Record NRestarts value N0 for Task 5 comparison.
  </verify>
  <done>omnigraph-daily-ingest is stopped (not activating, not auto-restarting); afternoon/evening timers confirmed not firing during the window; baseline NRestarts (N0) recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Install docker-ce engine (clean apt install, NO wipe)</name>
  <files>REMOTE aliyun: docker-ce, docker-ce-cli, docker-buildx-plugin, docker-compose-plugin (apt install)</files>
  <action>
    Complete the half-install per LOCKED decision: this is a missing-engine case, NOT a broken install. containerd.io is already `ii` and `containerd.service` is active since 6/18. The official source `/etc/apt/sources.list.d/docker.list` (mirrors.aliyun.com/docker-ce) is already configured.

    CRITICAL — DO NOT wipe `/var/lib/docker` or `/var/lib/containerd`. containerd is running; wiping breaks it. docker-ce will take over the existing containerd.io as its runtime.

    ssh aliyun-vitaclaw, then:
    1. Refresh metadata + confirm candidate version:
       `apt-get update`
       `apt-cache policy docker-ce`
       (expect Candidate `5:29.6.0-1~ubuntu.22.04~jammy`, from mirrors.aliyun.com/docker-ce. Use apt candidate — do NOT pin, per Claude's Discretion.)
    2. Install the four packages (containerd.io intentionally omitted — already installed):
       `apt-get install -y docker-ce docker-ce-cli docker-buildx-plugin docker-compose-plugin`
    3. Enable + start the daemon:
       `systemctl enable --now docker`
    4. Confirm:
       `docker version`            (Client + Server both report)
       `systemctl is-active docker`  (= active)
       `systemctl is-active containerd` (still = active — confirm install did NOT disrupt it)

    If apt install fails for any reason, STOP and report (do not improvise a wipe). Rollback for this task = `apt-get remove docker-ce docker-ce-cli docker-buildx-plugin docker-compose-plugin` (containerd.io + its data untouched).
  </action>
  <verify>
    `ssh aliyun-vitaclaw "docker version >/dev/null 2>&1 && systemctl is-active docker && systemctl is-active containerd"` prints `active` twice and exits 0; `dpkg -l docker-ce docker-ce-cli | grep -c '^ii'` returns 2.
  </verify>
  <done>docker version reports both Client and Server; `systemctl is-active docker` = active and enabled; containerd still active (not disrupted); /var/lib/docker and /var/lib/containerd untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Launch Qdrant on existing data (version check first, NO wipe)</name>
  <files>REMOTE aliyun: docker image qdrant/qdrant:v1.11.0 (pull); REMOTE aliyun:/var/lib/qdrant (mount, read existing data — NO wipe)</files>
  <action>
    Bring up Qdrant reusing the existing 6/16 collections per LOCKED decision (mount, NO wipe, NO reingest). The CONTEXT-noted full-rebuild Section-0 path (`rm -rf /var/lib/qdrant/*`) is EXPLICITLY SKIPPED here.

    ssh aliyun-vitaclaw, then:
    1. BEFORE launching — confirm version compatibility (INVENTORY.md:350 caveat). The collection layout for reused data may be at either path; grep both:
       `grep -r '"version"' /var/lib/qdrant/storage/collections/*/config.json 2>/dev/null | head -3`
       `grep -r '"version"' /var/lib/qdrant/collections/*/config.json 2>/dev/null | head -3`
       Also confirm the 3 expected collection dirs exist and the data is the 6/16 set:
       `ls -la /var/lib/qdrant/collections/ 2>/dev/null; ls -la /var/lib/qdrant/storage/collections/ 2>/dev/null`
       Expect 3 collections suffixed `_gemini_embedding_2_3072d` (chunks/entities/relationships).
       If the on-disk storage version is NEWER than the v1.11.0 engine would accept (Qdrant refuses to load a forward-version on-disk format), HALT and report the version mismatch to the user before pulling a different tag (do NOT guess a tag).
    2. Pull the image (Aliyun can reach docker hub mirror per CONTEXT). If hub is unreachable: fallback to `registry.cn-hangzhou.aliyuncs.com` mirror, or offline `docker load` (INVENTORY.md:312 — ask user if no offline tar exists):
       `docker pull qdrant/qdrant:v1.11.0`
    3. Confirm no stale qdrant container exists (fresh box, should be none), then launch with the EXACT recipe (INVENTORY.md:179-182) — `--restart=unless-stopped` is MANDATORY (memory `qdrant_docker_no_restart_policy_trap`):
       ```
       docker rm -f qdrant 2>/dev/null || true
       docker run -d --name qdrant --restart=unless-stopped \
         -p 6333:6333 -p 6334:6334 \
         -v /var/lib/qdrant:/qdrant/storage \
         qdrant/qdrant:v1.11.0
       ```
       NOTE the mount target is `/qdrant/storage`; if step-1 found the collections at `/var/lib/qdrant/collections/` (no `storage/` segment), they are correctly exposed because the container reads `/qdrant/storage/collections/`. If collections were found under `/var/lib/qdrant/storage/collections/` on the host, the mount already lines up. Confirm via the count check in Task 4 — if collections come back empty, the mount path/layout is wrong and must be reconciled before proceeding (do NOT wipe to "fix" it).
    4. `sleep 5`, then confirm the container is Up:
       `docker ps --filter name=qdrant`
  </action>
  <verify>
    `ssh aliyun-vitaclaw "docker ps --filter name=qdrant --format '{{.Names}} {{.Status}} {{.Image}}'"` shows `qdrant Up ... qdrant/qdrant:v1.11.0`; and `docker inspect -f '{{.HostConfig.RestartPolicy.Name}}' qdrant` returns `unless-stopped`.
  </verify>
  <done>qdrant container is Up with image v1.11.0; RestartPolicy=unless-stopped; on-disk storage version confirmed compatible; /var/lib/qdrant data NOT wiped.</done>
</task>

<task type="auto">
  <name>Task 4: Verify Qdrant health + collections served (no data loss)</name>
  <files>REMOTE aliyun: Qdrant HTTP API :6333 (read-only health + collection introspection)</files>
  <action>
    Prove Qdrant is healthy AND serving the existing data (not an empty fresh DB). This is the gate before any ingest.

    ssh aliyun-vitaclaw, then:
    1. Health endpoint:
       `curl -sf http://localhost:6333/healthz && echo HEALTHZ_OK`  (fallback `/readyz` if /healthz 404 on this build)
    2. Collections list — expect 3, suffix `_gemini_embedding_2_3072d`:
       `curl -s http://localhost:6333/collections | jq '.result.collections[].name'`
    3. Point counts must be >0 (matches 6/16 data; proves data reused not lost). Check the chunks collection explicitly and capture the BEFORE count for Task 5 diff:
       `curl -s http://localhost:6333/collections/lightrag_vdb_chunks_gemini_embedding_2_3072d | jq '.result.points_count'`
       (also spot-check entities + relationships counts > 0)
       Record this chunks points_count as C_before.
    If collections list is empty or counts are 0, STOP — the mount layout is wrong (Task 3 step-3 note); reconcile the host path WITHOUT wiping data, then re-run Task 3 step-3 launch.
  </action>
  <verify>
    `ssh aliyun-vitaclaw "curl -sf http://localhost:6333/healthz >/dev/null && curl -s http://localhost:6333/collections | jq '.result.collections | length'"` prints `3`; and chunks `points_count` > 0 (record as C_before).
  </verify>
  <done>/healthz (or /readyz) returns 200; exactly 3 collections listed with the _gemini_embedding_2_3072d suffix; all three point counts > 0; C_before recorded.</done>
</task>

<task type="auto">
  <name>Task 5: Verification ingest + restart-loop stopped + cron re-armed</name>
  <files>REMOTE aliyun:/etc/systemd/system/omnigraph-daily-ingest.service (start only, no edit); REMOTE aliyun: timers re-arm</files>
  <action>
    Prove the end-to-end ingest path now works (NO Connection refused, real article lands) and the restart-loop is gone. Per LOCKED decision #3, this single run IS the natural-drain — it uses the existing unit at `--max-articles 5`; NO big catch-up batch.

    ssh aliyun-vitaclaw, then:
    1. Start ONE ingest run via the unit (uses EnvironmentFile, so env is correct — no manual source needed):
       `systemctl start omnigraph-daily-ingest`
    2. Tail the journal live and confirm it reaches ainsert without the error:
       `journalctl -u omnigraph-daily-ingest -f --since "5 min ago"`
       PASS signals: NO `ConnectError`/`Connection refused`; LightRAG ainsert progresses; >=1 article reports ingested/committed. Let the run finish (it will process up to 5 articles; note: post-completion the unit may hang ~50min before clean shutdown per memory `ingest_service_post_completion_asyncio_hang` — that hang is a KNOWN separate issue and does NOT invalidate this verification; the ingest landing is what matters).
    3. Confirm chunks count INCREASED (proves data actually landed in Qdrant):
       `curl -s http://localhost:6333/collections/lightrag_vdb_chunks_gemini_embedding_2_3072d | jq '.result.points_count'`
       Compare to C_before from Task 4 — expect C_after > C_before.
    4. Confirm the restart-loop is gone:
       `systemctl show omnigraph-daily-ingest -p NRestarts -p ActiveState -p SubState`
       NRestarts should be stable (not climbing every 10min); SubState should be `running` (during the run) then `dead`/`exited` after — NOT `auto-restart`. Compare NRestarts to N0 from Task 1: it must NOT keep incrementing on a 10-min cadence.
    5. Re-arm normal cron operation (undo any temporary timer stops from Task 1):
       `systemctl start omnigraph-daily-ingest.timer omnigraph-afternoon-ingest.timer omnigraph-evening-ingest.timer 2>/dev/null || true`
       `systemctl list-timers 'omnigraph-*' --all`  (confirm next-fire times are scheduled, in CST)

    FALLBACK (manual ingest if the unit start misbehaves): `set -a; source /root/.hermes/.env; set +a; cd /root/OmniGraph-Vault && venv-aim1/bin/python batch_ingest_from_spider.py --from-db --max-articles 5` — env source is REQUIRED for manual runs (memory `aliyun_ssh_manual_trigger_env`).
  </action>
  <verify>
    `ssh aliyun-vitaclaw "journalctl -u omnigraph-daily-ingest --since '10 min ago' | grep -c 'Connection refused'"` returns `0`; AND chunks `points_count` after run > C_before; AND `systemctl show omnigraph-daily-ingest -p SubState` is not `auto-restart`.
  </verify>
  <done>One ingest run completed with zero 'Connection refused' in the journal; chunks points_count increased over C_before (>=1 article landed); omnigraph-daily-ingest no longer in auto-restart loop (NRestarts stable); daily/afternoon/evening timers re-armed and scheduled.</done>
</task>

<task type="auto">
  <name>Task 6: long_form RAG smoke test (graph is queryable)</name>
  <files>REMOTE aliyun: kb synthesis / RAG query path (read-only smoke)</files>
  <action>
    Prove the graph is not just storing but actually retrievable end-to-end (LightRAG hybrid retrieve hits real entities/relationships from Qdrant, not a stub/FTS fallback).

    ssh aliyun-vitaclaw, then run a long_form query that should hit a recent article. Agent picks the exact query wording (Claude's Discretion) targeting a topic known to be in the recent corpus (e.g. an AI-agent / LLM topic). Two options:
    - If the kb-api/synthesize endpoint is locally reachable on this box, curl it (long_form mode), e.g.:
      `curl -s -X POST http://localhost:<kb_port>/api/synthesize -H 'Content-Type: application/json' -d '{"query":"<recent-topic query>","mode":"long_form"}' | jq '{sources: (.sources|length), chars: (.markdown|length)}'`
    - Otherwise run the synthesis CLI with env sourced:
      `set -a; source /root/.hermes/.env; set +a; cd /root/OmniGraph-Vault && venv-aim1/bin/python kg_synthesize.py "<recent-topic query>" hybrid`

    PASS signal: returns real graph-grounded markdown with sources > 0 (hybrid retrieve found entities/relationships) — NOT an empty result, NOT an fts5_fallback, NOT a Connection-refused error. This confirms Qdrant is wired to the retrieval path, not just the ingest path.

    KNOWN-DEBT note (do NOT block this quick on it): graphml<->Qdrant divergence (ISSUE #44) may make very-recent (6/17->today) articles under-retrieve since the backlog hasn't drained — that is expected and out of scope. The smoke only needs to prove the retrieval path is alive against the existing 6/16 graph, sources > 0.
  </action>
  <verify>
    `ssh aliyun-vitaclaw "<synthesize curl or CLI>"` returns markdown with sources > 0 and no 'Connection refused' / no fts5_fallback marker.
  </verify>
  <done>long_form query returns real graph-grounded content with sources > 0; confirms LightRAG hybrid retrieval against Qdrant works end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Approve recovery evidence before irreversible disk prune</name>
  <files>REMOTE aliyun: evidence captured across Tasks 1-6 (docker ps, /collections, journalctl, systemctl, long_form smoke)</files>
  <action>
    Pause for user review of the Tasks 1-6 recovery evidence before the irreversible Task 7 disk prune. The prune (`docker system prune -a -f`) cannot be undone; gate it behind explicit human confirmation that the stack is healthy and the verification ingest genuinely landed.
  </action>
  <what-built>
    Full recovery automated: docker-ce installed + active (Task 2); Qdrant Up with --restart=unless-stopped on existing 6/16 data (Task 3); 3 collections healthy with non-zero counts (Task 4); one verification ingest landed with zero Connection-refused + chunks count increased + restart-loop stopped + timers re-armed (Task 5); long_form RAG smoke returns sources>0 (Task 6).
  </what-built>
  <how-to-verify>
    Review the evidence captured across Tasks 1-6:
    1. `docker ps` shows qdrant Up; RestartPolicy=unless-stopped.
    2. `/collections` = 3 with non-zero counts.
    3. journalctl shows zero 'Connection refused'; chunks count C_after > C_before.
    4. daily-ingest no longer auto-restarting; timers scheduled (CST).
    5. long_form smoke sources>0.
    Confirm all PASS before authorizing the irreversible disk prune in Task 7.
  </how-to-verify>
  <verify>User reviews the Tasks 1-6 evidence and replies "approved" (or describes a failed check that must be fixed first).</verify>
  <done>User has approved; Task 7 disk prune is authorized to proceed.</done>
  <resume-signal>Type "approved" to authorize Task 7 disk prune, or describe any failed check.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Disk prune — reclaim orphan containerd layers (LAST, after verification PASS)</name>
  <files>REMOTE aliyun: docker/containerd orphan image layers (prune); /var/lib/containerd overlayfs + content store</files>
  <action>
    ONLY after Tasks 1-6 PASS and the checkpoint is approved. Per LOCKED decision: prune is sequenced AFTER verification PASS to avoid clearing layers still in use.

    ssh aliyun-vitaclaw, then:
    1. Capture before:
       `df -h /`  (expect ~85% used, ~15G avail)
    2. Prune orphan/unused images + build cache + stopped containers (the qdrant container is running so it and its image are NOT touched; /var/lib/qdrant is an independent volume so data is untouched):
       `docker system prune -a -f`
    3. Capture after:
       `df -h /`  (expect usage dropped toward target <70%)
    4. Sanity re-check that the prune did NOT disturb the running stack:
       `docker ps --filter name=qdrant`  (still Up)
       `curl -sf http://localhost:6333/healthz && echo STILL_HEALTHY`

    NOTE: `docker system prune -a -f` reclaims docker-managed layers. The 19G containerd `overlayfs` orphan snapshots from old k8s/containerd usage (CONTEXT.md) may be partly outside docker's view; if `df` does not drop meaningfully and the box is still >80%, report the residual to the user (do NOT hand-rm containerd snapshot dirs — that risks corrupting the live runtime). Confirming the docker-side reclaim is the in-scope deliverable.
  </action>
  <verify>
    `ssh aliyun-vitaclaw "df -h / | tail -1"` shows lower use% than the before-capture; AND `docker ps --filter name=qdrant` still shows Up; AND `curl -sf http://localhost:6333/healthz` exits 0.
  </verify>
  <done>docker system prune completed; df / use% dropped (toward <70%); qdrant container still Up and healthy after prune; /var/lib/qdrant data intact.</done>
</task>

</tasks>

<verification>
End-to-end recovery proof (maps to CONTEXT.md verification 1-8):
1. `docker version` + `systemctl is-active docker` = active; `docker ps` shows qdrant Up. (Task 2, 3)
2. `curl -sf localhost:6333/healthz` (or /readyz) passes. (Task 4)
3. `/collections` lists 3 collections; chunks/entities/relationships counts > 0. (Task 4)
4. `docker inspect qdrant` RestartPolicy = unless-stopped. (Task 3)
5. `systemctl start omnigraph-daily-ingest` -> journalctl no 'Connection refused' -> >=1 article -> chunks C_after > C_before. (Task 5)
6. omnigraph-daily-ingest no longer activating/auto-restart; NRestarts stable. (Task 5)
7. long_form RAG smoke hits recent corpus, sources > 0. (Task 6)
8. `df -h /` use% drops after prune (85% -> target <70%). (Task 7)
</verification>

<success_criteria>
- docker-ce installed, daemon active + enabled, containerd undisturbed
- Qdrant container Up, --restart=unless-stopped, reusing existing /var/lib/qdrant data (no wipe, no reingest)
- 3 collections served with non-zero counts (no data loss)
- Verification ingest lands with zero Connection refused; chunks count increases
- 10-min restart-loop stopped; all omnigraph timers re-armed and scheduled in CST
- long_form RAG smoke returns sources > 0
- Disk reclaimed via docker system prune (after verification PASS)
- ZERO local repo code edits; ZERO git commits for the fix (doc-only forward commit of quick artifacts at orchestrator close-out)
</success_criteria>

<output>
After completion, create `.planning/quick/260623-g6e-260623-aliyun-docker-qdrant-ingest-resto/260623-g6e-SUMMARY.md` capturing: docker-ce version installed, Qdrant image tag + RestartPolicy, C_before/C_after chunks counts, journalctl Connection-refused count (0), NRestarts before/after, long_form smoke sources count, df before/after, and any residual containerd-orphan disk note + ISSUE rows to file (e.g. post-completion asyncio hang #, backlog-197 natural-drain ETA, any version-mismatch finding).
</output>
