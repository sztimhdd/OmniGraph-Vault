# 260605-u17 — Execution Summary

**Quick:** 260605-u17-probe-vertex-rerun
**Date:** 2026-06-05/06 ADT
**Mode:** execute (quick)
**Status:** COMMITTED — all 3 tasks reached done condition; one Halt B fired and was recovered per plan rule
**Commit:** filled in post-commit (forward-only on origin/main; explicit-add discipline honored)

## Tasks completed

| # | Task | Status | Done condition |
|---|---|---|---|
| 1 | T1 Pre-flight verify + author probe-v2 (sha256 contract) | DONE | cert state 123/4 corp re-verified; parent probe sha256 `57308c595db37718...` unchanged; body_1.md (14172 B) + body_2.md (9431 B) copied to isolated u17 base dir; SA JSON (2400 B) present; 5 Vertex env vars confirmed in `.dev-runtime/.env`; probe-v2 authored at `.scratch/260605-u17-concurrent-probe-vertex.py` (78 LoC, AST-valid, contains `from lib.llm_complete import get_llm_func` + `llm_model_func=get_llm_func()`, NO `from lib.llm_deepseek` import); sha256 captured `1891839825103e4001f4271da518b35ade618befd1671cc110522dd6e2cb0dbc`. **Note:** initial author was 81 LoC (over 80 cap, Halt E condition); compressed docstring to 5 lines (matching parent shape) before any probe run, sha256 captured AFTER compression. |
| 2 | T2 Run probe-v2 with Vertex + capture output (sha256 verify) | DONE — Halt B fired & recovered | First launch attempt (`set -a; source .dev-runtime/.env; set +a;`) corrupted Windows-path env vars via bash escape interpretation (`\U`, `\h`, `\D` stripped → `c:UsershuxxhaDesktopOmniGraph-Vault.dev-runtimegcp-paid-sa.json`) → `DefaultCredentialsError` ghost-failure inside embedding workers (silent, both ainsert calls returned without raising; both_processed=False). Per Halt B rule ("fix and retry. Do NOT mark BLOCKED for this") relaunched with explicit `export GOOGLE_APPLICATION_CREDENTIALS="C:/Users/.../.dev-runtime/gcp-paid-sa.json"` (forward-slash form, no `source`). Second launch ran cleanly: PASS A serial 459.286s real Vertex work; PASS B never reached real ainsert due to probe-contract flaw (see below). sha256 byte-identical pre/post (`1891839825103e4001f4271da518b35ade618befd1671cc110522dd6e2cb0dbc` matches). JSON extracted with all 9 keys to `.scratch/260605-u17-probe-output.json` (325 B). Full stdout+stderr 296 lines captured to `.scratch/260605-u17-probe-output.txt` (35081 B). |
| 3 | T3 Apply decision matrix → Section 6 + SUMMARY + atomic commit | DONE | Section 6 appended to parent `260605-mz1-RESEARCH.md` (Sections 1-5 byte-identical pre/post; file 224 → 331 lines = +107 line append); verdict declared `BLOCKED-by-correctness`; Vertex caveat verbatim phrase `STRONG SIGNAL but NOT DEFINITIVE` present; SUMMARY.md (this file) written; atomic stage-commit-push of 3 files. |

## Probe metrics (verbatim from `.scratch/260605-u17-probe-output.json`)

| Metric | Value |
|---|---|
| `wall_s_serial` | `459.28633309999714` |
| `wall_s_concurrent` | `0.00549529999261722` (probe-contract artifact — NOT a real concurrent measurement) |
| `speedup_ratio` | `83578.027` (nonsense — denominator artifact) |
| `both_processed` | `False` (probe-contract artifact — see flaw below) |
| `graphml_valid` | `"missing"` (PASS B reset graphml then never wrote) |
| `kv_store_valid` | `True` |
| `serial_exception` | `null` (PASS A serial completed cleanly) |
| `concurrent_exception` | `null` (PASS B never threw — but never actually concurrent-inserted) |
| `hashes` | `["c8cc5b1fb7", "b37b0df5fb"]` |

**PASS A actually-observed real work** (independent useful signal):
- Article 1 (`c8cc5b1fb7`): 4 chunks → 53 entities + 46 relations
- Article 2 (`b37b0df5fb`): 3 chunks → 41 entities + 30 relations
- Final graph: **93 nodes / 76 edges** written by PASS A
- Wall: **459.286s** for 2 articles serial through full Vertex Gemini-2.5-flash-lite-preview pipeline (extract + merge + persist; vision skipped per `OMNIGRAPH_VISION_SKIP_PROVIDERS=siliconflow,openrouter,gemini`)

## Halt log

### Halt B fired (env-var override, recovered per plan rule)

- **Trigger:** First launch attempt 1 used `set -a; source .dev-runtime/.env; set +a;` to load env vars. Bash `source` interpreted backslashes in Windows paths as escape sequences and stripped them.
- **Symptom 1 — silent ghost-failure:** `google.auth.exceptions.DefaultCredentialsError: File c:UsershuxxhaDesktopOmniGraph-Vault.dev-runtimegcp-paid-sa.json was not found.` raised inside embedding-worker tasks; LightRAG caught the exception internally and marked both docs FAILED. JSON output showed `serial_exception: null` AND `concurrent_exception: null` (silent ghost-success class) but `both_processed: false`. Wall times very short (~6s + 7s) confirmed no real Vertex calls succeeded.
- **Symptom 2 — corrupted path:** verified bash mangling via `( set -a; source .dev-runtime/.env; set +a; echo $GOOGLE_APPLICATION_CREDENTIALS )` → `c:UsershuxxhaDesktopOmniGraph-Vault.dev-runtimegcp-paid-sa.json` (`\U`, `\h`, `\D`, `\O`, `\.`, `\g` stripped as escapes).
- **Action:** per PLAN Halt B rule ("fix and retry. Do NOT mark BLOCKED for this") rewrote launcher to pass env vars via explicit `export VAR="forward/slash/value"` (forward-slash paths, no `source`). Second launch ran clean.
- **Out-of-scope discovery:** `.dev-runtime/.env` has 3 Windows-path values (`GOOGLE_APPLICATION_CREDENTIALS`, `OMNIGRAPH_BASE_DIR`, `KOL_SCAN_DB_PATH`) — same flaw exists for any future bash-source consumer of this file. Documented for the orchestrator to consider for ISSUES.md (out-of-scope for this quick).

### Halt C dual-applied via probe-contract artifact (NOT real concurrent corruption)

- **Trigger:** PASS B post-conditions show `both_processed: false` AND `graphml_valid: "missing"`. Per Halt C symptom list ("`graphml_valid: "corrupt: ..."` OR `kv_store_valid: "corrupt: ..."` OR `concurrent_exception: "..." (not None)` OR `both_processed: false`"), the `both_processed: false` clause fires.
- **Probe-contract flaw exposed:** `reset_storage()` between PASS A and PASS B clears the on-disk `STORAGE` directory but does NOT reset the in-process LightRAG pipeline status. PASS B's second `make_rag()` invocation reuses the cached pipeline status from PASS A; `await asyncio.gather(rag.ainsert(body1, ids=[h1]), rag.ainsert(body2, ids=[h2]))` short-circuits at the dedup gate before any real ainsert work happens. Direct evidence at `.scratch/260605-u17-probe-output.txt` lines 271-280: "WARNING: Duplicate document detected: c8cc5b1fb7" / "No new unique documents were found." / "No valid documents to process after consistency check".
- **PASS B `wall_s_concurrent=0.005s`** is the time the dedup gate took to refuse the work, NOT the time of two concurrent ainserts. PASS B never tested what v1.2 actually proposes (asyncio.gather over real ainsert across distinct article IDs).
- **Action:** per PLAN Halt C rule ("STOP probe (single run already complete). Do NOT retry. Mark **BLOCKED-by-correctness** OR **BLOCKED-by-corruption** in Section 6"), picked `BLOCKED-by-correctness` (primary signal `both_processed: false`; graphml state is "missing" rather than "corrupt: ..."). Section 6 explicitly documents that the BLOCKED verdict is a probe-contract artifact, NOT real correctness failure of LightRAG concurrent ainsert.
- **Probe contract is inherited from parent `260605-mz1-concurrent-probe.py`** byte-for-byte (PLAN's minimal-diff requirement allowed only LLM-func source switch). The same flaw exists in the parent probe — it stayed latent in 260605-mz1 + 260605-pwl because neither quick reached PASS B execution.

### Halts A / D / E / F / G — NOT fired

- **A (cert regression since pwl):** NOT fired — `123 total / 4 corp hits` re-verified at T1 step 1
- **D (Vertex 429 RESOURCE_EXHAUSTED):** NOT fired — no quota errors in any Vertex LLM or embedding call
- **E (probe-v2 LoC > 80):** NOT fired in final state — first author was 81 LoC; compressed to 78 LoC by trimming docstring to parent's 5-line shape; sha256 `1891839825103e4001f4271da518b35ade618befd1671cc110522dd6e2cb0dbc` captured AFTER compression
- **F (corp blocks Vertex on model paths):** NOT fired — Vertex `/v1beta1/.../generateContent` and `/v1beta1/.../embedContent` calls all succeeded post-cert-rebuild
- **G (oauth2 / SA fail):** NOT fired in the final state — after Halt B fix gave SA JSON a valid file path, oauth2 issued tokens cleanly. (Initial Halt B presented as a `DefaultCredentialsError`, but root cause was bash path-escaping, NOT real auth failure.)

## Final verdict

**v1.2 batch_ingest concurrent rewrite viability: BLOCKED-by-correctness**

- **Decision matrix row hit:** `any | False (both_processed) | None (graphml="missing", not "corrupt:...") | BLOCKED-by-correctness`
- **Halt C dual-applies** (the matrix gives "BLOCKED-by-correctness OR BLOCKED-by-corruption" choice); picked `BLOCKED-by-correctness` per primary signal weight
- **Important caveat:** the BLOCKED verdict reflects a probe-contract flaw, NOT real v1.2 design failure. The probe never actually executed real concurrent ainsert in PASS B — `reset_storage()` does not reset in-process LightRAG pipeline state, so PASS B short-circuited at the dedup gate. v1.2 viability remains operationally UNKNOWN; this quick converted "UNKNOWN-due-to-corp-network" into "UNKNOWN-due-to-probe-contract-flaw"
- **Recommended next step (orchestrator):** NOT subprocess isolation (the matrix's BLOCKED-by-correctness alt-path) for production. Instead, file a probe-contract revision quick: most directly via subprocess invocation per pass (each pass = its own `python` invocation with fresh interpreter, so pipeline state cannot leak). Once probe contract is fixed, re-run Vertex probe to get a real `wall_s_concurrent` measurement, re-apply matrix
- **Independent positive signals from PASS A:** Vertex Gemini end-to-end pipeline works on corp laptop with rebuilt cert + explicit env vars (no `source` mangling). 459s for 2 articles serial = ~230 s/article, within Aliyun cron range (240..2867s). Vertex SA + endpoint + LLM + embedding combination validated end-to-end through full LightRAG ainsert (extract + merge + persist)

## Vertex caveat (mandatory, repeated verbatim from Section 6)

**Vertex Gemini measurement is a STRONG SIGNAL but NOT DEFINITIVE for v1.2 viability.**

Production uses DeepSeek as the LLM provider; this probe uses Vertex Gemini. Lock semantics inside LightRAG may differ between LLM clients — different httpx connection pool, different tokenizer call paths (Vertex SDK uses `google.genai` not `tiktoken` for token counting), different retry semantics, different async cancellation behavior. A GO verdict would suggest v1.2 concurrent-rewrite is FEASIBLE but not guarantee prod-parity speedup.

**For this quick's actual verdict (BLOCKED-by-correctness):** the probe-contract flaw is LLM-client-agnostic — `reset_storage()` defeats the in-process pipeline state regardless of whether DeepSeek or Vertex is the LLM. So the BLOCKED verdict here would also have fired against DeepSeek if the parent quick had reached PASS B. The Vertex caveat does NOT alter the BLOCKED verdict, but DOES alter the recommended next-step:

- **If a future probe-contract revision lands a GO/RISKY verdict:** the v1.2 plan-phase MUST include an explicit sub-task for an Aliyun-side prod-parity follow-up smoke (DeepSeek provider, prod-parity network) BEFORE declaring the asyncio.gather wrapper design final.
- **For this quick's BLOCKED-by-correctness:** the immediate next step is a probe-contract revision quick (subprocess-per-pass), not subprocess isolation in production.

## Artifacts produced

- `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md` — Section 6 appended (parent's Sections 1-5 byte-identical pre/post; file 224 → 331 lines = +107 lines including the new section, run-results table, probe-contract-flaw analysis, verdict, Vertex caveat, halt log, cross-refs)
- `.planning/quick/260605-u17-probe-vertex-rerun/260605-u17-PLAN.md` — committed (already authored by orchestrator pre-spawn; included in this quick's commit per PLAN's 3-file stage list)
- `.planning/quick/260605-u17-probe-vertex-rerun/260605-u17-SUMMARY.md` — this file
- `.scratch/260605-u17-concurrent-probe-vertex.py` (gitignored, 78 LoC) — sibling probe-v2 with Vertex provider switch via `lib.llm_complete.get_llm_func()`. sha256 `1891839825103e4001f4271da518b35ade618befd1671cc110522dd6e2cb0dbc` byte-identical pre/post run.
- `.scratch/260605-u17-probe-output.txt` (gitignored, 35081 B / 296 lines) — full stdout+stderr including PASS A real Vertex work, PASS B dedup-gate evidence, exit code receipt
- `.scratch/260605-u17-probe-output.json` (gitignored, 325 B) — extracted JSON, 9 keys present, parseable
- `.scratch/260605-u17-probe-sha256-before.txt` and `.scratch/260605-u17-probe-sha256-after.txt` (gitignored, identical) — byte-identical contract receipt
- `.dev-runtime/260605-u17-probe-vertex/` (gitignored runtime data dir) — isolated probe base; data/body_{1,2}.md, lightrag_storage/ (post-PASS-B reset state, mostly empty)

## Discipline notes

- **Probe contract:** byte-identical sha256 honored — `.scratch/260605-u17-concurrent-probe-vertex.py` NOT modified between launch attempts (sha256 `1891839825103e4001f4271da518b35ade618befd1671cc110522dd6e2cb0dbc` before+after second launch). Halt B "fix" was in the LAUNCHER shell, not in the probe file itself. Parent probe `260605-mz1-concurrent-probe.py` UNTOUCHED (sha256 `57308c595db37718...` unchanged).
- **Production source:** untouched — no edits to `batch_ingest_from_spider.py`, `ingest_wechat.py`, `kb/*`, `lib/*` (including `lib/llm_complete.py` which the probe USES via existing public API), `config.py`, `requirements.txt`.
- **Hermes runtime data:** untouched — `~/.hermes/` not mutated.
- **Aliyun:** no SSH attempted (per PLAN forbidden).
- **Settings:** `.dev-runtime/.env` not modified (only read). `~/.hermes/.env` not modified. No global env vars / registry changes. No corp CA env vars persisted (probe shell `unset` only, scoped to launch invocation).
- **Cert rebuild:** NOT re-run (per PLAN forbidden); state from sibling 260605-pwl re-verified at T1 (`123 total / 4 corp hits` matches expected).
- **STATE.md:** NOT modified by this quick (orchestrator owns separately for adversarial-verify pattern).
- **Git hygiene:** explicit `git add <files>` (no `-A`/`.`); single forward-only commit (no `--amend`/`reset`/`force`); no `Co-Authored-By:` line per CLAUDE.md global rule. Atomic stage-commit-push chain.
- **Discovered out-of-scope issue (not edited into ISSUES.md by this agent — orchestrator owns):** `.dev-runtime/.env` has 3 Windows-path values that bash `source` mangles via escape interpretation (`GOOGLE_APPLICATION_CREDENTIALS`, `OMNIGRAPH_BASE_DIR`, `KOL_SCAN_DB_PATH`). Surfacing for orchestrator's ISSUES.md transcription decision.
- **Lint:** Section 6 introduces 4× MD024 duplicate-heading warnings (`### Verdict`, `### Halt log`, `### Cross-references`, `### Run results` already exist in earlier sections) and 1× MD032 list-blank-line warning. Accepted per CLAUDE.md PRINCIPLE #3 (Surgical Changes — don't restructure parent's 5 prior sections to silence template-mandated heading reuse). Same precedent as 260605-pwl SUMMARY.md lines 92-93.

## Cross-references

- **Parent quick `260605-mz1`** (commits `b17bccb` + `adcb59d`) — verified 3 walls SYSTEMIC across 5-day Aliyun audit; Halt #1 (corp firewall) blocked local probe. Section 1 + 2 untouched by this quick.
- **Sibling quick `260605-pwl`** — cert rebuild succeeded (`123/4 corp`); Halt F filed two new blockers (DeepSeek TLS-block + missing `DEEPSEEK_API_KEY` in `~/.hermes/.env`). This quick (`260605-u17`) bypassed both via Vertex provider switch.
- **Decision matrix:** PLAN.md `<decision_matrix>` section — verdict `BLOCKED-by-correctness` maps to row 4 (`any | False | None | BLOCKED-by-correctness`); Halt C dual-mapping documented.
- **Memory `vertex_ai_smoke_validated`** — Vertex SA + endpoint pairing ground truth (validated 2026-04-30; reconfirmed in this quick's PASS A real work).
- **Memory `aliyun_oauth_pin`** — relevant if Halt G fires on Aliyun (NOT this quick — corp laptop ran cleanly).
- **Memory `feedback_git_add_explicit_in_parallel_quicks`** — atomic stage-commit-push pattern used in T3.
- **Memory `feedback_no_amend_in_concurrent_quicks`** — no `--amend` used; single forward-only commit.
- **Memory `corp_pem_rebuild_pattern`** — runbook used by sibling 260605-pwl T1; re-verified (NOT re-run) at this quick's T1.
- **ISSUES.md #38 / #39 / #40** (filed by parent 260605-mz1) — NOT modified by this quick; orchestrator may decide whether to file the new bash-source-mangles-Windows-paths discovery as a separate doc-class entry.
- **PLAN.md** of this quick — `.planning/quick/260605-u17-probe-vertex-rerun/260605-u17-PLAN.md` for full task / contract / halt-branch detail.
