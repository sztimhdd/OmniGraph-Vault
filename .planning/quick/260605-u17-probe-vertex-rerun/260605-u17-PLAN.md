---
quick: 260605-u17-probe-vertex-rerun
type: execute
mode: quick
wave: 1
depends_on: [260605-mz1, 260605-pwl]
files_modified:
  - .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md  # Section 6 append only (Sections 1-5 untouched)
  - .planning/quick/260605-u17-probe-vertex-rerun/260605-u17-PLAN.md               # this file
  - .planning/quick/260605-u17-probe-vertex-rerun/260605-u17-SUMMARY.md            # new
autonomous: true
requirements: [U17-01, U17-02, U17-03, U17-04]
must_haves:
  truths:
    - "A sibling probe `.scratch/260605-u17-concurrent-probe-vertex.py` exists, ≤80 LoC, valid Python 3.13, byte-identical (sha256 match) before vs after probe run"
    - "Probe-v2 differs from parent only in LLM-func source: `from lib.llm_complete import get_llm_func` + `llm_model_func=get_llm_func()` (replacing `from lib.llm_deepseek import deepseek_model_complete` + `llm_model_func=deepseek_model_complete`). Embedding/post-conditions/2-pass design/JSON output/probe contract — IDENTICAL."
    - "Probe runs to completion OR halts on a documented Halt branch (A/B/C/D/E/F/G)"
    - "Section 6 of 260605-mz1-RESEARCH.md states a single verdict: GO | RISKY | BLOCKED-by-{perf|correctness|corruption|quota|vertex-network|auth}"
    - "Section 6 contains the Vertex-not-prod-parity caveat verbatim (Vertex measurement is STRONG SIGNAL not DEFINITIVE for v1.2 viability)"
    - "Single forward-only commit lands on origin/main with explicit file list (4 files: parent RESEARCH.md, this PLAN.md, this SUMMARY.md, no STATE — orchestrator owns STATE separately)"
  artifacts:
    - path: ".scratch/260605-u17-concurrent-probe-vertex.py"
      provides: "Sibling probe with Vertex Gemini provider switch (1-line LLM-func diff vs parent)"
      contains: "from lib.llm_complete import get_llm_func"
      max_lines: 80
    - path: ".scratch/260605-u17-probe-output.json"
      provides: "Raw probe stdout JSON (gitignored evidence) — captured to disk for orchestrator's adversarial-verifier"
    - path: ".scratch/260605-u17-probe-output.txt"
      provides: "Human-readable probe summary — full stdout+stderr capture"
    - path: ".scratch/260605-u17-probe-sha256-before.txt"
      provides: "Probe-v2 byte-identical contract receipt (pre-run hash)"
    - path: ".scratch/260605-u17-probe-sha256-after.txt"
      provides: "Probe-v2 byte-identical contract receipt (post-run hash); MUST equal -before.txt"
    - path: ".planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md"
      provides: "Section 6 — Probe re-run results (260605-u17, Vertex provider) appended"
      contains: "## Section 6 — Probe re-run results (260605-u17, Vertex provider)"
    - path: ".planning/quick/260605-u17-probe-vertex-rerun/260605-u17-SUMMARY.md"
      provides: "Quick close-out: tasks T1-T3, halt log, verdict, matrix row hit, Vertex-caveat note"
  key_links:
    - from: ".scratch/260605-u17-concurrent-probe-vertex.py"
      to: "Vertex AI us-central1-aiplatform.googleapis.com via google.genai.Client(vertexai=True)"
      via: "lib.llm_complete.get_llm_func() reads OMNIGRAPH_LLM_PROVIDER=vertex_gemini → returns Vertex callable → LightRAG entity-extract calls hit Vertex /v1beta1/.../generateContent"
      pattern: "no DeepSeek import; sha256 byte-identical pre/post; HTTP 200 on first model-endpoint call (or Halt F if blocked)"
    - from: "Section 6 verdict"
      to: "decision matrix in this PLAN"
      via: "speedup_ratio + both_processed + corruption check + Halt-class fallthrough"
      pattern: "verdict_string ∈ {GO, RISKY, BLOCKED-by-perf, BLOCKED-by-correctness, BLOCKED-by-corruption, BLOCKED-by-quota, BLOCKED-by-vertex-network, BLOCKED-by-auth}"
---

<objective>
Convert v1.2 batch_ingest concurrent rewrite viability from UNKNOWN → GO / RISKY / BLOCKED — by re-running the parent quick's concurrent probe with the LLM provider switched from DeepSeek to Vertex Gemini, bypassing the corp Cisco Umbrella TLS-block on `api.deepseek.com` that halted parent quick `260605-pwl` Halt F.

Pre-step (already done by sibling quick `260605-pwl`, do NOT re-run unless Halt A fires):
- venv certifi rebuilt with corp CA roots (123 total / 4 corp hits, ssl ground-truth probe passes)
- Vertex/oauth2/aiplatform endpoints TLS-reachable on this corp laptop (HTTP 404 server response = TLS happy, pre-flight by orchestrator)
- `.dev-runtime/.env` has `GOOGLE_APPLICATION_CREDENTIALS`, `GOOGLE_CLOUD_PROJECT`, `GOOGLE_CLOUD_LOCATION`, `OMNIGRAPH_LLM_PROVIDER=vertex_gemini`, `OMNIGRAPH_LLM_MODEL` all set; `.dev-runtime/gcp-paid-sa.json` exists (2400 bytes, dated May 4)
- Body files at `.dev-runtime/260605-mz1-probe/data/body_{1,2}.md` exist (14172 B / 9431 B)

Purpose: unblock v1.2 plan-phase decision. With a verdict, the orchestrator either fires `/gsd:plan-phase v1.2` (GO/RISKY) or queues alt-paths from parent RESEARCH.md Section 3 (BLOCKED).

**Caveat baked in:** Vertex Gemini ≠ prod parity (prod uses DeepSeek). Lock semantics in LightRAG may differ between LLM clients (different httpx pool, different tokenizer call paths, different retry semantics). The Vertex measurement is a STRONG SIGNAL but not DEFINITIVE for v1.2 viability. If verdict is GO with Vertex, orchestrator should explicitly note in v1.2 plan-phase that an Aliyun-side prod-parity follow-up smoke is needed before declaring the design final.

Output:
- Sibling probe-v2 at `.scratch/260605-u17-concurrent-probe-vertex.py` (≤80 LoC, sha256 contract)
- Probe stdout JSON at `.scratch/260605-u17-probe-output.json`
- Probe human-readable summary at `.scratch/260605-u17-probe-output.txt`
- Section 6 appended to `260605-mz1-RESEARCH.md` (existing Sections 1-5 untouched)
- `260605-u17-SUMMARY.md` (this quick's close-out)
- Single forward-only commit on origin/main covering 4 files (RESEARCH, PLAN, SUMMARY; STATE.md owned by orchestrator separately for adversarial-verify-pattern)
</objective>

<execution_context>
@C:/Users/huxxha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/huxxha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md
@.planning/quick/260605-pwl-probe-rerun-cert-rebuild/260605-pwl-PLAN.md
@.planning/quick/260605-pwl-probe-rerun-cert-rebuild/260605-pwl-SUMMARY.md
@.scratch/260605-mz1-concurrent-probe.py
@lib/llm_complete.py
@CLAUDE.md
@C:/Users/huxxha/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/vertex_ai_smoke_validated.md
@C:/Users/huxxha/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/aliyun_oauth_pin.md
@C:/Users/huxxha/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/feedback_git_add_explicit_in_parallel_quicks.md
@C:/Users/huxxha/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/feedback_no_amend_in_concurrent_quicks.md

<known_state>
- **Cert rebuild already done** by sibling quick 260605-pwl: certifi has 123 total / 4 corp hits, ssl ground-truth probe passes. Backup at `venv/Lib/site-packages/certifi/cacert.pem.bak-260605-pre-pwl`. T1 includes a re-verify (Halt A) — do NOT re-run rebuild unless verify fails.
- **Vertex reachability already pre-flighted** by orchestrator (per task description): `https://us-central1-aiplatform.googleapis.com/` → HTTP 404, `https://oauth2.googleapis.com/` → HTTP 404, `https://aiplatform.googleapis.com/` → HTTP 404. All TLS-happy (server-side 404 = handshake succeeded).
- **`.dev-runtime/.env`** has `OMNIGRAPH_LLM_PROVIDER=vertex_gemini`, `OMNIGRAPH_LLM_MODEL=<set>`, `GOOGLE_APPLICATION_CREDENTIALS=<set>`, `GOOGLE_CLOUD_PROJECT=<set>`, `GOOGLE_CLOUD_LOCATION=<set>` all confirmed present (orchestrator pre-flighted).
- **`.dev-runtime/gcp-paid-sa.json`** exists, 2400 bytes, dated May 4. Validated 2026-04-30 per memory `vertex_ai_smoke_validated`.
- **Body files** at `.dev-runtime/260605-mz1-probe/data/body_{1,2}.md` exist (14172 B + 9431 B). Article hashes: `c8cc5b1fb7` (img=2, "OpenClaw 入门") + `b37b0df5fb` (img=5, "智能体网络综述"), both `layer2_verdict='ok'`.
- **Three corp CA env vars** are pre-set on this dev machine and OVERRIDE certifi: `REQUESTS_CA_BUNDLE` / `SSL_CERT_FILE` / `CURL_CA_BUNDLE`. T2 must `unset` these for the probe shell (Halt B from pwl).
- **`lib/llm_complete.py`** has `get_llm_func()` at line 41 reading `OMNIGRAPH_LLM_PROVIDER` (default `deepseek`); with env=`vertex_gemini` returns the Vertex callable. This is the EXISTING project dispatcher — do NOT modify.
- **Parent probe** `.scratch/260605-mz1-concurrent-probe.py` — DO NOT MODIFY (parent contract sha256 `57308c595db37718...`). The whole point of probe-v2 is sibling, not in-place edit.
- **No SSH needed** for this quick. If SSH would be required, halt the quick.
</known_state>

<contracts>
**`.scratch/260605-mz1-concurrent-probe.py`** — DO NOT MODIFY. Parent contract from 260605-mz1.

**`.scratch/260605-u17-concurrent-probe-vertex.py`** — sibling, ≤80 LoC, minimal-diff off parent. Allowed change is the LLM-func source ONLY:

Parent (lines 11-12):
```python
from lib import embedding_func
from lib.llm_deepseek import deepseek_model_complete
```
Probe-v2:
```python
from lib import embedding_func
from lib.llm_complete import get_llm_func
```

Parent `make_rag()` body (line 20):
```python
rag = LightRAG(working_dir=str(STORAGE), llm_model_func=deepseek_model_complete,
               embedding_func=embedding_func,
               default_embedding_timeout=int(os.environ.get("LIGHTRAG_EMBEDDING_TIMEOUT", "180")))
```
Probe-v2:
```python
rag = LightRAG(working_dir=str(STORAGE), llm_model_func=get_llm_func(),
               embedding_func=embedding_func,
               default_embedding_timeout=int(os.environ.get("LIGHTRAG_EMBEDDING_TIMEOUT", "180")))
```

Other allowed cosmetic diffs (count toward LoC ceiling but expected):
- Module docstring may reference `260605-u17` and `Vertex` instead of `260605-mz1` and `DeepSeek/Vertex`
- `BASE = Path(os.environ.get("OMNIGRAPH_BASE_DIR", ".dev-runtime/260605-u17-probe-vertex"))` — default base dir bumped to isolate from mz1's storage

NO OTHER CHANGES. Same body files (read from `$OMNIGRAPH_BASE_DIR/data/body_{1,2}.md`), same 2-pass design (PASS A serial, PASS B 2-concurrent), same post-condition checks (graphml_valid, kv_store_valid, both_processed via `aget_docs_by_ids`), same JSON output (keys: `wall_s_serial`, `wall_s_concurrent`, `speedup_ratio`, `both_processed`, `serial_exception`, `concurrent_exception`, `graphml_valid`, `kv_store_valid`, `hashes`).

**LoC ceiling:** 80 lines max. Parent is 78. Probe-v2 should be 78-80 (1-line LLM-func change has zero net LoC delta; docstring + base-dir lines may add 0-2 LoC).
</contracts>

<decision_matrix>
After probe completes, the verdict is determined by exactly ONE row of this matrix:

| speedup_ratio | both_processed | corruption | Verdict | Section 6 conclusion |
|---|---|---|---|---|
| ≥ 1.7× | True | None | **GO** | v1.2 plan-phase fires; refined LoC ~+150-300 (asyncio.gather wrapper + per-task budget + partial-failure handling + telemetry + tests). **Caveat:** Vertex ≠ prod-parity; Aliyun-side prod-parity smoke recommended before final design. |
| 1.4× ≤ s < 1.7× | True | None | **RISKY** | Spike follow-up quick to investigate which LightRAG/Qdrant lock site is serializing. Vertex caveat applies. |
| < 1.4× | True | None | **BLOCKED-by-perf** | 4 alt paths in parent RESEARCH.md Sec 3; subprocess isolation likely best. |
| any | False | None | **BLOCKED-by-correctness** | Concurrent ainsert breaks data; subprocess isolation forced. |
| any | any | True | **BLOCKED-by-corruption** | Halt C; same as above. |

"corruption" = `graphml_valid` is not `True` OR `kv_store_valid` is not `True` OR `concurrent_exception` is not None.

**Out-of-matrix verdicts** (Halt classes that pre-empt the matrix):
- `BLOCKED-by-quota` — Halt D fires (Vertex 429 RESOURCE_EXHAUSTED mid-probe)
- `BLOCKED-by-vertex-network` — Halt F fires (Vertex root TLS-reachable in pre-flight, but model-endpoint call fails — Cisco may have whitelisted root path but blocked model paths)
- `BLOCKED-by-auth` — Halt G fires (SA JSON load OR oauth2 token refresh fails)
- `BLOCKED-by-cert-rebuild` — Halt A fires (cert state regressed since pwl; wholly separate quick needed to fix)
</decision_matrix>

<halt_branches>
**Halt A — cert regression since pwl** (re-verify, do NOT re-run rebuild):
- Symptom: T1 ssl ground-truth probe returns total != 123 OR corp != 4. Means a `pip install` between pwl and now clobbered cacert.pem.
- Action: STOP this quick. Do NOT run `260525-rebuild-cacert.py` here — that's a separate environment-unblocker quick. Skip to T3 with verdict **BLOCKED-by-cert-rebuild** in Section 6, citing the verify output.

**Halt B — env-var override forgotten**:
- Symptom: probe shell has `REQUESTS_CA_BUNDLE` / `SSL_CERT_FILE` / `CURL_CA_BUNDLE` set when probe runs; SSL handshakes fail despite rebuilt certifi.
- Action: ALL THREE must be `unset` in the probe-launching shell BEFORE the python invocation (encoded inline in T2 action). Do NOT unset globally / persistently.
- If unset is forgotten and probe SSL-fails on Vertex despite cert rebuild, this is the cause — fix and retry. Do NOT mark BLOCKED for this.

**Halt C — kv_store / graphml corruption during concurrent ainsert** (the original Halt #2 from parent quick):
- Symptom: probe stdout shows `graphml_valid: "corrupt: ..."` OR `kv_store_valid: "corrupt: ..."` OR `concurrent_exception: "..."` (not None) OR `both_processed: false`
- Action: STOP probe (single run already complete). Do NOT retry. Mark **BLOCKED-by-correctness** OR **BLOCKED-by-corruption** in Section 6. Capture full probe JSON to `.scratch/260605-u17-probe-output.json`.

**Halt D — Vertex 429 RESOURCE_EXHAUSTED**:
- Symptom: probe exception traceback contains `429` / `RESOURCE_EXHAUSTED` / `Quota exceeded`
- Action: probe is small (2 articles × 2 passes); if 429 fires, surface in Section 6 and mark **BLOCKED-by-quota**. Capture exception. Do NOT retry. Out-of-matrix verdict.

**Halt E — probe-v2 LoC ceiling violated** (≤80 LoC contract):
- Symptom: T1 verify shows `wc -l` > 80 on `.scratch/260605-u17-concurrent-probe-vertex.py`
- Action: STOP before T2 launch. Refuse to run a probe that violates the contract. Re-edit probe-v2 to compress, OR halt with verdict **BLOCKED-by-loc-violation** + commit nothing.

**Halt F — corp blocks Vertex on model paths despite root-path pre-flight**:
- Symptom: pre-flight HTTP 404 said TLS happy on `us-central1-aiplatform.googleapis.com`, but actual probe call to `/v1beta1/.../models/.../generateContent` raises `SSLV3_ALERT_HANDSHAKE_FAILURE` / `SSL: CERTIFICATE_VERIFY_FAILED` / `Connection timed out`. Cisco may have whitelisted root path but block model paths.
- Action: STOP probe. Mark **BLOCKED-by-vertex-network** in Section 6. Capture exception traceback. Recommend Aliyun-side fallback as next quick.

**Halt G — oauth2 fails to issue token**:
- Symptom: SA JSON load raises (`FileNotFoundError`, `KeyError`), OR `google.auth.exceptions.RefreshError`, OR oauth2 metadata service refuse with `401`/`403`/timeout.
- Action: STOP probe. Mark **BLOCKED-by-auth** in Section 6. Capture exception. Note: memory `aliyun_oauth_pin.md` covers Aliyun-only `/etc/hosts` pin to `142.250.73.106` for `oauth2.googleapis.com`; corp laptop may need different fix (likely none — pre-flight succeeded, so token issue would be SA-related).

**Halt log requirement:** any halt fires → STOP at that boundary, jump to T3 with the appropriate Section 6 verdict (do NOT swallow the failure into "GO/RISKY/BLOCKED" generic).
</halt_branches>

<task_count_justification>
3 tasks within standard 1-3 quick cap (no justification required, but for clarity):
- **T1 (pre-flight + probe-v2 author)** — verify cert state (Halt A), verify env config + body files, AUTHOR `.scratch/260605-u17-concurrent-probe-vertex.py` ≤80 LoC, capture sha256 (byte-identical contract). Halt E fires here if LoC violated.
- **T2 (probe run + capture)** — single shell invocation: source `.dev-runtime/.env`, unset corp CA env vars (Halt B), launch probe with Vertex provider, capture stdout JSON + stderr to two files. Halts C/D/F/G fire here.
- **T3 (Section 6 append + SUMMARY + commit)** — apply decision matrix, append Section 6 with Vertex caveat, write SUMMARY, atomic stage-commit-push (3 files; STATE.md owned by orchestrator separately).
</task_count_justification>

<forbidden>
- ❌ Modify `.scratch/260605-mz1-concurrent-probe.py` (parent contract sha256 `57308c595db37718...` — verify in T1)
- ❌ Modify any production source (`batch_ingest_from_spider.py`, `ingest_wechat.py`, `kb/`, `lib/`, `kg_synthesize.py`, `config.py`, `requirements.txt`, etc.)
- ❌ Modify `~/.hermes/` anything
- ❌ SSH to `aliyun-vitaclaw` or any other host (this is a local-only quick — if SSH would be needed, halt the quick instead)
- ❌ Run prod batch_ingest at any scale
- ❌ `git --amend` / `git reset --hard` / `git push --force` / `git rebase -i` (per `feedback_no_amend_in_concurrent_quicks`)
- ❌ `git add -A` / `git add .` (always explicit file list per `feedback_git_add_explicit_in_parallel_quicks`)
- ❌ `Co-Authored-By:` in commit message (CLAUDE.md global rule)
- ❌ Run probe with corp CA env vars still set (`REQUESTS_CA_BUNDLE` / `SSL_CERT_FILE` / `CURL_CA_BUNDLE`)
- ❌ Modify settings globally (registry / system env vars / `~/.hermes/.env`)
- ❌ Re-run `260525-rebuild-cacert.py` (cert rebuild is pwl's job; if cert regressed → Halt A → separate quick)
- ❌ Modify `lib/llm_complete.py` or any other dispatcher source (probe-v2 USES the existing dispatcher; do not edit it)
- ❌ Probe-v2 LoC > 80 (Halt E)
- ❌ Update `.planning/STATE.md` (orchestrator owns STATE update separately for adversarial-verify-pattern)
- ❌ Create a new RESEARCH.md for this quick — Section 6 appends to the parent's existing file
</forbidden>

<tasks>

<task type="auto">
  <name>T1: Pre-flight verify + author probe-v2 (sha256 contract)</name>
  <files>
    .scratch/260605-u17-concurrent-probe-vertex.py (new; gitignored)
    .scratch/260605-u17-probe-sha256-before.txt (new; gitignored)
  </files>
  <action>
    **Step 1 — verify cert state (Halt A)** by running the canonical ssl ground-truth probe (memory `corp_pem_rebuild_pattern`):
    ```
    venv/Scripts/python.exe -c "import ssl, certifi; ctx=ssl.SSLContext(ssl.PROTOCOL_TLS_CLIENT); ctx.load_verify_locations(cafile=certifi.where()); certs=ctx.get_ca_certs(); hits=sum(1 for c in certs if any(k in dict(x[0] for x in c['subject']).get('commonName','') for k in ('Cisco','Umbrella','EDC','cPKI'))); assert len(certs) >= 119 and hits == 4, f'FAIL: total={len(certs)}, corp={hits}'; print(f'OK: total={len(certs)}, corp={hits}')"
    ```
    Expected: `OK: total=123, corp=4` (or total within 119-130; corp MUST be exactly 4).
    If FAIL → Halt A: STOP this quick; skip to T3 with verdict `BLOCKED-by-cert-rebuild`.

    **Step 2 — verify parent probe sha256 unchanged** (parent contract):
    ```
    sha256sum .scratch/260605-mz1-concurrent-probe.py
    ```
    Expected: `57308c595db37718f9a845a311f65a4c25c6957d987b464649a22ff8bcf3ad23`. If diff → STOP; parent contract violated.

    **Step 3 — verify body files + env config:**
    ```
    test -s .dev-runtime/260605-mz1-probe/data/body_1.md && \
    test -s .dev-runtime/260605-mz1-probe/data/body_2.md && \
    test -s .dev-runtime/gcp-paid-sa.json && \
    grep -E '^(OMNIGRAPH_LLM_PROVIDER|OMNIGRAPH_LLM_MODEL|GOOGLE_APPLICATION_CREDENTIALS|GOOGLE_CLOUD_PROJECT|GOOGLE_CLOUD_LOCATION)=' .dev-runtime/.env | sed 's/=.*/=<set>/'
    ```
    Expected output: 5 lines all `<set>`. If `OMNIGRAPH_LLM_PROVIDER` value is not `vertex_gemini` (verify via `grep '^OMNIGRAPH_LLM_PROVIDER=' .dev-runtime/.env | cut -d= -f2-` — read literal value), STOP and surface — env config drift.

    **Step 4 — set up isolated probe base dir + body data:**
    ```
    mkdir -p .dev-runtime/260605-u17-probe-vertex/data/
    cp .dev-runtime/260605-mz1-probe/data/body_1.md .dev-runtime/260605-u17-probe-vertex/data/body_1.md
    cp .dev-runtime/260605-mz1-probe/data/body_2.md .dev-runtime/260605-u17-probe-vertex/data/body_2.md
    test -s .dev-runtime/260605-u17-probe-vertex/data/body_1.md && test -s .dev-runtime/260605-u17-probe-vertex/data/body_2.md
    ```

    **Step 5 — author `.scratch/260605-u17-concurrent-probe-vertex.py`** by minimal-diff off parent. Use Read on parent then Write the new file. Allowed diffs ONLY:

    1. Module docstring (line 1 area) — reference `260605-u17` + Vertex provider:
       ```python
       """260605-u17 LightRAG ainsert thread-safety probe — serial vs 2-concurrent (Vertex provider).
       Sibling of 260605-mz1-concurrent-probe.py with LLM provider switched DeepSeek → Vertex Gemini
       to bypass corp Cisco Umbrella TLS-block on api.deepseek.com.
       Usage: python 260605-u17-concurrent-probe-vertex.py <hash_1> <hash_2>
       Reads body from $OMNIGRAPH_BASE_DIR/data/body_{1,2}.md
       Requires: OMNIGRAPH_LLM_PROVIDER=vertex_gemini, GOOGLE_APPLICATION_CREDENTIALS set,
       Vertex AI reachable (corp cert rebuild required — see 260605-pwl).
       """
       ```
    2. LLM-func import (replaces parent line 12):
       ```python
       from lib.llm_complete import get_llm_func
       ```
       (parent had: `from lib.llm_deepseek import deepseek_model_complete`)
    3. `BASE` line (replaces parent line 14) — default to isolated u17 dir:
       ```python
       BASE = Path(os.environ.get("OMNIGRAPH_BASE_DIR", ".dev-runtime/260605-u17-probe-vertex")).resolve()
       ```
    4. `LightRAG(...)` constructor (replaces parent's `llm_model_func=deepseek_model_complete`):
       ```python
       rag = LightRAG(working_dir=str(STORAGE), llm_model_func=get_llm_func(),
                      embedding_func=embedding_func,
                      default_embedding_timeout=int(os.environ.get("LIGHTRAG_EMBEDDING_TIMEOUT", "180")))
       ```

    **NO OTHER CHANGES.** All other lines (PASS A, PASS B, post-conditions, JSON output, `__main__` entry) byte-identical to parent.

    **Step 6 — sha256 capture (byte-identical contract):**
    ```
    sha256sum .scratch/260605-u17-concurrent-probe-vertex.py > .scratch/260605-u17-probe-sha256-before.txt
    cat .scratch/260605-u17-probe-sha256-before.txt
    ```
    Save the hash; T2 will compare post-run.

    **Step 7 — verify probe-v2 LoC ceiling (Halt E)** + AST-parse validity:
    ```
    LOC=$(wc -l < .scratch/260605-u17-concurrent-probe-vertex.py)
    test "$LOC" -le 80 || { echo "HALT E: LoC=$LOC > 80"; exit 1; }
    venv/Scripts/python.exe -c "import ast; ast.parse(open('.scratch/260605-u17-concurrent-probe-vertex.py', encoding='utf-8').read()); print('AST OK')"
    echo "LoC=$LOC"
    ```
    Expected: `AST OK` printed, `LoC=78` to `LoC=80`. If LoC > 80 → Halt E: STOP, refuse to run probe; skip to T3 with verdict `BLOCKED-by-loc-violation` (orchestrator can decide whether to retry with compressed probe).
  </action>
  <verify>
    <automated>
      venv/Scripts/python.exe -c "import ssl, certifi; ctx=ssl.SSLContext(ssl.PROTOCOL_TLS_CLIENT); ctx.load_verify_locations(cafile=certifi.where()); certs=ctx.get_ca_certs(); hits=sum(1 for c in certs if any(k in dict(x[0] for x in c['subject']).get('commonName','') for k in ('Cisco','Umbrella','EDC','cPKI'))); print(f'cert: total={len(certs)}, corp={hits}'); assert len(certs) >= 119 and hits == 4" && \
      test -f .scratch/260605-u17-concurrent-probe-vertex.py && \
      LOC=$(wc -l < .scratch/260605-u17-concurrent-probe-vertex.py); test "$LOC" -le 80 && echo "LoC=$LOC" && \
      venv/Scripts/python.exe -c "import ast; ast.parse(open('.scratch/260605-u17-concurrent-probe-vertex.py', encoding='utf-8').read())" && \
      grep -q "from lib.llm_complete import get_llm_func" .scratch/260605-u17-concurrent-probe-vertex.py && \
      ! grep -q "from lib.llm_deepseek" .scratch/260605-u17-concurrent-probe-vertex.py && \
      grep -q "llm_model_func=get_llm_func()" .scratch/260605-u17-concurrent-probe-vertex.py && \
      test -s .scratch/260605-u17-probe-sha256-before.txt && \
      test -s .dev-runtime/260605-u17-probe-vertex/data/body_1.md && \
      test -s .dev-runtime/260605-u17-probe-vertex/data/body_2.md && \
      echo "T1 OK"
    </automated>
    Expected stdout includes `cert: total=123, corp=4`, `LoC=78` (or up to 80), and ends with `T1 OK`.
    If any check fails: Halt A (cert), Halt E (LoC), or env/body verify failure — STOP, jump to T3 with corresponding `BLOCKED-by-*` verdict.
  </verify>
  <done>
    cert state verified (123/4 corp). Parent probe sha256 unchanged. Body files present in isolated u17 base dir. Probe-v2 authored at `.scratch/260605-u17-concurrent-probe-vertex.py`, ≤80 LoC, AST-valid, contains `get_llm_func` import (NOT `deepseek_model_complete`), `llm_model_func=get_llm_func()` constructor kwarg. sha256 captured to `.scratch/260605-u17-probe-sha256-before.txt`.
  </done>
</task>

<task type="auto">
  <name>T2: Run probe-v2 with Vertex + capture output (sha256 verify)</name>
  <files>
    .scratch/260605-u17-probe-output.txt (new; gitignored — full stdout+stderr)
    .scratch/260605-u17-probe-output.json (new; gitignored — extracted JSON only)
    .scratch/260605-u17-probe-sha256-after.txt (new; gitignored)
  </files>
  <action>
    Run the probe in a single shell invocation that:
    1. Sources `.dev-runtime/.env` (gives `OMNIGRAPH_LLM_PROVIDER=vertex_gemini`, `OMNIGRAPH_LLM_MODEL`, `GOOGLE_APPLICATION_CREDENTIALS`, `GOOGLE_CLOUD_PROJECT`, `GOOGLE_CLOUD_LOCATION`, `OMNIGRAPH_BASE_DIR`, `GEMINI_API_KEY` for embedding)
    2. Unsets the 3 corp CA env vars for THIS shell only (Halt B fix; non-persistent)
    3. Overrides `OMNIGRAPH_BASE_DIR` to the isolated u17 dir (in case `.dev-runtime/.env` has a different value)
    4. Sets `OMNIGRAPH_VISION_SKIP_PROVIDERS=siliconflow,openrouter,gemini` (probe targets ainsert thread-safety, NOT vision)
    5. Invokes the probe with the 2 article hashes as positional args
    6. Captures stdout+stderr to `.scratch/260605-u17-probe-output.txt`

    **Single-shell run (Git Bash; PowerShell variant in fallback below):**
    ```
    set -a; source .dev-runtime/.env; set +a; \
    unset REQUESTS_CA_BUNDLE SSL_CERT_FILE CURL_CA_BUNDLE; \
    OMNIGRAPH_BASE_DIR="$(realpath .dev-runtime/260605-u17-probe-vertex)" \
    OMNIGRAPH_LLM_PROVIDER=vertex_gemini \
    OMNIGRAPH_VISION_SKIP_PROVIDERS=siliconflow,openrouter,gemini \
    LIGHTRAG_EMBEDDING_TIMEOUT=180 \
    venv/Scripts/python.exe .scratch/260605-u17-concurrent-probe-vertex.py c8cc5b1fb7 b37b0df5fb \
    > .scratch/260605-u17-probe-output.txt 2>&1
    echo "exit=$?"
    ```

    **PowerShell variant** (if Git Bash unavailable):
    ```
    Get-Content .dev-runtime/.env | ForEach-Object { if ($_ -match '^([^=#]+)=(.*)$') { Set-Item -Path "Env:$($matches[1])" -Value $matches[2] } }
    Remove-Item Env:REQUESTS_CA_BUNDLE,Env:SSL_CERT_FILE,Env:CURL_CA_BUNDLE -ErrorAction SilentlyContinue
    $env:OMNIGRAPH_BASE_DIR = (Resolve-Path .dev-runtime/260605-u17-probe-vertex).Path
    $env:OMNIGRAPH_LLM_PROVIDER = 'vertex_gemini'
    $env:OMNIGRAPH_VISION_SKIP_PROVIDERS = 'siliconflow,openrouter,gemini'
    $env:LIGHTRAG_EMBEDDING_TIMEOUT = '180'
    venv\Scripts\python.exe .scratch\260605-u17-concurrent-probe-vertex.py c8cc5b1fb7 b37b0df5fb *> .scratch\260605-u17-probe-output.txt
    "exit=$LASTEXITCODE"
    ```

    **Time budget:** 5-15 min wall. Vertex Gemini-2.5-flash typical entity-extract: 30-60s/article × 2 articles × 2 passes ≈ 4-8 min compute + asyncio.gather overlap on PASS B. If still running at T+30 min → halt: stuck embedding worker (see ISSUES #31/#32/#33 background). Mark `BLOCKED-by-stuck-worker` (extension of Halt C).

    **After probe completes** (regardless of exit code), capture sha256 + compare:
    ```
    sha256sum .scratch/260605-u17-concurrent-probe-vertex.py > .scratch/260605-u17-probe-sha256-after.txt
    diff .scratch/260605-u17-probe-sha256-before.txt .scratch/260605-u17-probe-sha256-after.txt
    ```
    Diff MUST be empty (byte-identical contract). If non-empty: surface as CRITICAL finding in T3 — probe was modified during run, voiding experiment.

    **Extract JSON:** the probe prints a JSON object at end of stdout. Extract to dedicated file for orchestrator's adversarial-verifier:
    ```
    venv/Scripts/python.exe -c "import re,sys; t=open('.scratch/260605-u17-probe-output.txt', encoding='utf-8').read(); m=re.search(r'\{[\s\S]+\}\s*$', t); open('.scratch/260605-u17-probe-output.json', 'w', encoding='utf-8').write(m.group(0) if m else '{\"crash\": true}'); print('extracted' if m else 'crash')"
    ```

    **Parse + classify:** read `.scratch/260605-u17-probe-output.json`. If valid JSON with all 9 keys (`wall_s_serial`, `wall_s_concurrent`, `speedup_ratio`, `both_processed`, `serial_exception`, `concurrent_exception`, `graphml_valid`, `kv_store_valid`, `hashes`) → probe completed; classify against decision matrix in T3.

    If JSON crash:
    - Look for `429` / `RESOURCE_EXHAUSTED` in stderr → Halt D (BLOCKED-by-quota)
    - Look for `RefreshError` / `default_credentials` / SA-related → Halt G (BLOCKED-by-auth)
    - Look for SSL / handshake / `Connection timed out` on a `*-aiplatform.googleapis.com` URL → Halt F (BLOCKED-by-vertex-network)
    - Look for SSL on any URL with corp env vars set → Halt B (re-launch with unset; do NOT mark BLOCKED)
    - Look for `corrupt` or `concurrent_exception` in JSON → Halt C
    - Anything else → unknown halt; capture full traceback for orchestrator's verifier; mark with closest matrix row

    **No retries.** Single run is the data point. Multiple runs may smear results from cached entities and bias speedup.
  </action>
  <verify>
    <automated>
      test -s .scratch/260605-u17-probe-output.txt && \
      diff .scratch/260605-u17-probe-sha256-before.txt .scratch/260605-u17-probe-sha256-after.txt && \
      test -s .scratch/260605-u17-probe-output.json && \
      venv/Scripts/python.exe -c "import json; j=json.loads(open('.scratch/260605-u17-probe-output.json', encoding='utf-8').read()); print('OK' if 'wall_s_serial' in j else 'CRASH'); print(json.dumps(j, indent=2)[:800])"
    </automated>
    Expected first stdout token is either `OK` (probe completed, JSON has `wall_s_serial`) or `CRASH` (probe halted before printing — content of `.txt` identifies which Halt fired).
    sha256 diff MUST be empty (byte-identical contract preserved).
  </verify>
  <done>
    Probe stdout+stderr captured to `.scratch/260605-u17-probe-output.txt`. JSON extracted to `.scratch/260605-u17-probe-output.json` (9-key dict OR `{"crash": true}` placeholder). sha256 byte-identical contract verified (before == after). Halt class identified if probe crashed.
  </done>
</task>

<task type="auto">
  <name>T3: Apply decision matrix → Section 6 + SUMMARY + atomic commit</name>
  <files>
    .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md (append-only; existing Sections 1-5 untouched)
    .planning/quick/260605-u17-probe-vertex-rerun/260605-u17-PLAN.md (this plan)
    .planning/quick/260605-u17-probe-vertex-rerun/260605-u17-SUMMARY.md (new)
  </files>
  <action>
    **Step 1 — apply decision matrix.** Read `.scratch/260605-u17-probe-output.json`, parse JSON, look up verdict in matrix above. Verdict string MUST be exactly one of:
    - `GO` (speedup ≥1.7× AND both_processed AND no corruption)
    - `RISKY` (1.4× ≤ speedup < 1.7× AND both_processed AND no corruption)
    - `BLOCKED-by-perf` (speedup < 1.4× AND both_processed AND no corruption)
    - `BLOCKED-by-correctness` (both_processed = False, no graph corruption)
    - `BLOCKED-by-corruption` (graphml/kv corrupt OR concurrent_exception)
    - `BLOCKED-by-quota` (Halt D Vertex 429)
    - `BLOCKED-by-vertex-network` (Halt F)
    - `BLOCKED-by-auth` (Halt G)
    - `BLOCKED-by-cert-rebuild` (Halt A — only if T1 failed and we skipped to T3)
    - `BLOCKED-by-loc-violation` (Halt E — only if T1 LoC check failed)

    **Step 2 — append Section 6 to existing RESEARCH.md.** Use Read then Edit (NOT Write — Edit prevents accidental whole-file overwrite of parent's Sections 1-5). Insertion point: end-of-file (after Section 5's `### Cross-references` block). Section 6 template:

    ```markdown


    ## Section 6 — Probe re-run results (260605-u17, Vertex provider)

    **Quick:** `260605-u17-probe-vertex-rerun`
    **Date:** 2026-06-05/06 ADT
    **Pre-step:** sibling quick `260605-pwl` cert rebuild — `123 total / 4 corp hits` (re-verified at T1; ssl ground-truth probe passes)
    **Vertex reachability:** TLS-happy on `us-central1-aiplatform.googleapis.com` + `oauth2.googleapis.com` + `aiplatform.googleapis.com` (HTTP 404 server response = handshake succeeded; orchestrator pre-flighted)
    **Probe contract:** `.scratch/260605-u17-concurrent-probe-vertex.py` is sibling of parent `260605-mz1-concurrent-probe.py` with one change — LLM provider switched DeepSeek → Vertex Gemini via `lib.llm_complete.get_llm_func()` dispatcher. Embedding/post-conditions/2-pass design/JSON output IDENTICAL. sha256 byte-identical pre/post run (`{hash}` matches before+after). LoC = `{N}` ≤ 80 cap.

    ### Run results

    | Metric | Value |
    |---|---|
    | PASS A serial wall_s | `{wall_s_serial}` |
    | PASS B 2-concurrent wall_s | `{wall_s_concurrent}` |
    | Speedup ratio | `{speedup_ratio}` |
    | both_processed | `{both_processed}` |
    | graphml_valid | `{graphml_valid}` |
    | kv_store_valid | `{kv_store_valid}` |
    | serial_exception | `{serial_exception_or_None}` |
    | concurrent_exception | `{concurrent_exception_or_None}` |

    ### Verdict — v1.2 batch_ingest concurrent rewrite viability

    **{VERDICT}** — {one-line rationale citing the decision-matrix row hit}

    {2-3 paragraphs of analysis tied to the verdict:}
    - If GO: refined LoC estimate, critical path through `batch_ingest_from_spider.py`, telemetry hooks needed
    - If RISKY: which lock site to spike (Qdrant client pool / kv_store_doc_status mutex / LightRAG entity-merge global lock)
    - If BLOCKED-*: which alt path from Section 3 is recommended (subprocess isolation / parallel systemd / wait upstream / raise wrapper cap)

    ### Vertex caveat (mandatory)

    **Vertex Gemini measurement is a STRONG SIGNAL but NOT DEFINITIVE for v1.2 viability.**

    Production uses DeepSeek as the LLM provider; this probe uses Vertex Gemini. Lock semantics inside LightRAG may differ between LLM clients — different httpx connection pool, different tokenizer call paths (Vertex SDK uses `google.genai` not `tiktoken` for token counting), different retry semantics, different async cancellation behavior. A GO verdict here suggests v1.2 concurrent-rewrite is FEASIBLE but does not guarantee prod-parity speedup.

    **If verdict is GO or RISKY:** the v1.2 plan-phase MUST include an explicit sub-task for an Aliyun-side prod-parity follow-up smoke (DeepSeek provider, prod-parity network) BEFORE declaring the asyncio.gather wrapper design final. The Aliyun-side smoke is the only way to lock-in the prod-parity signal — corp laptop cannot reach `api.deepseek.com` (Halt F from sibling quick `260605-pwl`).

    **If verdict is BLOCKED-by-*:** the BLOCKED branch's alt paths (subprocess isolation / parallel systemd / wait upstream / raise wrapper cap) are LLM-client-agnostic — the prod-parity caveat does NOT change the verdict.

    ### Halt log

    {list any Halt branches that fired in this re-run; if none: "No halts fired — probe completed clean."}

    ### Cross-references

    - Sibling probe-v2: `.scratch/260605-u17-concurrent-probe-vertex.py` (gitignored, ≤80 LoC, sha256 contract)
    - Probe output JSON: `.scratch/260605-u17-probe-output.json` (gitignored, raw decision-matrix input)
    - Probe full stdout+stderr: `.scratch/260605-u17-probe-output.txt` (gitignored)
    - sha256 contract receipts: `.scratch/260605-u17-probe-sha256-{before,after}.txt` (gitignored, identical)
    - Quick close-out: `.planning/quick/260605-u17-probe-vertex-rerun/260605-u17-SUMMARY.md`
    - Parent RESEARCH Section 5 (260605-pwl Halt F): now bypassed by Vertex provider switch
    - Memory `vertex_ai_smoke_validated` — Vertex SA + endpoint pairing ground truth
    - Memory `aliyun_oauth_pin` — relevant if Halt G fires (oauth2 endpoint)
    ```

    Fill placeholders `{wall_s_serial}` etc from `.scratch/260605-u17-probe-output.json`. If probe crashed, use `N/A — Halt {X}` and cite the halt class in the rationale paragraph.

    **Step 3 — write SUMMARY.md** (`.planning/quick/260605-u17-probe-vertex-rerun/260605-u17-SUMMARY.md`):
    - Header: quick id, date, mode (execute), status, commit hash (filled post-commit; forward-fix follow-up commit is acceptable per `feedback_no_amend_in_concurrent_quicks`)
    - Tasks completed table (T1-T3 + halt status per task)
    - Halt log (which Halts fired, with symptom + action taken)
    - Verdict + decision-matrix row hit + verbatim probe metrics from JSON
    - **Vertex-not-prod-parity caveat** repeated (mandatory; same wording as Section 6)
    - Artifacts produced (Section 6 anchor in RESEARCH.md, this SUMMARY, .scratch/* gitignored evidence files, probe-v2 sibling script)
    - Discipline notes: explicit `git add` only, no `--amend`, byte-identical probe-v2 contract honored, no SSH used, no parent probe modified, no production source touched
    - Cross-refs: sibling quicks `260605-mz1` + `260605-pwl`, memory `vertex_ai_smoke_validated`, decision matrix in PLAN.md

    **Step 4 — atomic stage-commit-push** (single Bash &&-chain per `feedback_git_add_explicit_in_parallel_quicks` strengthened pattern):

    ```
    git add \
      .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md \
      .planning/quick/260605-u17-probe-vertex-rerun/260605-u17-PLAN.md \
      .planning/quick/260605-u17-probe-vertex-rerun/260605-u17-SUMMARY.md && \
    git commit -m "$(cat <<'EOF'
    docs(quick-260605-u17): probe-v2 vertex results — v1.2 viability {VERDICT}

    Pre-step: pwl cert rebuild + Vertex reachability verified (HTTP 404 server response = TLS happy)
    Probe v2 (sibling .scratch/260605-u17-concurrent-probe-vertex.py, ≤80 LoC):
    - Provider: vertex_gemini (NOT prod parity; orchestrator caveat noted)
    - serial: {N}s
    - 2-concurrent: {M}s
    - speedup: {ratio}x
    - both_processed: {bool}
    - corruption: {None|description}

    Verdict for v1.2 batch_ingest concurrent rewrite: {GO|RISKY|BLOCKED-by-*}
    {One-line rationale citing decision-matrix row}

    Caveat: Vertex measurement is STRONG SIGNAL not DEFINITIVE for v1.2 viability —
    prod uses DeepSeek; lock semantics may differ between LLM clients. Aliyun-side
    prod-parity follow-up smoke recommended in v1.2 plan-phase if GO/RISKY.

    Files: 260605-mz1-RESEARCH.md (Section 6 append), 260605-u17-PLAN.md, 260605-u17-SUMMARY.md
    EOF
    )" && \
    git push origin main
    ```

    **Forbidden in commit message:** `Co-Authored-By:` (CLAUDE.md global rule).

    **Post-commit audit** (per `feedback_git_add_explicit_in_parallel_quicks`):
    ```
    git show --stat HEAD
    ```
    Verify exactly 3 files in commit (no sibling-quick artifacts absorbed, no `.scratch/*` leaked, no STATE.md included — orchestrator owns STATE separately). If absorption detected → forward-fix only via attribution-drift note in SUMMARY.md, NEVER `--amend` per `feedback_no_amend_in_concurrent_quicks`.
  </action>
  <verify>
    <automated>
      grep -q '^## Section 6 — Probe re-run results (260605-u17, Vertex provider)$' .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md && \
      grep -qE '\*\*(GO|RISKY|BLOCKED-by-(perf|correctness|corruption|quota|vertex-network|auth|cert-rebuild|loc-violation))\*\*' .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md && \
      grep -q "STRONG SIGNAL but NOT DEFINITIVE" .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md && \
      grep -q '^## Section 5' .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md && \
      grep -q '^## Section 1' .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md && \
      test -s .planning/quick/260605-u17-probe-vertex-rerun/260605-u17-SUMMARY.md && \
      git log --oneline -1 | grep -q "quick-260605-u17" && \
      git show --stat HEAD | grep -qE "(260605-mz1-RESEARCH|260605-u17-(PLAN|SUMMARY))\.md" && \
      ! git show --name-only HEAD | grep -qE "(\\.scratch/|STATE\\.md|lib/|kb/|batch_ingest|ingest_wechat)" && \
      echo "T3 OK"
    </automated>
    Expected stdout ends with `T3 OK`.
    Manual cross-check (no automated test):
    - Parent RESEARCH.md still has Sections 1-5 byte-identical pre/post (read first ~220 lines, confirm `## Cross-references` of Section 5 remains intact at line ~213)
    - Commit subject contains exactly one verdict token from {GO, RISKY, BLOCKED-by-perf, BLOCKED-by-correctness, BLOCKED-by-corruption, BLOCKED-by-quota, BLOCKED-by-vertex-network, BLOCKED-by-auth, BLOCKED-by-cert-rebuild, BLOCKED-by-loc-violation}
    - Commit body contains numeric serial/concurrent/speedup OR `N/A — Halt X` if probe halted
    - Commit body contains the Vertex caveat
    - `git show --name-only HEAD` lists exactly 3 files (no `.scratch/`, no STATE.md, no production source)
  </verify>
  <done>
    Section 6 appended to RESEARCH.md with verdict + run results + Vertex caveat + halt log + cross-refs. SUMMARY.md created with caveat repeated. Single forward-only commit on origin/main with explicit 3-file list. v1.2 viability gate is now GO/RISKY/BLOCKED-by-* — orchestrator can pick next action (plan-phase v1.2, alt-path quick, or environment unblocker quick) without re-running probe. Orchestrator runs adversarial-verify Workflow on the artifacts before final orchestrator commit.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, the orchestrator can ask one closure question: **"What's the v1.2 batch_ingest concurrent rewrite verdict from the Vertex probe?"**

Answer must be exactly one of:
- `GO` → fire `/gsd:plan-phase v1.2` next (refined LoC ~+150-300); v1.2 plan-phase MUST include Aliyun-side prod-parity smoke as sub-task
- `RISKY` → fire follow-up spike quick to investigate the dominant lock site; v1.2 plan-phase MUST include Aliyun-side prod-parity smoke as sub-task
- `BLOCKED-by-perf` → pick alt path from parent RESEARCH.md Section 3 (subprocess isolation likely best); LLM-client-agnostic, no Vertex caveat
- `BLOCKED-by-correctness` / `BLOCKED-by-corruption` → subprocess isolation forced; concurrent ainsert is unsafe; LLM-client-agnostic
- `BLOCKED-by-quota` → embedding/LLM RPM cap hit; investigate per-key rotation OR Vertex paid-tier migration before retrying
- `BLOCKED-by-vertex-network` → Aliyun-side fallback is now the only viable next quick (corp laptop blocks BOTH DeepSeek and Vertex)
- `BLOCKED-by-auth` → SA JSON / oauth2 fix needed; investigate corp-laptop-specific auth issue
- `BLOCKED-by-cert-rebuild` → cert regressed since pwl; rerun `260525-rebuild-cacert.py` in a separate environment-unblocker quick
- `BLOCKED-by-loc-violation` → probe-v2 LoC ceiling violated; compress probe and retry

The verdict must be cited in the commit subject AND in Section 6's "Verdict" line, identical strings.
</verification>

<success_criteria>
1. `.scratch/260605-u17-concurrent-probe-vertex.py` exists, ≤80 LoC, AST-valid, contains `from lib.llm_complete import get_llm_func` AND `llm_model_func=get_llm_func()` AND does NOT contain `from lib.llm_deepseek` / `deepseek_model_complete`.
2. `.scratch/260605-u17-concurrent-probe-vertex.py` byte-identical before/after probe run (sha256 match) — captured to `.scratch/260605-u17-probe-sha256-{before,after}.txt`.
3. `.scratch/260605-u17-probe-output.txt` + `.scratch/260605-u17-probe-output.json` exist; JSON contains either (a) 9 keys (`wall_s_serial`, `wall_s_concurrent`, `speedup_ratio`, `both_processed`, `serial_exception`, `concurrent_exception`, `graphml_valid`, `kv_store_valid`, `hashes`); OR (b) `{"crash": true}` placeholder + `.txt` contains traceback identifying Halt B/C/D/F/G.
4. `260605-mz1-RESEARCH.md` Section 6 appended (parent's existing Sections 1-5 byte-identical pre/post — verified by reading first ~220 lines).
5. `260605-mz1-RESEARCH.md` Section 6 contains the **Vertex-not-prod-parity caveat** verbatim phrase: `STRONG SIGNAL but NOT DEFINITIVE`.
6. `260605-u17-SUMMARY.md` exists with verdict + halt log + matrix row hit + Vertex caveat repeated.
7. **NOT** updated: `STATE.md` (orchestrator owns separately), `.scratch/260605-mz1-concurrent-probe.py` (parent contract), `lib/llm_complete.py` (existing dispatcher), any production source.
8. Single forward-only commit on `origin/main` with explicit 3-file list (RESEARCH.md, PLAN.md, SUMMARY.md). Push accepted (forward-only). No `--amend` / `git reset` / `--force-push`. No `git add -A`. No `Co-Authored-By:`.
9. v1.2 viability state transitioned UNKNOWN → one of {GO, RISKY, BLOCKED-by-{perf,correctness,corruption,quota,vertex-network,auth,cert-rebuild,loc-violation}}.
10. No SSH used in this quick.
</success_criteria>

<output>
After completion, create `.planning/quick/260605-u17-probe-vertex-rerun/260605-u17-SUMMARY.md` with:
- Header: quick id, date, mode (execute), status, commit hash (post-commit)
- Tasks completed table (T1-T3 + halt status per task)
- Halt log (which Halts fired, with symptom + action taken)
- Verdict + decision-matrix row hit + verbatim probe metrics from `.scratch/260605-u17-probe-output.json`
- **Vertex-not-prod-parity caveat** repeated verbatim from Section 6 (mandatory)
- Artifacts list (Section 6 anchor in parent RESEARCH.md, this SUMMARY, `.scratch/*` gitignored evidence files, probe-v2 sibling script)
- Discipline notes: explicit `git add` only, no `--amend`/`reset`/`force`, byte-identical probe-v2 contract honored, no SSH used, no parent probe modified, no production source touched, no STATE.md update (orchestrator owns)
- Cross-refs: sibling quicks `260605-mz1` (parent) + `260605-pwl` (sibling pre-step), memory `vertex_ai_smoke_validated`, memory `aliyun_oauth_pin`, decision matrix in PLAN.md, ISSUES #38/#39/#40
</output>
