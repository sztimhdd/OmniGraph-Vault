# Quick 260519-s65 — SUMMARY

**Quick ID:** 260519-s65
**Date:** 2026-05-19
**Status:** ✅ complete (all must_haves met, UAT passed end-to-end)

## What was done

Fixed two `/api/synthesize?mode=long_form` bugs reported in
`.scratch/kb-long-form-bug-report-260519.md`:

- **Bug 1**: long_form markdown emitted `![](http://localhost:8765/{hash}/{n}.jpg)` →
  browser 404 (KB serves at `/static/img/{hash}/{n}.jpg`).
- **Bug 2**: long_form emitted `[Entity: X]` style citations instead of
  `/article/{hash}.html`. `_resolve_sources_from_markdown` regex matched 0 →
  `sources=[]`, `confidence='no_results'`.

**Mirrored the FU-1 (`3be03f5`) pattern**: tightened the prompt template +
added a belt-and-suspenders regex rewrite. Did NOT touch `kg_synthesize.py`
(legacy Hermes path).

## Files changed

- `kb/services/synthesize.py` — 3 edits, all surgical:
  1. `_LONG_FORM_PROMPT_TEMPLATE_ZH` (line ~95): image-instruction rewritten to
     `![alt](/static/img/{{hash}}/{{n}}.jpg)` + explicit `localhost:8765`
     prohibition (browser permanent counter-example, port not exposed).
  2. `_LONG_FORM_PROMPT_TEMPLATE_EN` (line ~114): same change in English.
  3. NEW module-level `_LEGACY_IMAGE_URL_PATTERN` regex constant +
     `_rewrite_image_urls(markdown)` helper (idempotent, pure function).
  4. `kb_synthesize` happy-path (between C1 return and
     `_resolve_sources_from_markdown`): 1-line `markdown = _rewrite_image_urls(markdown)`.

- `tests/unit/kb/test_synthesize_long_form_prompt.py` — NEW, 9 tests pinning
  template substrings + lang dispatch (mirrors `test_synthesize_qa_prompt.py`).
- `tests/integration/kb/test_long_form_url_rewrite.py` — NEW, 7 tests:
  rewrite-pure / rewrite-idempotent / rewrite-preserves-non-8765 / DB-backed
  source resolution / end-to-end `kb_synthesize(mode='long_form')` belt-and-
  suspenders verification.

## Test results

### New tests (16/16 GREEN)

`venv/Scripts/python.exe -m pytest tests/unit/kb/test_synthesize_long_form_prompt.py
tests/integration/kb/test_long_form_url_rewrite.py tests/unit/kb/test_synthesize_qa_prompt.py -v`
→ **21 passed in 3.37s**.
Log: `.scratch/quick-260519-s65-pytest-new.log`.

### Regression check

`venv/Scripts/python.exe -m pytest tests/integration/kb/test_long_form_synthesis.py
tests/integration/kb/test_synthesize_citation_format.py tests/integration/kb/test_qa_link_contract.py
tests/integration/kb/test_synthesize_structured.py tests/integration/kb/test_api_synthesize.py`
→ 30 passed, **4 failed**.
Log: `.scratch/quick-260519-s65-pytest-regression.log`.

The 4 failures (`test_default_mode_is_qa_when_unspecified`, `test_qa_mode_uses_existing_prompt`,
`test_kb_synthesize_accepts_mode_kwarg`, `test_synthesize_zh_lang_directive_used`) are
**pre-existing baseline failures** caused by the FU-1 commit `3be03f5`: those tests
assert `query_text.startswith("请用中文回答。\n\n")` (the bare-directive shape from before
FU-1), but FU-1 made QA mode wrap the question in the new `_QA_PROMPT_TEMPLATE_*`. **Verified
independent of this quick** by `git stash push -- kb/services/synthesize.py` followed
by re-running the same 4 tests → same 4 failures on baseline. Out of scope for s65.

## Local UAT (CLAUDE.md Rule 6)

**Server:** `venv/Scripts/python.exe .scratch/local_serve.py` against `.dev-runtime/`
DB on port 8766; Vertex Gemini LLM via paid SA at `.dev-runtime/gcp-paid-sa.json`.
Server log: `.scratch/quick-260519-s65-uat-server.log`.

**API direct submission** — POST `/api/synthesize` `mode=long_form lang=en`
question="What is an Agent" → job `04dd710fb234`. Polled to terminal,
saved at `.scratch/quick-260519-s65-uat-result.json`. Verified via
`.scratch/quick-260519-s65-uat-summary.txt`:

```
status= done
confidence= kg
fallback_used= False
localhost:8765 count = 0
/static/img/ count   = 4
/article/ count      = 14
sources= 1
entities= 8
```

Both bugs are fixed in the LLM output:
- ✅ 0 `localhost:8765` URLs (was the bug)
- ✅ 4 `/static/img/` paths
- ✅ 14 `/article/{hash}.html` citations (was 0)
- ✅ `confidence='kg'` (was `'no_results'`)
- ✅ 1 source resolved + 8 entities resolved

**Browser UAT** — submitted `What is an Agent` mode=Deep-research lang=en via
`http://127.0.0.1:8766/ask/`. Result rendered with:
- 2 inline `<img>` elements with alt text "ReAct Framework" / "Context Engineering"
  whose `src` is `/static/img/...` (verified via accessibility snapshot)
- 19 `[/article/{hash}.html]` citations across body + 5-item References list
- Right sidebar `参考来源 Sources` shows the resolved article
  (`用 Agent 团队来做知识管理，非常顶！` → `/articles/19c08ca449.html`)
- Right sidebar `相关实体 Related Entities` shows 8 chips (Agent团队, Claude Code,
  Moxt, Notion, Obsidian, Skill, Slack, Webhook)

Screenshots:
- `.playwright-mcp/long-form-fix-uat-01-ask-page-deep-research-mode.png` (132 KB) —
  empty form with `Deep research` mode pre-selected, question typed
- `.playwright-mcp/long-form-fix-uat-02-result-top-with-images-and-sources.png` (443 KB) —
  result top: title, intro paragraphs with `[/article/...]` citation tokens, first
  inline image visible at viewport bottom (proves `/static/img/` rendering works)
- `.playwright-mcp/long-form-fix-uat-03-references-and-sources-resolved.png` (3.4 MB,
  fullPage=true) — full result page with all body content, References list, Sources
  sidebar populated, Related Entities sidebar populated

## Hard scope guards honored

- ❌ `kg_synthesize.py` — **not touched**. Legacy Hermes path; the `IMAGE_URL_DIRECTIVE`
  + `custom_prompt` construction at lines 33-38 / 167-173 are unchanged.
- ❌ `lang.js` / SSG / translation / Wave 3-6 — not touched.
- ❌ Explicit `git add` of every file — no `-A` / `.`.
- ❌ No `--amend` / `git reset` / `--force-push`.
- ❌ Anti-fabrication: every numeric claim cites `.scratch/<log>` path.
- ❌ Prompt template uses `localhost:8765` only as a forbidden literal mention
  (negation), never as a positive instruction. Verified by tests
  `test_long_form_zh_template_forbids_localhost_8765` /
  `test_long_form_en_template_forbids_localhost_8765`.

## Skills used

- `Skill(skill="python-patterns")` — matched existing `_QA_PROMPT_TEMPLATE_*`
  module-level constant style (doubled-brace `{{hash}}` / `{{n}}` for
  `str.format(question=...)` literal escape).
- `Skill(skill="writing-tests")` — matched `test_synthesize_qa_prompt.py` style:
  one assertion per behavior, one test per file, real-DB integration tests with
  pre-existing `fixture_db` from conftest.

## Commit

`974f888` — `fix(kb-quick-260519-s65): long_form image URL + citation bugs`

Forward-only commit on main; no `--amend`, no `git reset`, no force-push.
This SUMMARY hash backfilled in a follow-up forward-only commit per
`feedback_no_amend_in_concurrent_quicks.md` discipline.
