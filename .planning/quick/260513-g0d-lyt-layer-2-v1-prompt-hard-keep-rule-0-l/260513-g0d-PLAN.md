---
quick_id: 260513-g0d
type: quick
description: "lyt: Layer 2 v1 prompt — HARD-KEEP RULE 0 + LF-2.7 English long-form"
files_modified:
  - lib/article_filter.py
  - tests/unit/test_article_filter.py
autonomous: true
must_haves:
  truths:
    - "PROMPT_VERSION_LAYER2 == 'layer2_v1_20260513' (bumped from layer2_v0_20260507)"
    - "_LAYER2_V1_PROMPT_BODY exists; _LAYER2_V0_PROMPT_BODY is deleted (zero references in lib/ and tests/)"
    - "v1 prompt body contains RULE 0 HARD-KEEP section with all 14 keywords + 5-line precision matching defense"
    - "v1 prompt body contains LF-2.7 English long-form section with 3 conditions"
    - "v1 prompt body contains v0 Sections A-E + depth_score (1/2/3) + relevant (true/false) + verdict formula + JSON output schema (byte-identical from v0)"
    - "5 new pytest tests pass (literal-string assertions, no live LLM calls)"
    - "All previous Layer 1/2 unit tests still pass (no regression)"
    - "layer2_full_body_score function signature unchanged; verdict alphabet unchanged ('ok'/'reject'/'scrape_fail'/None)"
  artifacts:
    - path: "lib/article_filter.py"
      provides: "Layer 2 v1 prompt + version bump"
      contains: "PROMPT_VERSION_LAYER2: str = \"layer2_v1_20260513\""
    - path: "lib/article_filter.py"
      provides: "v1 prompt body constant"
      contains: "_LAYER2_V1_PROMPT_BODY"
    - path: "tests/unit/test_article_filter.py"
      provides: "5 new prompt-shape tests"
      contains: "test_prompt_version_layer2_is_v1_20260513"
  key_links:
    - from: "layer2_full_body_score (line ~609)"
      to: "_LAYER2_V1_PROMPT_BODY"
      via: "prompt assembly: prompt = _LAYER2_V1_PROMPT_BODY + '\\n' + json.dumps(payload, ...)"
      pattern: "_LAYER2_V1_PROMPT_BODY"
    - from: "FilterResult.prompt_version on Layer 2 results"
      to: "PROMPT_VERSION_LAYER2 module constant"
      via: "all FilterResult instances stamp prompt_version=PROMPT_VERSION_LAYER2"
      pattern: "prompt_version=PROMPT_VERSION_LAYER2"
    - from: "RULE 0 keyword list + 5-line precision rules in _LAYER2_V1_PROMPT_BODY"
      to: "RULE 0 keyword list + 5-line precision rules in _LAYER1_V1_PROMPT_BODY (lib/article_filter.py:207)"
      via: "byte-identical cross-layer mirror per spec § Constraints"
      pattern: "(OpenClaw|Hermes Agent|OmniGraph|Claude Code|Cursor|Aider|Codex CLI|Continue|MCP|Model Context Protocol)"
---

<objective>
Translate the user-designed Patch A spec (2026-05-13 Layer 2 audit response) into a single-commit code change that ships Layer 2 v1 prompt:

1. Bump `PROMPT_VERSION_LAYER2` from `layer2_v0_20260507` -> `layer2_v1_20260513`
2. Replace `_LAYER2_V0_PROMPT_BODY` constant with new `_LAYER2_V1_PROMPT_BODY` containing:
   - NEW: RULE 0 HARD-KEEP section (mirrors Layer 1 v1, byte-identical keyword list + 5-line precision rules)
   - NEW: LF-2.7 English long-form section (>=5000 chars + >=90% ASCII + relevant=true -> depth=2 default)
   - VERBATIM v0: Sections A-E judgment heuristics, depth_score (1/2/3), relevant (true/false), verdict formula, JSON output schema
3. Update reference site (line 610) from `_LAYER2_V0_PROMPT_BODY` to `_LAYER2_V1_PROMPT_BODY`
4. Add 5 literal-string pytest tests pinning v1 invariants

Purpose: Close 52% true-body Layer 2 reject FN rate against English long-form tech blogs (seangoedecke / lucumr / antirez / grantslatton) by adding RULE 0 hard-keep + LF-2.7 English-narrative depth default. Bumping `PROMPT_VERSION_LAYER2` automatically triggers LF-2.6 reclassify storm at next 08:20 ADT cron (~600 KOL + ~71 RSS, ~$2.22 / ~4h wallclock at deepseek-v4-flash batch=5x60s x 444 batches).

Output: 1 commit on `main`. Operator ships via separate gate (cron post-pull or manual trigger). Reclassify storm is OUT OF SCOPE for this task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@lib/article_filter.py
@tests/unit/test_article_filter.py

<reference_files_for_byte_identical_mirroring>
<!-- Layer 1 v1 RULE 0 keyword list + 5-line precision rules -- MUST be mirrored byte-identical into Layer 2 v1 (per spec section Constraints "Reuse Layer 1 v1 (commit aea2872) RULE 0 keyword list + 5-line precision rules - should be byte-identical for cross-layer consistency"). -->

Source: `lib/article_filter.py:207-296` constant `_LAYER1_V1_PROMPT_BODY`
Target sections to mirror verbatim:
  - "RULE 0 - HARD-KEEP" header line
  - [A] / [B] / [C] keyword groups (lines 221-233)
  - 精确匹配规则 + 5 bullet lines (lines 235-240)
  - "重要: 即使文章是新闻..." + "不要单看模糊概念..." (lines 242-246)

These sections are written for Layer 1 (pre-scrape, title+digest only). Layer 2 v1 inserts the SAME RULE 0 block adapted for "title 或 body" wording (Layer 2 sees full body) - keyword list + precision rules byte-identical, only the header sentence shifts from "title 或 digest" -> "title 或 body" per spec section Change 2 section "RULE 0 v1 新增".
</reference_files_for_byte_identical_mirroring>

<interfaces>
<!-- Module-level constants and types the executor needs. Already in lib/article_filter.py. -->

```python
# lib/article_filter.py - current state (pre-change)

PROMPT_VERSION_LAYER1: str = "layer1_v1_20260512"
PROMPT_VERSION_LAYER2: str = "layer2_v0_20260507"  # <-- bump this

# Layer 1 v1 prompt body - byte-identical RULE 0 + 5-line precision rules
# at lines 215-246. Layer 2 v1 must mirror this block.
_LAYER1_V1_PROMPT_BODY: str = """..."""  # line 207

# Layer 2 v0 prompt body - to be replaced
_LAYER2_V0_PROMPT_BODY: str = """..."""  # line 309 (definition)

# Layer 2 prompt assembly site
async def layer2_full_body_score(articles):
    ...
    prompt = (
        _LAYER2_V0_PROMPT_BODY  # line 610 - rename to _LAYER2_V1_PROMPT_BODY
        + "\n"
        + json.dumps(payload, ensure_ascii=False)
    )
```

```python
# tests/unit/test_article_filter.py - current import block (line 60)
from lib.article_filter import (
    ArticleMeta,
    ArticleWithBody,
    FilterResult,
    LAYER1_BATCH_SIZE,
    LAYER2_BATCH_SIZE,
    PROMPT_VERSION_LAYER1,
    PROMPT_VERSION_LAYER2,
    layer1_pre_filter,
    layer2_full_body_score,
    persist_layer1_verdicts,
    persist_layer2_verdicts,
)
# Tests file uses pytest-asyncio mode='auto' (configured in pyproject.toml).
```
</interfaces>

<grep_audit>
<!-- Pre-flight grep run by planner - confirms exactly 2 occurrences in lib/ -->

```
$ grep -rn _LAYER2_V0_PROMPT_BODY lib/ tests/
lib/article_filter.py:309:_LAYER2_V0_PROMPT_BODY: str = """\
lib/article_filter.py:610:        _LAYER2_V0_PROMPT_BODY
```

(Other matches in `.planning/` are historical PLAN/SUMMARY docs and MUST NOT be edited.)

Note: spec mentions "line ~277" and "line ~554" but actual lines are 309 and 610 due to scrape_fail Patch B addition (commit `a7a8ab6`) shifting code down. Executor should use grep, not line numbers, to locate sites.
</grep_audit>

<spec_summary>
<!-- Concrete shape of v1 prompt body - translated from spec section Change 2 -->

`_LAYER2_V1_PROMPT_BODY` structure (top-to-bottom):

1. **Header** (verbatim from v0):
   "你是一个 AI/LLM 文章 Layer 2 深度过滤器..." + "知识库核心兴趣..." (v0 lines 310-312)

2. **NEW: RULE 0 - HARD-KEEP section** (mirrors Layer 1 v1):
   - Header with separator lines containing "RULE 0 - HARD-KEEP(项目核心硬规则,优先级最高) v1 新增"
   - Trigger sentence: "title 或 body 整词包含以下任一关键字(大小写不敏感) -> 立刻输出 verdict='ok' (depth_score=3, relevant=true, skip 深度判断):"
   - Group [A] 项目自身 / Anthropic CLI / 用户文档 (5 keywords, byte-identical to L1):
     `OpenClaw`, `Hermes Agent`, `OmniGraph`, `Harness` (Anthropic/agent/CLI 上下文), `CLAUDE.md`, `AGENTS.md`
   - Group [B] 用户核心 stack 工具 (3 keywords, byte-identical to L1):
     `Claude Code`, `Cursor` (IDE/agent 上下文), `Aider` / `Codex CLI` / `Continue` (IDE 插件)
   - Group [C] Agent SDK / 协议 (4 keywords, byte-identical to L1):
     `MCP` / `Model Context Protocol`, `OpenAI Agents SDK`, `Anthropic Agents SDK`, `Anthropic Skills` / `Skills.md`
   - 精确匹配规则 + EXACTLY 5 bullet lines (byte-identical to L1):
     - `"Claude Code" 必须作为完整两词短语出现才算命中`
     - `裸 "Claude" ... -> 不走 R0`
     - `"Cursor" 必须在 IDE / agent 上下文,排除 "cursor pointer" / "DB cursor"`
     - `"Harness" 只在同句含 "Anthropic" / "agent" / "Claude Code" / "CLAUDE.md" 才触发`
     - `"MCP" 优先假设是 "Model Context Protocol"`
   - Note: "即使文章是新闻 / 八卦 / 软文,只要严格命中 RULE 0,仍输出 depth_score=3 relevant=true"

3. **NEW: LF-2.7 - 英文长文宽放 section**:
   - Header containing "LF-2.7 - 英文长文宽放(v1 新增)"
   - 3 conditions (ALL must hold):
     - body 前 500 字符 >= 90% ASCII
     - body 总长度 >= 5000 chars
     - relevant = true (核心仍在 agent/LLM/AI 工程范围)
   - Action: depth_score default = 2 (unless 纯产品发布 / 课程营销 / 招生招聘 -> depth=1)
   - Rationale: "英文 tech blog (seangoedecke / lucumr / antirez / grantslatton) narrative+analytical reasoning 风格本身就是 effort signal, 不应因缺少中文式 mechanism 章节就被打入 depth=1"
   - Out of scope note: "中文文章不适用 LF-2.7;沿用 v0 calibration"

4. **VERBATIM v0**: depth_score (1/2/3) + relevant (true/false) sections
   - Copy from `_LAYER2_V0_PROMPT_BODY` lines 314-331 (sections "## 1. depth_score", "## 2. relevant", "## 3. reason") byte-identical
   - EXTEND only the `reason 字段` description / examples to allow:
     - R0 hits: `"R0:OpenClaw"` / `"R0:Claude Code"`
     - LF-2.7 hits: `"LF2.7:lucumr 英文长文"` / `"LF2.7:seangoedecke"`
     - Standard: `"depth=2:具体场景拆解"` / `"depth=3:架构源码"`
     - Reject: `"reject:营销"` / `"reject:非AI"`

5. **NEW: verdict 决策 section** (verbatim formula from existing layer2_full_body_score code at line 689):
   - "verdict = 'ok' if (relevant AND depth_score>=2) else 'reject'"
   - Note: RULE 0 hits ALSO produce verdict='ok' (because depth_score=3 + relevant=true, formula consistent)

6. **VERBATIM v0**: "## 关键判断窍门" + Sections A / B / C / D / E
   - Copy from `_LAYER2_V0_PROMPT_BODY` lines 333-364 byte-identical
   - DO NOT reword Section B "正文 mechanism 检查" - LF-2.7 is the new safety net for English long-form, B stays as-is for Chinese articles

7. **VERBATIM v0**: "## 输出格式(严格)" + JSON example
   - Copy from `_LAYER2_V0_PROMPT_BODY` lines 366-384 byte-identical
   - Triple-backtick example block stays unchanged

Final assembly: header + RULE 0 + LF-2.7 + depth/relevant/reason + verdict + 关键判断窍门 + 输出格式 + 输入文章列表 hint.

**Length sanity**: v1 should be ~1000-1500 chars longer than v0. v0 is ~3300 chars (lines 309-384 minus quotes); v1 target ~4300-4800 chars.
</spec_summary>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump PROMPT_VERSION_LAYER2 + replace v0 prompt body with v1 + rename reference site</name>
  <files>lib/article_filter.py</files>
  <behavior>
    Three coupled edits in one file (single atomic change):

    Edit 1 (line 80): version bump
      - From: `PROMPT_VERSION_LAYER2: str = "layer2_v0_20260507"`
      - To: `PROMPT_VERSION_LAYER2: str = "layer2_v1_20260513"`
      - Update the docstring on line 81-84 to reference v1 spike date `2026-05-13` and the audit doc instead of the v0 spike report. Keep LF-2.7 reference number same (it remains 2.6 in REQUIREMENTS - re-evaluation predicate semantics are unchanged).

    Edit 2 (line 309): rename + replace prompt body
      - DELETE: `_LAYER2_V0_PROMPT_BODY` constant (entire definition lines ~309-384). Do NOT leave dead variable in active code (per spec section Change 2 "Old _LAYER2_V0_PROMPT_BODY should be DELETED"). Git history preserves the v0 text; commit `aea2872` for Layer 1 followed the same pattern.
      - INSERT: `_LAYER2_V1_PROMPT_BODY: str = """..."""` triple-quoted constant.

      v1 body assembly (top-to-bottom - see spec_summary for full content shape):

      a) **Header** - copy v0 lines 310-312 byte-identical:
         "你是一个 AI/LLM 文章 Layer 2 深度过滤器。..." through "...Agent 框架 / 工程实践。"

      b) **RULE 0 - HARD-KEEP section** (NEW - mirror Layer 1 v1 from `_LAYER1_V1_PROMPT_BODY` lines 215-246):
         - Use the same separator-line style as Layer 1 v1 (byte-identical separators).
         - Header: "RULE 0 - HARD-KEEP(项目核心硬规则,优先级最高) v1 新增"
         - Trigger sentence ADAPTED for Layer 2: "title 或 body 整词包含以下任一关键字(大小写不敏感) -> 立刻输出 verdict='ok' (depth_score=3, relevant=true, skip 深度判断):" (note: `body` not `digest` because Layer 2 has full body)
         - Groups [A] / [B] / [C] - byte-identical to Layer 1 v1 (see lines 221-233 of current `_LAYER1_V1_PROMPT_BODY`). Each keyword group must be a literal copy.
         - 精确匹配规则 + EXACTLY 5 bullet lines - byte-identical to Layer 1 v1 (lines 235-240).
         - Closing notes: "即使文章是新闻 / 八卦 / 软文,只要严格命中 RULE 0,仍输出 depth_score=3 relevant=true,verdict='ok'。" (adapted from Layer 1 wording - Layer 2 outputs depth/relevant explicitly).

      c) **LF-2.7 - 英文长文宽放 section** (NEW):
         - Header: "LF-2.7 - 英文长文宽放(v1 新增)"
         - 3 conditions (write each as a bullet):
           - body 前 500 字符 >= 90% ASCII
           - body 总长度 >= 5000 chars
           - relevant = true (核心仍在 agent / LLM / AI 工程范围)
         - When ALL hold -> depth_score default = 2 (unless 纯产品发布 / 课程营销 / 招生招聘 -> depth=1)
         - Rationale: 英文 tech blog (seangoedecke / lucumr / antirez / grantslatton) 风格 narrative + analytical reasoning, 本身是 effort signal, 不应因缺少中文 mechanism 章节就被打入 depth=1。
         - Out of scope: 中文文章不适用 LF-2.7;沿用 v0 校准。

      d) **depth_score / relevant / reason sections** (verbatim v0, line 314-331):
         - Copy `## 1. depth_score (1 / 2 / 3)` block byte-identical
         - Copy `## 2. relevant (true / false)` block byte-identical
         - Copy `## 3. reason (<=30 中文字符)` block, then EXTEND with reason format guidance:
           - R0 命中: "R0:OpenClaw" / "R0:Claude Code"
           - LF-2.7 命中: "LF2.7:lucumr 英文长文" / "LF2.7:seangoedecke"
           - 标准 keep: "depth=2:具体场景拆解" / "depth=3:架构源码"
           - reject: "reject:营销" / "reject:非AI"

      e) **verdict 决策 section** (NEW header, formula codified from existing logic at line 689):
         - "## verdict 决策(同 v0 LF-2.5 决策规则)"
         - "verdict = 'ok' if (relevant AND depth_score >= 2) else 'reject'"
         - Note: RULE 0 命中输出 depth=3 + relevant=true 自然落入 'ok'(formula 一致)。

      f) **关键判断窍门 + Sections A-E** (verbatim v0, lines 333-364):
         - Copy `## 关键判断窍门(Layer 2 的核心战场)` block + Sections A / B / C / D / E byte-identical.
         - DO NOT modify Section B "正文 mechanism 检查"; LF-2.7 is the safety net for English long-form, B stays as-is for the Chinese article path.

      g) **输出格式 section** (verbatim v0, lines 366-384):
         - Copy `## 输出格式(严格)` + JSON 输出示例 byte-identical.
         - Final line: "输入文章列表 (JSON):" stays unchanged.

    Edit 3 (line 610): rename reference
      - From: `_LAYER2_V0_PROMPT_BODY` (inside the `prompt = (...)` assembly in `layer2_full_body_score`)
      - To: `_LAYER2_V1_PROMPT_BODY`

    Validation pre-commit:
      - `grep -rn "_LAYER2_V0_PROMPT_BODY" lib/` returns ZERO matches
      - `grep -rn "_LAYER2_V1_PROMPT_BODY" lib/` returns EXACTLY 2 matches (definition + reference site)
      - `grep -rn "_LAYER2_V0_PROMPT_BODY" tests/` returns ZERO matches (tests don't reference v0 by name; they assert via `PROMPT_VERSION_LAYER2`)
      - `python -m py_compile lib/article_filter.py` exits 0

    DO NOT MODIFY:
      - `layer2_full_body_score` function signature
      - verdict decision formula (line 689 - `'ok' if (relevant and depth_score >= 2) else 'reject'`)
      - verdict alphabet (still `'ok'` / `'reject'` / `'scrape_fail'` / `None`)
      - Patch B `_detect_scrape_failed` helper or its constants
      - Layer 1 prompt or `PROMPT_VERSION_LAYER1`
      - Persist helpers (`persist_layer1_verdicts` / `persist_layer2_verdicts`)
      - `__all__` exports - `PROMPT_VERSION_LAYER2` already exported, the new `_LAYER2_V1_PROMPT_BODY` constant is private (leading underscore) so does NOT need to be added.

    Reference for byte-identical mirroring:
      Read `lib/article_filter.py` lines 207-295 (`_LAYER1_V1_PROMPT_BODY` constant). The RULE 0 section + 5-line precision rules in Layer 1 v1 are the source of truth - copy character-for-character.
  </behavior>
  <action>
    1. Read `lib/article_filter.py` lines 207-295 to capture exact `_LAYER1_V1_PROMPT_BODY` content for the RULE 0 + 5-line precision rules block.
    2. Read `lib/article_filter.py` lines 309-384 to capture exact `_LAYER2_V0_PROMPT_BODY` content (header + sections A-E + 输出格式).
    3. Use Edit tool 3 times on `lib/article_filter.py`:
       a) Line 80 - version bump string (and adjust docstring on lines 81-84 to reference 2026-05-13 audit instead of v0 spike).
       b) Lines 309-384 - replace entire `_LAYER2_V0_PROMPT_BODY` definition block with new `_LAYER2_V1_PROMPT_BODY` per behavior section's 7-block assembly (a-g).
       c) Line 610 - rename `_LAYER2_V0_PROMPT_BODY` -> `_LAYER2_V1_PROMPT_BODY` in the prompt assembly inside `layer2_full_body_score`.
    4. Verify with grep (see verify automated cmd).
    5. Verify static syntax with `python -m py_compile`.
  </action>
  <verify>
    <automated>cd C:\Users\huxxha\Desktop\OmniGraph-Vault &amp;&amp; .venv/Scripts/python.exe -m py_compile lib/article_filter.py &amp;&amp; .venv/Scripts/python.exe -c "from lib.article_filter import PROMPT_VERSION_LAYER2, _LAYER2_V1_PROMPT_BODY; assert PROMPT_VERSION_LAYER2 == 'layer2_v1_20260513'; assert 'RULE 0' in _LAYER2_V1_PROMPT_BODY; assert 'LF-2.7' in _LAYER2_V1_PROMPT_BODY; assert 'OpenClaw' in _LAYER2_V1_PROMPT_BODY; assert 'Claude Code' in _LAYER2_V1_PROMPT_BODY; assert 'CLAUDE.md' in _LAYER2_V1_PROMPT_BODY; assert 'MCP' in _LAYER2_V1_PROMPT_BODY; assert '精确匹配规则' in _LAYER2_V1_PROMPT_BODY; print(f'PASS len={len(_LAYER2_V1_PROMPT_BODY)}')"</automated>
  </verify>
  <done>
    - Line 80 reads `PROMPT_VERSION_LAYER2: str = "layer2_v1_20260513"`
    - `_LAYER2_V0_PROMPT_BODY` no longer exists in `lib/article_filter.py`
    - `_LAYER2_V1_PROMPT_BODY` exists and is referenced exactly once in `layer2_full_body_score` prompt assembly
    - v1 body length >= v0 length + 800 chars (sanity check for new RULE 0 + LF-2.7 sections)
    - All RULE 0 keywords + 5 precision rules present in v1 body (literal-string check passes)
    - `python -m py_compile lib/article_filter.py` exits 0
    - Sections A-E + depth_score/relevant/output blocks remain byte-identical from v0 (manual diff check; tests in Task 2 will pin the new sections specifically)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 5 prompt-shape unit tests + verify full test suite</name>
  <files>tests/unit/test_article_filter.py</files>
  <behavior>
    Add 5 NEW pytest test functions at the end of `tests/unit/test_article_filter.py`. All tests are pure literal-string assertions - NO live LLM calls, NO database, NO async fixtures.

    Test 1: `test_prompt_version_layer2_is_v1_20260513`
      - Synchronous (no `async`).
      - `from lib.article_filter import PROMPT_VERSION_LAYER2`
      - `assert PROMPT_VERSION_LAYER2 == "layer2_v1_20260513"`
      - Purpose: pin version-bump invariant; if any future change touches PROMPT_VERSION_LAYER2 without updating this test, the bump signal is loud.

    Test 2: `test_v1_prompt_body_contains_rule_0`
      - Synchronous.
      - `from lib.article_filter import _LAYER2_V1_PROMPT_BODY`
      - `assert "RULE 0" in _LAYER2_V1_PROMPT_BODY`
      - `assert "HARD-KEEP" in _LAYER2_V1_PROMPT_BODY`
      - Purpose: pin the section header presence.

    Test 3: `test_v1_prompt_body_contains_lf_2_7`
      - Synchronous.
      - `from lib.article_filter import _LAYER2_V1_PROMPT_BODY`
      - `assert "LF-2.7" in _LAYER2_V1_PROMPT_BODY`
      - `assert "英文长文" in _LAYER2_V1_PROMPT_BODY`
      - Purpose: pin the new English long-form section presence.

    Test 4: `test_v1_prompt_body_contains_core_keywords`
      - Synchronous.
      - `from lib.article_filter import _LAYER2_V1_PROMPT_BODY`
      - For each keyword in `["OpenClaw", "Hermes Agent", "OmniGraph", "Claude Code", "MCP", "CLAUDE.md"]`:
        `assert kw in _LAYER2_V1_PROMPT_BODY, f"missing core keyword: {kw}"`
      - Purpose: pin the project-core keyword list (subset - full list cross-layer pinned to Layer 1 v1 docstring; this test guarantees the 6 highest-priority hits are present).

    Test 5: `test_v1_prompt_body_contains_precision_matching_rules`
      - Synchronous.
      - `from lib.article_filter import _LAYER2_V1_PROMPT_BODY`
      - `assert "精确匹配规则" in _LAYER2_V1_PROMPT_BODY`
      - For each rule fragment in this list (>=5 fragments - one per precision rule):
        - `'裸 "Claude"' in _LAYER2_V1_PROMPT_BODY`
        - `'"Cursor" 必须' in _LAYER2_V1_PROMPT_BODY`  (anchors the IDE/agent context rule)
        - `'"Harness" 只在' in _LAYER2_V1_PROMPT_BODY`
        - `'"MCP" 优先假设' in _LAYER2_V1_PROMPT_BODY`
        - `'"Claude Code" 必须作为完整两词短语' in _LAYER2_V1_PROMPT_BODY`
      - Purpose: pin the 5-line precision matching defense; if RULE 0 ever drifts, this test fails and forces audit.

    Anti-patterns to AVOID (per CLAUDE.md Lesson "Tests must pin independently-verifiable values, not mirror impl"):
      - Do NOT compute the prompt length from a formula matching the implementation.
      - Do NOT assert `_LAYER2_V1_PROMPT_BODY == _LAYER1_V1_PROMPT_BODY[a:b] + ...` - that mirrors impl.
      - DO assert literal strings the user explicitly chose (RULE 0, LF-2.7, keyword list, precision rules) - these are user-verifiable spec values, not impl artifacts.

    Test placement:
      - Append to end of file, after `test_layer2_over_max_raises` (current last test).
      - Add a section header comment indicating "Patch A v1 prompt (2026-05-13 Layer 2 audit) - prompt-shape tests".

    Imports:
      - `_LAYER2_V1_PROMPT_BODY` must be added to the existing `from lib.article_filter import (...)` block (line 60).
      - Module-level imports already cover everything else needed.
  </behavior>
  <action>
    1. Read `tests/unit/test_article_filter.py` to confirm current import block and last test position.
    2. Use Edit tool to add `_LAYER2_V1_PROMPT_BODY` to the import block (line 60ish).
    3. Use Edit tool to append the section header comment + 5 new test functions at end of file.
    4. Run full test file with pytest to confirm:
       - 5 new tests PASS
       - All existing Layer 1 + Layer 2 + scrape_fail tests still PASS (no regression - verifies Task 1 didn't break anything either)
  </action>
  <verify>
    <automated>cd C:\Users\huxxha\Desktop\OmniGraph-Vault &amp;&amp; .venv/Scripts/python.exe -m pytest tests/unit/test_article_filter.py -v 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - `tests/unit/test_article_filter.py` imports `_LAYER2_V1_PROMPT_BODY` from `lib.article_filter`
    - 5 new tests exist with names matching behavior
    - `pytest tests/unit/test_article_filter.py -v` reports >= (previous_count + 5) tests, ALL passing
    - No test in the file is skipped or errored
    - Test 1 fails loudly if version constant drifts (independent verifiable value `"layer2_v1_20260513"`)
    - Tests 2-5 fail loudly if RULE 0 / LF-2.7 / keyword list / precision rules drift
  </done>
</task>

<task type="auto">
  <name>Task 3: Length sanity check + commit with explicit file paths</name>
  <files>lib/article_filter.py, tests/unit/test_article_filter.py</files>
  <action>
    1. Run length sanity check:
       ```
       .venv/Scripts/python.exe -c "
       # Compare v1 body length against v0 baseline (~3300 chars per spec).
       # v1 should be 1000-1500 chars longer for new RULE 0 + LF-2.7 sections.
       from lib.article_filter import _LAYER2_V1_PROMPT_BODY
       v1_len = len(_LAYER2_V1_PROMPT_BODY)
       assert v1_len >= 4100, f'v1 too short: {v1_len} chars (expected >= v0+800)'
       assert v1_len <= 6500, f'v1 too long: {v1_len} chars (suspicious - possible duplication)'
       print(f'OK: v1 body length = {v1_len} chars (target ~4300-4800)')
       "
       ```
       If outside [4100, 6500], stop and audit - likely missing/duplicated section.

    2. Run final grep audit:
       - `grep -rn "_LAYER2_V0_PROMPT_BODY" lib/ tests/` -> must return ZERO matches
       - `grep -rn "_LAYER2_V1_PROMPT_BODY" lib/ tests/` -> must return >= 3 matches (1 definition in lib/article_filter.py + 1 reference in lib/article_filter.py + 1 import in tests/unit/test_article_filter.py)

    3. Commit with EXPLICIT file paths only (per CLAUDE.md Lesson 2026-05-11 - concurrent quicks share staging area; `git add -A` would absorb sibling artifacts; per spec section Constraints "Staging race"):
       ```
       git add lib/article_filter.py tests/unit/test_article_filter.py
       git status   # verify ONLY those 2 files are staged
       git commit -m "feat(layer2): add HARD-KEEP RULE 0 + LF-2.7 English long-form (Patch A of 2026-05-13 audit)"
       ```
       DO NOT add `.planning/quick/260513-g0d-*` artifacts to this commit - those are committed by the GSD wrapper after this task succeeds. DO NOT use `git add -A` or `git add .`. DO NOT push.

    4. Report final state:
       - Commit SHA + message
       - Test results: N passing
       - Length stats: v0 length, v1 length, delta
       - Reference site rename: 1 occurrence at line ~610
  </action>
  <verify>
    <automated>cd C:\Users\huxxha\Desktop\OmniGraph-Vault &amp;&amp; git log -1 --format="%H %s" &amp;&amp; git diff --stat HEAD~1 HEAD -- lib/article_filter.py tests/unit/test_article_filter.py</automated>
  </verify>
  <done>
    - Length sanity check passes (v1_len in [4100, 6500])
    - `grep _LAYER2_V0_PROMPT_BODY lib/ tests/` returns 0 lines
    - 1 commit on `main` with message exactly: `feat(layer2): add HARD-KEEP RULE 0 + LF-2.7 English long-form (Patch A of 2026-05-13 audit)`
    - Commit touches EXACTLY 2 files: `lib/article_filter.py` + `tests/unit/test_article_filter.py`
    - No push, no SSH, no reclassify trigger (operator gate per spec section Constraints)
    - Working tree clean except for `.planning/quick/260513-g0d-*` (GSD wrapper handles those separately)
  </done>
</task>

</tasks>

<verification>
End-to-end check (executor runs all three at end of Task 3):

1. **Static**: `python -m py_compile lib/article_filter.py` exits 0
2. **Constants**: `python -c "from lib.article_filter import PROMPT_VERSION_LAYER2; assert PROMPT_VERSION_LAYER2 == 'layer2_v1_20260513'"` exits 0
3. **Test suite**: `pytest tests/unit/test_article_filter.py -v` reports all tests passing (previous count + 5)
4. **Reference grep**: `grep -rn "_LAYER2_V0_PROMPT_BODY" lib/ tests/` returns ZERO matches
5. **Length sanity**: `_LAYER2_V1_PROMPT_BODY` length >= v0 + 800 chars
6. **Commit**: `git log -1` shows the spec-mandated message; `git diff --stat HEAD~1 HEAD` shows EXACTLY 2 files changed.

Optional (executor's discretion if quick):
7. **Cross-layer byte-identity**: read Layer 1 v1 RULE 0 keyword list + 5-line precision rules; spot-check Layer 2 v1 has byte-identical text. (Tests 4-5 already cover the literal strings; this is a manual eye check.)
</verification>

<success_criteria>
- [ ] `PROMPT_VERSION_LAYER2 == "layer2_v1_20260513"`
- [ ] `_LAYER2_V0_PROMPT_BODY` deleted (zero references in lib/ + tests/)
- [ ] `_LAYER2_V1_PROMPT_BODY` exists and is referenced from `layer2_full_body_score`
- [ ] v1 body contains: header (verbatim v0) + RULE 0 section (mirror Layer 1 v1) + LF-2.7 section (new) + depth_score/relevant/reason (verbatim v0 + extended reason format) + verdict formula + Sections A-E (verbatim v0) + 输出格式 (verbatim v0)
- [ ] v1 body length >= v0 + 800 chars
- [ ] 5 new pytest tests pass (literal-string assertions)
- [ ] All previous Layer 1 + Layer 2 + scrape_fail unit tests still pass (no regression)
- [ ] `layer2_full_body_score` signature unchanged
- [ ] Verdict alphabet unchanged (`'ok'` / `'reject'` / `'scrape_fail'` / `None`)
- [ ] Single commit on main with mandated message; touches exactly 2 files
- [ ] No push, no SSH, no reclassify storm triggered
- [ ] `git add` used explicit file paths (not `-A` or `.`)
</success_criteria>

<output>
After completion, create `.planning/quick/260513-g0d-lyt-layer-2-v1-prompt-hard-keep-rule-0-l/260513-g0d-SUMMARY.md` per the standard quick-task summary template, including:
- Final v0 length / v1 length / delta
- Number of tests added / total passing
- Commit SHA + message
- Confirmation that operator gate is respected (no push, no SSH, no reclassify trigger)
- Note for operator: at next 08:20 ADT `daily-classify-rss-layer2` cron post-pull (or manual trigger), LF-2.6 reclassify storm fires automatically (~600 KOL + ~71 RSS rows whose `layer2_prompt_version != "layer2_v1_20260513"`); estimated cost ~$2.22 / wallclock ~4h at deepseek-v4-flash batch=5x60s x ~134 batches.
</output>
