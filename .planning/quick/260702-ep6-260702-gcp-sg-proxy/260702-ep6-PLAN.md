---
phase: 260702-ep6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/aliyun/systemd/omnigraph-vertex-proxy.service
autonomous: false
requirements:
  - EP6-01
must_haves:
  truths:
    - "omnigraph-vertex-proxy.service is active (running) on Aliyun"
    - "Port 18080 LISTEN on 127.0.0.1 on Aliyun"
    - "Embedding smoke: EMBED OK dim=3072 via GCP SG proxy"
    - "L1 classify runs with null=0 (no Connection refused on Vertex embedding)"
  artifacts:
    - path: "deploy/aliyun/systemd/omnigraph-vertex-proxy.service"
      provides: "Updated ExecStart targeting GCP SG VM (35.198.243.36:22)"
      contains: "35.198.243.36"
    - path: ".planning/quick/260702-ep6-260702-gcp-sg-proxy/260702-ep6-SUMMARY.md"
      provides: "Closure record with SSH params, smoke evidence, commit hash"
  key_links:
    - from: "omnigraph-vertex-proxy.service ExecStart"
      to: "35.198.243.36:22"
      via: "ssh -D 127.0.0.1:18080"
      pattern: "35\\.198\\.243\\.36"
    - from: "OMNIGRAPH_EMBED_PROXY (already set in .env)"
      to: "127.0.0.1:18080"
      via: "httpx.AsyncClient(proxy=socks5h://127.0.0.1:18080)"
      pattern: "socks5h://127.0.0.1:18080"
---

<objective>
Switch omnigraph-vertex-proxy.service's SSH SOCKS5 egress tunnel exit node from
Hermes (ohca.ddns.net) to GCP Singapore VM (35.198.243.36:22), restoring the
Aliyun → Vertex AI embedding path after Hermes went down (10149 restart loop).

Purpose: Unblock the 194-article L1 NULL backlog. TCP:22 to 35.198.243.36 is
already confirmed reachable from Aliyun. GCP VM is the WireGuard peer so it has
direct Google API egress. OMNIGRAPH_EMBED_PROXY and all other .env / service
settings stay unchanged — only the SSH target changes.

Output:
- Updated omnigraph-vertex-proxy.service with GCP SG exit node
- Service active + :18080 LISTEN on Aliyun
- Embedding smoke passes (dim=3072)
- SUMMARY.md with commit hash + evidence
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@deploy/aliyun/systemd/omnigraph-vertex-proxy.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover GCP VM SSH login parameters from Aliyun</name>
  <files>N/A (read-only recon — no file modifications)</files>
  <action>
SSH into Aliyun as root and discover the SSH user + key that can reach 35.198.243.36:22.

Run the following probes in order:

1. Inspect ~/.ssh/config for any Host block matching 35.198.243.36 or a GCP alias:
   ssh aliyun-vitaclaw "cat /root/.ssh/config 2>/dev/null || echo NO_SSH_CONFIG"

2. List keys in /root/.ssh/:
   ssh aliyun-vitaclaw "ls -la /root/.ssh/"

3. Check WireGuard config for any deployment key references:
   ssh aliyun-vitaclaw "grep -r 'IdentityFile\|User\|35\.198\.243\.36' /root/.ssh/config /etc/wireguard/*.conf 2>/dev/null | grep -v PrivateKey || echo NO_MATCH"

4. If ~/.ssh/config has a matching Host block, extract User + IdentityFile from it.

5. Attempt a connection test with the discovered key (or id_rsa / id_ed25519 fallback) as root:
   ssh aliyun-vitaclaw "ssh -i /root/.ssh/<discovered_key> -o BatchMode=yes -o ConnectTimeout=10 -o StrictHostKeyChecking=accept-new root@35.198.243.36 'echo GCP_OK' 2>&1"

   Try common users in order if root fails: ubuntu, user, ec2-user, gcp-user.
   Try common key names in order: id_ed25519, id_rsa, gcp_key, google_compute_engine.

Record: confirmed SSH user (e.g. root or ubuntu), confirmed key path (e.g. /root/.ssh/id_ed25519).
These values are used verbatim in Task 2.

Do NOT attempt to create new keys or change authorized_keys. Read-only discovery only.
  </action>
  <verify>
    <automated>
      ssh aliyun-vitaclaw "ssh -i /root/.ssh/&lt;key&gt; -o BatchMode=yes -o ConnectTimeout=10 -o StrictHostKeyChecking=accept-new &lt;user&gt;@35.198.243.36 'echo GCP_OK' 2>&amp;1" | grep GCP_OK
    </automated>
  </verify>
  <done>Output contains "GCP_OK" — SSH user + key path confirmed reachable from Aliyun to 35.198.243.36:22.</done>
</task>

<task type="auto">
  <name>Task 2: Update proxy service unit + deploy to Aliyun</name>
  <files>deploy/aliyun/systemd/omnigraph-vertex-proxy.service</files>
  <action>
Using the SSH user and key path confirmed in Task 1, update the service unit.

ONLY change ExecStart — all other directives stay byte-identical:

Current ExecStart (last line):
  -D 127.0.0.1:18080 \
  hermes

New ExecStart (replace the `hermes` target with the GCP VM):
  -D 127.0.0.1:18080 \
  -i /root/.ssh/&lt;key_from_task1&gt; \
  &lt;user_from_task1&gt;@35.198.243.36

Also update the [Unit] Description to reflect the new exit node (replace "Hermes" with "GCP-SG"):
  Description=OmniGraph SSH SOCKS5 egress proxy: Aliyun -> GCP-SG (Vertex/Google API unblock, #75 mitigation)

Also update the inline comment above ExecStart:
  # Python processes with ALL_PROXY=socks5h://127.0.0.1:18080 route Google traffic via GCP SG VM

Preserve exactly:
  -N -T
  -o ConnectTimeout=15
  -o ExitOnForwardFailure=yes
  -o ServerAliveInterval=30
  -o ServerAliveCountMax=3
  -o BatchMode=yes
  -o StrictHostKeyChecking=accept-new
  Restart=always
  RestartSec=10
  StartLimitIntervalSec=0

After writing the local file, deploy to Aliyun and restart the service:

  # SCP unit file to Aliyun
  scp deploy/aliyun/systemd/omnigraph-vertex-proxy.service \
      aliyun-vitaclaw:/etc/systemd/system/omnigraph-vertex-proxy.service

  # Reload + restart on Aliyun
  ssh aliyun-vitaclaw "systemctl daemon-reload && systemctl restart omnigraph-vertex-proxy.service"

Then commit locally (forward-only, no --amend):
  git add deploy/aliyun/systemd/omnigraph-vertex-proxy.service
  git commit -m "fix(proxy): switch vertex-proxy exit from Hermes to GCP-SG 35.198.243.36 (#75 mitigation)"

NOTE: Do NOT push yet — wait until Task 3 smoke passes before pushing.
  </action>
  <verify>
    <automated>
      ssh aliyun-vitaclaw "systemctl is-active omnigraph-vertex-proxy.service && ss -tlnp | grep 18080"
    </automated>
  </verify>
  <done>
    - omnigraph-vertex-proxy.service shows "active (running)"
    - ss output shows LISTEN on 127.0.0.1:18080
    - Local file deploy/aliyun/systemd/omnigraph-vertex-proxy.service contains "35.198.243.36"
    - One forward-only git commit exists (not pushed)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: E2E smoke verification + push</name>
  <what-built>
    SSH SOCKS5 proxy tunnel via GCP Singapore VM (35.198.243.36:22) is active on Aliyun port
    18080. Embedding code (httpx injected via HttpOptions) routes through it to Vertex AI.
  </what-built>
  <how-to-verify>
Run these three verification commands in order (paste output back):

1. Embedding smoke (confirms SOCKS5 tunnel routes Vertex calls correctly):
   ssh aliyun-vitaclaw "source /root/.hermes/.env && cd /root/OmniGraph-Vault && venv-aim1/bin/python -c \"import asyncio; from lib.lightrag_embedding import embedding_func; r=asyncio.run(embedding_func(['gcp proxy smoke'])); print('EMBED OK dim=%d' % len(r[0]))\""

   Expected: EMBED OK dim=3072

2. Mini-batch ingest (2 articles, confirms L1 classify + embedding path live):
   ssh aliyun-vitaclaw "source /root/.hermes/.env && cd /root/OmniGraph-Vault && MAX_ARTICLES=2 venv-aim1/bin/python batch_ingest_from_spider.py --topics ai 2>&1 | tail -30"

   Expected: no "Connection refused" errors, L1 classify runs, ainsert attempts visible.

3. Service still active after batch run:
   ssh aliyun-vitaclaw "systemctl is-active omnigraph-vertex-proxy.service"

   Expected: active

ONLY after all three pass:
   git push origin main
  </how-to-verify>
  <resume-signal>
    Type "smoke-ok" if all three commands pass (include EMBED OK line + brief batch output).
    Type "smoke-fail: &lt;symptom&gt;" if any command fails so we can diagnose.
  </resume-signal>
</task>

</tasks>

<verification>
- omnigraph-vertex-proxy.service is active + :18080 LISTEN on Aliyun
- EMBED OK dim=3072 returned (Vertex AI reached via GCP SG tunnel)
- Batch ingest of 2 articles completes without Connection refused on embedding
- deploy/aliyun/systemd/omnigraph-vertex-proxy.service contains "35.198.243.36" in ExecStart
- One forward-only commit on main, pushed after smoke passes
- SUMMARY.md created in quick directory with SSH params, smoke output, commit hash
</verification>

<success_criteria>
- proxy service: active + :18080 LISTEN
- embedding smoke: EMBED OK dim=3072
- L1 classify batch: no Connection refused / embedding errors
- git: one commit pushed to origin/main
- file: deploy/aliyun/systemd/omnigraph-vertex-proxy.service updated + committed
</success_criteria>

<output>
After completion, create .planning/quick/260702-ep6-260702-gcp-sg-proxy/260702-ep6-SUMMARY.md with:
- Confirmed SSH user + key path used
- systemctl is-active output
- EMBED OK dim=3072 evidence (or failure + diagnosis)
- Batch ingest tail output
- Git commit hash
- Status: CLOSED or BLOCKED (with reason)
</output>
