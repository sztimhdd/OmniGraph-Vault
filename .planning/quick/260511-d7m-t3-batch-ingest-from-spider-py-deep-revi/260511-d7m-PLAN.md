---
phase: quick-260511-d7m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-PLAN.md
  - .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md
autonomous: true
requirements:
  - T3-RELEASE-AUDIT
must_haves:
  truths:
    - "REVIEW.md covers all 7 audit angles A1..A7 (no skipped section, even if 'no findings in A_X')"
    - "REVIEW.md cross-references all 8 critical CLAUDE.md 'Lessons Learned' entries with explicit status (still-applicable / fixed / partial / N/A)"
    - "Every finding cites raw evidence (file:line / commit SHA / log path) — no speculation"
    - "Already-audited regions are NOT re-audited; overlaps cite originating quick_id (260509-s29 W2/W3, 260509-p1n, 260510-h09, 260510-rl2 F-4, 260511-b3y)"
    - "Release verdict is given (clear / soft-gating / gating) with explicit HIGH/MEDIUM/LOW counts and reasoning"
    - "NO business code modified, NO .env touched, NO Hermes SSH performed, NO pytest greens-pretending"
    - "Two artifact files committed atomically: PLAN.md + REVIEW.md (no business files)"
  artifacts:
    - path: ".planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md"
      provides: "Read-only deep audit of batch_ingest_from_spider.py covering 7 angles + CLAUDE.md cross-ref + release verdict"
      min_lines: 250
      contains: "Release verdict"
    - path: ".planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-PLAN.md"
      provides: "This plan"
  key_links:
    - from: ".planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md"
      to: "batch_ingest_from_spider.py:<line>"
      via: "explicit file:line citation per finding"
      pattern: "batch_ingest_from_spider\\.py:[0-9]+"
    - from: ".planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md"
      to: ".planning/audit/INGEST-WECHAT-REVIEW.md"
      via: "format / depth mirror (T2 cousin)"
      pattern: "INGEST-WECHAT-REVIEW"
---

<objective>
Read-only deep audit of `batch_ingest_from_spider.py` (2035 LOC, last hash `a66622c`) before next-week release.

Purpose: determine release readiness. User just shipped 260511-b3y (Vertex location fix); production 24h healthy (14 ok / 0 failed). This audit gates the release decision:

- HIGH = 0 + MEDIUM ≤ 3 → release directly
- HIGH ≥ 1 → spawn fix quick first
- MEDIUM many → user picks which to fix pre-release

Output: TWO files — this PLAN.md and a single comprehensive REVIEW.md mirroring `.planning/audit/INGEST-WECHAT-REVIEW.md`'s format/depth.

**Hard constraints (anti-fabrication):**
- ❌ NO code changes / NO business file commits / NO `.env` modifications / NO Hermes SSH / NO pytest greens-pretending
- ❌ NO re-auditing already-fixed regions (cite quick_id when finding overlaps)
- ❌ NO speculation — every finding MUST cite raw evidence (file:line / commit SHA / log path)
- ❌ NO "this code looks fine" — when no finding, write explicit "no findings in A_X"
- ✅ READ-ONLY (Read / Grep / Glob / git log + grep Bash only)
- ✅ Already-audited region findings cite originating quick id
- ✅ Release verdict MUST be given (clear / soft-gating / gating)
</objective>

<execution_context>
@C:\Users\huxxha\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\huxxha\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./batch_ingest_from_spider.py
@.planning/audit/INGEST-WECHAT-REVIEW.md

<file_facts>
- batch_ingest_from_spider.py: 2035 LOC, last commit `a66622c` (verified via `wc -l` + `git log -1 --format=%h`)
- T2 cousin review (format/depth reference): `.planning/audit/INGEST-WECHAT-REVIEW.md` (491 LOC, generated 2026-05-10)
- T1 boot-time pollution closure quick: `260510-l14` (3 atomic commits — Defects A/B/C/D/E, NOT batch_ingest scope)
- T1.5 mop-up: `260510-onk`
- F-4 ingest_wechat trivial cleanups: `260510-rl2` (commit `5d4e294`)
- Today's date: 2026-05-11
</file_facts>

<already_audited_regions>
TRUST these — DO NOT re-audit. If a finding overlaps, cite the originating quick.

| Quick | Commit | Region (DO NOT re-audit) |
|---|---|---|
| 260509-s29 W2 | `42a1b79` | `skip_reason_version` cohort gate + 7 INSERT sites + `_build_topic_filter_query` SQL/params 4-tuple |
| 260509-s29 W3 | `e538b2d` | LLM dispatcher (`get_llm_func`) — 7 hardcoded sites unified |
| 260509-p1n | `f715f06` | Vision drain `_drain_pending_vision_tasks()` delegated to `lib/vision_tracking.py` |
| 260510-h09 | (recent) | PROCESSED-gate retry 3→30 (`OMNIGRAPH_PROCESSED_RETRY` / `_BACKOFF` env-tunable) |
| 260510-rl2 F-4 | `5d4e294` | trivial cleanups (3 spots: dup import / hardcoded model / vestigial recompute) — `ingest_wechat.py` only |
| 260511-b3y | `b1e7fc8` | Vertex `GOOGLE_CLOUD_LOCATION=global` default fix |
</already_audited_regions>

<cousin_modules_dont_audit>
KNOW about, do NOT audit:

- `ingest_wechat.py` — T2 already deeply reviewed (`.planning/audit/INGEST-WECHAT-REVIEW.md`)
- `lib/llm_complete.py` — dispatcher (already unified, don't touch)
- `lib/article_filter.py` — Layer 1/2, **user locked OUT-OF-SCOPE**
- `lib/vision_tracking.py` — vision drain (already audited)
- `lib/lightrag_embedding.py` / `lib/scraper.py` — not yet audited, **leave alone** (may auto-clear after F-1)
</cousin_modules_dont_audit>

<claude_md_lessons_to_cross_reference>
The 8 critical "Lessons Learned" entries that MUST be cross-referenced in REVIEW.md §2.
For each: cite batch_ingest evidence (file:line) and judge status (still applicable / fixed / partial / N/A).

1. **2026-05-04 #3** — schema CHECK vs INSERT value drift (latent bug pattern)
2. **2026-05-05 #2** — scrape-first classify has irreducible Apify waste on filter-rejected articles
3. **2026-05-05 #3** — verify ingest progress from DB, not in-process counter
4. **2026-05-05 #4** — half-fix pattern: producer↔consumer data shape changes need joint grep
5. **2026-05-05 #5** — body atomic persist at scrape moment, before downstream gate
6. **2026-05-05 #6** — DB candidate SELECT didn't exclude skipped (already fixed by W2)
7. **2026-05-07 #1** — schema/SQL changes need production-shape simulation
8. **2026-05-07 #2** — migration reverse must come with INSERT call site reverse

Optional bonus mentions (lower priority but cite if encountered):
- 2026-05-08 #1 — cascade order divergence (`lib/scraper.py` vs `ingest_wechat.py`) — relevant to A4
- 2026-05-06 #5 — never `git reset --soft` on shared worktree (relevant to A5/process)
</claude_md_lessons_to_cross_reference>

<seven_audit_angles>
REVIEW.md §3 MUST cover all 7. Each gets its own subsection (HIGH/MEDIUM/LOW findings within), even if "no findings in A_X".

### A1 — Dead code / half-finished migration traces
**What to grep:**
- `# TODO` / `# FIXME` / `# Phase X` / `# Was:` / `# Wave X` / `# 0c.` / `# Plan 05-` / `# DEPRECATED` / `# Quick \d+`
- Per-occurrence judgment: still applicable / cleanup candidate / load-bearing tag
- Mirror T2 review §D ("Migration markers") format: marker count + dense-pocket density

**Output format (table):**
```
| line | marker | text excerpt | judgment |
```

### A2 — Function god-ness
**What to do:**
- List every top-level `def` with LOC + max nesting depth (visual estimate or `grep -c "^[[:space:]]\{12,\}"` per function block)
- `ingest_from_db` main loop is the prime suspect — list its stages (scrape / Layer 1 / Layer 2 / classify / ingest / persist)
- Identify which stages can be cleanly split, which are leaky abstractions
- Mirror T2 review §C format (line-by-line decomposition, safe extraction slices)

**Output format (table):**
```
| line | def | LOC | depth | top-level? | callers | role | extraction-safe? |
```

### A3 — Cross-module coupling (F-2 candidate)
**What to grep:** `from lib.X import` patterns + reverse `from batch_ingest_from_spider import` (in lib/)
- Identify **app→lib forward** (OK) vs **lib→app inverse** (violation)
- **lib should NOT import root-level modules** — this is F-2 evidence

**Specific commands:**
- `grep -n "^from lib" batch_ingest_from_spider.py` → forward imports (categorize)
- `grep -rn "from batch_ingest_from_spider\|import batch_ingest_from_spider" --include='*.py' lib/` → inverse violations (red flag)

### A4 — Cascade / duplicate logic (F-1 candidate)
**What to do:**
- Search scrape / Layer 1 / Layer 2 cascade in batch_ingest vs `lib/scraper.py` / `ingest_wechat.py` for divergence
- CLAUDE.md 2026-05-08 #1 already flagged cascade order divergence — verify if still present (post-`260508-ev2 F1b` cascade reorder), exact line numbers

**Specific commands:**
- `grep -n "_scrape\|cascade\|Apify\|CDP\|MCP\|UA fallback" batch_ingest_from_spider.py`
- Compare to `grep -n "_scrape\|cascade" lib/scraper.py` and `ingest_wechat.py`
- Flag if same cascade tier ordering exists in 2+ files (DRY violation)

### A5 — Error handling silent fail
**What to grep:**
- `try:` / `except` blocks — count + judge each
- Patterns to flag: silent swallow (`except Exception: pass`), log-and-continue without status update, "FAILED labeled ok" patterns
- Recall pre-Bug 3 era (`8ac3cb1`) "body persisted only in memory and discarded on reject" — has the atomic-body-persist invariant been re-violated anywhere?

**Specific commands:**
- `grep -n "except\|raise\|logger.error\|logger.warning" batch_ingest_from_spider.py`
- For each `except` block: cite line, classify (re-raise / log+continue / silent / status-update)

### A6 — Async engineering
**What to grep:**
- `async def` / `await` / `asyncio.create_task` / `nest_asyncio` / `asyncio.gather` / `asyncio.wait_for`
- 260509-p1n fixed vision drain — any other task-escape risks (background tasks not awaited, gather-with-return_exceptions silently swallowing)?
- Look for: `asyncio.create_task(...)` without later `await` or registry; `asyncio.gather(..., return_exceptions=True)` without per-result inspection

**Specific commands:**
- `grep -n "async\|await\|asyncio\|nest_asyncio" batch_ingest_from_spider.py`

### A7 — Test coverage
**What to do:**
- `ls tests/unit/test_batch_ingest_*.py` and `wc -l` each
- For each test file: 1-line summary of what it pins (read first 30-50 lines)
- Identify functions/branches with 0 test coverage (cross-ref A2 inventory vs test pinning surface)
- `_build_topic_filter_query` already has 22+ tests (260509-s29 W2) — cite that and move on

**Specific commands:**
- `ls tests/unit/test_batch_ingest_*.py 2>/dev/null` → list
- For each: `head -50 <file>` → extract docstrings + test function names
- Cross-ref A2 function inventory: which functions have ≥1 test? Which have 0?
</seven_audit_angles>

<review_md_output_schema>
Paste this schema verbatim into REVIEW.md. Sections must appear in this exact order.

```markdown
# T3 — batch_ingest_from_spider.py Deep Review
Generated: 2026-05-11 ADT
File: batch_ingest_from_spider.py (2035 LOC, last hash a66622c)
Reviewer: read-only audit, executed via /gsd:quick 260511-d7m
Format reference: .planning/audit/INGEST-WECHAT-REVIEW.md (T2 cousin)

## TL;DR
- HIGH: N · MEDIUM: N · LOW: N
- Cross-cutting: N
- Estimated cleanup work: X-Y h across M quicks
- **Release verdict**: clear / soft-gating / gating (mandatory)
- One-paragraph executive summary (3-5 sentences)

## 1. File sectional map
[function list table: line | symbol | LOC | top-level? | callers | role | tests]
[mirror T2 review §A format]

## 2. CLAUDE.md "Lessons Learned" cross-reference
[8 critical entries — all MUST appear in this table, even N/A ones]

| Lesson | Status (still-applicable / fixed / partial / N/A) | Evidence (file:line) | Notes |

## 3. Findings by audit angle
[7 subsections A1..A7, EACH with at least one paragraph — even if "no findings in A_X"]

### A1 — Dead code / half-finished migration traces
[markers table + density assessment]

### A2 — Function god-ness
[per-function LOC/depth table + ingest_from_db decomposition]

### A3 — Cross-module coupling (F-2 candidate)
[forward vs inverse imports]

### A4 — Cascade / duplicate logic (F-1 candidate)
[batch_ingest vs lib/scraper / ingest_wechat divergence]

### A5 — Error handling silent fail
[try/except classification table]

### A6 — Async engineering
[task-escape / gather risks]

### A7 — Test coverage
[test file inventory + uncovered-function list]

## 4. Findings by severity
### HIGH (release blocker / bound to break)
F-X — title (NEW or maps to existing F-1/F-2/...)
  Evidence: <file:line>
  Why HIGH: <reason>
  Fix scope: ~LOC / quick type / risk

### MEDIUM (real but not urgent)
[same shape]

### LOW (nice-to-have)
[same shape]

## 5. Cross-cutting issues
[issues spanning batch_ingest + N other files]

## 6. Recommended fix quick sequence
[ordered list + hours estimate + dependency graph]

## 7. Module verdict
- Pollution score: HIGH / MEDIUM / LOW (qualitative + reasoning)
- Release readiness: clear / soft-gating / gating
- Recommendation: ship now / fix N items first / spike

## 8. Open questions for user
[reviewer can't decide alone — explicit list, not implied]

## 9. Anti-fabrication compliance
- Read-only: confirmed (no Edit/Write to .py / .env / Hermes)
- Every finding cites file:line, commit SHA, or log path: confirmed (or list exceptions)
- Already-audited regions cite originating quick: confirmed
- All 7 angles covered: confirmed
- All 8 CLAUDE.md lessons cross-referenced: confirmed
- Release verdict given: confirmed (verdict = ___)
```
</review_md_output_schema>

<wall_time_budget>
**3-5h target. If exceeding 6h with no end → produce partial REVIEW.md** marked "completed angles 1-X, angles X+1..7 incomplete due to time bound". Partial > stale. Section 9 ("Anti-fabrication compliance") MUST list which angles are partial in that case.
</wall_time_budget>

</context>

<tasks>

<task type="auto">
  <name>T1: Execute audit angles A1-A4 (markers / god-ness / coupling / cascade) and synthesize the structural half of REVIEW.md</name>
  <files>
    .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md
  </files>
  <action>
Execute the structural audit angles (A1, A2, A3, A4) and the file-sectional map. Begin REVIEW.md with §1 (file map) + §2 (CLAUDE.md cross-ref) + §3 partial (A1-A4 subsections only). DO NOT commit yet — committing happens in T3.

**Step 1 — File reconnaissance (READ-ONLY):**
- `Read` `batch_ingest_from_spider.py` in chunks (file is 2035 LOC, exceeds single-read limit). Use `Read` with `offset` + `limit` to traverse: 1-500, 500-1000, 1000-1500, 1500-2035.
- `Bash`: `git log -1 --format='%H %s' batch_ingest_from_spider.py` (confirm last commit metadata)
- `Bash`: `git log --oneline -20 -- batch_ingest_from_spider.py` (last 20 touch points — informs marker decay)
- `Read` `.planning/audit/INGEST-WECHAT-REVIEW.md` (491 LOC, fully) — use as format/depth reference

**Step 2 — Build §1 file sectional map (mirror T2 §A):**
- For every top-level `def` and module-level constant: line number, name, approximate LOC, callers (grep `<name>(` across batch_ingest + lib/ + scripts/ + tests/), role, test pinning.
- Use `Grep` with `pattern: "^(def|async def|class) "` `path: batch_ingest_from_spider.py` `output_mode: content` `-n: true` to enumerate top-level defs.
- For each, count LOC by next-def-line minus current-def-line.
- Output as Markdown table (one row per def/constant). This populates REVIEW.md §1.

**Step 3 — Build §2 CLAUDE.md "Lessons Learned" cross-reference (8 entries):**
For each lesson listed in `<claude_md_lessons_to_cross_reference>`, search batch_ingest for evidence:
- 2026-05-04 #3 (CHECK vs INSERT drift) — grep `INSERT INTO\|CHECK (\|status IN`
- 2026-05-05 #2 (scrape-first irreducible Apify waste) — grep `scrape\|Apify\|topic_filter\|reject`
- 2026-05-05 #3 (DB-vs-counter) — grep `ingestions\|status='ok'\|count\|in-process`
- 2026-05-05 #4 (producer↔consumer data shape) — grep `markdown\|content_html\|body`
- 2026-05-05 #5 (atomic body persist) — grep `articles.*body\|UPDATE articles SET body`
- 2026-05-05 #6 (skipped exclusion in candidate SELECT) — grep `WHERE status\|skip_reason_version`
- 2026-05-07 #1 (production-shape simulation) — meta-judgment, no grep
- 2026-05-07 #2 (migration reverse + INSERT site reverse) — grep `ON CONFLICT`

Each row: lesson | status | evidence file:line | notes. Status MUST be one of: still-applicable / fixed / partial / N/A.

**Step 4 — Execute A1 (dead code / migration markers):**
- `Grep` patterns (one per): `# TODO`, `# FIXME`, `# Phase \d+`, `# Was:`, `# Wave \d+`, `# Plan \d+-`, `# DEPRECATED`, `# Quick \d+`, `# D-\d+`, `# Hot-?fix`, `# 0c\.`
- Aggregate count + dense-pocket assessment (same way T2 review §D did).
- For each notable marker: line / text excerpt / judgment (still-applicable / cleanup candidate / load-bearing tag)
- Output as table in §3.A1 + 1-2 paragraph density assessment.

**Step 5 — Execute A2 (function god-ness):**
- From §1 inventory, list every top-level def with LOC. Sort descending.
- Identify functions ≥80 LOC (god-function threshold per T2 review §C).
- For `ingest_from_db` (the suspected prime god-function): line-by-line decomposition.
  - `Read` the function body (use offset/limit to grab just it)
  - Identify stages: scrape / Layer 1 / Layer 2 / classify / ingest / persist (cite line ranges per stage)
  - Per stage: leaky-abstraction signal (env reads inside, SQL inside, async-context-switch inside)
  - Per stage: extraction-safe? (Y/N + 1-line reasoning)
- Output as table in §3.A2 + ingest_from_db deep-dive subsection.

**Step 6 — Execute A3 (cross-module coupling):**
- `Grep`: `pattern: "^from lib\\.|^import lib"` `path: batch_ingest_from_spider.py` `output_mode: content` `-n: true` → forward imports
- `Grep`: `pattern: "from batch_ingest_from_spider|import batch_ingest_from_spider"` `path: lib/` `output_mode: content` `-n: true` → inverse violations
- Categorize forward imports: which are clean (function call only) vs which use lib internals (constants, classes, private helpers)
- Output as table in §3.A3 + verdict: lib↔app inversion present? (F-2 evidence)

**Step 7 — Execute A4 (cascade / duplicate logic):**
- `Grep`: `pattern: "(_scrape_wechat|cascade|Apify|CDP|MCP|UA fallback)"` across batch_ingest_from_spider.py, lib/scraper.py, ingest_wechat.py
- Compare cascade tier ordering across the 3 files — flag any divergence
- Cross-ref CLAUDE.md 2026-05-08 #1 (cascade order divergence flagged + post-`260508-ev2 F1b` reorder fix)
- If cascade is duplicated logic: F-1 evidence; if cascade now lives in `lib/scraper.py` only: ✅ flag as resolved
- Output as table in §3.A4 + verdict.

**Step 8 — Write REVIEW.md (PARTIAL — sections 1, 2, 3.A1-A4 only):**
- Use `Write` to create `.planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md`
- Include exact schema header (file path, LOC, last hash, format reference)
- Sections to populate in this task: §1 file map, §2 lessons cross-ref, §3 audit-by-angle (A1, A2, A3, A4 ONLY)
- Stub §3.A5, A5, A6, A7 with `<!-- T2 task will fill these -->` placeholders
- Stub §4-§9 with `<!-- T2 task will fill these -->` placeholders
- Write a TL;DR placeholder at top: `<!-- T2 task will populate after A5-A7 + severity tally -->`

**Anti-fabrication enforcement (this task):**
- Every finding must cite `batch_ingest_from_spider.py:<line>` exactly. Use `-n: true` on every Grep.
- Already-audited regions: when grepping reveals e.g. `_build_topic_filter_query` SQL/params, do NOT re-audit — note "see quick 260509-s29 W2 (commit 42a1b79)" and skip.
- For each ingest_from_db stage, cite line range explicitly (e.g. "L1140-1155 scrape stage").
- No "looks fine" verdicts — when no finding, write `**Finding: none.** Coverage rationale: [explicit reason].`
- READ-ONLY: only `Read`, `Grep`, `Glob`, `Bash` (for `git log` + `wc -l` + `ls` only). Zero `Edit`. `Write` only for REVIEW.md.
  </action>
  <verify>
    <automated>test -f .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md && grep -q "## 1. File sectional map" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md && grep -q "## 2. CLAUDE.md" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md && grep -q "### A1" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md && grep -q "### A4" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md</automated>
  </verify>
  <done>
    - REVIEW.md exists with §1 file map populated (table with all top-level defs + key constants)
    - §2 CLAUDE.md lessons cross-ref table has all 8 entries with status + evidence
    - §3.A1 (markers) + §3.A2 (god-ness, with ingest_from_db decomposition) + §3.A3 (coupling) + §3.A4 (cascade) populated with file:line citations
    - §3.A5/A6/A7 + §4-§9 stubbed with placeholders for T2 task
    - Already-audited overlaps cite originating quick_id
    - No business code modified, no commits yet
  </done>
</task>

<task type="auto">
  <name>T2: Execute audit angles A5-A7 (errors / async / tests), synthesize severity findings, populate TL;DR + verdict</name>
  <files>
    .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md
  </files>
  <action>
Continue REVIEW.md from T1's stub. Fill §3.A5-A7 + §4 severity findings + §5 cross-cutting + §6 fix sequence + §7 verdict + §8 open questions + §9 compliance + TL;DR.

**Step 1 — Execute A5 (error handling silent fail):**
- `Grep`: `pattern: "except|raise|try:"` `path: batch_ingest_from_spider.py` `output_mode: content` `-n: true`
- For each `except` block: cite line, classify into one of:
  - re-raise (`raise` or `raise X from e`) → ✅ propagating
  - log+continue with status update (`logger.X(...)` then DB write) → ✅ accountable
  - log+continue WITHOUT status update → ⚠️ silent-ish
  - bare `except: pass` or `except Exception: pass` → ❌ silent swallow
- Cross-ref CLAUDE.md 2026-05-05 #5 (atomic body persist) — does any except block discard scraped body without UPDATE articles SET body?
- Output as table in §3.A5 + 1-2 paragraph synthesis.

**Step 2 — Execute A6 (async engineering):**
- `Grep`: `pattern: "(async def|await |asyncio\\.|nest_asyncio)"` `path: batch_ingest_from_spider.py` `output_mode: content` `-n: true`
- Specifically look for:
  - `asyncio.create_task(...)` calls — is each task either awaited later, registered to a list/set, or explicitly fire-and-forget with rationale?
  - `asyncio.gather(..., return_exceptions=True)` — does caller iterate results to surface exceptions?
  - `asyncio.wait_for(...)` — is timeout proportional to underlying work?
  - `nest_asyncio.apply()` calls — count + judgment
- Cross-ref 260509-p1n vision-drain fix (already audited via `lib/vision_tracking.py`)
- Output as table in §3.A6 + verdict on task-escape risks.

**Step 3 — Execute A7 (test coverage):**
- `Bash`: `ls tests/unit/test_batch_ingest_*.py 2>/dev/null` (or use `Glob`)
- For each: `wc -l` + `Read` first 50 lines (extract docstring + test function names)
- Cross-ref T1's §1 function inventory: which top-level defs have ≥1 test? Which have 0?
- `_build_topic_filter_query` has 22+ tests per 260509-s29 W2 — cite and move on
- Output as table in §3.A7: function | LOC (from §1) | test file(s) | covered? | gap notes

**Step 4 — Build §4 severity findings:**
- Gather every finding from A1-A7. Triage each into HIGH / MEDIUM / LOW per these criteria:
  - **HIGH**: release blocker, bound-to-break, latent CHECK-vs-INSERT-style drift, lib↔app inversion that breaks cleanly, missing atomic-persist invariant, untested critical path
  - **MEDIUM**: real but not urgent — duplicate logic with no current divergence, dense markers without bug, god-function still readable
  - **LOW**: cleanup candidates, nice-to-haves, stale comments
- For each finding F-N: title, evidence (file:line), why severity, fix scope (~LOC / quick type / risk)
- Map "F-X NEW" or "F-X maps to existing F-1/F-2/F-3 from T2" if applicable

**Step 5 — Build §5 cross-cutting issues:**
- Issues spanning batch_ingest + ingest_wechat + lib/scraper.py + lib/article_filter.py
- Particularly: cascade duplication, lib↔app inversion, cohort-gate / skip_reason_version reach
- Cite T2 review §F (refactor blast radius) as cross-ref for any issue already mapped there

**Step 6 — Build §6 recommended fix sequence:**
- Ordered list of suggested quicks (or in-place fixes) with hours estimate per quick
- Dependency graph (which must precede which)
- Decision tree: "if HIGH ≥ 1, do X first; if HIGH=0 + MEDIUM ≤ 3, ship; if MEDIUM many, user picks"

**Step 7 — Build §7 module verdict (MANDATORY):**
- **Pollution score**: HIGH / MEDIUM / LOW with qualitative reasoning (3-5 sentences)
- **Release readiness**: clear / soft-gating / gating per the user's gate criteria
- **Recommendation**: ship now / fix N items first / spike (one specific recommendation, no waffle)

**Step 8 — Build §8 open questions:**
- Anything reviewer cannot decide alone (e.g., "is the X stage extraction worth the test rewrite cost? user judgment")
- Each question must be answerable yes/no or A/B/C — no "it depends" questions

**Step 9 — Build §9 anti-fabrication compliance:**
- Confirm: read-only, every finding cited, already-audited regions cite quick id, all 7 angles covered, all 8 lessons cross-referenced, release verdict given
- If any partial: list explicitly with reason

**Step 10 — Populate TL;DR (top of file):**
- HIGH/MEDIUM/LOW counts (from §4)
- Cross-cutting count (from §5)
- Estimated cleanup work hours (from §6)
- Release verdict (from §7) — verbatim
- 3-5 sentence executive summary

**Step 11 — Edit REVIEW.md** to fill in all stubbed sections from T1.

**Wall-time clause:** if T1+T2 combined > 6h, mark as PARTIAL in §9 listing which angles are incomplete. Partial > stale.
  </action>
  <verify>
    <automated>grep -q "Release verdict" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md && grep -q "### A5" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md && grep -q "### A7" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md && grep -qE "(clear|soft-gating|gating)" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md && wc -l .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md | awk '$1 >= 250 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    - REVIEW.md §3.A5/A6/A7 fully populated with file:line citations
    - REVIEW.md §4 severity findings has explicit HIGH/MEDIUM/LOW lists (or "0 in this severity")
    - REVIEW.md §5 cross-cutting populated (or "no cross-cutting" with rationale)
    - REVIEW.md §6 fix sequence populated
    - REVIEW.md §7 verdict has clear/soft-gating/gating verdict + recommendation
    - REVIEW.md §8 open questions populated (or "none for user")
    - REVIEW.md §9 compliance assertion present
    - TL;DR populated at top with counts + verdict + executive summary
    - REVIEW.md ≥ 250 lines
    - No business code modified, no commits yet
  </done>
</task>

<task type="auto">
  <name>T3: Atomic commit of PLAN.md + REVIEW.md ONLY (no business files)</name>
  <files>
    .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-PLAN.md,
    .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md
  </files>
  <action>
Commit ONLY the two artifact files. NO business code, NO `.env`, NO Hermes, NO push.

**Step 1 — Pre-commit hygiene check:**
- `Bash`: `git status --short` — verify ONLY the 2 files in `.planning/quick/260511-d7m-*/` are staged-or-modified. If any other file is dirty, abort and report (do not auto-stage business code).
- `Bash`: `git diff --stat .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/` — show LOC delta of artifacts only.

**Step 2 — Commit using gsd-tools (forward-only, no amend, no reset per CLAUDE.md 2026-05-06 #5):**
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(quick-260511-d7m): T3 read-only audit of batch_ingest_from_spider.py — release-readiness REVIEW.md

Read-only deep audit covering 7 angles (A1 markers / A2 god-ness / A3 coupling / A4 cascade / A5 errors / A6 async / A7 tests) on batch_ingest_from_spider.py (2035 LOC, last hash a66622c).

Cross-references all 8 critical CLAUDE.md 'Lessons Learned' entries with explicit status (still-applicable / fixed / partial / N/A). Already-audited regions (260509-s29 W2/W3, 260509-p1n, 260510-h09, 260510-rl2 F-4, 260511-b3y) cited but not re-audited.

Release verdict: <CLEAR|SOFT-GATING|GATING — fill from REVIEW.md §7 before commit>.

Artifacts:
- 260511-d7m-PLAN.md (this plan)
- 260511-d7m-REVIEW.md (audit findings + verdict)

Constraints honored: NO business code modified, NO .env touched, NO Hermes SSH, NO pytest greens-pretending. Format mirrors .planning/audit/INGEST-WECHAT-REVIEW.md (T2 cousin)." --files .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-PLAN.md .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md
```

**IMPORTANT — replace `<CLEAR|SOFT-GATING|GATING>` placeholder** with the actual verdict from REVIEW.md §7 BEFORE running the commit. Read REVIEW.md §7, extract the literal verdict word, substitute. If REVIEW.md is partial (T2 wall-time clause fired), the verdict may be "PARTIAL — verdict deferred"; commit with that.

**Step 3 — Post-commit verification:**
- `Bash`: `git log -1 --format='%H %s'` → confirm exactly 1 new commit landed on main, subject starts with `docs(quick-260511-d7m):`
- `Bash`: `git show --stat HEAD` → confirm exactly 2 files changed, both under `.planning/quick/260511-d7m-*/`. If ANY other file appears in the changed list, the staging was contaminated — STOP and report (do not push, do not amend).

**Step 4 — DO NOT push.** Push is operator-side, post-review. End the task with a console message:
```
T3 complete. Commit <SHA> on local main.
NEXT (operator): review REVIEW.md, then push when ready.
```

**Anti-fabrication enforcement:**
- The commit message verdict line MUST match REVIEW.md §7 verdict literal — no inference, no softening.
- If `git status` shows uncommitted business code, ABORT — do not stash, do not auto-stage, just report.
- Never `git commit --amend` or `git reset` (CLAUDE.md 2026-05-06 #5 — shared worktree hazard).
- No `git push` — operator decides when to push.
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q "quick-260511-d7m" && git show --stat HEAD | grep -E "^\.planning/quick/260511-d7m" | wc -l | awk '$1 == 2 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    - Exactly one commit on local main, subject starts with `docs(quick-260511-d7m):`
    - `git show --stat HEAD` shows exactly 2 files (PLAN.md + REVIEW.md), both under `.planning/quick/260511-d7m-*/`
    - Commit message body cites the verdict from REVIEW.md §7 verbatim
    - NO business files in the commit
    - NOT pushed (operator decides)
  </done>
</task>

</tasks>

<verification>
After all 3 tasks:

1. **REVIEW.md completeness:**
   ```bash
   grep -E "^## [0-9]+\." .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md
   ```
   Expected: lines for §1..§9 (9 top-level sections).

2. **All 7 audit angles present:**
   ```bash
   grep -E "^### A[1-7]" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md | wc -l
   ```
   Expected: 7.

3. **Release verdict present:**
   ```bash
   grep -E "(clear|soft-gating|gating|PARTIAL — verdict deferred)" .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md
   ```
   Expected: ≥1 match in §7.

4. **Single atomic commit, 2 files only:**
   ```bash
   git show --stat HEAD | tail -5
   ```
   Expected: 2 files under `.planning/quick/260511-d7m-*/`.

5. **No business files modified:**
   ```bash
   git status --short
   ```
   Expected: empty (all dirty files committed) OR only `.planning/`-prefixed paths.
</verification>

<success_criteria>
- ✅ REVIEW.md exists, ≥250 LOC, mirrors `.planning/audit/INGEST-WECHAT-REVIEW.md` format
- ✅ All 7 audit angles A1-A7 covered (each with at least "no findings in A_X" if empty)
- ✅ All 8 CLAUDE.md "Lessons Learned" entries cross-referenced in §2 with status (still-applicable / fixed / partial / N/A)
- ✅ Every finding cites raw evidence (file:line / commit SHA / log path) — no speculation
- ✅ Already-audited regions cite originating quick_id (260509-s29 W2/W3, 260509-p1n, 260510-h09, 260510-rl2, 260511-b3y)
- ✅ Release verdict given (clear / soft-gating / gating / PARTIAL deferred) in §7
- ✅ Single atomic commit, exactly 2 files (PLAN.md + REVIEW.md), no business code
- ✅ NOT pushed (operator decides)
- ✅ No `.env` touched, no Hermes SSH, no pytest greens-pretending
</success_criteria>

<output>
After commit, the executor should print to console:

```
T3 audit complete.
- REVIEW.md: <line count> LOC
- Findings: HIGH=N, MEDIUM=N, LOW=N
- Release verdict: <verdict>
- Commit: <SHA>

NEXT (operator):
1. Read .planning/quick/260511-d7m-t3-batch-ingest-from-spider-py-deep-revi/260511-d7m-REVIEW.md §7 (verdict) + §6 (fix sequence)
2. Decide: ship release directly | spawn fix quick(s) first | spike
3. Push when ready: `git push origin main`
```

Do NOT write a separate SUMMARY.md — REVIEW.md IS the deliverable.
</output>
