---
phase: 260605-mz1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md
  - .scratch/260605-mz1-concurrent-probe.py  # gitignored — local-only artifact
  - .scratch/260605-mz1-aliyun-audit-raw.txt  # gitignored — raw audit dump
autonomous: true
requirements:
  - VERIFY-WALLS-38-39-40  # 5-day Aliyun audit per ISSUES #38/#39/#40 (entries to be filed during this quick)
  - LOCAL-CONCURRENT-PROBE  # Corp-laptop 2-article concurrent thread-safety probe
  - RESEARCH-DOC           # RESEARCH.md with verdicts + LoC estimate refinement
  - FORWARD-COMMIT          # Single forward-only commit, no amend, no force
must_haves:
  truths:
    - "5-day Aliyun ingest log audit produces per-wall verdict (SYSTEMIC / INTERMITTENT / ONE_OFF) for ISSUES #38, #39, #40"
    - "Corp laptop concurrent probe runs serial-baseline + 2-concurrent ingest of same 2 articles in isolated OMNIGRAPH_BASE_DIR (NOT prod, NOT ~/.hermes)"
    - "RESEARCH.md exists at .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md with all 4 sections populated"
    - "v1.2 design viability declared GO / RISKY / BLOCKED with explicit alternative path if BLOCKED"
    - "Single atomic forward-only commit on main; no --amend, no reset --hard, no push --force"
    - "Zero prod state mutated — no edit to batch_ingest_from_spider.py, ingest_wechat.py, Aliyun config, systemd, Hermes, or kb/"
  artifacts:
    - path: ".planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md"
      provides: "4-section research deliverable: 5-day audit verdicts / local concurrent probe results / v1.2 viability / ISSUES.md row recommendations"
      min_lines: 80
    - path: ".scratch/260605-mz1-concurrent-probe.py"
      provides: "≤80-LoC local-only probe script (gitignored)"
      max_lines: 80
    - path: ".scratch/260605-mz1-aliyun-audit-raw.txt"
      provides: "Raw audit dump (batch_timeout_metrics + journalctl + sqlite output) for evidence trail"
  key_links:
    - from: "TASK 1 audit raw output"
      to: "RESEARCH.md Section 1"
      via: "Per-wall verdict cited with file/line/count evidence"
      pattern: "SYSTEMIC|INTERMITTENT|ONE_OFF"
    - from: "TASK 2 probe wall_s measurements"
      to: "RESEARCH.md Section 2"
      via: "serial_s / concurrent_s / speedup_ratio table"
      pattern: "wall_s.*serial.*concurrent"
    - from: "RESEARCH.md Section 1+2"
      to: "RESEARCH.md Section 3 (v1.2 viability) + Section 4 (ISSUES.md row recommendations)"
      via: "Synthesis: audit verdict + thread-safety verdict → GO/RISKY/BLOCKED + ISSUES severity adjustments"
      pattern: "GO|RISKY|BLOCKED"
---

<objective>
Verify whether ISSUES #38 / #39 / #40 represent SYSTEMIC walls (3+ days, count > 5) warranting v1.2 P0 candidates, vs INTERMITTENT (1-2 days then 0) or ONE_OFF (today only) anomalies. Pair the 5-day Aliyun audit with a local corp-laptop 2-article concurrent thread-safety probe to determine whether a v1.2 batch_ingest concurrent rewrite is design-viable.

Purpose: User has explicitly pushed back that 1-day evidence is insufficient to file v1.2 P0 candidates AND that any concurrent rewrite is a big architectural change requiring local thread-safety verification before design. This quick produces both deliverables in a single research-only artifact.

Output: `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md` (4 sections) + single forward-only commit. **Zero prod mutation. Zero code changes outside `.scratch/` and `.planning/`.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ISSUES.md
@./CLAUDE.md

# Permissions / boundary anchors (executor MUST honor)

This is a **RESEARCH-ONLY quick**. The executor:

- **MAY** SSH read-only to `aliyun-vitaclaw` via the Bash tool for diagnostic queries (per CLAUDE.md PRINCIPLE #5 + memory `feedback_ssh_readonly_vs_writeop_boundary` — read-only diagnostic SSH is allowed; write ops are NOT).
- **MAY** run a small ≤80-LoC probe script under `.scratch/` on the local corp laptop (gitignored; `.scratch/` is in `.gitignore` line 9).
- **MAY** write `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md` (this is the deliverable).
- **MUST NOT** edit `batch_ingest_from_spider.py`, `ingest_wechat.py`, any `kb/` file, any Aliyun systemd unit, Aliyun `/root/.hermes/.env`, or anything on the Hermes box.
- **MUST NOT** run any prod-mutating SSH command (no `systemctl restart`, no file edit on Aliyun, no `pip install` on Aliyun).
- **MUST NOT** push the local probe via `scripts/local_e2e.sh` — see TASK 2 below for why this quick uses a custom isolated probe instead of the standard harness.

# OmniGraph local-dev env (for TASK 2 isolation)

Per CLAUDE.md "Local dev env vars" section, the executor MUST set BEFORE running the probe:

```
OMNIGRAPH_BASE_DIR=C:/Users/huxxha/Desktop/OmniGraph-Vault/.dev-runtime/260605-mz1-probe
```

This is a **fresh isolated dir** — NOT `~/.hermes/omonigraph-vault` (that's prod). Probe MUST also create:

- `OMNIGRAPH_BASE_DIR/lightrag_storage/` (empty; LightRAG will populate)
- `OMNIGRAPH_BASE_DIR/images/` (empty; vision will populate)
- `OMNIGRAPH_BASE_DIR/data/kol_scan.db` (copy from local sqlite OR pull 2 hashes from Aliyun read-only)

`DEEPSEEK_API_KEY=dummy` is acceptable since this probe is testing LightRAG ainsert thread-safety, NOT LLM correctness — but if probe needs real LLM (entity extraction calls into DeepSeek/Vertex), the executor surfaces this and asks orchestrator before proceeding (do NOT silently fall back to prod creds).

# Aliyun SSH alias

`aliyun-vitaclaw` resolves to root@101.133.154.49 via `~/.ssh/config` (memory `aliyun_vitaclaw_ssh.md`). Read-only commands only.

# Halt triggers (executor MUST halt + surface, NOT retry/silent-fail)

1. **Corp laptop hits SiliconFlow firewall** (Cisco Umbrella TLS interception or DNS block) → ALERT, surface to orchestrator, **do NOT fall back to prod batch_ingest** to "make the probe work". Probe halts with partial results.
2. **LightRAG ainsert race produces kv_store / graphml corruption** during concurrent run → ALERT, halt probe immediately, do NOT retry. Capture stack trace + corruption signature in RESEARCH.md Section 2 as BLOCKED evidence.
3. **Vertex AI 429 quota fire** during probe → ALERT, halt, mark probe RISKY in RESEARCH.md.
4. **5-day audit data shows ALL three walls are NOT systemic (ONE_OFF / INTERMITTENT)** → halt design exploration, change RESEARCH.md Section 3 conclusion to recommend close ISSUES #38/#39/#40 (annotate "needs more data") and SKIP TASK 2's design speculation in Section 3 (still run TASK 2 probe — data has independent value).

# Cross-references

- ISSUES.md #32 (PROCESSED-gate 60s tight) — related but DIFFERENT timer than #39
- ISSUES.md #33 (wrapper 1200s cap image-heavy) — related but DIFFERENT than #38 CUMULATIVE
- ISSUES.md #31 (LightRAG embedding worker timeout — RESOLVED) — context for understanding embedding-side timeout chain
- Memory `wave3_batch_budget_serial_starve` — single 8h budget × serial → starvation root cause (#40)
- Memory `feedback_ssh_readonly_vs_writeop_boundary` — read-only SSH is allowed
- Memory `feedback_dont_speculative_ssh_ask_hermes` — read-only SSH for KNOWN queries only, not exploration
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5-day Aliyun audit — batch_timeout_metrics + journalctl + sqlite ingestions</name>
  <files>.scratch/260605-mz1-aliyun-audit-raw.txt</files>
  <action>
SSH read-only to `aliyun-vitaclaw` and collect 3 evidence sources, dumping raw output into `.scratch/260605-mz1-aliyun-audit-raw.txt` (gitignored).

**Sub-step 1a — batch_timeout_metrics 5-day trend:**

```bash
ssh aliyun-vitaclaw 'ls -la /root/OmniGraph-Vault/data/batch_timeout_metrics_*.json | tail -20' >> .scratch/260605-mz1-aliyun-audit-raw.txt
```

For the 10-20 most recent files:

```bash
ssh aliyun-vitaclaw 'for f in $(ls -t /root/OmniGraph-Vault/data/batch_timeout_metrics_*.json | head -20); do echo "=== $f ==="; cat "$f"; done' >> .scratch/260605-mz1-aliyun-audit-raw.txt
```

For each file extract: date / time / total_articles / completed_articles / timed_out_articles / not_started_articles / avg_article_time_sec / timeout_histogram. Compute 5-day moving avg of completed_articles per cron run, 5-day moving avg avg_article_time_sec, 5-day timeout count trend.

**Sub-step 1b — journalctl 3-day error pattern:**

```bash
ssh aliyun-vitaclaw 'journalctl -u omnigraph-daily-ingest.service --since "5 days ago" --no-pager 2>&1 | grep -iE "TIMEOUT|PROCESSED verification failed|Worker timeout|Detected stuck task|killed process"' >> .scratch/260605-mz1-aliyun-audit-raw.txt
```

Count per error pattern per day. Trend: increasing / stable / sporadic?

**Sub-step 1c — sqlite ingestions 7-day pattern:**

```bash
ssh aliyun-vitaclaw 'sqlite3 /root/OmniGraph-Vault/data/kol_scan.db "SELECT DATE(ingested_at) AS day, status, COUNT(*) FROM ingestions WHERE ingested_at > datetime(\"now\",\"-7 days\") GROUP BY day, status ORDER BY day DESC, status"' >> .scratch/260605-mz1-aliyun-audit-raw.txt
```

Surface 7-day per-day breakdown: ok / failed / skipped / skipped_ingested counts. Trend ok rate?

**Sub-step 1d — verdict computation (in-memory analysis, NOT a separate file):**

For each wall, classify per definition:

| Wall | Definition | SYSTEMIC threshold | INTERMITTENT | ONE_OFF |
|---|---|---|---|---|
| #38 wrapper-cap CUMULATIVE | TIMEOUT(>1200s) wrapper kill events | 3+ days fire AND total count > 5 | 1-2 days fire then 0 | only today, count = 1 |
| #39 PROCESSED-gate silent drop | "PROCESSED verification failed" log lines | same | same | same |
| #40 serial-processing starvation | not_started_articles > 0 in batch_timeout_metrics, OR 1/N completed in 6h+ window | same | same | same |

Cite verdict per wall in RESEARCH.md Section 1 with raw line counts + dates from audit file. **If ALL three walls verdict = ONE_OFF or INTERMITTENT → trigger Halt #4** — change Section 3 conclusion accordingly.

**Constraint:** SSH commands are READ-ONLY. NO `systemctl`, NO `>`, NO `rm`, NO file mutation on Aliyun.
  </action>
  <verify>
    <automated>test -s .scratch/260605-mz1-aliyun-audit-raw.txt &amp;&amp; grep -cE "batch_timeout_metrics_|journalctl|ingestions" .scratch/260605-mz1-aliyun-audit-raw.txt | awk '$1 &gt;= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <done>`.scratch/260605-mz1-aliyun-audit-raw.txt` exists and contains all three evidence sources (batch_timeout_metrics file dumps, journalctl error pattern matches, sqlite 7-day breakdown). Per-wall verdict computed (SYSTEMIC / INTERMITTENT / ONE_OFF) for #38, #39, #40 with raw evidence count. NO Aliyun mutation. SSH commands all read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Local corp-laptop 2-article concurrent thread-safety probe</name>
  <files>.scratch/260605-mz1-concurrent-probe.py</files>
  <action>
**Pre-flight (before writing the probe):**

1. Verify `.scratch/` is gitignored: `grep -E '^\.scratch/$' .gitignore` — must return a match. (Verified pre-plan: line 9.)
2. Set isolated env in the shell that will run the probe (do NOT export to user env permanently):
   - `OMNIGRAPH_BASE_DIR=C:/Users/huxxha/Desktop/OmniGraph-Vault/.dev-runtime/260605-mz1-probe`
   - `OMNIGRAPH_LLM_PROVIDER=deepseek` (prod-parity; switch to `vertex_gemini` only if DeepSeek key unavailable)
   - `DEEPSEEK_API_KEY=<from .env>` (real key needed for entity extraction; if blocked by corp firewall → trigger Halt #1)
   - `OMNIGRAPH_VISION_SKIP_PROVIDERS=siliconflow,openrouter,gemini` (skip ALL vision providers; probe is testing LightRAG ainsert thread-safety NOT vision cascade)
3. `mkdir -p $OMNIGRAPH_BASE_DIR/{lightrag_storage,images,data,checkpoints}`.
4. Pull 2 article hashes via read-only SSH from Aliyun where layer2_verdict='ok' AND image_count BETWEEN 2 AND 5 (avoid image-heavy outliers that would confound the timing measurement):
   ```bash
   ssh aliyun-vitaclaw 'sqlite3 /root/OmniGraph-Vault/data/kol_scan.db "SELECT hash FROM articles WHERE layer2_verdict=\"ok\" AND image_count BETWEEN 2 AND 5 ORDER BY ingested_at DESC LIMIT 2"'
   ```
   Capture the 2 hashes. (Read-only; allowed per PRINCIPLE #5.)

**Probe script `.scratch/260605-mz1-concurrent-probe.py` (≤80 LoC hard ceiling):**

The probe MUST:

1. Accept the 2 hashes as `sys.argv[1]` and `sys.argv[2]`.
2. Build markdown content for each hash by either (a) reading `body_cleaned` from a local sqlite copy if present, OR (b) using a hardcoded fixture string per hash. **Do NOT scrape WeChat fresh** — that adds Apify/CDP variance and is not what the probe measures.
3. Run **PASS A — serial baseline:**
   - Initialize ONE LightRAG instance pointing at `$OMNIGRAPH_BASE_DIR/lightrag_storage/`.
   - `await rag.ainsert(content_1)` (await fully)
   - `await rag.ainsert(content_2)` (await fully)
   - Capture `wall_s_serial` = elapsed wall time.
4. **Tear down + recreate** isolated dir (`shutil.rmtree(lightrag_storage); mkdir`) so PASS B starts from same clean state.
5. Run **PASS B — 2-concurrent:**
   - Initialize ONE LightRAG instance.
   - `await asyncio.gather(rag.ainsert(content_1), rag.ainsert(content_2))`
   - Capture `wall_s_concurrent` = elapsed wall time.
6. Compute `speedup_ratio = wall_s_serial / wall_s_concurrent`.
7. After PASS B, validate post-conditions:
   - `lightrag_storage/graphml_*.graphml` exists and parses (no XML corruption).
   - `lightrag_storage/kv_store_*.json` parses as valid JSON (no truncation).
   - `aget_docs_by_ids([hash_1, hash_2])` returns BOTH as status='processed'.
8. Print results to stdout:
   ```
   PASS A serial:      wall_s = {N}
   PASS B concurrent:  wall_s = {M}
   speedup ratio:      {ratio}x
   graphml valid:      {True/False}
   kv_store valid:     {True/False}
   both docs processed: {True/False}
   exceptions:         {none / stack trace}
   ```

**Interpretation table (executor records this in RESEARCH.md Section 2):**

| speedup_ratio | All post-conditions pass? | Verdict |
|---|---|---|
| ≥ 1.7x (concurrent ≈ 0.5× serial) | YES | GO — asyncio.gather works, ROI strong |
| 1.4x to 1.7x | YES | RISKY — partial lock conflict, research lock site before design |
| < 1.4x (concurrent ≈ serial) | YES | BLOCKED — LightRAG/Qdrant has lock serialization, need different approach |
| any | NO (corruption / unprocessed docs / exception) | BLOCKED — thread-safety failure, halt per Halt trigger #2 |

**LoC budget:** probe script ≤ 80 LoC including imports + comments. If executor finds itself >80 LoC, simplify (drop config flexibility, hardcode hashes inline, drop sub-options) — per CLAUDE.md PRINCIPLE #2 Simplicity First.

**Halt behavior:**
- Halt #1 fires (SiliconFlow blocked, but probe skips vision so this only matters if DeepSeek also blocked) → record in RESEARCH.md Section 2 "probe BLOCKED by corp firewall" and skip Section 3 GO/RISKY/BLOCKED determination (mark UNKNOWN).
- Halt #2 fires (kv_store/graphml corruption) → record stack trace + corruption signature, mark BLOCKED.
- Halt #3 fires (Vertex 429) → switch to `OMNIGRAPH_LLM_PROVIDER=deepseek` if originally on Vertex; if already on DeepSeek and 429 fires (rare), record and mark RISKY.

**This task does NOT use `scripts/local_e2e.sh`** — the harness is for full-pipeline smoke tests against the prod OMNIGRAPH_BASE_DIR layout; here we want a tiny isolated probe that bypasses scrape + vision and measures only LightRAG ainsert concurrent behavior. CLAUDE.md "Always use scripts/local_e2e.sh" feedback applies to full-pipeline smoke runs; this is a measurement probe, intentional carve-out.
  </action>
  <verify>
    <automated>test -f .scratch/260605-mz1-concurrent-probe.py &amp;&amp; awk 'END{exit (NR&gt;80)}' .scratch/260605-mz1-concurrent-probe.py &amp;&amp; python -c "import ast; ast.parse(open('.scratch/260605-mz1-concurrent-probe.py').read())"</automated>
  </verify>
  <done>`.scratch/260605-mz1-concurrent-probe.py` exists, ≤80 LoC, parses as valid Python, and the executor has run it once (or recorded explicit halt) — wall_s_serial / wall_s_concurrent / speedup_ratio / post-condition validation captured in stdout AND echoed into RESEARCH.md Section 2. NO prod state mutated; isolated dir under `.dev-runtime/260605-mz1-probe/` used throughout. NO `~/.hermes` touch. NO Aliyun ingest run.</done>
</task>

<task type="auto">
  <name>Task 3: Write 4-section RESEARCH.md</name>
  <files>.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md</files>
  <action>
Synthesize TASK 1 + TASK 2 outputs into the 4-section deliverable.

**File:** `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md`

**Structure (sections MUST be H2 headings exactly as below):**

```markdown
# 260605-mz1 — v1.2 Research: Walls Verify

**Quick:** 260605-mz1-v1-2-research-walls-verify
**Date:** 2026-06-05
**Mode:** RESEARCH-ONLY (no prod mutation, no code change)
**Owner:** quick task — orchestrator pickup TBD

## Section 1 — 5-day Aliyun audit per-wall verdicts

### Methodology
- batch_timeout_metrics_*.json: {N} files, dates {first}..{last}
- journalctl omnigraph-daily-ingest.service: 5 days, grep TIMEOUT|PROCESSED|Worker|stuck|killed
- sqlite ingestions 7-day: status × day breakdown

### Verdicts

| Wall | Verdict | Days fired | Total count | Evidence |
|---|---|---|---|---|
| #38 wrapper-cap CUMULATIVE | SYSTEMIC / INTERMITTENT / ONE_OFF | {N}/5 | {count} | {file:line + dates} |
| #39 PROCESSED-gate silent drop | ... | ... | ... | ... |
| #40 serial-processing starvation | ... | ... | ... | ... |

### Trend signals
- 5-day avg completed_articles/cron: {N} → {M} ({increase/stable/decrease})
- 5-day avg avg_article_time_sec: {N}s → {M}s
- 5-day timeout count: day-by-day {a, b, c, d, e}

## Section 2 — Local corp-laptop concurrent probe

### Setup
- Probe script: `.scratch/260605-mz1-concurrent-probe.py` ({N} LoC)
- Isolated OMNIGRAPH_BASE_DIR: `.dev-runtime/260605-mz1-probe/`
- Articles: hash_1={short_hash}, hash_2={short_hash} (image_count ∈ [2,5])
- LLM provider: deepseek / vertex_gemini
- Vision: SKIPPED (OMNIGRAPH_VISION_SKIP_PROVIDERS=all)

### Results

| Metric | Value |
|---|---|
| PASS A serial wall_s | {N} |
| PASS B 2-concurrent wall_s | {M} |
| Speedup ratio | {ratio}x |
| graphml valid (xml.etree parses) | True / False |
| kv_store_doc_status valid (json.loads) | True / False |
| Both docs status='processed' | True / False |
| Exceptions during ainsert | none / {trace} |
| Errors observed | {list or "none"} |

### Verdict
- {GO / RISKY / BLOCKED} per Section 3 interpretation table
- Reasoning: {1-3 sentences}
- If RISKY: identified lock candidates {list}
- If BLOCKED: alternative paths {list}

## Section 3 — v1.2 design viability

**v1.2 batch_ingest concurrent rewrite viability: {GO / RISKY / BLOCKED}**

### Decision matrix

If Section 1 walls = ALL ONE_OFF/INTERMITTENT:
- Recommendation: **DO NOT** file v1.2 P0 candidates yet. Annotate ISSUES #38/#39/#40 "needs 3+ day evidence". Re-evaluate after 7 more days of cron data.

If Section 1 walls = ANY SYSTEMIC AND Section 2 verdict = GO:
- Recommendation: v1.2 concurrent rewrite is design-viable. **Refined LoC estimate: {N} LoC** (vs original orchestrator estimate of {M}).
- Critical path: {ordered list of changes — e.g., asyncio.gather wrapper / config knob / per-task budget}
- Risks: {list}

If Section 1 walls = ANY SYSTEMIC AND Section 2 verdict = RISKY:
- Recommendation: v1.2 needs lock-site research phase BEFORE design. Spike a follow-up `/gsd:quick` or `/gsd:plan-phase` with explicit "research LightRAG/Qdrant lock candidates" goal. **Refined LoC estimate UNKNOWN** — depends on lock investigation outcome.

If Section 1 walls = ANY SYSTEMIC AND Section 2 verdict = BLOCKED:
- Recommendation: v1.2 concurrent rewrite **NOT viable** with current LightRAG 1.4.16 + Qdrant 1.18. Alternative paths:
  1. Per-article subprocess isolation (process-level concurrency, not asyncio)
  2. Parallel Aliyun ingest workers (multiple systemd services with disjoint candidate pools)
  3. Wait for LightRAG upstream concurrency support
  4. Accept current serial behavior, raise per-article wrapper cap, throughput-by-cadence not throughput-by-concurrency

### LoC estimate refinement
- Original orchestrator estimate: {if known}
- Refined estimate based on Section 2 evidence: {N ± M LoC, or UNKNOWN with reason}

## Section 4 — ISSUES.md row update recommendations

For each of #38, #39, #40, recommend ONE of:

- **KEEP AS-IS** — verdict SYSTEMIC, severity matches current row
- **DOWNGRADE P0 → P1** or **P1 → P2** — verdict INTERMITTENT, severity overstated
- **ANNOTATE "needs more data"** — verdict ONE_OFF, may be transient
- **CLOSE / RESOLVE** — verdict ONE_OFF AND root cause already addressed by another in-flight fix (cite commit / quick)
- **MERGE** — verdict overlaps another open issue (cite #N)

| ISSUE # | Current severity | Recommendation | Suggested annotation |
|---|---|---|---|
| #38 | {P?} | KEEP / DOWNGRADE / ANNOTATE / CLOSE / MERGE | {1-line annotation text for orchestrator to paste} |
| #39 | {P?} | ... | ... |
| #40 | {P?} | ... | ... |

## Halt log

If any halt trigger fired during TASK 1 or TASK 2, record here:
- Trigger #N fired at {step}
- Symptoms: {description}
- Action taken: {halt / partial-data / surfaced to orchestrator}
- Impact on Section 3: {UNKNOWN / partial / unaffected}
```

**Length target:** 80-200 lines. Bias toward EVIDENCE (raw counts, file paths, dates) over PROSE. Section 3 is the orchestrator-actionable section — be unambiguous in GO/RISKY/BLOCKED.

**Cross-reference:** RESEARCH.md MUST cite the audit raw file (`.scratch/260605-mz1-aliyun-audit-raw.txt`) and probe script (`.scratch/260605-mz1-concurrent-probe.py`) by path. These files are gitignored so reviewers running on a fresh clone won't see them — that's intended; the EVIDENCE in RESEARCH.md is what matters for downstream decisions, the .scratch/ files are local audit trail only.

**Per CLAUDE.md PRINCIPLE #1 Think Before Coding:** if Section 1 verdicts contradict each other (e.g., #38 SYSTEMIC but #40 ONE_OFF) make that contradiction explicit in Section 3, do NOT silently pick one interpretation.
  </action>
  <verify>
    <automated>test -f .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md &amp;&amp; grep -cE "^## Section [1-4]" .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md | awk '$1 == 4 {exit 0} {exit 1}' &amp;&amp; awk 'END{exit (NR&lt;80)}' .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md</automated>
  </verify>
  <done>RESEARCH.md exists at canonical path with all 4 H2 sections present, ≥ 80 lines total. Section 1 has per-wall verdicts (#38/#39/#40 each one of SYSTEMIC/INTERMITTENT/ONE_OFF) with evidence counts. Section 2 has wall_s table + verdict. Section 3 declares GO/RISKY/BLOCKED unambiguously. Section 4 has 3 ISSUES.md row recommendations.</done>
</task>

<task type="auto">
  <name>Task 4: Single forward-only commit + push</name>
  <files>.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-PLAN.md, .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md</files>
  <action>
**Pre-commit checks (executor MUST run these and confirm output before staging):**

1. Verify `.scratch/` is NOT staged (those files are local-only): `git status --short | grep '^.. \\.scratch/' && echo "BLOCK: .scratch/ files showing up — abort" || echo "OK: .scratch/ clean"`
2. Verify NO prod source files modified: `git status --short | grep -E '\\b(batch_ingest_from_spider|ingest_wechat|kg_synthesize|config|kb/|databricks-deploy/|scripts/)\\b' && echo "BLOCK: prod files modified — abort" || echo "OK: only quick artifacts"`
3. Verify only the 2 expected files in stage: should be exactly `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-PLAN.md` and `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md`. **Do NOT include SUMMARY.md** — that's orchestrator close-out, not this quick's deliverable.

**Stage explicitly (NEVER `-A`, NEVER `git add .`):**

```bash
git add .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-PLAN.md
git add .planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md
```

**Commit message (HEREDOC, executor fills in `{verdict}` placeholders from Section 1+2+3):**

```
git commit -m "$(cat <<'EOF'
docs(v1.2-research): walls-verify — 5-day trend audit + local concurrent probe

3 walls (ISSUES #38/#39/#40) 5-day evidence verify systemic vs anomaly:
- #38 wrapper-cap CUMULATIVE: {SYSTEMIC|INTERMITTENT|ONE_OFF}
- #39 PROCESSED-gate silent drop: {SYSTEMIC|INTERMITTENT|ONE_OFF}
- #40 serial-processing starvation: {SYSTEMIC|INTERMITTENT|ONE_OFF}

Local concurrent probe (2 articles, corp laptop, isolated .dev-runtime/):
- serial baseline: {N}s
- 2-concurrent:    {M}s
- speedup:         {ratio}x
- thread-safety verdict: {GO|RISKY|BLOCKED}

v1.2 design viability: {GO|RISKY|BLOCKED} for batch_ingest concurrent rewrite.
{Alternative path summary if BLOCKED, or "no alternative needed" if GO}.

NO prod state mutated. NO Aliyun config edit. Corp local probe ran in
isolated OMNIGRAPH_BASE_DIR=.dev-runtime/260605-mz1-probe.
EOF
)"
```

**Push forward-only:**

```bash
git log --oneline -1   # confirm new commit at HEAD
git push origin main   # forward-only push; if rejected (non-fast-forward), HALT and surface — do NOT force
```

**Discipline anchors (per CLAUDE.md + memories):**
- NO `git commit --amend` (memory `feedback_no_amend_in_concurrent_quicks`)
- NO `git reset --hard`, NO `git push --force`
- NO `git add -A` / NO `git add .` — explicit per-file (memory `feedback_git_add_explicit_in_parallel_quicks`)
- NO `Co-Authored-By` line (CLAUDE.md global "Note: Attribution disabled globally via ~/.claude/settings.json")
- If push rejected (non-FF): halt, surface to orchestrator with `git status` + `git log origin/main..HEAD --oneline`. Do NOT pull/rebase autonomously — that may conflict with concurrent quicks.
- If `.scratch/` files accidentally staged: `git restore --staged .scratch/` and verify clean before commit.

**No SUMMARY.md from this agent** — orchestrator writes SUMMARY.md during close-out (per quick workflow). This planner only owns PLAN.md + the executor will produce RESEARCH.md.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -E "v1\\.2-research|walls-verify" &amp;&amp; git diff HEAD~1 HEAD --name-only | sort | diff - &lt;(printf ".planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-PLAN.md\n.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md\n" | sort)</automated>
  </verify>
  <done>Single forward-only commit on main containing exactly PLAN.md + RESEARCH.md. Commit message follows template with placeholders filled. Pushed to origin/main (or halted with surface if non-FF). NO `--amend`, NO force, NO `.scratch/` files leaked into the commit, NO prod source files in diff.</done>
</task>

</tasks>

<verification>

**Quick-level verification (executor runs after all 4 tasks complete):**

1. `git log --oneline -1` shows the v1.2-research commit at HEAD.
2. `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md` exists, ≥ 80 lines, all 4 sections.
3. `.scratch/260605-mz1-aliyun-audit-raw.txt` exists locally (gitignored, not committed).
4. `.scratch/260605-mz1-concurrent-probe.py` exists locally, ≤ 80 LoC, valid Python (gitignored, not committed).
5. `git status --short` clean OR shows only unrelated working-tree changes — NO untracked files under `.planning/quick/260605-mz1*/` or `kb/` or top-level production paths.
6. `git diff HEAD~1 HEAD --name-only` returns exactly 2 files: PLAN.md + RESEARCH.md.
7. `git diff HEAD~1 HEAD --stat | grep -cE 'batch_ingest_from_spider|ingest_wechat|kg_synthesize|kb/|systemd|app\\.yaml'` returns 0 (no prod source touched).
8. `OMNIGRAPH_BASE_DIR` was set to `.dev-runtime/260605-mz1-probe/` during the probe run AND that dir does NOT pollute `~/.hermes/omonigraph-vault/`. (Spot-check: `ls ~/.hermes/omonigraph-vault/lightrag_storage/ -la --time=ctime | head -5` shows no files created today by the probe.)

**Halt-trigger verification:**

- If ANY halt trigger fired, RESEARCH.md "Halt log" section MUST document it.
- If Halt #4 fired (all walls non-systemic), RESEARCH.md Section 3 conclusion MUST recommend "do not file v1.2 P0 candidates yet, annotate #38/#39/#40 needs more data" — NOT a v1.2 design plan.

**ISSUES.md row addition (orchestrator-only, NOT executor):**

The executor does NOT edit `.planning/ISSUES.md`. Per CLAUDE.md PRINCIPLE #10: "Orchestrator (NOT agent) adds rows to ISSUES.md before marking the quick CLOSED." Section 4 of RESEARCH.md is the executor's INPUT to that orchestrator step; orchestrator transcribes recommendations into ISSUES.md row updates during quick close-out.

</verification>

<success_criteria>

This quick is COMPLETE when:

1. ✅ `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md` exists with 4 H2 sections.
2. ✅ All 3 walls (#38, #39, #40) have explicit verdicts (SYSTEMIC / INTERMITTENT / ONE_OFF) cited with evidence counts in Section 1.
3. ✅ Section 2 has wall_s_serial / wall_s_concurrent / speedup_ratio populated, OR an explicit halt-fired record if probe was blocked.
4. ✅ Section 3 declares v1.2 viability as one of {GO, RISKY, BLOCKED} with refined LoC estimate or alternative path.
5. ✅ Section 4 has 3 row-update recommendations (one per wall) for orchestrator pickup.
6. ✅ Single forward-only commit on main with exactly PLAN.md + RESEARCH.md staged. NO amend, NO force, NO `-A`.
7. ✅ Zero prod state mutated — `git diff HEAD~1 HEAD` touches no `batch_ingest_from_spider.py` / `ingest_wechat.py` / `kb/` / `databricks-deploy/` / `scripts/` (other than these 2 quick artifacts).
8. ✅ `.scratch/` files local-only (gitignored, not in commit).

This quick is HALTED (NOT failed) if:

- Halt #1 fires (corp firewall blocks DeepSeek for the probe) → Section 2 marked BLOCKED-by-firewall, Section 3 keeps Section 1 verdicts but flags v1.2 viability UNKNOWN. Quick still commits with partial data.
- Halt #2 fires (concurrent corruption) → Section 2 marked BLOCKED with corruption evidence, Section 3 conclusion = BLOCKED. Quick commits with this finding (this IS valuable evidence — concurrent rewrite is NOT viable, design BLOCKED is the answer).
- Halt #3 fires (Vertex 429) → handled by switching provider; only escalates if no provider works.
- Halt #4 fires (all walls non-systemic) → Section 3 recommends "do not file v1.2", quick still commits with this finding (this IS valuable evidence — saves user from filing premature v1.2 P0 candidates).

</success_criteria>

<output>

After completion, the orchestrator will create `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-SUMMARY.md` during quick close-out, transcribing Section 4 recommendations into `.planning/ISSUES.md` row updates per CLAUDE.md PRINCIPLE #10. Executor does NOT write SUMMARY.md.

Quick artifacts:
- `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-PLAN.md` (this file, committed)
- `.planning/quick/260605-mz1-v1-2-research-walls-verify/260605-mz1-RESEARCH.md` (deliverable, committed)
- `.scratch/260605-mz1-aliyun-audit-raw.txt` (local-only audit trail, gitignored)
- `.scratch/260605-mz1-concurrent-probe.py` (local-only probe script, gitignored)

</output>
