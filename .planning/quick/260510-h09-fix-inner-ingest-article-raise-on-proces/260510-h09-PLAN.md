---
quick_id: 260510-h09
title: Inner ingest_article raise on PROCESSED verification failure
date: 2026-05-10
type: hot-fix
parallel_safe: false
status: in-progress
---

# Quick 260510-h09 — PROCESSED-gate hot-fix

## Goal

Hot-fix the inner-ingest verification gate so it RAISES `RuntimeError` on PROCESSED verification failure (was: silent log+skip), letting the outer `batch_ingest_from_spider.ingest_article` catch path convert to `status='failed'` (mig 009 retry pool re-queues next cron).

## Context (verbatim user spec — APPENDIX A)

Path A hot-fix for the 2026-05-10 09:00 ADT cron contract violation: 4 ingestions=ok wechat / 0 LightRAG status='processed' / 4 mystery rows.

Root cause:
- inner `ingest_wechat.ingest_article` verified `rag.aget_docs_by_ids` but only `logger.warning(...)` and skipped its inner INSERT
- outer `batch_ingest_from_spider.ingest_article` treated no-exception as `success=True`; main loop `INSERT OR REPLACE` clobbered the gate
- inner verification was dead code

## Tasks

### Task 1 — Apply `ingest_wechat.py` changes (~30 LOC)

- Add module-level constants `PROCESSED_VERIFY_MAX_RETRIES=3`, `PROCESSED_VERIFY_BACKOFF_S=2.0`
- Add private async helper `_verify_doc_processed_or_raise(rag, doc_id, *, max_retries=..., backoff_s=...)` near `_status_is_processed`. Retries with backoff; raises `RuntimeError` on terminal failure. Skips wasted final sleep via `if attempt < max_retries - 1: await asyncio.sleep(...)`.
- Replace verification region in `ingest_article` (lines ~1256-1281 body) with `await _verify_doc_processed_or_raise(rag, doc_id); doc_confirmed = True`. Keep the comment header at 1249-1255.
- Update INSERT at line ~1297-1300 to include `source='wechat'` (consistent with mig 008 schema + outer main-loop INSERT).

### Task 2 — Write `tests/unit/test_ingest_article_processed_gate.py` (NEW, ~150-200 LOC, 5-6 tests)

Mock-only tests targeting the new helper directly (AsyncMock the LightRAG instance's `aget_docs_by_ids`). Pass `backoff_s=0.0` to skip real sleeps.

1. `test_processed_verification_passes_first_try`
2. `test_processed_promotes_after_retry`
3. `test_never_promotes_raises_runtime_error` (production regression)
4. `test_doc_missing_from_status_raises`
5. `test_aget_docs_raises_then_recovers`
6. `test_outer_catches_inner_runtime_error_returns_failed` (outer integration; SKIP if too much fixture setup needed)

### Task 3 — Commit + push

2-commit strategy (forward-only, no `--amend` on shared worktree per CLAUDE.md lessons #5):

1. Main fix commit: `fix(ingest-260510-h09): inner ingest_article raise on PROCESSED verification failure to close ainsert async-pipeline race`
2. SHA-backfill: `docs(quick-260510-h09): record commit SHA in SUMMARY`

`git push origin main` after Commit #2.

## Hard scope constraints

ZERO touches to:
- `batch_ingest_from_spider.py` (READ-only for context)
- `lib/*` / `cognee_*.py` / `lightrag_embedding.py`
- `tests/unit/test_ainsert_persistence_contract.py`
- Any other `tests/unit/*.py` file
- `migrations/*.sql` / `requirements.txt` / `pyproject.toml` / `pytest.ini`

NO Hermes deploy / SSH / production smoke trigger.
NO follow-up fix beyond this hot-fix scope.

## Success criteria

- `_verify_doc_processed_or_raise` callable + module constants importable
- Targeted test file 5-6 GREEN
- Full unit suite delta: +5 or +6 passing, no new RED
- Two commits on origin/main (push success)
