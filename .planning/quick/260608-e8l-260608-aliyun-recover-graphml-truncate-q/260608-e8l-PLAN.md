---
quick_id: 260608-e8l
description: 260608-aliyun-recover graphml truncate + Qdrant restart + atomic write structural fix
date: 2026-06-08
mode: quick
---

# Quick Task 260608-e8l — Aliyun Recovery

## Goal

Recover Aliyun stack from 3 independent failures discovered by workflow `wad5a5ysc` (read-only diag):

1. 🔴 **graphml truncate** 2026-06-07 08:40 CST — 41 MB XML mid-write SIGTERM, no atomic write
2. 🟡 **Qdrant docker exited** 2026-06-07 09:11 CST — manual stop during build storm, no restart policy
3. 🟡 **OOM trend** 2026-06-05/06 — anon-rss 10-11 GB on 14 GB box, multi-day pattern

## 7-step recovery DAG (per orchestrator brief, ranked by safety)

```
Step 0 (forensic snapshot) — no deps
  └─> Step 1 (stop crash loop)
        └─> Step 2 (kb-api dump, BEST) ─┐
            └─> Step 3 (Hermes transplant, FALLBACK)
                  └─> Step 4 (atomic-write patch) ───────────┤
                        └─> Step 5 (Qdrant restart) ────────┤
                              └─> Step 6 (kb-api restart) ──┘
                                    └─> Step 7 (cron resume)
                                          └─> Step 8 (final verify)
```

## Constraints

- kb-api preserved as in-memory recovery surface until graphml restored.
- Hermes RO until 2026-06-22 — read-only `scp` pull only, NO Hermes writes.
- 14-day data loss bound if Step 2 (kb-api dump) abandoned → Step 3 (Hermes) used.
- A3 BACKOFF=10 NOT touched — workflow verdict ruled out A3 as cause.
- `qdrant-snapshot.timer` stays disabled (#41 streaming-write fix not shipped).

## Decisions (locked)

- Atomic write patch applies to BOTH venvs:
  - `venv-aim1/lib/python3.11/site-packages/lightrag/kg/networkx_impl.py` (ingest cron)
  - `venv/lib/python3.10/site-packages/lightrag/kg/networkx_impl.py` (kb-api)
- Cron resume policy: `--max-articles 10 → 5`, `MemoryHigh 3G → 10G`, `MemoryMax 4G → 12G`. `Conflicts=` already exists in all three units.

## Single forward-only commit

```
fix(aliyun-recover): graphml truncate + Qdrant restart + atomic write structural fix
```

## NOT do

- ❌ NOT touch Hermes (read-only scp pull only)
- ❌ NOT --force / --amend / reset --hard
- ❌ NOT git add -A
- ❌ NOT enable qdrant-snapshot.timer
- ❌ NOT pip install lightrag (overwrites atomic-write patch — track in ISSUES.md)
- ❌ NOT skip Step 6 pre-flight gates

## Cross-references

- Workflow result: `C:/Users/huxxha/AppData/Local/Temp/claude/.../tasks/wad5a5ysc.output`
- aim-2 cutover memory: `project_aim2_closed_260524.md` (Hermes baseline 27654/39604 byte-identical)
- LightRAG networkx storage: vendored in two venvs as listed above
