---
phase: 260612-dv7-aliyun-48h-healthcheck
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260612-dv7-260612-aliyun-48h-healthcheck-read-only-/HEALTH-REPORT.md
autonomous: true
requirements: [HEALTH-AUDIT-48H]
---

<objective>
Read-only Aliyun production health audit over the past 48 hours (CST). Diagnostic-only — ZERO prod mutation, NO fixes, NO Hermes touches, NO systemctl start/stop/restart, NO git ops on Aliyun. Produce a single `HEALTH-REPORT.md` (committed locally) with a GREEN/YELLOW/RED verdict per 8 check areas, an explicit ⭐ #45 hang-fix verdict (cron fires, not just the 2026-06-11 manual fire), an Apify a5ccc0c verdict, and any newly-surfaced issues listed as candidate ISSUES.md rows (do NOT edit ISSUES.md — orchestrator transcribes per PRINCIPLE #10).

Purpose: Confirm the 4-issue cluster (#45 os._exit, #47 .pth atomic-write, #48 quiesce gate, #29 citation sweep) closed 2026-06-11 is holding in live automated operation, and surface any 48h regression before it compounds.
Output: `.planning/quick/260612-dv7-260612-aliyun-48h-healthcheck-read-only-/HEALTH-REPORT.md` + optional raw-evidence file under `.scratch/` (gitignored, cited by sha256).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ISSUES.md
@CLAUDE.md

<operator_protocol>
- The executor IS the operator (CLAUDE.md Principle #5). Run ALL SSH yourself via the Bash tool. NEVER hand an SSH command to the user.
- SSH alias: `ssh aliyun-vitaclaw "<cmd>"`.
- Any command that needs API keys / scoped env MUST wrap: `ssh aliyun-vitaclaw "set -a; source /root/.hermes/.env; set +a; <cmd>"` (bare SSH misses DEEPSEEK_API_KEY etc — memory aliyun_ssh_manual_trigger_env). Pure systemctl/journalctl/ls/stat/free/df/docker/curl-localhost do NOT need the env wrap.
- venvs: `/root/OmniGraph-Vault/venv-aim1/bin/python` (ingest), `/root/OmniGraph-Vault/venv/bin/python` (kb-api).
- TIMEZONE: Aliyun=CST (UTC+8), user=ADT (UTC-3), 11h delta. Tag EVERY timestamp in the report with an explicit `CST` marker. "Past 48h" = (now in CST) minus 48h.
- READ-ONLY ONLY: SELECT-only SQL (open sqlite with `?mode=ro`), systemctl status / list-timers / journalctl reads, ls / stat / free / df, nx.read_graphml parse-check, curl localhost health/search/synthesize (these are read endpoints). ZERO mutation, ZERO restart, ZERO git pull on Aliyun.
- SSH THROTTLE handling (memory feedback_ssh_throttle_poll_loop — #27/#42/#43 class, self-heals 1-5h): if SSH banner-throttles, do NOT foreground retry-storm. Launch a BACKGROUND `until ssh aliyun-vitaclaw "echo ok"; do sleep 180; done` poll-loop with a 60-min ceiling, continue other probes meanwhile, and if it never clears mark the throttled areas HONEST UNKNOWN.
- DO NOT block the whole audit on one failed probe. If a port isn't found, a container name is wrong, or SSH throttles for one area, record UNKNOWN for that area and CONTINUE the rest.
- DISCOVER-AT-EXECUTION (do NOT hardcode): kb-api port (from ExecStart / override.conf), qdrant container name (from `docker ps -a`), sqlite `articles` schema column names (run `.schema articles` FIRST). The spec gives the discovery method; use the live value.
</operator_protocol>

<prior_state>
- #45 os._exit fix (commit `352dd01`, quick 260610-rgm) was ALIYUN-VERIFY-CONFIRMED 2026-06-11 via a MANUAL fire (Metrics-written → exit gap = 0.62s, R29). This audit checks the AUTOMATED cron fires over the past 48h show the same ≤5s clean exit — manual confirmation ≠ cron confirmation.
- #47 atomic-write delivered via `.pth` (`zz_omnigraph_atomic_write.pth`, commit `62e49a3`/`ba1121c`) — both Aliyun venvs reported `os.replace=True` at startup 2026-06-11. The atomic-write patch presence check (Area 3) verifies it survived.
- Apify a5ccc0c "Run object not subscriptable" fix — verification of 0 occurrences in 48h is part of this audit (Area 6).
- Open issues the report must touch: #44 (graphml↔Qdrant divergence → long_form sources=0), #51 (disk 87% + 313 stale checkpoints), #30 (translate throughput drift), #46 (SiliconFlow balance / vision fallback), #40 (serial-batch starvation, BLOCKED).
- NOTE: #51 is referenced in the spec but does NOT have a numbered row in current ISSUES.md (disk/checkpoints context lives in the 260609-presleep-audit STATE entry). Report it under Area 5 by the disk/checkpoint observations directly; flag as candidate row if regressed.
</prior_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe areas 1-4 — cron/ingest, ⭐#45 hang-fix, graphml integrity, Qdrant+kb-api</name>
  <files>(SSH probes only — writes raw evidence to .scratch/dv7-evidence-areas-1-4.txt locally)</files>
  <action>
Establish the 48h CST window first: `NOW_CST=$(ssh aliyun-vitaclaw "date '+%Y-%m-%d %H:%M:%S %Z'")` — record it; "48 hours ago" in journalctl is relative to Aliyun clock so `--since '48 hours ago'` is correct on the Aliyun side. Tag the report window explicitly (e.g. "Window: 2026-06-10 HH:MM CST → 2026-06-12 HH:MM CST").

If SSH banner-throttles at any point, launch the background poll-loop (operator_protocol) and proceed to whatever areas are reachable; mark blocked areas UNKNOWN.

=== AREA 1 — CRON FIRING + INGEST OUTCOMES ===
- `ssh aliyun-vitaclaw "systemctl list-timers 'omnigraph-*' --all --no-pager"` — confirm daily(08:00)/afternoon(14:00)/evening(20:00)-ingest + housekeeping timers present; flag any DEAD/failed.
- Per ingest service (daily, afternoon, evening): `ssh aliyun-vitaclaw "journalctl -u omnigraph-<X>-ingest.service --since '48 hours ago' --no-pager | grep -E 'Started|Deactivated|Done —|candidates processed|Metrics written|Successfully finalized|Failed|error|Traceback|Killed|OOM'"` — derive fire count, clean completions, articles/fire.
- `ssh aliyun-vitaclaw "ls -lt /root/OmniGraph-Vault/data/batch_timeout_metrics_*.json | head -8"` then cat the recent ones — record completed vs total articles, timed_out, avg_article_time_sec, safety_margin_triggered.
- Verdict GREEN if timers active + fires completed cleanly; YELLOW if degraded/partial; RED if DEAD timer or repeated Traceback/Killed.

=== AREA 2 — ⭐ #45 HANG-FIX VERIFICATION (THE KEY ITEM) ===
- Confirm fix is pulled on Aliyun: `ssh aliyun-vitaclaw "cd /root/OmniGraph-Vault && git log --oneline -10 | grep -E '352dd01|os._exit|260610-rgm'"` AND `ssh aliyun-vitaclaw "grep -n 'os._exit' /root/OmniGraph-Vault/batch_ingest_from_spider.py"`.
- For EACH 48h cron fire: compute the gap between the final `Metrics written` journal timestamp and the service `Deactivated`/`Stopped` timestamp (both from the journalctl output in Area 1, or `systemctl show omnigraph-<X>-ingest.service -p ExecMainExitTimestamp,ActiveExitTimestamp`). PASS ≤5s, FAIL = minutes.
- `ssh aliyun-vitaclaw "ps aux | grep batch_ingest_from_spider | grep -v grep"` — any S-state proc with no active work = recurrence. Cross-check ELAPSED vs last journal line (if ELAPSED >> last activity, that's the 50min hang signature).
- ⭐ Explicit verdict: do all 48h fires exit ≤5s post-`Metrics written`? → #45 stays RESOLVED. Any minutes-long gap or live S-state hang → RE-OPEN #45 (file candidate P0 row). NOTE the 2026-06-11 manual fire (0.62s) is prior evidence, not a substitute for cron-fire evidence — if NO automated fire happened in 48h, mark "no cron-fire data in window; manual fire confirmed 06-11; cron-confirmation PENDING" (HONEST UNKNOWN, do not fabricate green).

=== AREA 3 — GRAPHML INTEGRITY (atomic-write patch holding?) ===
- `ssh aliyun-vitaclaw "ls -la /root/.hermes/omonigraph-vault/lightrag_storage/graph_chunk_entity_relation.graphml*"` — flag any `.tmp` orphan or new `.corrupt-*` within 48h (note: dir is `omonigraph-vault`, typo is canonical — do NOT 'correct' it).
- Parse: `ssh aliyun-vitaclaw "/root/OmniGraph-Vault/venv-aim1/bin/python -c \"import networkx as nx; g=nx.read_graphml('/root/.hermes/omonigraph-vault/lightrag_storage/graph_chunk_entity_relation.graphml'); print('nodes',g.number_of_nodes(),'edges',g.number_of_edges())\""` — MUST parse. Compare to baselines (06-11 was ~31263 nodes / 45227 edges; 06-11 probe earlier showed 30558/44030). Growth = healthy; shrink or unparseable = ALARM (RED, candidate P0 — cross-ref #44).
- Patch present (pip reinstall wipes it — memory lightrag_networkx_write_not_atomic; #47 now delivered via .pth not in-place edit): check BOTH delivery shapes — `ssh aliyun-vitaclaw "grep -c 'os.replace\|os.fsync' /root/OmniGraph-Vault/venv-aim1/lib/python3.11/site-packages/lightrag/kg/networkx_impl.py"` (in-place edit, may be 0 if .pth path is now sole delivery) AND verify the .pth monkey-patch fires: `ssh aliyun-vitaclaw "/root/OmniGraph-Vault/venv-aim1/bin/python -c 'import sys; import lightrag.kg.networkx_impl as m; src=__import__(\"inspect\").getsource(m.NetworkXStorage); print(\"os.replace\" in src or \"lightrag_atomic_write_patch\" in str(sys.modules.keys()))'\"` OR simpler: `ssh aliyun-vitaclaw "/root/OmniGraph-Vault/venv-aim1/bin/python -c 'import lib.lightrag_atomic_write_patch as p; print(\"patch importable\")'"` from repo root. Record which delivery mechanism is live. GREEN if atomic-write is active by either mechanism.

=== AREA 4 — QDRANT + kb-api HEALTH ===
- `ssh aliyun-vitaclaw "docker ps -a --filter name=qdrant --format '{{.Names}} {{.Status}} {{.RunningFor}}'"` — discover the container NAME (do not hardcode); must be Up. Then restart policy (memory qdrant_docker_no_restart_policy_trap): `ssh aliyun-vitaclaw "docker inspect <discovered-name> --format '{{.HostConfig.RestartPolicy.Name}}'"` — want unless-stopped/always, NOT "no" (if "no", flag candidate P2 — single docker/daemon restart leaves Qdrant down indefinitely).
- kb-api status + DISCOVER PORT: `ssh aliyun-vitaclaw "systemctl status kb-api.service --no-pager | head -5"` and `ssh aliyun-vitaclaw "systemctl cat kb-api.service | grep -iE 'ExecStart|--port|PORT='"` plus check override.conf — extract the live port (do not hardcode). Then `ssh aliyun-vitaclaw "curl -s -m 10 localhost:<port>/health"`.
- FTS (memory kb_api_restart_fixes_search_endpoint): `ssh aliyun-vitaclaw "curl -s -m 15 'localhost:<port>/api/search?q=agent&mode=fts'"` — items>0 expected; items=0 with a non-empty DB = stale-FTS connection symptom (flag YELLOW + candidate P1, do NOT restart kb-api).
- long_form KG smoke (#44 symptom): `ssh aliyun-vitaclaw "curl -s -m 60 -X POST localhost:<port>/api/synthesize -H 'Content-Type: application/json' -d '{\"query\":\"什么是AI Agent\",\"mode\":\"long_form\"}'"` — sources>0 = KG join healthy; sources=0 = #44 divergence still biting (flag, cross-ref open #44 P0 — this is a known-open issue, so sources=0 confirms #44 still open, not a NEW issue).

Save all raw outputs (journal excerpts, counts, curl bodies) to `.scratch/dv7-evidence-areas-1-4.txt` locally (gitignored). Do NOT write any file on Aliyun.
  </action>
  <verify>
    <automated>test -f .scratch/dv7-evidence-areas-1-4.txt &amp;&amp; grep -qE 'AREA 1|cron|timer' .scratch/dv7-evidence-areas-1-4.txt</automated>
  </verify>
  <done>Areas 1-4 probed; raw evidence captured to .scratch/dv7-evidence-areas-1-4.txt; each area has a GREEN/YELLOW/RED/UNKNOWN verdict noted; ⭐#45 cron-fire exit-gap measured (or HONEST UNKNOWN if no cron fire in window); kb-api port + qdrant container name discovered live. ZERO Aliyun mutation performed.</done>
</task>

<task type="auto">
  <name>Task 2: Probe areas 5-8 (resources/OOM, Apify a5ccc0c, translate, vision) and write HEALTH-REPORT.md</name>
  <files>.planning/quick/260612-dv7-260612-aliyun-48h-healthcheck-read-only-/HEALTH-REPORT.md</files>
  <action>
Continue read-only probes. Same SSH discipline and throttle/UNKNOWN handling as Task 1.

=== AREA 5 — RESOURCES + OOM ===
- `ssh aliyun-vitaclaw "free -m"` (14G box — record free/used) + `ssh aliyun-vitaclaw "df -h /"` (the 260609-presleep-audit noted 87% on /dev/vda3 — record current %; if >90% flag candidate P1, if grown vs 87% note trend).
- `ssh aliyun-vitaclaw "journalctl --since '48 hours ago' --no-pager | grep -iE 'out of memory|oom-kill|killed process' | head"` — any OOM-kill in 48h = flag (cross-ref #41 converter OOM if it's the snapshot service; note snapshot.timer was DISABLED 06-05 so converter OOM should NOT recur).
- Stale checkpoints (260609-presleep-audit noted 313): `ssh aliyun-vitaclaw "ls /root/OmniGraph-Vault/checkpoints/ 2>/dev/null | wc -l"` — record count; if growing unbounded flag candidate P2 (disk pressure contributor).

=== AREA 6 — APIFY a5ccc0c FIX VERIFICATION ===
- `ssh aliyun-vitaclaw "journalctl -u 'omnigraph-*-ingest.service' --since '48 hours ago' --no-pager | grep -cE \"Run.*subscriptable|'Run' object\""` — MUST be 0. Any hit = regression (candidate P1, re-open the a5ccc0c fix).
- Scrape method mix: `ssh aliyun-vitaclaw "journalctl -u 'omnigraph-*-ingest.service' --since '48 hours ago' --no-pager | grep -cE 'method: apify|method: cdp|method: mcp|Scraping successful'"` — record per-method counts for context (apify primary working vs falling to cdp/mcp).
- Explicit verdict: 0 subscript errors confirmed → a5ccc0c fix HOLDING.

=== AREA 7 — TRANSLATE THROUGHPUT (#30 drift) ===
- `ssh aliyun-vitaclaw "systemctl status omnigraph-translate.service --no-pager | head -5"` + last exit code (note: also check if a translate TIMER exists or if it's manual — memory aliyun_translate_pipeline_not_automated flagged the cron hook gap; record whether it auto-fires).
- Coverage SQL — INSPECT SCHEMA FIRST (do not assume column names): `ssh aliyun-vitaclaw "set -a; source /root/.hermes/.env; set +a; /root/OmniGraph-Vault/venv-aim1/bin/python -c \"import sqlite3; c=sqlite3.connect('file:/root/OmniGraph-Vault/data/kol_scan.db?mode=ro',uri=True); print([r[1] for r in c.execute('PRAGMA table_info(articles)')])\""` — confirm `layer2_verdict` and `body_translated` (or actual names) exist. THEN run coverage: `ssh aliyun-vitaclaw "set -a; source /root/.hermes/.env; set +a; /root/OmniGraph-Vault/venv-aim1/bin/python -c \"import sqlite3; c=sqlite3.connect('file:/root/OmniGraph-Vault/data/kol_scan.db?mode=ro',uri=True); cur=c.cursor(); cur.execute(\\\"SELECT COUNT(*) FROM articles WHERE layer2_verdict='ok'\\\"); ok=cur.fetchone()[0]; cur.execute(\\\"SELECT COUNT(*) FROM articles WHERE layer2_verdict='ok' AND body_translated IS NOT NULL AND body_translated != ''\\\"); tr=cur.fetchone()[0]; print(f'translated {tr}/{ok} = {100*tr/ok:.1f}%')\""`. Adjust column names to live schema if PRAGMA differs. Compare to #30's last-known 84.1% — drifting further down = confirms #30 still open (known issue, not new).
- WeChat session (memory feedback_wechat_cookie_refresh_runbook): `ssh aliyun-vitaclaw "journalctl --since '48 hours ago' --no-pager | grep -cE 'ret=200003|session.*expired|session-expired'"` — any storm = flag (candidate P1, needs cookie refresh).

=== AREA 8 — VISION CASCADE (SiliconFlow balance proxy — #46) ===
- From recent batch_validation_report.json or image_batch_complete journal: `ssh aliyun-vitaclaw "ls -lt /root/OmniGraph-Vault/data/batch_validation_report*.json 2>/dev/null | head -3"` then cat recent and extract `provider_usage`/`provider_mix`. OR `ssh aliyun-vitaclaw "journalctl -u 'omnigraph-*-ingest.service' --since '48 hours ago' --no-pager | grep -iE 'provider_mix|provider_usage|gemini|siliconflow|openrouter|CASCADE ALERT'"`.
- Verdict: gemini fallback >10% of images = flag SiliconFlow depletion (candidate, cross-ref open #46 — #46 already flags the balance-field unreliability; high gemini fallback confirms it's biting). Note OMNIGRAPH_VISION_SKIP_PROVIDERS state if visible.

=== WRITE HEALTH-REPORT.md ===
Write `.planning/quick/260612-dv7-260612-aliyun-48h-healthcheck-read-only-/HEALTH-REPORT.md` with this structure (all timestamps CST):

1. **Header**: audit date (CST), 48h window (start CST → end CST), Aliyun `date` output captured, executor = read-only diagnostic.
2. **TL;DR verdict table**: 8 rows (one per area) with GREEN/YELLOW/RED/UNKNOWN + one-line summary.
3. **⭐ #45 verdict** (its own section): every 48h cron fire exit ≤5s post-Metrics-written? → RESOLVED (cron-confirmed) / still-PENDING (no cron fire in window, manual-only) / RE-OPEN (hang recurred). Cite the per-fire exit-gap measurements.
4. **Apify a5ccc0c verdict**: 0 'Run object not subscriptable' errors confirmed? + scrape method mix.
5. **Per-area detail** (Areas 1-8): each with verdict + key evidence (counts, timestamps, curl status+key fields).
6. **Known-open issues status** (#44 long_form sources, #30 translate %, #46 vision fallback, #40 starvation, disk/checkpoints): confirm each is unchanged / improved / worsened vs ISSUES.md last-known.
7. **NEW candidate ISSUES rows** (P0/P1/P2): any regression NOT already tracked — formatted as ready-to-transcribe rows (# / Issue / Suggested slug / Notes). State clearly "candidate rows for orchestrator to transcribe — this report does NOT edit ISSUES.md (PRINCIPLE #10)". If none, say "No new issues surfaced".
8. **HONEST UNKNOWN section**: any probe that couldn't run (SSH throttle / missing port / schema mismatch) — list explicitly, do NOT fabricate green.
9. **Raw evidence**: inline key excerpts OR cite `.scratch/dv7-evidence-areas-1-4.txt` + `.scratch/dv7-evidence-areas-5-8.txt` by `sha256sum` (record the hashes in the report).

Save Areas 5-8 raw outputs to `.scratch/dv7-evidence-areas-5-8.txt` locally before writing the report.
  </action>
  <verify>
    <automated>test -f .planning/quick/260612-dv7-260612-aliyun-48h-healthcheck-read-only-/HEALTH-REPORT.md &amp;&amp; grep -qiE '#45|RESOLVED|RE-OPEN|PENDING' .planning/quick/260612-dv7-260612-aliyun-48h-healthcheck-read-only-/HEALTH-REPORT.md &amp;&amp; grep -qiE 'a5ccc0c|subscriptable|Apify' .planning/quick/260612-dv7-260612-aliyun-48h-healthcheck-read-only-/HEALTH-REPORT.md</automated>
  </verify>
  <done>Areas 5-8 probed; HEALTH-REPORT.md written with TL;DR verdict table (8 areas), explicit ⭐#45 cron-fire verdict, Apify a5ccc0c verdict, known-open-issue status (#44/#30/#46/#40/disk), candidate NEW issue rows (or "none"), HONEST UNKNOWN section, and raw-evidence citations. ISSUES.md NOT edited. ZERO Aliyun mutation performed.</done>
</task>

</tasks>

<verification>
- ZERO prod mutation: no systemctl start/stop/restart, no git ops on Aliyun, no Hermes touches, SELECT-only SQL (`?mode=ro`), all writes are local-repo-only.
- All timestamps in HEALTH-REPORT.md carry an explicit CST marker; the 48h window is stated.
- ⭐ #45 has an explicit RESOLVED / PENDING / RE-OPEN verdict with per-cron-fire exit-gap evidence (or HONEST UNKNOWN if no cron fire in window).
- Apify a5ccc0c has an explicit 0-error verdict.
- Every probe that couldn't run is listed under HONEST UNKNOWN — no fabricated greens.
- Discover-at-execution values (kb-api port, qdrant container name, sqlite schema columns) were read live, not hardcoded.
</verification>

<success_criteria>
- `HEALTH-REPORT.md` exists at the report path with all 9 sections.
- 8 areas each carry a GREEN/YELLOW/RED/UNKNOWN verdict.
- #45 cron-fire verdict + Apify a5ccc0c verdict are explicit.
- New issues (if any) are listed as candidate ISSUES.md rows; ISSUES.md itself is untouched.
- Single forward-only commit of just the report + PLAN/SUMMARY via explicit `git add`, pushed to origin/main.
</success_criteria>

<output>
After completion, create `.planning/quick/260612-dv7-260612-aliyun-48h-healthcheck-read-only-/260612-dv7-SUMMARY.md`.
Commit: explicit `git add` of HEALTH-REPORT.md + 260612-dv7-PLAN.md + 260612-dv7-SUMMARY.md only; single forward-only commit `docs(260612-dv7): Aliyun 48h read-only health audit`; push origin/main.
</output>
