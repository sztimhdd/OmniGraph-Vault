---
quick_id: 260510-p1s
description: >-
  Standalone batch_classify_rss_layer2.py cron — Layer 2 verdict for RSS
  articles with layer1_verdict='candidate' AND layer2_verdict IS NULL,
  reusing lib.article_filter.layer2_full_body_score + persist_layer2_verdicts.
  Mirrors batch_classify_kol.py CLI/logging shape but is a thin wrapper.
files_modified:
  - batch_classify_rss_layer2.py            # NEW
  - tests/unit/test_batch_classify_rss_layer2.py  # NEW
  - scripts/register_phase5_cron.sh         # EDIT (one add_job block)
  - .planning/STATE.md                      # EDIT (closure row)
files_NOT_touched:
  - batch_ingest_from_spider.py
  - batch_classify_kol.py
  - lib/article_filter.py
  - tests/unit/test_ainsert_persistence_contract.py
autonomous: true
---

<objective>
Add a standalone Hermes cron, `batch_classify_rss_layer2.py`, that drains the
RSS Layer 2 backlog: rows in `rss_articles` where `layer1_verdict='candidate'`
AND `layer2_verdict IS NULL` AND `body IS NOT NULL`. The script:

1. Loads `LIMIT --max-articles` rows from `rss_articles` (default cap from
   env `OMNIGRAPH_RSS_LAYER2_DAILY_CAP=500`, non-int falls back to 500).
2. Builds `list[ArticleWithBody]` chunks of `LAYER2_BATCH_SIZE` (=5).
3. For each chunk, awaits `layer2_full_body_score(chunk)` and writes back
   verdicts via `persist_layer2_verdicts(conn, chunk, results)`.
4. Logs per-row outcome (`id url verdict reason`) and a final summary.
5. `--dry-run` mode: skip both LLM call and DB UPDATE.

Why standalone (not folded into `batch_ingest_from_spider`): the ingest cron
runs scrape+ainsert, gated on `layer2_verdict='ok'`. Without an upstream
classifier cron, RSS articles never leave `layer2_verdict=NULL` and the
gate skips them forever. This is the missing classifier tick — KOL has an
analogous tick (`daily-classify-kol` @ 08:15) but RSS has none post-ir-4.

Purpose: unblock RSS ingestion in production without touching the ingest
hot-path (D-19 surgical-changes principle).

Output: a script + one cron entry + 7 unit tests + dry-run smoke evidence.
</objective>

<context>

@.planning/STATE.md
@./CLAUDE.md
@batch_classify_kol.py                # mirror CLI/logging shape (DO NOT EDIT)
@lib/article_filter.py                # USE layer2_full_body_score + persist_layer2_verdicts (DO NOT EDIT)
@batch_ingest_from_spider.py          # reference drain pattern at lines 1629-1679 (DO NOT EDIT)
@scripts/register_phase5_cron.sh      # EDIT — add one add_job block

<verified_facts>
<!-- Phase 0 facts pre-verified by orchestrator. Cite directly; do not re-investigate. -->

A. rss_articles schema (post-migrations 006/007):
   - 23 columns; layer2 columns are: layer2_verdict, layer2_reason, layer2_at, layer2_prompt_version
   - There is NO layer2_score column.

B. Layer 1 verdict alphabet (lib/article_filter.py:446):
   - {"candidate", "reject"}.
   - Candidate SELECT MUST use layer1_verdict='candidate' (NOT 'ok').

C. layer2_full_body_score (lib/article_filter.py:458):
   - async def layer2_full_body_score(articles: list[ArticleWithBody]) -> list[FilterResult]
   - LAYER2_BATCH_SIZE = 5 — RAISES ValueError if exceeded. Caller chunks.
   - LAYER2_TIMEOUT_SEC = 60 per batch.
   - Body auto-truncated to LAYER2_BODY_TRUNCATION_CHARS = 8000 internally.
   - Returns 1:1 list of FilterResult with verdict ∈ {"ok","reject",None}.
   - On any LLM/parse error, ALL results have verdict=None — caller persists
     NULL so next tick re-evaluates (LF-2.6).

D. ArticleWithBody dataclass (lib/article_filter.py:134-148):
   @dataclass(frozen=True)
   class ArticleWithBody:
       id: int
       source: Literal["wechat", "rss"]
       title: str
       body: str

E. persist_layer2_verdicts (lib/article_filter.py:639-683):
   - Synchronous, atomic (BEGIN/COMMIT/rollback).
   - Groups by source: 'wechat'→articles, 'rss'→rss_articles.
   - Writes ONLY 4 cols: layer2_verdict, layer2_reason, layer2_at, layer2_prompt_version.
   - REUSE — do NOT write a custom UPDATE.

F. KOL drain reference (batch_ingest_from_spider.py:1629-1679):
   - Build list[ArticleWithBody] from queue snapshot
   - await layer2_full_body_score(...)
   - persist_layer2_verdicts(conn, articles_with_body, results)
   - Per-row post-processing for ingestions table — NOT NEEDED here
     (we ONLY classify; ingestions stays untouched).

G. Cron registration pattern (scripts/register_phase5_cron.sh:27-43, 117-119):
   - add_job <name> <schedule> <prompt> — EXISTING grep makes it idempotent.
   - reconcile-ingestions block (117-119) is the cleanest template for
     `cd && source venv && python ... 2>&1 | tee /tmp/...log`.

H. Working-tree hygiene:
   - Other parallel quicks have uncommitted edits in shared files.
   - The executor MUST stage only THIS quick's files explicitly.
   - NEVER `git add -A` / `git add .` / `git commit -a`.
</verified_facts>

<interfaces>
<!-- Symbols the executor will import. Use these directly — no exploration. -->

From lib/article_filter.py:
```python
LAYER2_BATCH_SIZE: int = 5
LAYER2_TIMEOUT_SEC: int = 60
LAYER2_BODY_TRUNCATION_CHARS: int = 8000
PROMPT_VERSION_LAYER2: str = "..."  # used internally; not consumed here

@dataclass(frozen=True)
class ArticleWithBody:
    id: int
    source: Literal["wechat", "rss"]
    title: str
    body: str

@dataclass(frozen=True)
class FilterResult:
    verdict: Literal["ok", "reject", None]
    reason: str
    prompt_version: str

async def layer2_full_body_score(
    articles: list[ArticleWithBody],
) -> list[FilterResult]: ...

def persist_layer2_verdicts(
    conn: sqlite3.Connection,
    articles: list[ArticleWithBody],
    results: list[FilterResult],
) -> None: ...
```

From config.py:
```python
def load_env() -> None: ...  # loads ~/.hermes/.env
```
</interfaces>

</context>

<tasks>

<task id="T1" type="auto" tdd="true">
  <name>T1: Create batch_classify_rss_layer2.py + 7 unit tests</name>
  <files>
    batch_classify_rss_layer2.py
    tests/unit/test_batch_classify_rss_layer2.py
  </files>
  <behavior>
    <!-- Tests scaffold the contract; production code makes them green. -->
    Test 1 (selects_only_candidates_with_null_layer2):
      Fixture: 3 rows in rss_articles —
        row A (layer1_verdict='candidate', layer2_verdict=NULL, body='x') → SELECTED
        row B (layer1_verdict='reject',    layer2_verdict=NULL, body='x') → SKIPPED
        row C (layer1_verdict='candidate', layer2_verdict='ok', body='x') → SKIPPED
      Monkeypatch layer2_full_body_score to a sentinel that records args.
      Assert sentinel called with exactly 1 article (id=A).

    Test 2 (max_articles_limit_respected):
      Fixture: 6 candidate rows with NULL layer2.
      CLI: --max-articles 3.
      Monkeypatched layer2_full_body_score records calls.
      Assert: total articles passed across all batched calls == 3.
      (Since 3 ≤ LAYER2_BATCH_SIZE=5, exactly 1 batch of 3.)

    Test 3 (dry_run_skips_call_and_update):
      Fixture: 1 candidate row, layer2 NULL.
      CLI: --dry-run.
      Monkeypatch layer2_full_body_score to raise AssertionError if called.
      Run script.
      Assert: exit_code == 0, post-run row.layer2_verdict IS NULL,
              row.layer2_at IS NULL (no UPDATE happened).

    Test 4 (verdict_ok_persisted):
      Fixture: 1 candidate row id=42, layer2 NULL.
      Monkeypatch layer2_full_body_score → returns
        [FilterResult(verdict='ok', reason='deep-tech', prompt_version='layer2_v0_test')]
      Run script.
      Assert: post-run row.layer2_verdict == 'ok',
              row.layer2_reason == 'deep-tech',
              row.layer2_at IS NOT NULL,
              row.layer2_prompt_version == 'layer2_v0_test'.

    Test 5 (verdict_reject_persisted):
      Same as Test 4 but verdict='reject', reason='off-topic'.
      Assert post-run row.layer2_verdict == 'reject', reason matches.

    Test 6 (exception_yields_null_persistence_and_continues):
      Fixture: 10 candidate rows (forces 2 chunks of 5).
      Monkeypatch layer2_full_body_score: first call raises an exception
        wrapped INSIDE layer2_full_body_score's _all_null branch behavior
        — i.e. simulate by returning [FilterResult(verdict=None, reason='timeout',
          prompt_version='layer2_v0_test')] * 5 for first chunk;
        second chunk returns 5 'ok' verdicts.
      Assert: exit_code == 0, first 5 rows still layer2_verdict IS NULL
        (script does NOT call persist on all-null batch — see LF-2.6),
        last 5 rows have layer2_verdict='ok'.

    Test 7 (custom_db_path_honored):
      Build tmp_path / "custom.db" with seeded fixture.
      Monkeypatch layer2_full_body_score to return 1 'ok' verdict.
      Run script with --db-path <tmp_path>/custom.db.
      Assert: tmp DB row updated; default DB (if exists) untouched.
  </behavior>
  <action>
    Step A — Write tests/unit/test_batch_classify_rss_layer2.py FIRST (RED):

    - Use pytest, tmp_path fixture, sqlite3 directly (no project DB helpers).
    - Build a `_seed_db(path: Path, rows: list[dict]) -> None` helper that
      creates a minimal rss_articles table with all 23 cols, INSERTs the rows,
      and commits.
    - The columns the tests need to assert are the 4 layer2_* cols + body +
      url + title + id + layer1_verdict + layer2_verdict (for the WHERE).
      The other cols can default to NULL or empty string. SQL:
        CREATE TABLE rss_articles (
            id INTEGER PRIMARY KEY,
            feed_id INTEGER, title TEXT, url TEXT, author TEXT, summary TEXT,
            content_hash TEXT, published_at TEXT, fetched_at TEXT,
            enriched INTEGER, content_length INTEGER, body TEXT,
            body_scraped_at TEXT, depth INTEGER, topics TEXT,
            classify_rationale TEXT,
            layer1_verdict TEXT, layer1_reason TEXT, layer1_at TEXT,
            layer1_prompt_version TEXT,
            layer2_verdict TEXT, layer2_reason TEXT, layer2_at TEXT,
            layer2_prompt_version TEXT
        )
    - Drive the script via subprocess.run(
        [sys.executable, "batch_classify_rss_layer2.py", "--db-path", str(p), ...],
        env={**os.environ, "DEEPSEEK_API_KEY": "dummy", ...},
        capture_output=True, text=True,
      ) — but monkeypatching across processes is hard. PREFERRED PATTERN:
      import the script as a module and call its `run()` entrypoint directly,
      monkeypatching `lib.article_filter.layer2_full_body_score` BEFORE the
      call. The script's `run()` looks up the symbol via
      `from lib.article_filter import layer2_full_body_score` at module
      level — make sure tests monkeypatch the symbol in the
      `batch_classify_rss_layer2` namespace (the module's local binding),
      NOT in `lib.article_filter` (because Python imported it into the
      script's namespace already).
        e.g. `monkeypatch.setattr(
                  "batch_classify_rss_layer2.layer2_full_body_score", fake)`
    - Run pytest, confirm RED (script doesn't exist yet).

    Step B — Write batch_classify_rss_layer2.py (GREEN):

    Module docstring (mirror batch_classify_kol.py:1-14 style):
      """Standalone Layer 2 classifier for RSS — drains rss_articles where
      layer1_verdict='candidate' AND layer2_verdict IS NULL.

      Quick 260510-p1s. Wraps lib.article_filter.layer2_full_body_score
      + persist_layer2_verdicts. Reuses LAYER2_BATCH_SIZE=5 chunking.

      Usage:
          python batch_classify_rss_layer2.py
          python batch_classify_rss_layer2.py --max-articles 50
          python batch_classify_rss_layer2.py --dry-run
          python batch_classify_rss_layer2.py --db-path .dev-runtime/data/kol_scan.db

      Cron daily-classify-rss-layer2 @ 20 8 * * * (between
      daily-classify-kol @ 08:15 and daily-enrich @ 08:30).
      """

    Imports:
      import argparse
      import asyncio
      import logging
      import os
      import sqlite3
      import sys
      from pathlib import Path
      from config import load_env
      from lib.article_filter import (
          ArticleWithBody, LAYER2_BATCH_SIZE,
          layer2_full_body_score, persist_layer2_verdicts,
      )

    Constants:
      PROJECT_ROOT = Path(__file__).parent
      DEFAULT_DB_PATH = PROJECT_ROOT / "data" / "kol_scan.db"
      DEFAULT_DAILY_CAP_FALLBACK = 500
      logger = logging.getLogger("batch_classify_rss_layer2")

    Helpers:
      def _resolve_default_max_articles() -> int:
          raw = os.environ.get("OMNIGRAPH_RSS_LAYER2_DAILY_CAP", "")
          try:
              v = int(raw)
              return v if v > 0 else DEFAULT_DAILY_CAP_FALLBACK
          except (TypeError, ValueError):
              return DEFAULT_DAILY_CAP_FALLBACK

      def _check_schema(conn: sqlite3.Connection) -> bool:
          cols = {row[1] for row in conn.execute(
              "PRAGMA table_info(rss_articles)"
          ).fetchall()}
          required = {
              "id", "title", "url", "body",
              "layer1_verdict", "layer2_verdict",
              "layer2_reason", "layer2_at", "layer2_prompt_version",
          }
          missing = required - cols
          if missing:
              logger.error(
                  "rss_articles missing required cols: %s — run migrations 006/007",
                  sorted(missing),
              )
              return False
          return True

    Core (sync entrypoint that wraps a single asyncio.run):
      def _select_candidates(
          conn: sqlite3.Connection, limit: int
      ) -> list[tuple[int, str, str, str]]:
          # 4-col tuple: (id, url, title, body)
          rows = conn.execute(
              """SELECT id, url, COALESCE(title, ''), body
                 FROM rss_articles
                 WHERE layer1_verdict = 'candidate'
                   AND layer2_verdict IS NULL
                   AND body IS NOT NULL
                 ORDER BY id ASC
                 LIMIT ?""",
              (limit,),
          ).fetchall()
          return [(r[0], r[1], r[2], r[3]) for r in rows]

      async def _classify_all(
          conn: sqlite3.Connection,
          rows: list[tuple[int, str, str, str]],
          dry_run: bool,
      ) -> tuple[int, int, int]:
          # returns (ok_count, reject_count, null_count)
          ok = rej = nul = 0
          for chunk_start in range(0, len(rows), LAYER2_BATCH_SIZE):
              chunk = rows[chunk_start:chunk_start + LAYER2_BATCH_SIZE]
              articles = [
                  ArticleWithBody(id=r[0], source="rss", title=r[2], body=r[3] or "")
                  for r in chunk
              ]
              if dry_run:
                  logger.info(
                      "[DRY-RUN] would call layer2 on %d articles: ids=%s",
                      len(articles), [a.id for a in articles],
                  )
                  continue
              try:
                  results = await layer2_full_body_score(articles)
              except Exception as exc:  # noqa: BLE001
                  # Defensive: layer2_full_body_score already returns _all_null
                  # on internal errors, but if a callsite exception leaks out
                  # we treat the chunk as all-null and continue.
                  logger.warning(
                      "[layer2] unexpected exception %s: %s — chunk %d-%d skipped",
                      type(exc).__name__, str(exc)[:200],
                      chunk_start, chunk_start + len(chunk),
                  )
                  nul += len(chunk)
                  continue

              # LF-2.6: if ALL results are NULL, do NOT persist — leave rows
              # untouched so next tick re-evaluates.
              null_in_batch = sum(1 for r in results if r.verdict is None)
              if null_in_batch == len(results):
                  logger.warning(
                      "[layer2] batch all-NULL reason=%s n=%d — rows stay NULL, retry next tick",
                      results[0].reason if results else "empty",
                      len(results),
                  )
                  nul += len(results)
                  continue

              persist_layer2_verdicts(conn, articles, results)

              for (rid, url, _t, _b), result in zip(chunk, results):
                  if result.verdict == "ok":
                      ok += 1
                  elif result.verdict == "reject":
                      rej += 1
                  else:
                      nul += 1
                  logger.info(
                      "  id=%d url=%s verdict=%s reason=%s",
                      rid, url[:80], result.verdict, result.reason,
                  )
          return ok, rej, nul

      def run(db_path: Path, max_articles: int, dry_run: bool) -> int:
          if not db_path.exists():
              logger.error("DB not found: %s", db_path)
              return 1
          conn = sqlite3.connect(str(db_path))
          try:
              if not _check_schema(conn):
                  return 1
              load_env()
              rows = _select_candidates(conn, max_articles)
              if not rows:
                  logger.info("No RSS candidates with NULL layer2_verdict.")
                  return 0
              logger.info(
                  "Loaded %d candidate rows (cap=%d, dry_run=%s).",
                  len(rows), max_articles, dry_run,
              )
              ok, rej, nul = asyncio.run(_classify_all(conn, rows, dry_run))
              logger.info(
                  "processed: %d (ok: %d, reject: %d, null: %d)",
                  len(rows), ok, rej, nul,
              )
              return 0
          finally:
              conn.close()

      def main() -> None:
          parser = argparse.ArgumentParser(
              description="Layer 2 classifier for RSS articles (standalone cron)."
          )
          parser.add_argument(
              "--max-articles",
              type=int,
              default=_resolve_default_max_articles(),
              help="Max rows to classify per run. Default: env "
                   "OMNIGRAPH_RSS_LAYER2_DAILY_CAP or 500.",
          )
          parser.add_argument(
              "--dry-run",
              action="store_true",
              help="Log candidates but skip LLM call + DB UPDATE.",
          )
          parser.add_argument(
              "--db-path",
              type=Path,
              default=DEFAULT_DB_PATH,
              help=f"Path to SQLite DB. Default: {DEFAULT_DB_PATH}",
          )
          args = parser.parse_args()

          logging.basicConfig(
              level=logging.INFO,
              format="%(asctime)s %(levelname)s %(message)s",
              datefmt="%H:%M:%S",
          )
          rc = run(args.db_path, args.max_articles, args.dry_run)
          sys.exit(rc)

      if __name__ == "__main__":
          main()

    Step C — Re-run pytest, confirm GREEN. If a test fails, fix the
    SCRIPT, not the test (the contract is the test).

    PEP 8 / type hints: every function signature must be typed. No
    print() — logger only.
  </action>
  <verify>
    <automated>cd C:/Users/huxxha/Desktop/OmniGraph-Vault &amp;&amp; .venv/Scripts/python.exe -m pytest tests/unit/test_batch_classify_rss_layer2.py -v</automated>
    Also: .venv/Scripts/python.exe batch_classify_rss_layer2.py --help → argparse usage prints, exit 0.
  </verify>
  <done>
    7/7 tests GREEN. `--help` prints usage. Module imports without error.
    No edits to any file outside the two NEW files for this task.
  </done>
</task>

<task id="T2" type="auto">
  <name>T2: Register cron in scripts/register_phase5_cron.sh</name>
  <files>
    scripts/register_phase5_cron.sh
  </files>
  <action>
    Insert ONE new add_job block in scripts/register_phase5_cron.sh
    AFTER the daily-classify-kol block (currently lines 88-90) and
    BEFORE the daily-enrich block (currently lines 95-97). Match the
    reconcile-ingestions wrapper style at lines 117-119 EXACTLY,
    including the escaped `\$(date +%Y%m%d)` and `2>&1 | tee` redirect.

    New block (use these exact strings; the prompt body is load-bearing
    for Hermes skill resolution):

        add_job "daily-classify-rss-layer2" \
          "20 8 * * *" \
          "cd ~/OmniGraph-Vault && source venv/bin/activate && python batch_classify_rss_layer2.py 2>&1 | tee /tmp/rss-layer2-\$(date +%Y%m%d).log"

    Also add a one-line comment ABOVE the block citing this quick:

        # quick-260510-p1s: standalone Layer 2 classifier for RSS — drains
        # rss_articles where layer1_verdict='candidate' AND layer2_verdict IS NULL.

    Do NOT touch any other line in the file. Do NOT change schedules
    on existing jobs. Do NOT reorder anything else.
  </action>
  <verify>
    <automated>cd C:/Users/huxxha/Desktop/OmniGraph-Vault &amp;&amp; bash -n scripts/register_phase5_cron.sh</automated>
    Plus: grep -n 'daily-classify-rss-layer2' scripts/register_phase5_cron.sh prints
      both the comment + the add_job block, and shows ordering: it appears
      AFTER daily-classify-kol and BEFORE daily-enrich.
    The actual `hermes cron list` verification is a Hermes-side operator
    step (CLAUDE.md "Operator follow-up" pattern) — out of scope for
    local verification.
  </verify>
  <done>
    bash -n exits 0. grep confirms the new block is between
    daily-classify-kol and daily-enrich. No other lines modified
    (verify via `git diff scripts/register_phase5_cron.sh` showing
    only an inserted block).
  </done>
</task>

<task id="T3" type="auto">
  <name>T3: Dry-run smoke + closure SUMMARY + STATE.md row</name>
  <files>
    .scratch/rss-layer2-pytest-&lt;ts&gt;.log     # NEW
    .scratch/rss-layer2-dryrun-&lt;ts&gt;.log     # NEW
    .scratch/rss-layer2-cron-syntax-&lt;ts&gt;.txt # NEW (bash -n output)
    .planning/quick/260510-p1s-standalone-batch-classify-rss-layer2-cro/260510-p1s-SUMMARY.md  # NEW
    .planning/STATE.md                       # EDIT (one closure row appended)
  </files>
  <action>
    Step A — Capture pytest evidence.
      ts=$(date +%Y%m%d-%H%M%S)
      .venv/Scripts/python.exe -m pytest tests/unit/test_batch_classify_rss_layer2.py -v 2>&1 \
        | tee .scratch/rss-layer2-pytest-${ts}.log
      Last 50 lines must show "7 passed".

    Step B — Dry-run against .dev-runtime DB.
      .venv/Scripts/python.exe batch_classify_rss_layer2.py \
        --dry-run \
        --db-path .dev-runtime/data/kol_scan.db \
        2>&1 | tee .scratch/rss-layer2-dryrun-${ts}.log
      Expected EITHER:
        - "No RSS candidates with NULL layer2_verdict." → exit 0
          (this is a successful no-op smoke if the local DB has no
           backlog), OR
        - "[DRY-RUN] would call layer2 on N articles: ids=[...]" with
          NO DeepSeek HTTP call (verify: no "DeepSeek" in stderr/stdout
          beyond the loaded prompt-version string), exit 0.
      In either case verify post-run via sqlite3:
        sqlite3 .dev-runtime/data/kol_scan.db \
          "SELECT COUNT(*) FROM rss_articles WHERE layer2_at >= datetime('now','-1 minute');"
      → must return 0 (no UPDATE happened).

    Step C — Cron syntax check.
      bash -n scripts/register_phase5_cron.sh \
        > .scratch/rss-layer2-cron-syntax-${ts}.txt 2>&1
      Plus, append the grep output:
        echo "--- grep ---" >> .scratch/rss-layer2-cron-syntax-${ts}.txt
        grep -n -A1 'daily-classify-rss-layer2' scripts/register_phase5_cron.sh \
          >> .scratch/rss-layer2-cron-syntax-${ts}.txt

    Step D — Write SUMMARY.md at
        .planning/quick/260510-p1s-standalone-batch-classify-rss-layer2-cro/260510-p1s-SUMMARY.md
      Required sections (all claims MUST cite log file + line range —
      NO unverifiable claims, per CLAUDE.md "agent fabrication" lesson 2026-05-08):

        # Quick 260510-p1s — Standalone batch_classify_rss_layer2.py cron

        ## What changed
        - NEW batch_classify_rss_layer2.py (NN lines)
        - NEW tests/unit/test_batch_classify_rss_layer2.py (NN lines, 7 tests)
        - EDIT scripts/register_phase5_cron.sh (one add_job block, +N lines)

        ## Why
        Post-ir-4 (LF-5.2 retired enrichment/rss_classify.py), RSS articles
        had no Layer 2 classifier tick. Their layer2_verdict stayed NULL
        forever, blocking the ingest cron's gate. KOL's analogous tick
        (daily-classify-kol @ 08:15) had no RSS counterpart. This adds it.

        ## Smoke evidence
        - Pytest: 7/7 GREEN — see .scratch/rss-layer2-pytest-${ts}.log L<lo>-L<hi>
        - Dry-run: <ONE OF: "No candidates" OR "[DRY-RUN] would call layer2
          on N articles, no UPDATE applied (post-run COUNT=0)"> — see
          .scratch/rss-layer2-dryrun-${ts}.log L<lo>-L<hi>
        - Cron syntax: bash -n exit 0 + grep confirms ordering
          daily-classify-kol → daily-classify-rss-layer2 → daily-enrich —
          see .scratch/rss-layer2-cron-syntax-${ts}.txt L<lo>-L<hi>

        ## Operator follow-up (Hermes side)
        SSH Hermes, then:
          cd ~/OmniGraph-Vault && git pull --ff-only
          bash scripts/register_phase5_cron.sh
          hermes cron list | grep daily-classify-rss-layer2
        Expected: ADD daily-classify-rss-layer2 @ 20 8 * * * on first run,
        SKIP on subsequent. Tomorrow 08:20 ADT first natural fire. Watch
        /tmp/rss-layer2-YYYYMMDD.log for the per-row classification output.

        ## Files NOT touched (per surgical-changes principle)
        - batch_ingest_from_spider.py
        - batch_classify_kol.py
        - lib/article_filter.py
        - tests/unit/test_ainsert_persistence_contract.py

        ## Risk
        LOW. Net-new code path. Existing crons untouched. Cron syntax
        validated locally. Rollback: revert the 4 changed/new files.

    Step E — Append closure row to .planning/STATE.md.
      Find the most recent quick-closure row table (search for "## Recent
      activity" or similar). Append:

        | 2026-05-10 | quick-260510-p1s | Standalone batch_classify_rss_layer2.py cron | shipped |

      If no such table exists, append a one-line bullet under the most
      recent dated section. Surgical: do NOT rewrite the file structure.

    Step F — Stage + commit ONLY this quick's files (NEVER `git add -A`):
      git add batch_classify_rss_layer2.py
      git add tests/unit/test_batch_classify_rss_layer2.py
      git add scripts/register_phase5_cron.sh
      git add .planning/STATE.md
      git add .planning/quick/260510-p1s-standalone-batch-classify-rss-layer2-cro/

      git status   # verify ONLY these paths are staged

      git commit -m "$(cat <<'EOF'
      feat(quick-260510-p1s): standalone batch_classify_rss_layer2.py cron

      Drains rss_articles where layer1_verdict='candidate' AND layer2_verdict
      IS NULL via lib.article_filter.layer2_full_body_score (chunked at
      LAYER2_BATCH_SIZE=5) + persist_layer2_verdicts. Cron daily-classify-rss-layer2
      @ 20 8 * * * sits between daily-classify-kol (08:15) and daily-enrich (08:30).

      Closes the v3.5 ir-4 RSS gap: post-LF-5.2 retirement of
      enrichment/rss_classify.py, RSS had no Layer 2 classifier tick — rows
      stayed layer2_verdict=NULL and the ingest cron's gate skipped them.

      Files:
      - NEW batch_classify_rss_layer2.py — argparse CLI + asyncio.run drain
      - NEW tests/unit/test_batch_classify_rss_layer2.py — 7 mock-only tests
      - EDIT scripts/register_phase5_cron.sh — one add_job block
      - EDIT .planning/STATE.md — closure row

      Smoke: 7/7 pytest GREEN, dry-run on .dev-runtime DB no-op, bash -n clean.
      EOF
      )"
  </action>
  <verify>
    <automated>cd C:/Users/huxxha/Desktop/OmniGraph-Vault &amp;&amp; ls -la .scratch/rss-layer2-pytest-*.log .scratch/rss-layer2-dryrun-*.log .scratch/rss-layer2-cron-syntax-*.txt &amp;&amp; ls -la .planning/quick/260510-p1s-standalone-batch-classify-rss-layer2-cro/260510-p1s-SUMMARY.md &amp;&amp; git log --oneline -1</automated>
    Plus: `git status` after staging shows ONLY the 4 expected paths
    + the planning quick directory. NO unrelated files in the staging
    area (verify zero leak from parallel quicks per CLAUDE.md hygiene).
  </verify>
  <done>
    All 3 .scratch logs exist, SUMMARY.md exists with cited evidence,
    STATE.md has a closure row, single commit landed locally on main
    (NOT pushed — push is operator-driven). `git diff --name-only HEAD~1 HEAD`
    shows ONLY: batch_classify_rss_layer2.py,
    tests/unit/test_batch_classify_rss_layer2.py,
    scripts/register_phase5_cron.sh, .planning/STATE.md,
    .planning/quick/260510-p1s-…/260510-p1s-{PLAN,SUMMARY}.md.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (after T1+T2+T3):

- 7/7 pytest GREEN with no project-side regressions (run only the new test
  file — do not touch the unrelated `test_ainsert_persistence_contract.py`).
- Dry-run smoke on `.dev-runtime/data/kol_scan.db` exits 0 with NO DeepSeek
  HTTP traffic and NO `layer2_at` UPDATE in the last 60 seconds.
- `bash -n scripts/register_phase5_cron.sh` exits 0.
- `git diff --name-only HEAD~1 HEAD` shows EXACTLY the expected 5-6 paths
  (the 4 production files + 2 planning files) — zero leak.
- No edit to: batch_ingest_from_spider.py, batch_classify_kol.py,
  lib/article_filter.py, tests/unit/test_ainsert_persistence_contract.py.
</verification>

<success_criteria>
- New script `batch_classify_rss_layer2.py` is import-safe and CLI-functional.
- 7 mock-only tests passing — verdict 'ok'/'reject'/None paths all covered.
- Cron block inserted between daily-classify-kol and daily-enrich, idempotent
  via the existing `add_job` grep-check pattern.
- SUMMARY.md cites every smoke claim by log file + line range (no fabrication).
- STATE.md has one closure row appended; no rewrite.
- Single local commit on main, ready for operator-driven push to Hermes.
</success_criteria>

<output>
Files created/modified:
- batch_classify_rss_layer2.py (NEW, project root)
- tests/unit/test_batch_classify_rss_layer2.py (NEW)
- scripts/register_phase5_cron.sh (one add_job block inserted)
- .planning/STATE.md (one closure row)
- .planning/quick/260510-p1s-standalone-batch-classify-rss-layer2-cro/260510-p1s-SUMMARY.md (NEW)
- .scratch/rss-layer2-pytest-<ts>.log (NEW, smoke evidence)
- .scratch/rss-layer2-dryrun-<ts>.log (NEW, smoke evidence)
- .scratch/rss-layer2-cron-syntax-<ts>.txt (NEW, smoke evidence)

Git: one commit landed locally on main, NOT pushed. Operator next steps:
  git push origin main          # local → GitHub
  ssh hermes …                  # then on Hermes:
    cd ~/OmniGraph-Vault && git pull --ff-only
    bash scripts/register_phase5_cron.sh
    hermes cron list | grep daily-classify-rss-layer2
</output>
