# OmniGraph-Vault Issue Tracker

**Status:** Living document — single source of truth for known unfixed issues outside active phases.
**Owner:** Orchestrator (main session) maintains; agents read but do not delete entries.
**Created:** 2026-05-30
**Last updated:** 2026-06-05 evening (T12 partial — qdrant-migration close commit `442ec78` shipped before snapshot.service real-data verify; first manual trigger surfaced **#41 converter OOM on relationships dump** (71773 × 3072d float32 in-memory accumulation killed by oom-kill after ~2h47min wall) + **#42 SLB cross-border throttle** (~50-80min SSH banner timeout, same mechanism as resolved #27 but different trigger). T12 partial verify: vdb_chunks.json 65 MB ✅ + vdb_entities.json 833 MB ✅ written; vdb_relationships.json stays 49-byte placeholder until #41 streaming-write fix lands. Hermes RO until 2026-06-22 unchanged so cross-station read traffic NOT acutely blocked. — qdrant-migration phase CLOSED — #22 #25 #26 #27 → R24-R27; new rows #34 Hermes detour postmortem, #35 4 absent hashes, #36 ingest single-article wall ~48min Vertex 跨境 130s+ physics, #37 21 Qdrant orphan articles from Wave 3 v1/v2/v3 partial-write; new P0 walls promoted from 260605-mz1 walls-verify quick: #38 wrapper-cap CUMULATIVE wall — wrapper 1200s cap fires consistently morning cron, parallelize is real fix NOT raise cap; #39 PROCESSED-gate silent drop — #32 dynamic-budget 150s STILL insufficient on heavy KOL, MERGE candidate w/ #32; #40 serial-processing batch starvation — 1/233 throughput 99.57% starved, bounded asyncio.gather 4-8 concurrent is real fix) + 2026-06-02 (YOLO night shift Task 1+2+3+4 — #7 stale synthesize-audit quick archived → R19; #8 + #9 local untracked junk gc'd 3.5 GB → R20 + R21; #16 P7 /api/search/kg mode-arg Pydantic schema fix shipped via 5d41940 + 5 new tests → R22; #24 markdownlint cleanup MD022/031/032 cleared 4468 violations across 598 .planning/**/*.md files via a59ccb5 → R23, MD060 7759 retained as not auto-fixable) + 2026-06-01 (260601-ipo Aliyun ingest OOM mitigation IN FLIGHT — #4 marked stale + replaced by #25 RAM-mitigation entry; commit 91b33f1 deployed) + perf-fix-B HT-4 halt — reconcile follow-up filed (#26) + #27 kb-api hydrate throttle DoS-by-restart filed post double-deploy + #28 DeepSeek image-emit gap + #29 client-side citation regex sweep server-side carry-over (References fix session 7-commit series) + #28 measurement re-evaluated (intermittent NOT systematic; user counter-example screenshot) + #13 P1 K2 PARKED v1.2 (overtaken by qa.js client-side path) + 2026-06-02 #1 stuck rows 12→1 auto-healed (P0→near-resolved) + #30 translate cron throughput lag filed (84.1% drift) + 2026-06-03 #31 root cause FINAL: ingest_wechat.py:398 LightRAG ctor missing default_embedding_timeout kwarg (kg_synthesize.py side already has it), 2-line fix shipped + #32 PROCESSED-gate 60s tight (image-heavy article async post-ainsert race) + #33 wrapper 1200s cap kills image-heavy KOL (by-design, cache heals next pass)

---

## How to use this file

This is the **issue tracker** for known problems / tech debt / deferred work that is NOT currently in flight. Active phase work lives in `.planning/phases/<milestone>/STATE-*.md`. Active quick work lives in `.planning/quick/<slug>/`. Once a phase or quick closes, issues that surfaced during it but were filed as out-of-scope follow-up land HERE.

**Lifecycle of an entry:**

1. Surfaced (during phase / quick / orchestration) → orchestrator adds row with severity + slug
2. Picked up by a quick / phase → entry annotated `In flight: <quick-slug>` (don't move; let it block in place)
3. Resolved → entry annotated `RESOLVED <date> <commit>` and moved to "Resolved (recent)" section
4. After 30 days in Resolved, moved to `archive/issues-resolved-YYYY-MM.md`

**Severity levels:**

- 🔴 **P0** — Blocks main line / breaks user experience / data correctness. Fix in next available cycle.
- 🟡 **P1** — Important but not blocking. Schedule within ~1 week.
- 🟠 **P2** — Cleanup / tech debt. Schedule when matched cleanup window is open.
- 🟢 **P3** — Future scope. Park; revisit on milestone boundaries.
- 🔵 **Doc** — Configuration / documentation gap. Fix opportunistically.

**Update policy:**

- Orchestrator updates this file when new issues surface or when status changes.
- Agents may **read** to inform decisions but should NOT edit; they file new issues by reporting them in their close-out summary, orchestrator adds the row.
- When closing a quick / phase, orchestrator MUST scan the close-out report for newly surfaced issues and add rows here BEFORE marking quick CLOSED.

---

## Open issues

### 🔴 P0 — Blocking / Data correctness

| # | Issue | Suggested slug | Notes |
|---|---|---|---|
| 1 | id=24 (and 11 other rows) `body=''` permanently untranslatable — translate cron SQL filter `body != ''` skips them. They have `layer2_verdict='ok'` so DATA-07 still surfaces them on KB pages with original-only text. **2026-06-02 update: stuck rows 12 → 1 (auto-healed via background re-scrape between 5/30 and 6/2).** Severity downgraded P0 → P1 → almost-resolved. Sole remaining row should be either re-scraped one more time or flipped verdict to 'reject'. | `260530-id24-body-empty-cleanup` | Originally 12 stuck (~3.9% of 310 layer2='ok'). Now 1 stuck (~0.4% of 276). Filed 2026-05-30 P2-3 prep; auto-self-heal observed 2026-06-02 A.1 health probe. |
| 2 | Vertex 429 RESOURCE_EXHAUSTED on Layer 1 batch 4 burst (30 articles stay NULL classification). | `260530-vertex-burst-isolation` | Daily-ingest auto-retries next cron. Watch for chronic accumulation; if persistent across 3+ days, isolate to its own GCP project. Filed 2026-05-30. |
<!-- Issue #25 resolved 2026-06-05 — see Resolved (recent) row R24 below. -->
| 40 | **Serial-processing batch starvation** — 260605-mz1 walls-verify quick (5/5 days, 100% reproduction) — `batch_ingest_from_spider.py` processes the candidate pool serially under a single shared 8h budget, so any single 1200-2867s article starves the rest. T9 6h11min/252 articles measurement (1/252 done in initial wave 3 fire) attributed to this structural pattern. **Real fix:** bounded `asyncio.gather(..., concurrency=4-8)` over articles within the budget window; viability **UNKNOWN** pending 260605-mz1 probe re-run on non-corp network (probe halted by Cisco Umbrella TLS interception of every LLM endpoint + tiktoken bootstrap). Conditional plan in `.planning/quick/260605-mz1-v1-2-research-walls-verify/RESEARCH.md` Section 3: speedup ≥1.7x → GO `+150-300 LoC`; 1.4-1.7x → RISKY (lock-site spike); <1.4x or corruption → BLOCKED (4 alternatives: subprocess isolation / parallel systemd services / wait LightRAG upstream / raise wrapper cap to 1800s + denser cron cadence). | `v1.2-batch-concurrency` (suggested) | Filed 2026-06-05 from 260605-mz1 walls-verify TASK 4 recommendation as 🔴 P0 (only true throughput blocker among the 3 walls; structural-by-design). Dependency: probe re-run quick must land first to set GO/RISKY/BLOCKED gate. |
| 41 | **`scripts/qdrant_to_nanovdb.py` OOM-killed on relationships dump (71773 points × 3072d float32)** — 2026-06-05 evening orchestrator-triggered snapshot.service after `4d5e6ef`+`56a4112` fixes: chunks dump 65 MB ✅ (10s) + ents 833 MB ✅ (~3-4min) + rels stuck → systemd `oom-kill` after ~2h47min wall, `code=killed signal=KILL`. Aliyun has 14G RAM total, 10G free pre-trigger; converter accumulates **all** scrolled points + base64-encoded float32 matrix in memory before single `json.dump()`, peak RSS estimate ~3-5G for 71k×3072. Combined with kb-api ~2-3G + Qdrant docker ~1G + system overhead = OOM. **Real fix:** convert to **streaming write** — open `vdb_*.json.tmp`, write JSON header `{"embedding_dim":3072,"data":[`, scroll-and-flush per N points (batch flush data[] entries + matrix base64 chunks), close with `]}` then atomic rename. Keeps peak RSS bounded (~50-100 MB regardless of point count). ~50-100 LoC fix in `scripts/qdrant_to_nanovdb.py:export_collection_to_nanovdb`. **Cross-references #25 root pattern** — full-load OOM was the structural reason for qdrant-migration; converter shipped as the bridge from Qdrant → NanoVectorDB JSON, but the converter itself fell into the same full-load trap. **Mitigation interim** (operator-tier, no code): apply systemd `MemoryMax=2G` to qdrant-snapshot.service to surface OOM earlier as service-level failure (vs system-level which can punish other services); accept relationships dump WONT happen until streaming-write lands. | `v1.2-qdrant-converter-streaming` (suggested) | Filed 2026-06-05 evening from snapshot.service first real-data fire. P0 because Databricks deploy gate (T12) cannot pass without working converter; until streaming-write lands, vdb_relationships.json stays 49-byte placeholder and Databricks rerank gets thin context for relationship-heavy queries. Hermes RO until 2026-06-22 unchanged — Hermes still uses pre-cutover archive in that window so cross-station read traffic NOT blocked acutely. |

<!-- Issue #21 resolved 2026-05-31 — see Resolved (recent) row R18 below. -->
<!-- Issues #25 #26 #27 #22 resolved 2026-06-05 (qdrant-migration phase CLOSED) — see Resolved (recent) rows R24-R27 below. -->

### 🟡 P1 — Important but not blocking

| # | Issue | Suggested slug | Notes |
|---|---|---|---|
| 3 | **Wiki body single-language** — only outer chrome (breadcrumb / sources label / pill) is bilingual; wiki body markdown is whatever the .md author wrote (currently English for Copilot Studio entities). EN/中 toggle leaves body unchanged. | `260530-wiki-bilingual-decide` | Three paths: A manual dual-section per .md, B SSG-bake auto-translate (mirrors articles pipeline), C accept current. Decision delegated to user. Filed 2026-05-30. |
| 4 | ~~`daily-ingest.service` single-article LightRAG ainsert can run 8h+~~ | ~~`260530-ainsert-budget-timeout`~~ | **STALE — ALREADY SHIPPED** Phase 17 BTIMEOUT (2026-05-17). `_SINGLE_CHUNK_FLOOR_S=1200s` floor + chunk + image budget via `asyncio.wait_for` (`batch_ingest_from_spider.py:395`); env override `OMNIGRAPH_BATCH_TIMEOUT_SEC` (`batch_ingest_from_spider.py:298`). Verified during 260601-ipo Phase 0. Move to Resolved on next ISSUES update. |
<!-- Issue #25 promoted to P0 section above on 2026-06-01 — see #25 row under "🔴 P0 — Blocking / Data correctness". -->
| 5 | `kb/wiki/_suggestions/` (W1 hook fire-and-forget output) has no review queue; suggestions accumulate without human gate. | TBD | Post-Wave-3 wiki workflow audit. Decision needed: keep W1 gate, retire it, or build review UI. |
| 6 | `test_css_budget_within_2100` pre-existing failure — `kb/static/style.css` 2172 lines vs budget 2150. Last css change e05d597 (kb-3-qa F1+F2). | `260530-css-budget-overrun` | Two paths: trim style.css to ≤2150 (audit + remove unused tokens) or raise budget to 2200 with diff justification. NOT in P2-3 scope (SC#5 / HT-5). Filed 2026-05-30 P2-3 T4. |
<!-- Issue #22 resolved 2026-06-05 (qdrant-migration Wave 4 T10 ran HT-6 N=4 against Aliyun) — see Resolved (recent) row R26 below. -->
<!-- Issue #27 resolved 2026-06-05 (qdrant-migration T11 hydrate ×3 mean 13.1s) — see Resolved (recent) row R27 below. -->
| 36 | **Single-article ingest wall ~48min on Aliyun** — late-Wave-3 / 06-05 measurement: avg wall ~2867s (~48 min) per article through DeepSeek + Vertex Gemini cross-border (Aliyun → 海外 endpoint) with Cisco-Umbrella-class latency on the wire (~130s+ per Vertex call). LightRAG entity-extract + relation-build + embedding workers all serialize behind the network round-trip floor. Wave 3 root cause was wrapper 8h budget × serial × 48min/article = 1/8h done in worst case (today's 1/233 sample). **Real fix candidates:** (a) Hermes-fresh-ingest path — Hermes physically closer to海外 endpoints (~30s Vertex), 4-6x speedup, but needs ingest-write back to Aliyun Qdrant via batch sync; (b) regional Vertex endpoint with Aliyun-side proxy; (c) shrink ingest LLM scope (skip vision-cascade chain for low-image-count articles). | `v1.2-hermes-fresh-ingest` (suggested) | Filed 2026-06-05 from qdrant-migration Wave 3 post-mortem. P1 because it bottle-necks every nightly batch; complementary to #40 (concurrency) but addresses the per-article floor that concurrency cannot lower. v1.2 candidate. |
| 38 | **Wrapper-cap CUMULATIVE wall — wrapper 1200s cap fires 33 events in 5-day window** — 260605-mz1 walls-verify quick (4/5 days fire, 33 events in `timeout_histogram.900s+` bucket; morning 08:00-CST cron consistently hits 1200s wrapper cap). NOT same as #33 single-article cap (which is 20min per-article hard ceiling); this is the **cumulative** budget pattern: wrapper sums elapsed across all articles and self-exits at 1200s (1800s after `f7674b3` raise). Avg article 2867s, so 1 slow article + budget already spent = remaining articles get 0 wall. **Real fix:** parallelize via #40 (bounded asyncio.gather), NOT raise the cap further. Raising cap alone burns more wall on slow articles without unblocking siblings. **Caveat — MERGE candidate w/ #33** if the 1200s vs 1800s distinction collapses post-#40. | `v1.2-batch-concurrency` (folded into #40) | Filed 2026-06-05 from 260605-mz1 walls-verify TASK 4 recommendation as 🟡 P1 (self-heals on cron retry, but accumulating tech debt — every day +N articles ingested means coverage drops further if budget exhausts). Folded into #40 for fix path; standalone tracking row to surface count + cadence in trend audits. |
| 39 | **PROCESSED-gate silent drop — even 150s dynamic budget insufficient on heavy KOL** — 260605-mz1 walls-verify quick (3/5 days fire, 17 occurrences). #32 fix `f7674b3` raised dynamic budget to 150s, evidence cited 2026-06-05 `30 retries, backoff 5.0s, dynamic budget 150s` showing 150s STILL insufficient on heavy KOL articles. Status=PENDING last_exc=None pattern suggests LightRAG worker queue starvation OR Qdrant write latency (post-cutover) — NOT just async-finalization race that #32 originally targeted. **Mitigation paths:** (a) further bump retry budget (cost: +N seconds wall on slow articles), (b) instrument to find true bottleneck (worker queue backlog vs Qdrant rps cap), (c) wait for #40 concurrency landing — if workers drop from saturated to free, gate may pass naturally. | `260605-processed-gate-150s-still-tight` (suggested) | Filed 2026-06-05 from 260605-mz1 walls-verify TASK 4. **MERGE candidate with #32** — same gate, same budget knob, different days; differs only in budget value (60s → 150s) and presumed root (async race → worker starvation). Treat as continuation of #32 unless instrumentation shows distinct root. P1 because it correlates with the Wave 3 v3 PENDING-status batch tail. |
| 42 | **`qdrant-snapshot.service` triggers ~50min Aliyun cross-border SSH throttle** — 2026-06-05 18:33 ADT orchestrator-triggered snapshot.service caused Aliyun ECS public-network throttle: SSH banner exchange timed out for ~50 min (18:33 ADT → 19:25 ADT minimum, possibly ~80 min by direct probe at 19:52 ADT). Vitaclaw cohabit on same ECS unaffected (Caddy + kb-api still served via proxy). Converter `qdrant_to_nanovdb.py` accumulating in-memory points (see #41 OOM) likely caused massive RAM+swap pressure that triggered SLB cross-border throttling — same root mechanism as **#27** post-restart hydrate throttle (filed 2026-06-01, RESOLVED 2026-06-05 R27 because Qdrant mmap eliminated the hydrate-side trigger), but **different trigger** (snapshot scroll vs kb-api hydrate). #27 was 8 min historical; this incident 50-80 min and ended in OOM (#41). **Mitigation:** fix #41 streaming-write — once converter peak RSS is bounded, throttle won't fire. Until then: (a) avoid manual snapshot.service trigger during business hours, (b) consider adding systemd `MemoryMax=2G` to surface OOM early before throttle reaches network-stack saturation, (c) accept the timer's 6h cron schedule fires same OOM each cycle until #41 lands (impact: vdb_relationships.json stays placeholder, no kb-api impact, no user impact during Hermes RO window). | (folded into #41) | Filed 2026-06-05 evening. P1 because it duplicates #27 mechanism on a different trigger pathway and demonstrates that **any** large-RAM Aliyun process can DoS the SLB; closing #41 likely closes #42 incidentally (no peak-RSS spike → no SLB pressure). Tracking row to keep the throttle pattern visible across triggers. |
| 28 | **Aliyun (DeepSeek) long_form image emission is intermittent — frequency lower than Databricks (Claude) but NOT zero.** **2026-06-01 update — original "systematically does NOT" measurement was too strong.** User-side counter-example screenshot (long_form answer 含 LLM Claude 架构图, body 图正常嵌入, src `/static/img/<hash>/<n>.<ext>` 正确解析) 证明 DeepSeek **能** emit image markdown when KG context provides image URLs. Deploy session's "multiple long_form returned zero img" finding stands as one observation but does NOT generalize to "systematic capability gap". Real现象: DeepSeek emit rate < Claude emit rate, **不是** 0. Likely causes — (1) sample bias in deploy session test queries (KG retrieval results varied in image URL density), (2) DeepSeek prompt-following weaker than Claude on `IMAGE_URL_DIRECTIVE`, (3) prompt template image-emit instruction is mid-prompt, may benefit from front-loading. **Mitigation paths (downgraded P1→P2):** (a) prompt template ZH/EN front-load "MUST include 1-3 images per section" earlier than current position, (b) collect N≥10 long_form sample data measuring image-emit rate before deciding next action, (c) accept current behavior (Aliyun read-side primarily zh-CN audience, image presence is nice-to-have not user-blocking). | `260601-deepseek-image-emit` (suggested) | Filed 2026-06-01 from References + image fix session. **Re-measurement needed** before any prompt-template changes — current measurement N=? unclear. Provider behavior gap exists but degree unproven. |
| 29 | **LLM citation format is unreliable — 6+ orphan formats observed in prod.** 2026-06-01 References fix session shipped a client-side qa.js sanitizer (`a71741f`..`6af0735`, 5 commits) handling: `[/article/<hash>]` `[/article:<hash>]` `[article/<hash>]` `[article:<hash>]` `[article-<hash>]` `[article <hash>]` `[<hash>]`. Both DeepSeek + Claude emit some/all of these despite the prompt template specifying `[label](articles/<hash>.html)`. The client-side regex sweep is a stop-gap; structural fix needs server-side post-process so non-browser consumers (curl, /api/synthesize/<id> JSON consumers, Hermes skill, CLI scripts) also see clean markdown. **Mitigation paths:** (a) move regex sweep into `kb/services/synthesize.py:_rewrite_image_urls`-adjacent server function, (b) tighten prompt template wording (already at 6 instructions, may saturate), (c) keep client-side as-is and document `<a>` polyfill expectation for non-browser consumers. **Also:** LLM emits **two References sections** in ~30% of long_form answers (one disclaimer + one real link list, OR two duplicate link lists). qa.js dedupe (`146b16e`) keeps highest-`<a>`-count section client-side; same server-side gap as citation format. | `260601-citation-server-rewrite` (suggested) | Filed 2026-06-01 close-out of References fix session. Series shipped: `a71741f` (path) → `df0c87d` (orphan + img) → `0eb3603` (title labels) → `c71e66b` (dead-link sanitize) → `6d692c2` (bold-heading + img remove) → `146b16e` (score-based dedupe) → `6af0735` (widen orphan regex). All client-side qa.js. Server-side carry-over. |
| 30 | **Aliyun translate cron throughput lag** — `omnigraph-translate.service` runs daily with `--limit 20` but ingest速率超过 20/day. Coverage drift 96.4% (5/31, 215/223) → 84.1% (6/2, 232/276). Service healthy (last run 2026-06-02 22:49:55 CST exit=0/SUCCESS, --limit 20 完成翻 id=1431); just under-throughput. **Mitigation paths:** (a) raise `--limit 20` → `--limit 50` or `--limit 100` (single sed in service ExecStart or env), (b) timer cadence daily → 8h-once (3× throughput), (c) hybrid (a)+(b). Cost band: each translate is ~1500-2000 tokens DeepSeek (~$0.0001/article); 100 articles/day = $0.01/day, negligible. **Side surface:** ISSUES #1 stuck rows 12 → 1 (auto-healed via background re-scrape between 5/30-6/2), so #1 is now near-resolved on its own. | `260602-translate-throughput` (suggested) | Filed 2026-06-02 from A.1 health probe. P1 not blocking (service works, drift slow), but accumulating tech debt — every day +N articles ingested means coverage drops further. Quick-tier (≤10 LoC sed + systemctl daemon-reload). |
| 32 | **PROCESSED-gate verification 60s budget too tight under image-heavy article load** — Wave 3 v3 (2026-06-03) probe surfaced 1 article (id=1771, sqlite ingestions row 7999) failing PROCESSED-gate verification despite LightRAG `aget_docs_by_ids` eventually marking it processed. Wrapper `OMNIGRAPH_PROCESSED_RETRY=30 × OMNIGRAPH_PROCESSED_BACKOFF=2.0s = 60s` budget exhausted, doc_status still PROCESSING. Different timer than #31 (which fixed `default_embedding_timeout` ctor kwarg) — this is `_verify_doc_processed_or_raise()` post-ainsert poll loop in ingest_wechat.py:1559. Race: LightRAG entity-merge + relation-build + final flush is async; for image-heavy articles (20+ imgs) post-ainsert finalization can take >60s before doc_status flip. Article 1771 retried by next cron and likely succeeds (LightRAG doc itself was 'processed' eventually). NOT data loss — just sqlite ingestions row marked failed → mig 009 retry pool re-queues. **Mitigation:** `.env` 调高 OMNIGRAPH_PROCESSED_BACKOFF=5.0 (30×5s=150s) OR OMNIGRAPH_PROCESSED_RETRY=60 (60×2s=120s). Cost: +60-90s wall on slow articles, no token cost. | `260603-processed-gate-60s-tight` (suggested) | Filed 2026-06-03 from Wave 3 v3 monitor probe (~07:11 CST window). P2 — not blocking, not data loss, retry self-heals. Quick-tier 1-line .env edit on Aliyun + propagate to repo `kb/deploy/*.example`. |
| 33 | **Wrapper 1200s per-article hard cap kills image-heavy KOL articles** — Wave 3 v3 (2026-06-03) probe identified 7 articles failing TIMEOUT(1200s) — all from the same 2 KOLs (MzkxMTczNzkyMA== and MzkwMzE4NjU5NA==). Article ids 1718/1727/1730/1732/1764/1766/1769. Single article wall = vision cascade (~10min for 20 imgs × 30s SiliconFlow heavy) + LightRAG ainsert (~5-10min chunk + entity + relation + embedding) ~15-20min total, hits 1200s (20min) wrapper-imposed hard cap. NOT a bug — wrapper design. **Cache effect:** Next cron's retry of these articles will be much faster because LightRAG `__llm_cache__` retains entity-extraction LLM calls; second pass typically completes in 600-900s. **Decision deferred:** (a) raise wrapper cap to 1800s (30min) for first-pass, (b) accept current behavior (next cron heals via cache), (c) pre-compute image count and dynamically adjust cap (already partially done — wrapper bumped to 1410s on one article). Cost band: option (a) burns 30min/article in worst case, but recovers articles current cap loses; option (b) zero cost but 7-article backlog accumulates if KOL keeps publishing image-heavy. | `260603-wrapper-cap-image-heavy` (suggested) | Filed 2026-06-03 from Wave 3 v3 monitor probe. P3 — by-design behavior with self-healing retry path. Only escalate if same-KOL articles fail 3+ cron passes consecutively. |
| 31 | ~~**LightRAG embedding worker timeout three-layer mismatch — hardcoded 60s/75s NOT env-overridable**~~ ~~**env name typo `LIGHTRAG_*` vs `EMBEDDING_TIMEOUT`**~~ **2026-06-03 ROOT CAUSE FINAL — `ingest_wechat.py:398` LightRAG ctor missing `default_embedding_timeout=` kwarg.** Investigation chain: (1) lightrag/utils.py:781 `Worker timeout ... after 60s` 是 `max_execution_timeout` 变量, NOT hardcoded; (2) `max_execution_timeout = llm_timeout × 2`; (3) decorator `llm_timeout = self.default_embedding_timeout`; (4) `default_embedding_timeout` field default = `int(os.getenv("EMBEDDING_TIMEOUT", 30))`. **kg_synthesize.py:165 (kb-api 路径) 显式传 `default_embedding_timeout=_embedding_timeout_default()` (读 LIGHTRAG_EMBEDDING_TIMEOUT env, 默认 90s) → max_exec=180s ✓**. **ingest_wechat.py:398 (batch_ingest 路径) 没传这 kwarg → fall through `EMBEDDING_TIMEOUT` env (we don't set) → default 30 → max_exec=60s ✗**. 项目自定义 `LIGHTRAG_EMBEDDING_TIMEOUT` env 名是对的 (kg_synthesize side use it正确), 错在**ingest_wechat.py LightRAG ctor 漏传该 kwarg**, 跳过 wrapper, 直接撞 LightRAG `EMBEDDING_TIMEOUT` env (空) → 30s 默认. **Fix is 2 lines added to ingest_wechat.py:417 — `default_embedding_timeout=int(os.environ.get("LIGHTRAG_EMBEDDING_TIMEOUT", "180"))` + `default_llm_timeout=int(os.environ.get("LIGHTRAG_LLM_TIMEOUT", "300"))`**. NOT env rename, NOT monkey-patch. Cross-ref: GitHub Issue #2415 LightRAG release v1.4.7 changelog. | `260603-ingest-wechat-timeout-kwargs` (suggested) | **3-layer surface evolution 2026-06-03**: (1) initial "LightRAG hardcoded 60s/75s" → wrong; (2) "env name typo LIGHTRAG_*  vs EMBEDDING_TIMEOUT" → wrong (project `LIGHTRAG_*` IS correct, kg_synthesize.py uses it正确); (3) actual root = ingest_wechat.py LightRAG ctor 缺 kwarg, kb-api side has it via kg_synthesize.py:165. Fix shipped 2026-06-03 in main session. Wave 3 v3 needs Aliyun git pull to pickup before fire. |
| 26 | **v1.1.P2-3-perf-fix-B reconcile follow-up — Aliyun systemd drift + apply rerank env + HT-6 N=4 lock-break verification.** B execute-phase 2026-06-01 halted at T6 HT-4 because live `/etc/systemd/system/kb-api.service` on Aliyun has substantive drift vs repo template `kb/deploy/kb-api.service` (User=root vs kb, /root vs /home/kb paths, `EnvironmentFile=/root/.hermes/.env` unique to live, no sandboxing block in live, Aliyun-only `OMNIGRAPH_LLM_PROVIDER=deepseek` + `OMNIGRAPH_BASE_DIR` Environment= lines). Wholesale `cp` would clobber live customizations. **Recommended path:** systemd drop-in `/etc/systemd/system/kb-api.service.d/override.conf` is already used for Aliyun-specific overrides (MemoryMax=12G, KB_DEFAULT_LANG=zh-CN, KB_SYNTHESIZE_TIMEOUT=240, KB_LIGHTRAG_INNER_TIMEOUT=150, LIGHTRAG_EMBEDDING_TIMEOUT=90) — append the 4 rerank `Environment=` lines there; zero touch to base unit; trivial rollback. **MUST cover HT-6 N=4 concurrent /api/synthesize lock-break test (P5 contract verification on Aliyun)** — transferred from #22 because B's T6 was the original carrier and is now deferred. **Pre-flight already verified 2026-06-01 in B's T6:** SA JSON present at `/root/.hermes/gcp-paid-sa.json`, `GOOGLE_CLOUD_PROJECT/LOCATION/CREDENTIALS` set in `/root/.hermes/.env`, `/etc/hosts` Vertex pin in place. Backup `/etc/systemd/system/kb-api.service.bak-pre-perf-fix-B` taken pre-halt. **Acceptance:** SC#1-Aliyun (cold-start ≤60s after `systemctl restart`), SC#2-Aliyun (3 zh-CN smoke wall_s ≤65s mode='mix'), SC#6 (UNSET-env baseline still works), HT-6 (N=4 lock-break passes), evidence captured to `.planning/phases/v1.1-roadmap/<reconcile-slug>/aliyun-evidence/`. Closes HC-6 Aliyun parity gate. **2026-06-01 update — FOLDED INTO #25 per orchestrator decision** (orchestrator option (c)): rerank env block + HT-6 N=4 verification will be applied as part of the #25 Qdrant migration's Aliyun kb-api restart, minimizing #27 hydrate-throttle blast radius (single restart ships Qdrant cutover + rerank env + structural hydrate fix together). #26 not closed-as-WONTFIX — work still happens, just inside #25's restart window. Acceptance criteria + evidence path moved to #25. | `v1.1-qdrant-migration` (folded into #25) | Filed 2026-06-01 from B execute-phase HT-4 halt. Plan-phase tier (multi-step ops). Drift diff captured in `.planning/phases/v1.1-roadmap/P2-3-perf-fix-B/aliyun-evidence/systemd-drift-diff.txt`. **No standalone reconcile phase will run** — all reconcile work happens inside #25 plan-phase. |

### 🟠 P2 — Cleanup / Tech debt

| # | Issue | Suggested slug | Notes |
|---|---|---|---|
| 11 | `.planning/STATE.md` "Quick Tasks Completed" table style inconsistent — early entries are 1000+ char single-line dumps; recent (260530-d8j, 260530-gf1) entries use concise multi-line summary. | TBD | Decide canonical format and either reformat history or leave drift annotation. |
| 23 | **Trim `sentence-transformers` + `torch` from requirements files** after `v1.1.P2-3-perf-fix-B` closes. Saves ~1.2 GB deploy + faster pip install. Currently retained in `requirements.txt` + `databricks-deploy/requirements.txt` for Rollback Plan #4 (partial revert path keeps BGE wrapper restorable). Cleanup, not blocking. | TBD (post-B) | Filed 2026-05-31 P2-3-perf-fix-A RESEARCH §8. Surgical Changes principle delays this trim until rollback option no longer needed. |
| 35 | **4 absent article hashes — `3591562349`, `373a7538d2`, `bafe3773c7`, `e06dedf9bc`** — 06-05 sqlite ingestions audit: these 4 hashes appear in expected pool but no row exists. Likely scrape failures (WeChat 200003 ret-code window from 5/30+) or layer1 reject not yet flipped. Daily-ingest cron will naturally re-attempt them on next pass; no operator action needed unless they persist for 7+ days. | TBD | Filed 2026-06-05 from qdrant-migration Wave 3 close audit. Cleanup-tier. Cron self-heal expected. |
| 37 | **21 Qdrant orphan articles from Wave 3 v1/v2/v3 partial-write window** — Wave 3 had 3 ingest restart attempts (v1 ijson stuck, v2 retry, v3 orjson final). v1/v2 partial writes left 21 articles with chunks/entities in Qdrant but no relationships row, OR no doc_status='processed' marker in sqlite ingestions. Functional impact: zero (kb-api hybrid mode reads what's there; missing relationships just thin the rerank context for those articles). Tracking only. **Cleanup path:** sqlite query to identify the 21 hashes → re-trigger via batch_ingest_from_spider with explicit `--articles=<csv>` once #40 concurrency lands; OR write a one-shot Qdrant-side cleanup script that delete-by-payload-id for orphaned-by-no-rels chunks. | TBD | Filed 2026-06-05 from qdrant-migration close audit. P2 because no user-visible regression; queue when #40 lands or when convenient. |

### 🟢 P3 — Future scope (parked)

| # | Issue | Suggested slug | Notes |
|---|---|---|---|
| 13 | **P1 K2 citation revisit — PARKED v1.2** (α path ~+14 LoC) — extract `full_doc_id` from LightRAG chunk metadata to replace LLM-output regex citation. Deferred 2026-05-28 (γ choice) until P5 ships; P5 shipped 2026-05-29; **2026-06-01 update: 6 days wall clock since γ defer, in that window 7-commit qa.js client-side regex sweep (`a71741f`..`6af0735`) + ecd2076 prompt 加严 already covered ~80% of P1 K2's promised user-visible value (clickable References, dedupe, 6 orphan formats catch, dead-link demote). User has NOT raised citation accuracy as a complaint. P1 K2 was overtaken by client-side hotfix path, not deferred — orchestrator parks to v1.2 milestone.** Re-evaluate trigger: (a) qa.js 7-commit complexity becomes maintenance burden + breaks; (b) non-browser consumer (Hermes skill, /api/synthesize JSON consumer, CLI script) reports unrendered citation; (c) v1.1 milestone close — review qa.js debt + decide P1 K2 unfreeze. | `v1.2.P1-K2-revisit` (parked) | **PARKED v1.2 2026-06-01** per orchestrator decision. Not abandoned — preserved as v1.2 candidate. ROADMAP §10 Q1 lock still holds (`aquery_llm + rag.text_chunks.get_by_ids` two-step). #29 server-side citation rewrite is the other path that could partially or fully obviate this row when picked up. |
| 14 | **P4.0 / P4.1 ARAG audit + salvage** — read-only audit `lib/research/*` (P4.0), then mutating salvage + frontend "Deep Research" tab (P4.1). | `v1.1.P4-0` / `v1.1.P4-1` | Wave 3, blocked on Wave 1 + Wave 2. Path locked C (self-build) 2026-05-29 after MS ARAG evaluation. |
| 15 | **P6.1 fixture drift audit** — full pass over `tests/unit/_ingest_fixtures.py` schema vs production migrations + `test_search_kg_job_completes` mock rewire. | `v1.1.P6-1` | Wave 4 housekeeping. Schedule after Wave 2 stabilizes. |
| 17 | **wiki-bilingual-ssg-bake** — long-term: SSG bake auto-translates wiki body via DeepSeek/Vertex pipeline (mirrors `articles` `body_translated` pattern). Replaces Issue #3 path B. | `260530-wiki-bilingual-ssg-bake` | 1-2 day phase-tier. Triggered when Issue #3 decision = path B. |
| 18 | **Databricks PAT rotation** — token leaked once in earlier `env \| grep ANTHROPIC` Bash output. | TBD | Rotate at user's convenience. Not blocking; new PAT must update `~/.databrickscfg` `[dev]` profile. |
| 34 | **Hermes detour postmortem — qdrant-migration Wave 3 fresh-ingest path burned 4h scp** — Initial Wave 3 strategy was fresh-ingest 252 articles on Aliyun directly; T9 fired 06-02 02:12 ADT, wrapper self-exited at 6h11min, 1/252 done, blocked by Vertex 429 + Gemini RPD. After several retry-with-mitigation rounds across 06-02..06-04, orchestrator-delegate pivot 06-05 morning ADT to detour: scp Aliyun lightrag_storage tarball → Hermes → re-tarball → scp back to Aliyun. **4h wall on round-trip scp + repacking** ate the 06-05 morning window; the **right path** was always Aliyun in-place 5/30 archive transplant (which then fired in ~5min for chunks/ents + ~287s for rels via v3 orjson). **Lesson:** When a milestone has both "transplant existing data" and "re-ingest from scratch" as options for restoring KG state, distinguish them BEFORE committing to ingest path; transplant is bytes-on-disk, ingest is LLM RPD-bounded weeks of cron. **Cross-ref:** lessons doc + memory file pending — capture the Aliyun-in-place pattern as the default for any future Qdrant-restore op. | (postmortem only — no follow-up fix) | Filed 2026-06-05 by orchestrator-delegate post Wave 3 real-close. P3 because postmortem is documentation only (the wasted wall is sunk cost; the real close shipped). Memory file `feedback_transplant_vs_fresh_ingest_default.md` candidate. |

### 🔵 Doc / Config gaps

(currently empty — #19 and #20 resolved 2026-05-30 via `260530-gf1`)

---

## Resolved (recent — last 30 days)

| # | Issue | Resolved | Commit(s) | Quick / Phase |
|---|---|---|---|---|
| R1 | `daily_rebuild.sh` Phase 1 silent-fail since 2026-05-20 (KB_DB_PATH not exported, subprocess fell to nonexistent path). | 2026-05-30 | `f56a4a6` | `260530-d8j` |
| R2 | `/var/www/kb/` 9 days stale, no auto-sync from `kb/output/` after SSG bake. | 2026-05-30 | `f56a4a6` (Phase 5 rsync) | `260530-d8j` |
| R3 | `KB_BASE_PATH` empty default broke Aliyun KB site after `260530-d8j` shipped — HTML emitted bare `/static/*`, Caddy `/kb/*` handle never matched, all CSS/img 404. | 2026-05-30 | `d7b3749` | hot-fix follow-up to `260530-d8j` |
| R4 | Wiki SSG bake (`kb/export_knowledge_base.py:_convert_wiki_citations`) only handled legacy `^[article:hash]` tokens; SCHEMA-2026-05-20 introduced GFM `[^N]` + multi-type sources but bake path was not updated. | 2026-05-29 | `b4a87ce` | `260529-hlu` |
| R5 | Aliyun translate cron never registered as systemd timer; body translation backlog grew indefinitely without auto-trigger. | 2026-05-29 | `241d7dd` | `260529-arm-translate-cron` |
| R6 | `.scratch/deploy_inline_*.sh` recurring drift (2026-05-25 inline missed 3 critical `--include` flags → arx-3 singleton REGRESSION). | 2026-05-29 | `c2cfe0c` | `260529-d3p` |
| R7 | No script for Aliyun → Databricks data sync; the 22-step manual procedure from `260528-f1s` was not reusable. | 2026-05-29 | `7544234` | `260529-arx` |
| R8 | LightRAG version pin drift — root `requirements.txt` 1.4.16 ≠ `databricks-deploy/requirements.txt` 1.4.15 ≠ venv 1.4.15. P2-3 plan-phase Phase 0 surfaced. | 2026-05-30 | `2b922d0` | P2-3 Phase 0 cleanup |
| R9 | `databricks-deploy/requirements.txt` `python-frontmatter` line dirty for 5 days (2026-05-25 hot-fix slipped commit). | 2026-05-30 | `20a4094` | P2-3 Phase 0 cleanup |
| R10 | `sync_to_databricks.sh` `read -p` lost stdin when run as background task → silent abort exit 0 in 30s instead of 70-min full sync. | 2026-05-30 | `149130a` (`--yes` flag) | 4-cleanup batch |
| R11 | `sync_to_databricks.sh` Step 9c race: `apps start` auto-creates pending SNAPSHOT deployment that locks Step 9c `apps deploy` for 20+ min. | 2026-05-30 | `149130a` (poll-pending fix) | 4-cleanup batch |
| R12 | Aliyun SSH manual trigger silently 401s (`DEEPSEEK_API_KEY=dummy` fallback) because `EnvironmentFile=/root/.hermes/.env` is systemd-only, plain SSH shell doesn't source. | 2026-05-30 | `f6b3b97` (`run_with_env.sh`) | 4-cleanup batch |
| R13 | Tavily Python module not installed in Aliyun `venv-aim1` → translate cron WARNINGs every run (non-fatal but degrades quality). | 2026-05-30 | (Aliyun pip install only, no commit needed) | 4-cleanup Step 1 |
| R14 | CLAUDE.md PRINCIPLE #3 (Surgical Changes) didn't document "wiki cross-link reverse edits to other people's wiki pages = boundary breach". Captured 2026-05-29 commit f5da904 lesson but not propagated to CLAUDE.md until now. | 2026-05-30 | `ce59612` | `260530-gf1` |
| R15 | `databricks-deploy/deploy.sh` (Databricks deploy) and `kb/scripts/daily_rebuild.sh` (Aliyun) had asymmetric `KB_BASE_PATH` requirement (Databricks empty, Aliyun `/kb`) undocumented; future readers would copy one into the other. | 2026-05-30 | `1a1e31d` | `260530-gf1` |
| R16 | `databricks-deploy/_ssg/` build artifact path confusing (suggests committed source, actually gitignored). | 2026-05-30 | `758e21b` (`databricks-deploy/_ssg/.README.md` written) | `260530-gf1` |
| R17 | Memory `aliyun_drift_recovery_260528_lessons.md` Lesson 1 v3 + v4 wording overlap (same root mechanism: systemd `Requires=` cascade described twice). | 2026-05-30 | (out-of-tree memory file edit, no repo commit) | `260530-gf1` |
| R18 | **P2-3 reranker DEPLOYED-DISABLED** (BGE_FORCE_LOAD_FAIL=1 escape) — replaced with LLM-as-reranker (Databricks Haiku batch JSON). All 6 SC PASS on `01f15d1bcce2189db0557d701a97bf9f`: cold-start 28.15s, qa_seed mean 59.43s, prod-batch 21.07s, token-overlap 1.00 perfect, graceful-degrade verified, kb/static+templates 0 touches, legacy BGE env retained for rollback compat. | 2026-05-31 | `6feb210` `c257c64` `a26ea01` `664c14c` `b8f3baf` + T6 | `v1.1.P2-3-perf-fix-A` |
| R19 | **Stale synthesize-audit quick** — `.planning/quick/20260525-200047-synthesize-audit/` untracked since 5/25 (real audit doc + 4 logs from synthesize FTS5 syntax bug deep-audit, resolved later via e05d597). Archived to `.planning/quick/archive/` to declutter active quick listing. Logs (*.log) gitignored — only AUDIT.md + probe.py tracked under archive path. | 2026-06-02 | `9728726` | YOLO night shift Task 1 |
| R20 | **`databricks-deploy/_aliyun_pull/` 3.5 GB local sync residue** — from manual `databricks sync --full` pulls, .gitignored at line 98. Removed locally; can be re-pulled on demand. databricks-deploy/ size dropped 3.5G → 146M. | 2026-06-02 | (no commit; gitignored local-only delete) | YOLO night shift Task 2 |
| R21 | **`.scratch/sync-to-databricks-*.log` + `.scratch/databricks-deploy-*.log` accumulating** — 4 stale log files (2× sync + 2× deploy, ~672 KB total) from 5/29-5/31 deploy runs. All `*.log` gitignored at line 45. Removed locally. | 2026-06-02 | (no commit; gitignored local-only delete) | YOLO night shift Task 2 |
| R22 | **P7 Pydantic mode-arg silent ignore on `/api/search/kg`** — `_KgSearchRequest` body schema only had `query`; `mode` was silently dropped if a caller passed it (Pydantic default extra=ignore). `_kg_local_worker` hard-coded `mode='local'`. Fixed by adding `mode: Literal["local","global","hybrid","naive","mix"] = "local"` field + propagating through worker to `synthesize_response`. Allowed values mirror LightRAG `QueryParam.mode` minus `bypass`. 5 new tests pin behavior (2 unit: kwarg propagation + default; 3 integration: body accepts mode='mix', rejects invalid mode='BOGUS' with 422, defaults to 'local' when omitted). 4/4 unit + 32/32 integration pass. | 2026-06-02 | `5d41940` | YOLO night shift Task 3 |
| R23 | **Markdown lint cleanup MD022/031/032** — `.planning/**/*.md` had 12227 cosmetic violations (637 MD022 heading-blank + 1356 MD031 fence-blank + 2475 MD032 list-blank + 7759 MD060 table-pipe-spacing). Per-file `markdownlint-cli2 --fix` resolved 4468 (MD022 + MD031 + MD032 fully cleared); MD060 7759 retained as not auto-fixable (markdownlint has no fix for table-pipe-spacing). Net diff: 598 files, +4011/-1 (single -1 is MD032 list-marker line wrap on ROADMAP.md, semantically equivalent rendering). Out-of-scope rules left as-is per ISSUES #24 explicit list (MD013 line-length 35k, MD033 inline-HTML 5k, MD040 fence-language). | 2026-06-02 | `a59ccb5` | YOLO night shift Task 4 |
| R24 | **#25 Aliyun all-in-one + LightRAG nano-vectordb full-load OOM root cause** — Resolved by qdrant-migration phase. Aliyun Qdrant docker live on 127.0.0.1:6333 (mmap-backed); kb-api hydrates via QdrantVectorDBStorage in 13.1s mean (Wave 4 T11 ×3). LightRAG full-vdb load eliminated. Final state chunks 3294 / ents 51632 / rels 71773. Folded #26 (rerank env) + part of #27 (hydrate throttle) into the same restart per orchestrator decision 2026-06-01. | 2026-06-05 | `8a9abf3` (Wave 1 T1) `a3b08eb` (T2) `b015dbd` (T3) `dceaec3` (T4) Wave 2 T5-T8 (Aliyun-side ops) Wave 3 in-place transplant (Aliyun-side, no commit needed; sequence: chunks → ents → rels v3 orjson) `4d5e6ef` (Wave 4 T2 converter map fix) `56a4112` (Wave 4 T8 systemd timeout fix) | `v1.1-qdrant-migration` |
| R25 | **#26 v1.1.P2-3-perf-fix-B reconcile follow-up** — Resolved by qdrant-migration phase folding rerank env + HT-6 N=4 verification into the same Aliyun kb-api restart window (single-restart-three-changes per orchestrator decision 2026-06-01). HT-6 N=4 lock-break verified by Wave 4 T10 (P5 contract intact: serialized wall_s 40.94/68.5/147.81/154.3 + 4 distinct response bodies + DEEPSEEK marker). 4 rerank Environment lines applied to override.conf during Wave 2 T7. | 2026-06-05 | (rerank env applied via Wave 2 T7 Aliyun-side override.conf; HT-6 verified via Wave 4 T10 — see qdrant-migration aliyun-evidence) | `v1.1-qdrant-migration` |
| R26 | **#22 v1.1.P2-3-perf-fix-B Aliyun Vertex Gemini rerank parity** — Resolved by qdrant-migration HC-6 dual-station rerank parity close. Aliyun fresh-verify 2026-06-05 `provider=vertex_gemini` ×3 in `aliyun-evidence/aliyun-rerank-evidence.log`; Databricks Haiku side already verified per perf-fix-A (R18). N=4 lock-break HT-6 (originally A→B→#26→#22→qdrant-migration T10) ran on deployed Aliyun: lock contract PASS (test artifact `s.get("markdown")` schema bug, NOT regression). | 2026-06-05 | (HC-6 close — see qdrant-migration aliyun-evidence/) | `v1.1-qdrant-migration` |
| R27 | **#27 kb-api hydrate throttle DoS-by-restart risk** — Resolved by qdrant-migration. kb-api hydrate dropped from `wall_s=3362.65` (56min full-vdb JSON load) to mean **13.1s** (Wave 4 T11 ×3 measurements: 13.77 / 12.73 / 12.85). Qdrant mmap delivers orders-of-magnitude speedup over full-load. The 8min Aliyun public-network throttle window observed 2026-06-01 cannot recur because the I/O+RAM saturation that triggered SLB cross-border throttling is gone. | 2026-06-05 | (Aliyun-side via Wave 2 T7 + Wave 4 T11 measurement) | `v1.1-qdrant-migration` |

---

## Cross-references

- **Phase milestone state:** `.planning/phases/v1.1-roadmap/STATE-v1.1.md`
- **Roadmap & success criteria:** `.planning/phases/v1.1-roadmap/ROADMAP.md`
- **Project guide:** `CLAUDE.md` (PRINCIPLE #10 governs this file's update discipline)
- **Memory index:** `~/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/MEMORY.md`

---

## Adding a new issue

When orchestrator (or any agent during close-out) surfaces a new issue:

1. Choose severity (P0/P1/P2/P3/Doc).
2. Add a row to the appropriate table with:
   - Short title (≤ 100 chars)
   - Suggested slug (`260530-<short>` if known, `TBD` if not)
   - Notes (1-3 sentences: root cause / decision / next action)
3. If the issue had a triggering quick / phase, link the relevant SUMMARY in Notes.
4. Update `Last updated:` at top.

**Do NOT** delete entries when fixed — move them to `Resolved (recent)` with date + commit. The history matters for postmortem and pattern detection.
