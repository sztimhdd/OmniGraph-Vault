# P6.1 — Full fixture drift audit

**Wave:** 4 (housekeeping; runs last)
**LoC estimate:** 1–2 days fixture work (no production code touched)
**Risk:** Low
**Mainstream alignment:** ⭐⭐ (housekeeping; tests pin contract)
**Dependencies:** none (but runs LAST so fixtures align with final v1.1 contract, not a moving target)
**Recommended GSD ceremony:** `/gsd:quick`

## Goal

Resolve all remaining test fixture drift after Wave 1–3 stabilizes. Scope is the original P6 audit MINUS what P6.0 already fixed (which was the single `body_cleaned` schema reference unblocking Wave 1). Targets are: the `test_search_kg_job_completes` mock-rewire stale; the 3 audit-listed fixture-drift ERRORs; any new fixture drift introduced by P1 / P2-3 / P4.1; and any other fixture-vs-prod-schema drift surfaced during Wave 1–3 testing. This phase exists because v1.1 introduces new contract shapes (deterministic citation metadata, reranker score field, `mode='mix'` chunk ordering, possibly new ARAG response shapes) — fixtures must end up byte-aligned with these final contracts before v1.1 ships.

## File-touch list (best guess; verified at /gsd:quick time)

- `tests/unit/_ingest_fixtures.py` — bring `CREATE TABLE` declarations into byte-parity with production migrations
- `tests/unit/test_search_kg*.py` — re-wire `test_search_kg_job_completes` mock chain (drift surfaced 2026-05-26 in arx-3 diagnostic)
- `tests/integration/kb/conftest.py` — verify shared fixtures still align with v1.1 final contract
- ANY test fixture flagged in audit list (collected during Wave 1–3 execution; concrete list known at /gsd:quick time)
- (potentially) `tests/unit/test_ingest_from_db_orchestration.py` — if P1 / P2-3 changes touched `ingest_from_db` contract, the behavior-anchor harness needs corresponding updates per CLAUDE.md `# Behavior-Anchor Harness` discipline

## Success criteria

1. `venv/Scripts/python.exe -m pytest -v` produces 0 fixture-drift ERROR markers
2. Pytest baseline (953-test floor) unchanged or higher; no regressions
3. `tests/unit/_ingest_fixtures.py:in_memory_db()` schema is byte-identical to a fresh production SQLite migration (verified by `pragma table_info` diff)
4. `test_search_kg_job_completes` is GREEN (no skipped, no xfail)
5. Any newly-introduced fixtures (from P1 / P2-3 / P4.1) follow the same byte-parity discipline
6. CI green run posted to `P6.1-VERIFICATION.md`

## Why this is Wave 4 (not Wave 0/1)

P6.0 (Wave 0) handled the **only** drift that blocked Wave 1 testing. The remaining drift is **non-blocking** — tests RED-vs-GREEN signal works correctly with these ERRORs in the periphery, just noisy. P6.1 sits at the end so:

- Fixtures align with the **final v1.1 contract** (post-P1, post-P2-3, post-P4.1), not a midway snapshot
- New fixtures introduced by Waves 1–3 are caught in the same housekeeping pass
- The audit is comprehensive rather than incremental (one trip through the test tree, not three)

## Cross-references

- CLAUDE.md `# Behavior-Anchor Harness for Hot Orchestration Code` — `ingest_from_db` orchestrator changes mandate fixture sync
- 2026-05-15 lesson: "test fixture CREATE TABLE not synced with migration silently masks the downstream bug"
- 2026-05-08 lesson: behavior-anchor pinning > implementation-mirroring tests

---

**Execution detail TBD at `/gsd:quick v1.1.P6.1` time.**
