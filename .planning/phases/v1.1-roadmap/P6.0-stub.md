# P6.0 — `body_cleaned` schema fix (Wave 0 blocker)

**Wave:** 0 (must precede Wave 1)
**LoC estimate:** 5–15
**Risk:** Low
**Mainstream alignment:** ⭐ (housekeeping, but blocking)
**Dependencies:** none
**Recommended GSD ceremony:** `/gsd:quick`

## Goal

Repair the schema drift between `kb/data/article_query.py:326` and `tests/unit/_ingest_fixtures.py` so Wave 1 (P1, P5) testing has green fixtures to build on. Production code references a column the in-memory test fixture does not create; current test runs pass anyway because the test path either short-circuits before the column is read or because `_ingest_fixtures.py` was never updated when the production schema gained the column. Either way, this is the same fixture-vs-prod-schema drift class flagged in CLAUDE.md `# Behavior-Anchor Harness for Hot Orchestration Code` discipline (lesson from 2026-05-15) — a single quick fix unblocks P1's behavior-anchor tests against the canonical schema.

## File-touch list (best guess; verify at /gsd:plan-phase or /gsd:quick time)

- `kb/data/article_query.py` — line 326 column reference (verify column name vs. live schema)
- `tests/unit/_ingest_fixtures.py` — `CREATE TABLE` statement to add the missing column with the same type as production
- (potentially) `tests/unit/test_article_query*.py` — re-run to confirm RED → GREEN

## Success criteria

1. `venv/Scripts/python.exe -m pytest tests/unit/test_article_query*.py -v` is GREEN with no fixture-drift ERROR markers
2. `tests/unit/_ingest_fixtures.py:in_memory_db()` schema is byte-identical to a fresh production SQLite migration (verified by `pragma table_info`)
3. Pytest baseline (953 floor) still GREEN — this fix lifts ERRORs to PASS, never lowers count
4. No production code touched beyond the single column reference fix (Surgical Changes principle)

## Why this is Wave 0 (not folded into Wave 4 P6.1)

P1's deterministic-citation tests will read `kb/services/synthesize.py` which itself reads `kb/data/article_query.py`. If the fixture-drift ERROR is still in the test output when P1 starts, P1's test green/red signal becomes ambiguous — could be a P1 regression or a pre-existing P6.0 ERROR. Fixing P6.0 first makes Wave 1 RED-vs-GREEN unambiguous.

## Cross-references

- CLAUDE.md `# Behavior-Anchor Harness` discipline — fixture-drift = contract-shape change, mandates schema sync
- 2026-05-15 lesson: "test fixture CREATE TABLE not synced with migration silently masks the downstream bug"

---

**Execution detail TBD at `/gsd:quick` time.** This stub is a place-holder; the actual `/gsd:quick` invocation produces the fix + atomic commit + verification.
