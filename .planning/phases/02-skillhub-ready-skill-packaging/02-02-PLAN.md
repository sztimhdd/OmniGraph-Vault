---
phase: 02-skillhub-ready-skill-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/EMBEDDING_STRATEGY_DECISION.md
autonomous: true
requirements:
  - PKG-04

must_haves:
  truths:
    - "Embedding strategy experiment is completed with a documented decision"
    - "LightRAG multimodal vector compatibility is verified or ruled out"
  artifacts:
    - path: "specs/EMBEDDING_STRATEGY_DECISION.md"
      provides: "Embedding strategy decision"
      contains: "Decision:"
  key_links: []
---

<objective>
Run the KG-RAG embedding strategy experiment per the protocol in EMBEDDING_STRATEGY_DECISION.md. Determine whether to keep current (Vision+Embeddings), switch to multimodal embeddings, or use a hybrid approach.

Purpose: Phase 2 success criteria #8 requires this decision to be documented. The experiment determines cost/quality tradeoffs for image embedding in the ingestion pipeline.
Output: Updated EMBEDDING_STRATEGY_DECISION.md with completed experiment results and a final decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@specs/EMBEDDING_STRATEGY_DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research LightRAG multimodal vector support and document decision</name>
  <files>specs/EMBEDDING_STRATEGY_DECISION.md</files>
  <read_first>
    specs/EMBEDDING_STRATEGY_DECISION.md
    requirements.txt
    ingest_wechat.py
    multimodal_ingest.py
  </read_first>
  <action>
**Step 1: Check LightRAG multimodal support.**
- Read the LightRAG source code in the venv (`lightrag/lightrag.py` or similar) to check if `ainsert()` accepts pre-computed vectors or only text.
- Check if LightRAG's embedding function can be swapped to a multimodal embedding model.
- Use context7 to look up LightRAG docs if needed.

**Step 2: Check Google embedding-2 multimodal capability.**
- Verify whether `google-genai` SDK supports multimodal embeddings (image input to embedding model).
- Check the `models/text-embedding-004` or `models/embedding-001` endpoints for image support.

**Step 3: Make the decision based on findings.**
- If LightRAG does NOT support multimodal vectors natively -> Decision is "KEEP CURRENT (Method A)" with rationale "LightRAG requires text input for embeddings; multimodal vectors would require major architectural changes."
- If LightRAG DOES support multimodal vectors AND Google offers a multimodal embedding endpoint -> Document cost comparison and recommend based on the decision rules in EMBEDDING_STRATEGY_DECISION.md.

**Step 4: Update EMBEDDING_STRATEGY_DECISION.md.**
- Fill in the "Status" section: check off completed items.
- Fill in "Decision:" with the chosen option (A, B, or C).
- Fill in "Rationale:" with 2-3 sentences explaining why.
- Update the metrics table with findings (even if only partial — mark N/A for metrics that couldn't be collected).
  </action>
  <verify>
    <automated>bash -c "cd C:/Users/huxxha/Desktop/OmniGraph-Vault && grep -c 'Decision:' specs/EMBEDDING_STRATEGY_DECISION.md && grep 'Decision:' specs/EMBEDDING_STRATEGY_DECISION.md | grep -v '___'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Decision:' specs/EMBEDDING_STRATEGY_DECISION.md` returns a line with an actual decision (not blank underscores)
    - `grep 'Rationale:' specs/EMBEDDING_STRATEGY_DECISION.md` returns a line with actual rationale text
    - At least 3 checkboxes in the Progress section are marked `[x]`
  </acceptance_criteria>
  <done>EMBEDDING_STRATEGY_DECISION.md has a documented decision with rationale. The embedding experiment is complete.</done>
</task>

</tasks>

<verification>
- specs/EMBEDDING_STRATEGY_DECISION.md contains a non-blank Decision and Rationale
- Progress checkboxes show work was done
</verification>

<success_criteria>
Embedding strategy decision is documented. Phase 2 success criteria #8 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-skillhub-ready-skill-packaging/02-02-SUMMARY.md`
</output>
