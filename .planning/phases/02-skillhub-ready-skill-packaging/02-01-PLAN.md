---
phase: 02-skillhub-ready-skill-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/omnigraph_ingest/SKILL.md
  - skills/omnigraph_ingest/scripts/ingest.sh
  - skills/omnigraph_ingest/references/api-surface.md
  - skills/omnigraph_ingest/evals/evals.json
  - skills/omnigraph_query/SKILL.md
  - skills/omnigraph_query/scripts/query.sh
  - skills/omnigraph_query/references/api-surface.md
  - skills/omnigraph_query/evals/evals.json
  - scripts/install-for-hermes.sh
  - tests/skills/test_omnigraph_ingest.json
  - tests/skills/test_omnigraph_query.json
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
  - SKILL-05
  - SKILL-07
  - SKILL-08
  - SKILL-09
  - SKILL-10
  - SKILL-11
  - EVAL-01
  - EVAL-02
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Both SKILL.md descriptions are 100-200 words in pushy format"
    - "Shell wrappers work from any CWD and exit non-zero when GEMINI_API_KEY is unset"
    - "Each skill has evals/evals.json with >=3 SkillHub-schema test cases"
    - "Each skill has references/api-surface.md covering CLI args, env vars, exit codes"
    - "install-for-hermes.sh runs with human-readable errors on failure"
    - "SKILL.md bodies are <=500 lines each"
  artifacts:
    - path: "skills/omnigraph_ingest/SKILL.md"
      provides: "Ingest skill instructions"
      contains: "Use this skill when"
    - path: "skills/omnigraph_query/SKILL.md"
      provides: "Query skill instructions"
      contains: "Use this skill when"
    - path: "scripts/install-for-hermes.sh"
      provides: "One-shot installer"
      contains: "set -e"
  key_links:
    - from: "skills/omnigraph_ingest/SKILL.md"
      to: "skills/omnigraph_ingest/scripts/ingest.sh"
      via: "SKILL.md references ingest.sh in decision tree"
      pattern: "ingest\\.sh"
    - from: "skills/omnigraph_query/SKILL.md"
      to: "skills/omnigraph_query/scripts/query.sh"
      via: "SKILL.md references query.sh in decision tree"
      pattern: "query\\.sh"
---

<objective>
Audit all existing skill package files against the SkillHub contract requirements, fix any gaps (description word counts, missing content, schema compliance), and harden the install script.

Purpose: Most files already exist from pre-Phase-1 scaffolding. This plan verifies each file meets its requirement checklist and fixes any deviations found.
Output: All skill package files validated and compliant with PKG-*, SKILL-*, EVAL-*, and TEST-01/02 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/SKILL_PACKAGING_GUIDE.md
@.planning/phases/02-skillhub-ready-skill-packaging/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix both SKILL.md files against all SKILL-* requirements</name>
  <files>
    skills/omnigraph_ingest/SKILL.md
    skills/omnigraph_query/SKILL.md
    skills/omnigraph_ingest/references/api-surface.md
    skills/omnigraph_query/references/api-surface.md
  </files>
  <read_first>
    skills/omnigraph_ingest/SKILL.md
    skills/omnigraph_query/SKILL.md
    skills/omnigraph_ingest/references/api-surface.md
    skills/omnigraph_query/references/api-surface.md
    specs/SKILL_PACKAGING_GUIDE.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
Audit each SKILL.md against this checklist and fix any failures:

**Ingest SKILL.md (SKILL-02, SKILL-03, SKILL-04, SKILL-05, PKG-01, PKG-02):**
1. Frontmatter `name: omnigraph_ingest` — verify present, no `triggers:` key (SKILL-02)
2. Frontmatter `description:` word count is 100-200 words (PKG-01). Count words in the description YAML value only. If outside range, edit to fit. Must start with "Use this skill when...", include 3-5 quoted trigger phrases, end with "Do NOT use when..." redirects.
3. Total file <=500 lines (PKG-02) — currently 127 lines, should pass.
4. Decision tree has all 5 cases (SKILL-03): WeChat URL -> `ingest.sh <url>`, PDF path -> `ingest.sh <path>`, no URL (ask first), missing GEMINI_API_KEY (config error), non-WeChat URL (guard/reject).
5. "When NOT to Use" section has redirects to: omnigraph_query, omnigraph_synthesize, omnigraph_status, omnigraph_manage (SKILL-04).
6. references/api-surface.md covers: CLI args, required/optional env vars, dispatch logic, output format, exit codes, error messages, image server dependency (SKILL-05).

**Query SKILL.md (SKILL-08, SKILL-09, SKILL-10, SKILL-11, PKG-01, PKG-02):**
1. Frontmatter `name: omnigraph_query`, pushy description 100-200 words (SKILL-08, PKG-01).
2. Body has: image server warning (port 8765), decision tree with mode dispatch, empty KB response (advise ingest first), destructive-action guard (-> omnigraph_manage), distinct failure messages (SKILL-09).
3. "When NOT to Use" section: ingest -> omnigraph_ingest, synthesis -> omnigraph_synthesize, status -> omnigraph_status, manage -> omnigraph_manage, general web search -> agent default (SKILL-10).
4. references/api-surface.md covers: CLI args, env vars, query modes table, output file location, exit codes, error messages (SKILL-11).

For each file, if all checks pass, leave unchanged. If any check fails, make the minimal edit to fix it.
  </action>
  <verify>
    <automated>bash -c "cd C:/Users/huxxha/Desktop/OmniGraph-Vault && wc -l skills/omnigraph_ingest/SKILL.md skills/omnigraph_query/SKILL.md && echo '---' && grep -c 'Use this skill when' skills/omnigraph_ingest/SKILL.md skills/omnigraph_query/SKILL.md && echo '---' && grep -c 'When NOT to Use' skills/omnigraph_ingest/SKILL.md skills/omnigraph_query/SKILL.md && echo '---' && grep -c 'ingest.sh' skills/omnigraph_ingest/SKILL.md && grep -c 'query.sh' skills/omnigraph_query/SKILL.md && echo '--- description word count ---' && python -c \"import yaml,pathlib,sys; ok=True; [exec('wc=len(yaml.safe_load(pathlib.Path(p).read_text(encoding=chr(117)+chr(116)+chr(102)+chr(45)+chr(56)).split(chr(45)*3)[1])[chr(100)+chr(101)+chr(115)+chr(99)+chr(114)+chr(105)+chr(112)+chr(116)+chr(105)+chr(111)+chr(110)].split()); print(f\\\"  {p}: {wc} words\\\"); ok=ok and 100<=wc<=200') for p in ['skills/omnigraph_ingest/SKILL.md','skills/omnigraph_query/SKILL.md']]; sys.exit(0 if ok else 1)\""</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l skills/omnigraph_ingest/SKILL.md` returns <=500
    - `wc -l skills/omnigraph_query/SKILL.md` returns <=500
    - `grep -c 'Use this skill when' skills/omnigraph_ingest/SKILL.md` returns >=1
    - `grep -c 'Use this skill when' skills/omnigraph_query/SKILL.md` returns >=1
    - `grep -c 'When NOT to Use' skills/omnigraph_ingest/SKILL.md` returns >=1
    - `grep -c 'When NOT to Use' skills/omnigraph_query/SKILL.md` returns >=1
    - `grep 'triggers:' skills/omnigraph_ingest/SKILL.md` returns no match (no triggers key)
    - `grep -c 'omnigraph_query' skills/omnigraph_ingest/SKILL.md` returns >=1 (redirect present)
    - `grep -c 'omnigraph_ingest' skills/omnigraph_query/SKILL.md` returns >=1 (redirect present)
    - `grep -c '8765' skills/omnigraph_query/SKILL.md` returns >=1 (image server warning)
    - Both SKILL.md description fields are 100-200 words (verified by Python word count assertion)
  </acceptance_criteria>
  <done>Both SKILL.md files pass all SKILL-*, PKG-01, PKG-02 checks including description word count assertion. Both references/api-surface.md files are complete.</done>
</task>

<task type="auto">
  <name>Task 2: Audit shell wrappers, install script, evals, and test case files</name>
  <files>
    skills/omnigraph_ingest/scripts/ingest.sh
    skills/omnigraph_query/scripts/query.sh
    skills/omnigraph_ingest/evals/evals.json
    skills/omnigraph_query/evals/evals.json
    scripts/install-for-hermes.sh
    tests/skills/test_omnigraph_ingest.json
    tests/skills/test_omnigraph_query.json
  </files>
  <read_first>
    skills/omnigraph_ingest/scripts/ingest.sh
    skills/omnigraph_query/scripts/query.sh
    skills/omnigraph_ingest/evals/evals.json
    skills/omnigraph_query/evals/evals.json
    scripts/install-for-hermes.sh
    tests/skills/test_omnigraph_ingest.json
    tests/skills/test_omnigraph_query.json
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
Audit each file and fix gaps:

**Shell wrappers (PKG-03, SKILL-01, SKILL-07):**
1. ingest.sh: Resolves `OMNIGRAPH_ROOT` with fallback `$HOME/Desktop/OmniGraph-Vault`, activates venv (Windows Scripts/ + Unix bin/), validates `GEMINI_API_KEY`, announces "Starting ingestion -- 30-120 seconds...", dispatches URL vs PDF by extension. Exits non-zero with human-readable stderr on missing env/venv.
2. query.sh: Same pattern. Announces "Querying -- 15-60 seconds...", calls `kg_synthesize.py "$QUERY" "$MODE"`, default mode hybrid.
3. Both must work when CWD is not the project root (they `cd "$OMNIGRAPH_ROOT"`).
Verify all behaviors are present. Fix if any missing.

**install-for-hermes.sh (PKG-03):**
1. Verify it runs from scratch: checks GEMINI_API_KEY, creates runtime dirs, creates .env from template, creates venv, installs deps, validates imports, runs smoke test.
2. Check that `$1` argument handling for `--skip-test` works (currently line 179 uses `$1` but positional args are not consumed earlier -- this is correct since no other positional args are defined).
3. Verify human-readable error messages on each failure path (GEMINI_API_KEY missing, venv creation failed, import failed).

**Eval files (EVAL-01, EVAL-02):**
1. ingest evals: >=3 cases, SkillHub schema (`skill_name`, `evals[]` with `id`, `name`, `prompt`, `expected_output`, `files`). Currently 5 cases -- verify schema compliance.
2. query evals: same. Currently 5 cases.

**Test case files (TEST-01, TEST-02):**
1. test_omnigraph_ingest.json (TEST-01): Must cover trigger phrase matching (2+ phrases), WeChat URL guard, non-WeChat URL guard (9th case), missing key guard, wrong-skill redirect. Currently 9 cases.
2. test_omnigraph_query.json (TEST-02): Must cover trigger phrase matching, empty KB response, synthesis output format, wrong-skill redirect. Currently 10 cases.
Verify all required scenarios present. Add any missing cases.
  </action>
  <verify>
    <automated>bash -c "cd C:/Users/huxxha/Desktop/OmniGraph-Vault && python -c \"import json; d=json.load(open('skills/omnigraph_ingest/evals/evals.json')); assert d['skill_name']=='omnigraph_ingest'; assert len(d['evals'])>=3; print(f'ingest evals: {len(d[chr(101)+chr(118)+chr(97)+chr(108)+chr(115)])} cases OK')\" && python -c \"import json; d=json.load(open('skills/omnigraph_query/evals/evals.json')); assert d['skill_name']=='omnigraph_query'; assert len(d['evals'])>=3; print(f'query evals: {len(d[chr(101)+chr(118)+chr(97)+chr(108)+chr(115)])} cases OK')\" && python -c \"import json; t=json.load(open('tests/skills/test_omnigraph_ingest.json')); assert len(t)>=9; print(f'ingest tests: {len(t)} cases')\" && python -c \"import json; t=json.load(open('tests/skills/test_omnigraph_query.json')); assert len(t)>=10; print(f'query tests: {len(t)} cases')\""</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'OMNIGRAPH_ROOT' skills/omnigraph_ingest/scripts/ingest.sh` returns >=1
    - `grep -c 'GEMINI_API_KEY' skills/omnigraph_ingest/scripts/ingest.sh` returns >=1
    - `grep -c 'OMNIGRAPH_ROOT' skills/omnigraph_query/scripts/query.sh` returns >=1
    - `grep -c 'GEMINI_API_KEY' skills/omnigraph_query/scripts/query.sh` returns >=1
    - ingest evals.json parses as valid JSON with `skill_name` == "omnigraph_ingest" and >=3 evals
    - query evals.json parses as valid JSON with `skill_name` == "omnigraph_query" and >=3 evals
    - test_omnigraph_ingest.json has >=9 test cases
    - test_omnigraph_query.json has >=10 test cases
    - `grep -c 'skip-test' scripts/install-for-hermes.sh` returns >=1
  </acceptance_criteria>
  <done>All shell wrappers implement PKG-03/SKILL-01/SKILL-07 behaviors. Eval files comply with SkillHub schema. Test case files cover all TEST-01/TEST-02 scenarios. Install script has human-readable error paths.</done>
</task>

</tasks>

<verification>
- `wc -l skills/*/SKILL.md` both <=500
- Both evals.json files parse as valid JSON with >=3 cases each
- Both test JSON files parse with expected case counts
- Shell wrappers contain OMNIGRAPH_ROOT, GEMINI_API_KEY checks, venv activation
- install-for-hermes.sh contains all 7 steps
</verification>

<success_criteria>
All PKG-*, SKILL-*, EVAL-*, TEST-01, TEST-02 requirements verified by file audit. No requirement left unaddressed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-skillhub-ready-skill-packaging/02-01-SUMMARY.md`
</output>
