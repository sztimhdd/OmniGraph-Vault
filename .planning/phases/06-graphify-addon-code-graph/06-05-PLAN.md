---
phase: 06-graphify-addon-code-graph
plan: 05
type: execute
wave: 5
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-03b"
  - "06-04"
files_modified:
  - docs/testing/06-demo1-transcript.md
  - docs/testing/06-demo2-transcript.md
  - docs/testing/06-acceptance-signoff.md
autonomous: false
requirements:
  - REQ-05
  - REQ-06
  - REQ-07

must_haves:
  truths:
    - "Demo 1 transcript on disk shows BOTH `graphify` AND `omnigraph_search` were invoked by the agent autonomously"
    - "Demo 2 transcript on disk shows BOTH skills were invoked in a self-evolution / cross-referential task"
    - "Architectural-consistency sign-off captured from a human reviewer (REQ-07 qualitative)"
    - "Acceptance gate (all 8 REQs) has a single consolidated sign-off file"
  artifacts:
    - path: "docs/testing/06-demo1-transcript.md"
      provides: "Captured tool-use log and agent output from the Demo 1 streaming-tool-output scenario"
      contains: "graphify"
      min_lines: 40
    - path: "docs/testing/06-demo2-transcript.md"
      provides: "Captured tool-use log and agent output from the Demo 2 self-evolution scenario"
      contains: "graphify"
      min_lines: 40
    - path: "docs/testing/06-acceptance-signoff.md"
      provides: "Final sign-off on all 8 requirements, with links to evidence"
      contains: "REQ-08"
  key_links:
    - from: "docs/testing/06-demo1-transcript.md"
      to: "REQ-05"
      via: "assertion that both skills appear in tool-use log"
      pattern: "omnigraph_search"
    - from: "docs/testing/06-demo2-transcript.md"
      to: "REQ-06"
      via: "assertion that both skills appear in tool-use log"
      pattern: "omnigraph_search"
---

<objective>
Close Phase 6 by running Demo 1 (streaming tool output) and Demo 2 (self-evolution) live against the remote Hermes deployment, capturing transcripts, and producing a consolidated acceptance sign-off covering all 8 requirements.

Purpose: REQ-05 + REQ-06 + REQ-07 are demo-based and qualitative — they cannot be automated because there is no Hermes scripting API (research §Open Questions §4). The plan reserves this as a dedicated wave so the demo runs happen after all code is green.

Output: Both demo transcripts filled in, one consolidated acceptance sign-off file listing evidence for each REQ-01..REQ-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-graphify-addon-code-graph/06-RESEARCH.md
@.planning/phases/06-graphify-addon-code-graph/06-VALIDATION.md
@.planning/phases/06-graphify-addon-code-graph/06-CONTEXT.md
@.planning/phases/06-graphify-addon-code-graph/06-00-SUMMARY.md
@.planning/phases/06-graphify-addon-code-graph/06-01-SUMMARY.md
@.planning/phases/06-graphify-addon-code-graph/06-02-SUMMARY.md
@.planning/phases/06-graphify-addon-code-graph/06-03-SUMMARY.md
@.planning/phases/06-graphify-addon-code-graph/06-03b-SUMMARY.md
@.planning/phases/06-graphify-addon-code-graph/06-04-SUMMARY.md
@docs/testing/06-demo1-transcript.md
@docs/testing/06-demo2-transcript.md

<interfaces>
<!-- Acceptance gate per PRD §7.4 + VALIDATION.md REQ map. -->

REQ-01 graphify on Hermes               -> Plan 01 (verified)
REQ-02 graphify on OpenClaw             -> Plan 01 (verified or partial-defer with rationale)
REQ-03 omnigraph_search discoverable    -> Plan 03 (skill_runner --validate green)
REQ-04 omnigraph_search returns results -> Plan 03 (live smoke test green)
REQ-05 Demo 1 — both skills used        -> Plan 05 Task 5.1
REQ-06 Demo 2 — both skills used        -> Plan 05 Task 5.2
REQ-07 architecturally consistent       -> Plan 05 Task 5.3 (qualitative)
REQ-08 weekly cron atomic rebuild       -> Plan 04 (verified)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.1: Run Demo 1 (streaming tool output) and fill docs/testing/06-demo1-transcript.md</name>
  <files>docs/testing/06-demo1-transcript.md</files>
  <read_first>
    - docs/testing/06-demo1-transcript.md (placeholder from Plan 00)
    - .planning/phases/06-graphify-addon-code-graph/06-VALIDATION.md §Manual-Only Verifications (definition of Demo 1)
    - .planning/phases/06-graphify-addon-code-graph/06-CONTEXT.md §Acceptance Gate (PRD §7.4 Demo 1 text)
    - ~/.claude/projects/c--Users-huxxha-Desktop-OmniGraph-Vault/memory/hermes_ssh.md (SSH — do NOT commit)
  </read_first>
  <what-built>
    Orchestrator SSHes to remote, starts a Hermes session, runs the Demo 1 prompt (streaming tool output), captures the transcript, and writes it to `docs/testing/06-demo1-transcript.md`.

    **Step 1 — Start Hermes session:**
    ```
    ssh remote
    cd ~/OmniGraph-Vault
    hermes chat
    # (or the project's exact entry command)
    ```

    **Step 2 — Issue the Demo 1 prompt** (paraphrased from CONTEXT.md — orchestrator picks a concrete variant):
    > "Implement OpenClaw-style streaming tool output in the Rust fork. Look up both the design rationale (why OpenClaw chose this pattern) and the call-chain / function signatures needed to wire it in."

    This prompt is designed to elicit BOTH `omnigraph_search` (for design rationale — "why did OpenClaw choose this") AND `graphify` (for code structure — "call-chain / function signatures").

    **Step 3 — Observe the tool-use log during the session:**
    Hermes should autonomously:
    (a) invoke `omnigraph_search` (via `scripts/query.sh`) with a rationale query
    (b) invoke `graphify` (the installed skill from Plan 01) with a code-structure query
    (c) synthesize a response that draws from both

    **Step 4 — Save transcript:** Copy-paste the full session (prompt + tool-use events + final answer) into `docs/testing/06-demo1-transcript.md`, overwriting the placeholder. Use this structure:

    ```markdown
    # Phase 6 — Demo 1 Transcript (Streaming Tool Output)

    **Ran:** <YYYY-MM-DD HH:MM>
    **Platform:** Hermes (on remote WSL2)
    **Session:** <hermes session id or "interactive">

    ## Prompt

    [verbatim prompt text]

    ## Tool-use log (observed invocations in order)

    1. [tool name] — [brief query or args] — [outcome]
    2. [tool name] — [brief query or args] — [outcome]
    ...

    ## Assertion: both `graphify` AND `omnigraph_search` appear in tool-use log

    - graphify invocations: N (references: <lines>)
    - omnigraph_search invocations: M (references: <lines>)
    - BOTH ≥ 1 : YES | NO

    ## Agent's final answer (abridged)

    [key excerpts showing both sources were used]

    ## Qualitative observation

    [any surprises, routing errors the agent self-corrected, latency]
    ```

    If BOTH skills were NOT invoked: record the failure, attempt 1-2 prompt variations to nudge the agent (do NOT modify the skills to pass — that's cheating the demo). If all variations fail, mark REQ-05 as RED in the sign-off and propose remediation (e.g., strengthen disambiguation in SKILL.md descriptions).

    **Sanitize:** remove hostname, port, username from any path or greeting text before committing.
  </what-built>
  <how-to-verify>
    1. Local: `grep -q "graphify" docs/testing/06-demo1-transcript.md` exits 0
    2. Local: `grep -q "omnigraph_search" docs/testing/06-demo1-transcript.md` exits 0
    3. Local: `wc -l < docs/testing/06-demo1-transcript.md` >= 40
    4. Local: file does NOT contain the remote hostname substring (grep-verify returns no match)
    5. Local: file contains a line matching "BOTH ≥ 1" with YES or NO clearly marked
    6. Orchestrator reports the outcome to user: "Demo 1 YES" or "Demo 1 NO — remediation path in transcript"
  </how-to-verify>
  <resume-signal>Type "approved" once transcript captured and the BOTH-skills assertion is green. If NO, type "continue with-issues" and proceed to Demo 2 — sign-off will flag REQ-05 as unmet.</resume-signal>
  <acceptance_criteria>
    - `docs/testing/06-demo1-transcript.md` file size > 1500 bytes
    - Contains the literal string "graphify"
    - Contains the literal string "omnigraph_search"
    - Contains a `## Assertion` section with an explicit YES or NO
    - Does NOT contain the remote hostname, port, or username from `hermes_ssh.md`
    - Contains section headers: `## Prompt`, `## Tool-use log`, `## Assertion`, `## Agent's final answer`
  </acceptance_criteria>
  <action>See <what-built> above. This is a checkpoint task: the orchestrator performs the steps described in <what-built>, verifies per <how-to-verify>, and waits for <resume-signal> before continuing. Acceptance is gated by <acceptance_criteria>.</action>
  <verify>See <how-to-verify> above; final pass condition is <acceptance_criteria>.</verify>
  <done>User types the <resume-signal> token AND every bullet in <acceptance_criteria> is satisfied.</done>

</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.2: Run Demo 2 (self-evolution) and fill docs/testing/06-demo2-transcript.md</name>
  <files>docs/testing/06-demo2-transcript.md</files>
  <read_first>
    - docs/testing/06-demo2-transcript.md (placeholder from Plan 00)
    - .planning/phases/06-graphify-addon-code-graph/06-VALIDATION.md §Manual-Only Verifications (definition of Demo 2)
    - .planning/phases/06-graphify-addon-code-graph/06-CONTEXT.md §Acceptance Gate (PRD §7.4 Demo 2 text)
  </read_first>
  <what-built>
    Same procedure as Task 5.1 but with the Demo 2 prompt:

    > "Add Hermes-style self-evolution to the Rust fork. Look up how Hermes discovers, creates, and registers skills at runtime (design intent + code structure) and propose an equivalent mechanism in the Rust fork's tool registry."

    This prompt should elicit:
    (a) `omnigraph_search` for Hermes self-evolution design rationale (from WeChat/Zhihu articles about Hermes)
    (b) `graphify` for the call-chain (how `skills_list()`, `skill_view()`, skill registration work in the installed code-graph)

    Before running the prompt, the research §Open Questions §3 warns that the domain graph may not contain Hermes self-evolution articles. Pre-check with:
    ```
    ssh remote "source ~/OmniGraph-Vault/venv/bin/activate && python -m omnigraph_search.query 'hermes self-evolution skills' 2>&1 | head -20"
    ```
    If the result is empty or low-signal, note this in the transcript — Demo 2 may degrade to a qualitative "graphify worked, omnigraph_search returned thin data" outcome.

    Write the transcript to `docs/testing/06-demo2-transcript.md` using the same structure as Task 5.1.
  </what-built>
  <how-to-verify>
    Same verification checklist as Task 5.1, applied to demo2-transcript.md.
  </how-to-verify>
  <resume-signal>Type "approved" or "continue with-issues".</resume-signal>
  <acceptance_criteria>
    - `docs/testing/06-demo2-transcript.md` file size > 1500 bytes
    - Contains the literal string "graphify"
    - Contains the literal string "omnigraph_search"
    - Contains a `## Assertion` section with an explicit YES or NO
    - Does NOT contain the remote hostname, port, or username
    - Contains section headers: `## Prompt`, `## Tool-use log`, `## Assertion`, `## Agent's final answer`
  </acceptance_criteria>
  <action>See <what-built> above. This is a checkpoint task: the orchestrator performs the steps described in <what-built>, verifies per <how-to-verify>, and waits for <resume-signal> before continuing. Acceptance is gated by <acceptance_criteria>.</action>
  <verify>See <how-to-verify> above; final pass condition is <acceptance_criteria>.</verify>
  <done>User types the <resume-signal> token AND every bullet in <acceptance_criteria> is satisfied.</done>

</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.3: Consolidated acceptance sign-off across all 8 REQs</name>
  <files>docs/testing/06-acceptance-signoff.md</files>
  <read_first>
    - .planning/phases/06-graphify-addon-code-graph/06-CONTEXT.md §Acceptance Gate (PRD §7.4 — the 5 bullet acceptance list)
    - .planning/phases/06-graphify-addon-code-graph/06-VALIDATION.md §Per-Task Verification Map (REQ-01..REQ-08)
    - .planning/phases/06-graphify-addon-code-graph/06-00-SUMMARY.md through 06-04-SUMMARY.md (to cite evidence)
    - docs/testing/06-demo1-transcript.md + docs/testing/06-demo2-transcript.md (just-created)
    - docs/testing/06-remote-probe-result.md (for D-S10 scope impact on REQ-02)
  </read_first>
  <what-built>
    Write a new file `docs/testing/06-acceptance-signoff.md` consolidating evidence for all 8 requirements. Structure:

    ```markdown
    # Phase 6 — Acceptance Sign-Off

    **Signed off:** <YYYY-MM-DD>
    **Signed by:** <user / orchestrator>

    ## Acceptance Gate (PRD §7.4 + 06-VALIDATION.md)

    | ID | Requirement | Status | Evidence | Notes |
    |----|-------------|--------|----------|-------|
    | REQ-01 | graphify skill functional on Hermes | PASS \| FAIL | 06-01-SUMMARY.md + `ssh remote "hermes skills list"` output quoted here | — |
    | REQ-02 | graphify skill functional on OpenClaw | PASS \| PARTIAL \| FAIL | 06-01-SUMMARY.md + probe result | If PARTIAL: rationale from hermes_ssh.md D-S10 scope decision |
    | REQ-03 | omnigraph_search SKILL.md discoverable | PASS \| FAIL | 06-03b-SUMMARY.md skill_runner --validate output (structural scaffold in 06-03-SUMMARY.md) | — |
    | REQ-04 | omnigraph_search returns LightRAG results | PASS \| FAIL | 06-03b-SUMMARY.md live smoke test output (first 200 chars) | — |
    | REQ-05 | Demo 1 — agent routes to both skills | PASS \| FAIL | docs/testing/06-demo1-transcript.md assertion | — |
    | REQ-06 | Demo 2 — agent routes to both skills | PASS \| FAIL | docs/testing/06-demo2-transcript.md assertion | — |
    | REQ-07 | Architecturally consistent output | PASS \| FAIL | Qualitative human review of Demo 1/2 final answers | Compared against OpenClaw reference implementation |
    | REQ-08 | Weekly cron atomic rebuild works | PASS \| FAIL | 06-04-SUMMARY.md manual-run output + crontab registration | mtime delta + node count preserved |

    ## Summary

    - Total: 8
    - PASS: N
    - PARTIAL: M  (list REQ IDs + rationale)
    - FAIL: K     (list REQ IDs + remediation)

    ## Phase 6 Deferred Items (out-of-scope, documented)

    - Phase 5.5 bridge nodes (D-G07 — planned for a future phase)
    - T2/T3 repos (D-G04)
    - Rust fork in graph (D-G08)
    - Gemini free-tier embedding quota (separate infra track)
    - (Possibly) REQ-02 full CLI verification if claw absent on remote

    ## Sign-Off

    - [ ] All blocker-grade REQs are PASS
    - [ ] Any PARTIAL items have documented rationale and future-phase tickets
    - [ ] Any FAIL items have remediation plans filed as gap-closure candidates (`/gsd:plan-phase 6 --gaps`)

    **Decision:** ACCEPT | ACCEPT WITH PARTIALS | REOPEN (requires gap closure)
    ```

    Fill in the Status + Evidence columns using actual data from the SUMMARY files and transcripts. Do NOT invent data — if an earlier plan's SUMMARY doesn't exist or is incomplete, write "TBD — blocked on 06-0X-SUMMARY.md" and halt Phase 6 closure.
  </what-built>
  <how-to-verify>
    1. Local: `test -f docs/testing/06-acceptance-signoff.md`
    2. Local: `grep -c "REQ-0" docs/testing/06-acceptance-signoff.md` returns >= 8 (all 8 REQs listed)
    3. Local: file contains at least one "PASS" or "FAIL" per REQ row (no literal "PASS | FAIL" left unfilled — the `|` marker is the template, the actual file must have one specific value)
    4. Local: file contains a "Decision:" line with one of `ACCEPT`, `ACCEPT WITH PARTIALS`, or `REOPEN`
    5. If ACCEPT: orchestrator reports "Phase 6 CLOSED — all requirements met"
    6. If ACCEPT WITH PARTIALS: orchestrator notes which REQs were partial and why
    7. If REOPEN: orchestrator advises running `/gsd:plan-phase 6 --gaps` next
  </how-to-verify>
  <resume-signal>Type "approved" after sign-off content is confirmed accurate and decision line is committed.</resume-signal>
  <acceptance_criteria>
    - `docs/testing/06-acceptance-signoff.md` exists
    - `grep -c "^| REQ-0" docs/testing/06-acceptance-signoff.md` returns exactly 8 (one table row per REQ)
    - File does NOT contain the unfilled template alternation pattern `PASS | FAIL` — each status cell must have a single concrete value (grep `"PASS \\| FAIL"` must return no matches)
    - File contains the literal string `REQ-08`
    - File contains a "Decision:" line with exactly one of `ACCEPT`, `ACCEPT WITH PARTIALS`, `REOPEN`
    - `grep -q 'crontab' docs/testing/06-acceptance-signoff.md` (confirms REQ-08 row cites crontab evidence)
    - `! grep -qE 'TBD|TODO|FIXME' docs/testing/06-acceptance-signoff.md` (no placeholder text in any REQ row)
  </acceptance_criteria>
  <action>See <what-built> above. This is a checkpoint task: the orchestrator performs the steps described in <what-built>, verifies per <how-to-verify>, and waits for <resume-signal> before continuing. Acceptance is gated by <acceptance_criteria>.</action>
  <verify>See <how-to-verify> above; final pass condition is <acceptance_criteria>.</verify>
  <done>User types the <resume-signal> token AND every bullet in <acceptance_criteria> is satisfied.</done>

</task>

</tasks>

<verification>
- REQ-05: Demo 1 transcript captured with BOTH-skills assertion
- REQ-06: Demo 2 transcript captured with BOTH-skills assertion
- REQ-07: Sign-off records qualitative architectural judgment
- All 8 REQs consolidated in a single sign-off document with concrete decisions
</verification>

<success_criteria>
- Demo 1 and Demo 2 transcripts exist with BOTH-skills assertions
- docs/testing/06-acceptance-signoff.md records a decision: ACCEPT | ACCEPT WITH PARTIALS | REOPEN
- If REOPEN: phase close-out halts; orchestrator tells user to run gap-closure planning
- If ACCEPT | ACCEPT WITH PARTIALS: Phase 6 is eligible for merge/close
</success_criteria>

<output>
After completion, create `.planning/phases/06-graphify-addon-code-graph/06-05-SUMMARY.md` with:
- Demo 1 outcome (BOTH ≥ 1: YES / NO)
- Demo 2 outcome (BOTH ≥ 1: YES / NO)
- REQ-07 qualitative judgment (one paragraph)
- Final acceptance decision (ACCEPT / ACCEPT WITH PARTIALS / REOPEN)
- Recommended next step (merge to main, or gap-closure plan)
</output>
