---
phase: 01-bug-fixes-gate-6-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - kg_synthesize.py
  - ingest_wechat.py
  - cognee_batch_processor.py
  - cognee_wrapper.py
  - init_cognee.py
  - list_entities.py
  - query_lightrag.py
  - setup_cognee.py
  - tests/verify_gate_a.py
  - tests/verify_gate_b.py
  - tests/verify_gate_c.py
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04]

must_haves:
  truths:
    - "No Python file in the repo contains the string /home/sztimhdd/"
    - "kg_synthesize.py can load canonical_map.json without NameError (json is imported)"
    - "kg_synthesize.py defaults to hybrid mode when no mode argument is given"
    - "config.py exports ENTITY_BUFFER_DIR and CANONICAL_MAP_FILE constants"
    - "cognee_batch_processor.py uses config constants for buffer dir and map file"
    - "ingest_wechat.py ingest_pdf() references correct variable names (file_path, full_text, article_hash)"
  artifacts:
    - path: "config.py"
      provides: "ENTITY_BUFFER_DIR and CANONICAL_MAP_FILE constants"
      contains: "ENTITY_BUFFER_DIR"
    - path: "kg_synthesize.py"
      provides: "Working synthesis with json import and hybrid default"
      contains: "import json"
    - path: "ingest_wechat.py"
      provides: "Fixed ingest_pdf() and image download counter"
      contains: "image_success_count"
  key_links:
    - from: "ingest_wechat.py"
      to: "config.py"
      via: "import ENTITY_BUFFER_DIR"
      pattern: "from config import.*ENTITY_BUFFER_DIR"
    - from: "cognee_batch_processor.py"
      to: "config.py"
      via: "import ENTITY_BUFFER_DIR, CANONICAL_MAP_FILE"
      pattern: "from config import.*ENTITY_BUFFER_DIR"
    - from: "kg_synthesize.py"
      to: "config.py"
      via: "import CANONICAL_MAP_FILE"
      pattern: "from config import.*CANONICAL_MAP_FILE"
---

<objective>
Fix all four infrastructure bugs (INFRA-01..04) and adjacent code issues so the pipeline runs on any machine without path errors, NameErrors, or silent failures.

Purpose: These bugs block Gate 6 validation entirely — kg_synthesize.py crashes on first cross-article query, and hardcoded paths prevent running on any machine except the original dev box.
Output: All Python files use config.py constants for paths; kg_synthesize.py imports json and defaults to hybrid mode; bare excepts are replaced; ingest_pdf() variable names are fixed; image download has success/failure counter.
</objective>

<execution_context>
@C:\Users\huxxha\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\huxxha\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes-gate-6-validation/01-CONTEXT.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Current config.py exports (executor must extend, not replace) -->
From config.py:
```python
import os
from pathlib import Path

BASE_DIR = Path.home() / ".hermes" / "omonigraph-vault"
RAG_WORKING_DIR = BASE_DIR / "lightrag_storage"
BASE_IMAGE_DIR = BASE_DIR / "images"
SYNTHESIS_OUTPUT = BASE_DIR / "synthesis_output.md"
CDP_URL = os.environ.get("CDP_URL", "http://localhost:9223")
FIRECRAWL_API_KEY = os.environ.get("FIRECRAWL_API_KEY", "")
ENV_PATH = Path.home() / ".hermes" / ".env"
```

From ingest_wechat.py line 31 (existing config imports):
```python
from config import RAG_WORKING_DIR, BASE_IMAGE_DIR, load_env, CDP_URL
```

From kg_synthesize.py line 8 (existing config imports):
```python
from config import RAG_WORKING_DIR, load_env
```

From cognee_batch_processor.py (no config import yet — uses hardcoded paths at lines 9, 30, 35, 36)
From cognee_wrapper.py (no config import yet — uses hardcoded path at line 8)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config constants and replace all hardcoded paths (INFRA-01, INFRA-02)</name>
  <files>config.py, kg_synthesize.py, ingest_wechat.py, cognee_batch_processor.py, cognee_wrapper.py, init_cognee.py, list_entities.py, query_lightrag.py, setup_cognee.py, tests/verify_gate_a.py, tests/verify_gate_b.py, tests/verify_gate_c.py</files>
  <read_first>
    config.py
    kg_synthesize.py
    ingest_wechat.py
    cognee_batch_processor.py
    cognee_wrapper.py
    init_cognee.py
    list_entities.py
    query_lightrag.py
    setup_cognee.py
    tests/verify_gate_a.py
    tests/verify_gate_b.py
    tests/verify_gate_c.py
    .planning/codebase/CONCERNS.md
    .planning/phases/01-bug-fixes-gate-6-validation/01-CONTEXT.md
  </read_first>
  <action>
**Step 1 — config.py: Add two new constants (per D-01, D-02)**

After the existing `SYNTHESIS_OUTPUT` line (line 8), add:
```python
ENTITY_BUFFER_DIR = BASE_DIR / "entity_buffer"
CANONICAL_MAP_FILE = BASE_DIR / "canonical_map.json"
```

Do NOT change any existing constants. Do NOT change `load_env()`. Do NOT rename `omonigraph-vault`.

**Step 2 — Replace hardcoded paths in all affected files (per D-03, INFRA-02)**

For each file below, replace the hardcoded path with a config import. The exact replacements:

**cognee_batch_processor.py:**
- Line 9: Delete `VENV_SITE_PACKAGES = "/home/sztimhdd/OmniGraph-Vault/venv/lib/python3.12/site-packages"` and the `sys.path.insert(0, VENV_SITE_PACKAGES)` block (lines 9-11). These are not needed when running from the project venv.
- Line 30: Replace `handler = logging.FileHandler("/home/sztimhdd/OmniGraph-Vault/cognee_batch.log")` with `handler = logging.FileHandler(str(Path(__file__).resolve().parent / "cognee_batch.log"))` (log to project root, not hardcoded path).
- Line 35: Replace `BUFFER_DIR = "/home/sztimhdd/OmniGraph-Vault/entity_buffer"` with import from config: add `from config import ENTITY_BUFFER_DIR, CANONICAL_MAP_FILE` near top, then `BUFFER_DIR = str(ENTITY_BUFFER_DIR)`.
- Line 36: Replace `MAP_FILE = "/home/sztimhdd/OmniGraph-Vault/canonical_map.json"` with `MAP_FILE = str(CANONICAL_MAP_FILE)`.

**cognee_wrapper.py:**
- Line 8: Delete `VENV_SITE_PACKAGES = "/home/sztimhdd/OmniGraph-Vault/venv/lib/python3.12/site-packages"` and the `sys.path.insert(0, VENV_SITE_PACKAGES)` block (lines 8-9). Not needed when running from the project venv.

**ingest_wechat.py:**
- Add `ENTITY_BUFFER_DIR` to the existing config import at line 31: `from config import RAG_WORKING_DIR, BASE_IMAGE_DIR, load_env, CDP_URL, ENTITY_BUFFER_DIR`
- Line 279: Replace `os.makedirs('/home/sztimhdd/OmniGraph-Vault/entity_buffer', exist_ok=True)` with `os.makedirs(ENTITY_BUFFER_DIR, exist_ok=True)`
- Line 280: Replace `os.path.join('/home/sztimhdd/OmniGraph-Vault/entity_buffer', f'{article_hash}_entities.json')` with `os.path.join(ENTITY_BUFFER_DIR, f'{article_hash}_entities.json')`
- Line 368: Replace `os.path.join('/home/sztimhdd/OmniGraph-Vault/entity_buffer', f'{article_hash}_entities.json')` with `os.path.join(ENTITY_BUFFER_DIR, f'{article_hash}_entities.json')` (this is inside ingest_pdf())

**kg_synthesize.py:**
- Add `CANONICAL_MAP_FILE` to the existing config import at line 8: `from config import RAG_WORKING_DIR, load_env, CANONICAL_MAP_FILE`
- Line 50: Replace `map_file = "/home/sztimhdd/OmniGraph-Vault/canonical_map.json"` with `map_file = str(CANONICAL_MAP_FILE)`

**init_cognee.py:**
- Line 5: Delete `sys.path.append('/home/sztimhdd/.hermes/kg-vault/lightrag/venv/lib/python3.11/site-packages')`. Not needed from project venv.

**list_entities.py:**
- Line 5: Replace `graph_path = "/home/sztimhdd/.hermes/kg-vault/lightrag_storage/graph_chunk_entity_relation.graphml"` with:
  ```python
  from config import RAG_WORKING_DIR
  graph_path = str(RAG_WORKING_DIR / "graph_chunk_entity_relation.graphml")
  ```
  Move the import to the top of the file (after `import os`).

**query_lightrag.py:**
- Line 12: Delete `RAG_WORKING_DIR = "/home/sztimhdd/.hermes/kg-vault/lightrag_storage"` and add `from config import RAG_WORKING_DIR` at the top (after `import os`). Note: this replaces the local constant with the config import.
- Also delete the local `load_env()` function definition (lines 17-29) and the `load_env()` call (line 32), since config.py already calls `load_env()` at import time. Replace with `from config import load_env` or simply `import config` (which triggers `load_env()` at import time). Keep the `GEMINI_API_KEY = os.environ.get("GEMINI_API_KEY")` line.

**setup_cognee.py:**
- Line 23: Delete `venv_path = "/home/sztimhdd/.hermes/kg-vault/lightrag/venv/lib/python3.11/site-packages"` and the `sys.path.insert(0, venv_path)` line. Not needed from project venv.

**tests/verify_gate_a.py:**
- Lines 6-7: Delete both `sys.path.append(...)` lines:
  - `sys.path.append("/home/sztimhdd/.hermes/kg-vault/lightrag/venv/lib/python3.11/site-packages")`
  - `sys.path.append('/home/sztimhdd/OmniGraph-Vault/venv/lib/python3.12/site-packages')`
  These are not needed when running from the project venv.

**tests/verify_gate_b.py:**
- Line 6: Delete `sys.path.append('/home/sztimhdd/OmniGraph-Vault/venv/lib/python3.12/site-packages')`. Not needed.

**tests/verify_gate_c.py:**
- Line 33: Delete `venv_path = '/home/sztimhdd/OmniGraph-Vault/venv/lib/python3.12/site-packages'` and the `if venv_path not in sys.path: sys.path.append(venv_path)` block (lines 33-35). Not needed.
  </action>
  <verify>
    <automated>cd C:/Users/huxxha/Desktop/OmniGraph-Vault && grep -rn "/home/sztimhdd/" --include="*.py" . | head -20; echo "---"; python -c "from config import ENTITY_BUFFER_DIR, CANONICAL_MAP_FILE; print('ENTITY_BUFFER_DIR:', ENTITY_BUFFER_DIR); print('CANONICAL_MAP_FILE:', CANONICAL_MAP_FILE)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "/home/sztimhdd/" --include="*.py" .` returns NO matches (zero lines)
    - `config.py` contains `ENTITY_BUFFER_DIR = BASE_DIR / "entity_buffer"`
    - `config.py` contains `CANONICAL_MAP_FILE = BASE_DIR / "canonical_map.json"`
    - `ingest_wechat.py` contains `from config import` with `ENTITY_BUFFER_DIR` in the import list
    - `kg_synthesize.py` contains `from config import` with `CANONICAL_MAP_FILE` in the import list
    - `cognee_batch_processor.py` contains `from config import ENTITY_BUFFER_DIR, CANONICAL_MAP_FILE`
    - `list_entities.py` contains `from config import RAG_WORKING_DIR`
    - `query_lightrag.py` does NOT contain a local `RAG_WORKING_DIR = "` assignment
    - `python -c "from config import ENTITY_BUFFER_DIR, CANONICAL_MAP_FILE"` exits 0
  </acceptance_criteria>
  <done>Zero hardcoded /home/sztimhdd/ paths remain in any .py file. config.py exports ENTITY_BUFFER_DIR and CANONICAL_MAP_FILE. All scripts import paths from config.py.</done>
</task>

<task type="auto">
  <name>Task 2: Fix kg_synthesize.py bugs, bare excepts, ingest_pdf() variables, and image counter (INFRA-03, INFRA-04)</name>
  <files>kg_synthesize.py, cognee_wrapper.py, ingest_wechat.py</files>
  <read_first>
    kg_synthesize.py
    cognee_wrapper.py
    ingest_wechat.py
    .planning/codebase/CONCERNS.md
    .planning/phases/01-bug-fixes-gate-6-validation/01-CONTEXT.md
  </read_first>
  <action>
**Fix 1 — kg_synthesize.py: Add missing `import json` (INFRA-03)**

Add `import json` to the import block at the top of the file (after `import sys` on line 4). This fixes the `NameError: name 'json' is not defined` crash at line 54 when `canonical_map.json` exists.

**Fix 2 — kg_synthesize.py: Change default mode from "naive" to "hybrid" (INFRA-04)**

Line 93 currently reads:
```python
query, mode = sys.argv[1], sys.argv[2] if len(sys.argv) > 2 else "naive"
```
Change to:
```python
query, mode = sys.argv[1], sys.argv[2] if len(sys.argv) > 2 else "hybrid"
```

Also change the function signature at line 44 from:
```python
async def synthesize_response(query_text: str, mode: str = "naive"):
```
to:
```python
async def synthesize_response(query_text: str, mode: str = "hybrid"):
```

**Fix 3 — cognee_wrapper.py: Replace bare except at line 94**

Line 94 currently reads:
```python
    except: pass
```
Change to:
```python
    except Exception as e:
        logger.debug(f"log_query_pattern failed: {e}")
```

**Fix 4 — ingest_wechat.py: Replace bare except at line 158**

Line 158 currently reads:
```python
        except: pass
```
Change to:
```python
        except Exception as e:
            logger.warning(f"Could not extract publish_time: {e}") if 'logger' in dir() else None
```

Since ingest_wechat.py does not currently have a module-level logger, use a simpler approach: just use `print` to match the existing error handling style in that file:
```python
        except Exception as e:
            print(f"Warning: Could not extract publish_time: {e}")
```

**Fix 5 — ingest_wechat.py: Fix undefined variables in ingest_pdf() (lines 365-372)**

The `ingest_pdf()` function at lines 365-372 references variables from the wrong scope. Make these minimal variable-name repairs ONLY (per CONTEXT.md: do NOT fix async consistency, that is Phase 2 scope):

- Line 366: Replace `raw_entities = await extract_entities(full_content)` with `raw_entities = await extract_entities(full_text)`
- Line 367: Replace `'url': url` with `'url': file_path` (use the function parameter `file_path`)
- Add `article_hash = file_hash` before line 365 (the `file_hash` variable is already computed at line 317)
- Line 368: The hardcoded path was already fixed in Task 1. Also add `os.makedirs(ENTITY_BUFFER_DIR, exist_ok=True)` before the `with open(...)` at line 368 (create buffer dir before writing).

**Fix 6 — ingest_wechat.py: Add image download success/failure counter**

In the `ingest_article()` function, in the image download loop (around lines 250-269), add counters. Before the loop (before line 250), add:
```python
    image_success_count = 0
    image_fail_count = 0
```

Inside the loop, after the `if resp.status_code == 200:` block successfully writes and describes an image, increment `image_success_count += 1`. In the `else:` branch (line 266-267) and in the `except` branch (line 268-269), increment `image_fail_count += 1`.

After the loop ends (before line 271 "# Ingest into LightRAG"), add:
```python
    total_images = image_success_count + image_fail_count
    if total_images > 0 and image_success_count / total_images < 0.5:
        print(f"Warning: Only {image_success_count}/{total_images} images downloaded successfully (< 50% success rate)")
```
  </action>
  <verify>
    <automated>cd C:/Users/huxxha/Desktop/OmniGraph-Vault && python -c "import kg_synthesize; print('json imported:', 'json' in dir(__import__('json')))" 2>&1 | head -5; echo "---"; grep -n 'mode: str = "hybrid"' kg_synthesize.py; echo "---"; grep -n 'except:' cognee_wrapper.py ingest_wechat.py; echo "---"; grep -n 'image_success_count' ingest_wechat.py</automated>
  </verify>
  <acceptance_criteria>
    - `kg_synthesize.py` contains `import json` in the import block (top of file, before line 10)
    - `kg_synthesize.py` contains `mode: str = "hybrid"` (not "naive") in the `synthesize_response` function signature
    - `kg_synthesize.py` line 93 area contains `else "hybrid"` (not `else "naive"`)
    - `cognee_wrapper.py` does NOT contain `except: pass` (bare except replaced)
    - `cognee_wrapper.py` contains `except Exception as e:` followed by `logger.debug`
    - `ingest_wechat.py` does NOT contain `except: pass` (bare except replaced)
    - `ingest_wechat.py` ingest_pdf() function contains `full_text` (not `full_content`) in the extract_entities call
    - `ingest_wechat.py` ingest_pdf() function contains `'url': file_path` (not `'url': url`)
    - `ingest_wechat.py` ingest_pdf() function contains `article_hash = file_hash`
    - `ingest_wechat.py` contains `image_success_count` and `image_fail_count` variables
    - `ingest_wechat.py` contains `< 0.5` check with a warning print about success rate
    - `grep -n "except:" cognee_wrapper.py ingest_wechat.py` returns NO matches (zero bare excepts)
  </acceptance_criteria>
  <done>kg_synthesize.py imports json, defaults to hybrid, and uses config constant for canonical_map path. No bare except clauses remain in cognee_wrapper.py or ingest_wechat.py. ingest_pdf() references correct variable names. Image download loop tracks and reports success/failure rate.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "/home/sztimhdd/" --include="*.py" .` returns zero matches
2. `python -c "from config import ENTITY_BUFFER_DIR, CANONICAL_MAP_FILE; print(ENTITY_BUFFER_DIR, CANONICAL_MAP_FILE)"` prints paths under ~/.hermes/omonigraph-vault/
3. `python -c "import kg_synthesize"` does not crash (json is imported, config constants are resolved)
4. `grep -n "except:" cognee_wrapper.py ingest_wechat.py` returns zero matches
5. `grep -n 'mode: str = "hybrid"' kg_synthesize.py` returns a match
</verification>

<success_criteria>
- All 4 INFRA requirements addressed: constants added (INFRA-01), hardcoded paths replaced (INFRA-02), json imported (INFRA-03), default mode changed (INFRA-04)
- Adjacent fixes complete: bare excepts replaced, ingest_pdf() variable names fixed, image counter added
- No Python file in the repo contains "/home/sztimhdd/"
- All scripts import path constants from config.py
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes-gate-6-validation/01-01-SUMMARY.md`
</output>
