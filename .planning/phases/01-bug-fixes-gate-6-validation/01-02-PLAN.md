---
phase: 01-bug-fixes-gate-6-validation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - skills/omnigraph_ingest/SKILL.md
  - tests/skills/test_omnigraph_ingest.json
autonomous: false
requirements: [GATE6-01, GATE6-02, GATE6-03, GATE6-04, GATE6-05]

must_haves:
  truths:
    - "SKILL.md Case 5 rejects non-WeChat URLs with a guard message instead of running ingest_wechat.py"
    - "test_omnigraph_ingest.json contains a non-WeChat URL guard test case"
    - "skill_runner.py passes all test cases including the new non-WeChat URL guard"
    - "Manual ingestion of 3 WeChat articles completes without crash"
    - "Cross-article synthesis references entities from at least 2 of the 3 articles"
  artifacts:
    - path: "skills/omnigraph_ingest/SKILL.md"
      provides: "Updated Case 5 with non-WeChat URL guard"
      contains: "not a WeChat"
    - path: "tests/skills/test_omnigraph_ingest.json"
      provides: "9 test cases including non-WeChat URL guard"
  key_links:
    - from: "skill_runner.py"
      to: "skills/omnigraph_ingest/SKILL.md"
      via: "loads SKILL.md as system prompt"
      pattern: "load_skill"
    - from: "skill_runner.py"
      to: "tests/skills/test_omnigraph_ingest.json"
      via: "runs test cases from JSON file"
      pattern: "run_test_file"
---

<objective>
Validate the fixed pipeline through two layers: (1) skill_runner.py LLM routing test with updated SKILL.md and test cases, and (2) manual ingestion + cross-article synthesis.

Purpose: Gate 6 requires proof that the pipeline works end-to-end on this machine. The skill_runner test validates routing logic; the manual test validates real ingestion and cross-article synthesis.
Output: Updated SKILL.md with non-WeChat URL guard, expanded test file (9 cases), passing skill_runner output, manual proof of cross-article synthesis.
</objective>

<execution_context>
@C:\Users\huxxha\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\huxxha\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes-gate-6-validation/01-CONTEXT.md
@.planning/phases/01-bug-fixes-gate-6-validation/01-01-SUMMARY.md

<interfaces>
<!-- skill_runner.py test case format (from existing test file) -->
```json
{
  "description": "string — human-readable test name",
  "input": "string — the user message sent to the LLM with SKILL.md as system prompt",
  "expect_contains": ["array of strings — each must appear (case-insensitive) in LLM response"],
  "expect_not_contains": ["array of strings — each must NOT appear (case-insensitive) in LLM response"]
}
```

<!-- skill_runner.py CLI invocation -->
```bash
python skill_runner.py skills/omnigraph_ingest --test-file tests/skills/test_omnigraph_ingest.json
```
Exit code 0 = all tests pass. Exit code 1 = at least one failure.

<!-- Current SKILL.md Case 5 (to be replaced) -->
```markdown
### Case 5: User provides a URL but it is not a WeChat article

Still run `ingest_wechat.py` — it handles general web URLs as well, not just WeChat.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md Case 5 and add non-WeChat URL guard test case (D-07, D-08)</name>
  <files>skills/omnigraph_ingest/SKILL.md, tests/skills/test_omnigraph_ingest.json</files>
  <read_first>
    skills/omnigraph_ingest/SKILL.md
    tests/skills/test_omnigraph_ingest.json
    .planning/phases/01-bug-fixes-gate-6-validation/01-CONTEXT.md
  </read_first>
  <action>
**Step 1 — Update SKILL.md Case 5 to reject non-WeChat URLs (per D-08)**

Replace the current Case 5 section in `skills/omnigraph_ingest/SKILL.md`:

Current (lines ~65-67):
```markdown
### Case 5: User provides a URL but it is not a WeChat article

Still run `ingest_wechat.py` — it handles general web URLs as well, not just WeChat.
```

Replace with:
```markdown
### Case 5: User provides a URL but it is not a WeChat article

Respond: "This skill currently supports WeChat article URLs (mp.weixin.qq.com) and local PDF files only. The URL you provided is not a WeChat article. General web scraping support is planned for a future update."
Do not run `ingest_wechat.py` for non-WeChat URLs.
```

Do NOT change any other Case, trigger, frontmatter, or section of the file.

**Step 2 — Add non-WeChat URL guard test case to test file (per D-07)**

Read the existing `tests/skills/test_omnigraph_ingest.json` (8 test cases). Add the following test case as the 9th entry in the array (append before the closing `]`):

```json
{
  "description": "non-WeChat URL guard: rejects generic URLs with guidance",
  "input": "ingest this article: https://arxiv.org/abs/2301.00001",
  "expect_contains": ["WeChat", "mp.weixin.qq.com"],
  "expect_not_contains": ["ingest_wechat.py"]
}
```

This test case verifies that when a non-WeChat URL is provided, the SKILL.md instructions cause the LLM to:
- Mention "WeChat" and "mp.weixin.qq.com" (explaining the limitation)
- NOT suggest running ingest_wechat.py
  </action>
  <verify>
    <automated>cd C:/Users/huxxha/Desktop/OmniGraph-Vault && python -c "import json; cases = json.load(open('tests/skills/test_omnigraph_ingest.json')); print(f'{len(cases)} test cases'); print(cases[-1]['description'])" && grep -c "not a WeChat" skills/omnigraph_ingest/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `skills/omnigraph_ingest/SKILL.md` Case 5 contains "not a WeChat article" and "Do not run"
    - `skills/omnigraph_ingest/SKILL.md` Case 5 does NOT contain "Still run `ingest_wechat.py`"
    - `tests/skills/test_omnigraph_ingest.json` contains exactly 9 test case objects
    - The 9th test case has `"description"` containing "non-WeChat URL guard"
    - The 9th test case has `"expect_contains"` with "WeChat" and "mp.weixin.qq.com"
    - The 9th test case has `"expect_not_contains"` with "ingest_wechat.py"
    - `python -c "import json; json.load(open('tests/skills/test_omnigraph_ingest.json'))"` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>SKILL.md Case 5 guards against non-WeChat URLs. Test file has 9 cases including the new guard case. JSON is valid and parseable.</done>
</task>

<task type="auto">
  <name>Task 2: Run skill_runner.py validation (GATE6-05)</name>
  <files></files>
  <read_first>
    skill_runner.py
    skills/omnigraph_ingest/SKILL.md
    tests/skills/test_omnigraph_ingest.json
  </read_first>
  <action>
Run the skill_runner.py test suite to validate the ingest skill's decision tree routing:

```bash
python skill_runner.py skills/omnigraph_ingest --test-file tests/skills/test_omnigraph_ingest.json --verbose
```

This calls Gemini API with SKILL.md as the system prompt and each test case's input as the user message. It checks that the LLM response contains/excludes the expected strings.

**Expected result:** 9/9 tests pass, exit code 0.

**If any test fails:**
- Read the failure output to understand what the LLM responded
- If the failure is in the new Case 5 test, check that SKILL.md wording is unambiguous enough for the LLM to follow
- Adjust SKILL.md Case 5 wording if needed (make the instruction more explicit) and re-run
- Do NOT change the test expectations unless they are genuinely wrong
- Re-run until 9/9 pass

**Important:** This task requires GEMINI_API_KEY to be set in the environment (loaded from ~/.hermes/.env by config.py). If the key is missing, the test will fail with a clear error message. Do not proceed without it.
  </action>
  <verify>
    <automated>cd C:/Users/huxxha/Desktop/OmniGraph-Vault && python skill_runner.py skills/omnigraph_ingest --test-file tests/skills/test_omnigraph_ingest.json</automated>
  </verify>
  <acceptance_criteria>
    - `python skill_runner.py skills/omnigraph_ingest --test-file tests/skills/test_omnigraph_ingest.json` exits with code 0
    - Output shows "9/9 passed"
    - No FAIL lines in the output
  </acceptance_criteria>
  <done>skill_runner.py passes all 9 test cases for the ingest skill decision tree, including the new non-WeChat URL guard. Exit code is 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual pipeline validation — ingest 3 articles + cross-article synthesis (GATE6-01..04)</name>
  <files></files>
  <action>
CHECKPOINT: Human verification required. All code fixes from Plan 01-01 are applied and skill_runner routing tests pass. This checkpoint validates the real end-to-end flow by having the user ingest actual articles and run a cross-article synthesis query.

The user must perform 4 steps:

Step 1 (GATE6-01, GATE6-04): Ingest 3 WeChat articles with shared named entities. Run `python ingest_wechat.py "<URL>"` for each. Verify each exits cleanly with "Successfully Ingested!" and no NameError or path error.

Step 2 (GATE6-02): Run `python cognee_batch_processor.py`. Verify it produces `~/.hermes/omonigraph-vault/canonical_map.json`.

Step 3 (GATE6-03): Run `python kg_synthesize.py "What are the key concepts and tools discussed across the articles I ingested?" hybrid`. Verify synthesis references entities from at least 2 of the 3 articles.

Step 4: Run `grep -rn "/home/sztimhdd/" --include="*.py" .` and confirm zero matches.
  </action>
  <verify>
    <automated>cd C:/Users/huxxha/Desktop/OmniGraph-Vault && grep -rn "/home/sztimhdd/" --include="*.py" . | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 3 `ingest_wechat.py` invocations exit 0 with "Successfully Ingested!"
    - `cognee_batch_processor.py` completes and `~/.hermes/omonigraph-vault/canonical_map.json` exists
    - `kg_synthesize.py` produces synthesis output referencing entities from >= 2 articles
    - `grep -rn "/home/sztimhdd/" --include="*.py" .` returns 0 matches
  </acceptance_criteria>
  <done>Manual end-to-end pipeline validation complete: 3 articles ingested, batch processor ran, cross-article synthesis produced multi-source answer, no hardcoded paths remain.</done>
</task>

</tasks>

<verification>
Gate 6 is validated when:
1. skill_runner.py exits 0 with 9/9 tests passed (automated, Task 2)
2. 3 WeChat articles ingested without crash (manual, Task 3 Step 1)
3. cognee_batch_processor.py produces canonical_map.json (manual, Task 3 Step 2)
4. Cross-article synthesis references entities from >= 2 articles (manual, Task 3 Step 3)
5. No hardcoded paths remain (automated grep, Task 3 Step 4)
</verification>

<success_criteria>
- SKILL.md Case 5 rejects non-WeChat URLs with clear guidance
- test_omnigraph_ingest.json has 9 test cases, all passing in skill_runner
- skill_runner.py exits 0 (GATE6-05)
- Manual pipeline run confirms no crash on ingest + synthesis (GATE6-01, GATE6-04)
- canonical_map.json produced by batch processor (GATE6-02)
- Synthesis response references cross-article entities (GATE6-03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes-gate-6-validation/01-02-SUMMARY.md`
</output>
