---
phase: 07-model-key-management
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - cognee_wrapper.py
  - cognee_batch_processor.py
  - enrichment/extract_questions.py
  - init_cognee.py
  - setup_cognee.py
autonomous: false
decisions_addressed: [D-03]
requirements: [D-03]
must_haves:
  truths:
    - "cognee_wrapper.py initializes llm_config.llm_api_key via current_key() (NOT direct os.environ read)"
    - "cognee_wrapper.py still defines remember_article / remember_synthesis / recall_previous_context / disambiguate_entities with the SAME signatures and the SAME fire-and-forget semantics (Phase 4 convention preserved)"
    - "cognee_batch_processor.py polls entity_buffer/ and writes canonical_map.json atomically — behavior unchanged; only Gemini plumbing swapped"
    - "enrichment/extract_questions.py uses lib.generate_sync (or lib.generate if already async) — config.gemini_call references gone"
    - "init_cognee.py + setup_cognee.py use current_key() for any llm_api_key setup"
    - "After this wave, cognee_bridge observer chain is live end-to-end: rotate_key → on_rotate listener → propagate_key_to_cognee → Cognee singleton updated"
    - "Full test suite green; kg_synthesize.py smoke run exercises the Cognee bridge and succeeds"
  artifacts:
    - path: "cognee_wrapper.py"
      provides: "Wrapped Cognee API (remember/recall/disambiguate) — Phase 4 fire-and-forget semantics preserved"
      contains: "llm_config.llm_api_key = current_key()"
    - path: "cognee_batch_processor.py"
      provides: "Entity buffer async canonicalizer via lib/"
      contains: "from lib import"
    - path: "enrichment/extract_questions.py"
      provides: "Per-article question extraction via lib/"
      contains: "from lib import"
    - path: "init_cognee.py"
      provides: "Cognee DB init via lib/-sourced keys"
      contains: "current_key()"
    - path: "setup_cognee.py"
      provides: "Cognee setup script via lib/-sourced keys"
      contains: "current_key()"
  key_links:
    - from: "cognee_wrapper.py"
      to: "lib.current_key + lib.cognee_bridge (already wired in Wave 0)"
      via: "Initial llm_config.llm_api_key = current_key(); rotation updates handled by on_rotate listener registered at lib/__init__.py import"
      pattern: "llm_config.llm_api_key = current_key"
    - from: "cognee_batch_processor.py"
      to: "lib.generate_sync or lib.generate"
      via: "entity canonicalization LLM calls"
      pattern: "from lib import"
---

<objective>
Migrate Cognee-adjacent P1 files and complete the Cognee rotation loop end-to-end. After this wave, the `rotate_key → on_rotate listener → propagate_key_to_cognee → Cognee singleton` chain is exercised by real production code (cognee_wrapper init + cognee_batch_processor batches).

Purpose: P1 files handle supporting flows (entity canonicalization, question extraction for Zhihu enrichment). They're lower-blast-radius than P0 but validate the Cognee bridge module against real Cognee usage patterns before Wave 4 cleanup.

Output: 5 files migrated, one commit each. kg_synthesize.py smoke run exercises bridge rotation. Final task is a checkpoint that pauses for end-to-end wave acknowledgement (MED 2 resolution — autonomous flipped to false accordingly).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-model-key-management/07-CONTEXT.md
@.planning/phases/07-model-key-management/07-RESEARCH.md
@.planning/phases/07-model-key-management/07-02-PLAN.md
@.planning/phases/07-model-key-management/07-02-SUMMARY.md
@ingest_wechat.py
@kg_synthesize.py
@lib/__init__.py
@lib/cognee_bridge.py
@lib/llm_client.py
@cognee_wrapper.py
@cognee_batch_processor.py
@enrichment/extract_questions.py
@init_cognee.py
@setup_cognee.py

<interfaces>
<!-- Cognee init pattern — established in kg_synthesize.py Wave 2 -->

```python
from lib import current_key
from cognee.infrastructure.llm.config import get_llm_config

llm_config = get_llm_config()
llm_config.llm_api_key = current_key()   # initial; rotation auto-propagated via on_rotate listener
```

<!-- Phase 4 Cognee fire-and-forget preservation — DO NOT change -->

```python
# cognee_wrapper.remember_article / remember_synthesis
# Called as: asyncio.create_task(cognee_wrapper.remember_article(...))
# OR with timeout: await asyncio.wait_for(remember_article(...), timeout=5.0)
# The SIGNATURE of these functions must not change — only their internal LLM plumbing.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Migrate cognee_wrapper.py — preserve Phase 4 fire-and-forget semantics</name>
  <files>cognee_wrapper.py</files>
  <read_first>
    - cognee_wrapper.py (full contents — 9 Gemini touchpoints; line 41 is the reference pattern for `llm_config.llm_api_key = GEMINI_API_KEY`)
    - kg_synthesize.py (Wave 2 reference — shows the `llm_config.llm_api_key = current_key()` pattern applied once)
    - .planning/phases/07-model-key-management/07-CONTEXT.md § code_context § "cognee_wrapper.py (9 Gemini touchpoints)"
    - .planning/phases/07-model-key-management/07-RESEARCH.md § Cognee Interop Strategy (why the initial assignment is needed even though cognee_bridge handles rotation — rotation only fires AFTER the first rotate_key call, so the initial value must be set at init)
  </read_first>
  <action>
    Surgical migration. Function signatures for `remember_article`, `remember_synthesis`, `recall_previous_context`, `disambiguate_entities` are Phase 4 contract — do NOT change them. Only swap the Gemini plumbing inside.

    Step A — Imports:
    ```python
    from lib import INGESTION_LLM, current_key, generate, generate_sync, get_limiter
    ```

    Step B — Update the init pattern (around line 41):
    ```python
    # BEFORE:
    llm_config.llm_api_key = GEMINI_API_KEY
    # AFTER:
    llm_config.llm_api_key = current_key()
    ```
    Rationale: cognee_bridge's on_rotate listener keeps this in sync on rotation; the initial value must still be set explicitly before any rotation happens.

    Step C — Remove the module-level `GEMINI_API_KEY = os.environ.get("GEMINI_API_KEY", "")` constant (if present at the top of cognee_wrapper.py — mimics the config.py pattern). Replace any internal references to it with `current_key()`.

    Step D — For each of the 9 Gemini touchpoints in this file (grep to find them):
    - If it's a direct `genai.Client(api_key=...)` instantiation: replace `api_key=...` with `api_key=current_key()`, and wrap the call with `async with get_limiter(INGESTION_LLM):` if async.
    - If it's a `config.gemini_call(...)` reference: replace with `generate_sync(INGESTION_LLM, ...)` (or `await generate(INGESTION_LLM, ...)` if the call site is async).
    - If it's a hardcoded model string: replace with `INGESTION_LLM`.

    Step E — PRESERVE:
    - `remember_article`, `remember_synthesis`, `recall_previous_context`, `disambiguate_entities` function signatures (Phase 4 contract)
    - Fire-and-forget patterns (`asyncio.create_task`, `timeout=5.0` kwargs on internal calls)
    - The `_disambiguation_cache = {}` module-level dict (Phase 4 state)
    - Any `os.environ["LLM_PROVIDER"] = "gemini"` / `os.environ["LLM_MODEL"] = ...` env mutations that happen BEFORE `import cognee` — these are Cognee config handshake; leave them alone (they're not Gemini API auth)

    Step F — Run: `venv/Scripts/python -m pytest tests/ -v` + the verify_gate scripts as smoke:
    ```
    venv/Scripts/python -c "import cognee_wrapper; print('ok')"
    ```

    Commit: `refactor(07-03): migrate cognee_wrapper.py to lib/ (Phase 4 semantics preserved)`
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import cognee_wrapper; print('ok')" && venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from lib import" cognee_wrapper.py` matches
    - `grep "llm_config.llm_api_key = current_key()" cognee_wrapper.py` matches (init pattern updated)
    - `grep "GEMINI_API_KEY = os.environ" cognee_wrapper.py` returns ZERO matches (module constant removed)
    - `grep -E '"gemini-(2\.5|3\.1|embedding)' cognee_wrapper.py` returns ZERO matches (no hardcoded model strings)
    - `grep 'os.environ\["GEMINI_API_KEY"\]\|os.environ.get("GEMINI_API_KEY")' cognee_wrapper.py` returns ZERO matches
    - `grep "def remember_article\|def remember_synthesis\|def recall_previous_context\|def disambiguate_entities" cognee_wrapper.py` returns >= 4 matches (Phase 4 signatures preserved)
    - `grep "_disambiguation_cache" cognee_wrapper.py` still matches (Phase 4 state preserved)
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>cognee_wrapper.py migrated, Phase 4 semantics preserved, tests green.</done>
</task>

<task type="auto">
  <name>Task 3.2: Migrate cognee_batch_processor.py</name>
  <files>cognee_batch_processor.py</files>
  <read_first>
    - cognee_batch_processor.py (full contents; 7 Gemini touchpoints; runs as daemon-like poller)
    - cognee_wrapper.py (just migrated — same pattern for llm_config.llm_api_key init)
    - .planning/phases/07-model-key-management/07-CONTEXT.md § code_context (confirm atomic writes for canonical_map.json are not touched)
  </read_first>
  <action>
    Similar pattern to cognee_wrapper.py. This file runs as a background batch processor polling `entity_buffer/`.

    Step A — Imports:
    ```python
    from lib import INGESTION_LLM, current_key, generate_sync, get_limiter
    ```

    Step B — If the file has a `llm_config.llm_api_key = ...` init assignment, update to `current_key()`.

    Step C — For each Gemini touchpoint:
    - `config.gemini_call(...)` → `generate_sync(INGESTION_LLM, ...)`
    - Direct `genai.Client(api_key=...)` → `api_key=current_key()` + `async with get_limiter(INGESTION_LLM):` if async
    - Hardcoded model string → `INGESTION_LLM`

    Step D — PRESERVE:
    - Atomic write pattern for `canonical_map.json` (`.tmp` + `os.rename`) — CLAUDE.md rule
    - `.processed` marker writes (Phase idempotency convention)
    - `entity_buffer/` polling logic
    - FileHandler logging to `cognee_batch.log`

    Step E — Remove the top-level `GEMINI_API_KEY = os.environ.get("GEMINI_API_KEY", "")` if present.

    Commit: `refactor(07-03): migrate cognee_batch_processor.py to lib/`
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import cognee_batch_processor; print('ok')" && venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from lib import" cognee_batch_processor.py` matches
    - `grep "GEMINI_API_KEY = os.environ" cognee_batch_processor.py` returns ZERO matches
    - `grep -E '"gemini-(2\.5|3\.1|embedding)' cognee_batch_processor.py` returns ZERO matches
    - `grep 'os.environ\["GEMINI_API_KEY"\]\|os.environ.get("GEMINI_API_KEY")' cognee_batch_processor.py` returns ZERO matches
    - `grep "config.gemini_call\|from config import gemini_call" cognee_batch_processor.py` returns ZERO matches
    - `grep "\.processed" cognee_batch_processor.py` still matches (idempotency preserved)
    - `grep "\.tmp" cognee_batch_processor.py` still matches OR `grep "os.rename\|Path.rename" cognee_batch_processor.py` matches (atomic write preserved)
    - `grep "canonical_map" cognee_batch_processor.py` still matches (core function preserved)
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>cognee_batch_processor.py migrated, atomic writes + idempotency preserved.</done>
</task>

<task type="auto">
  <name>Task 3.3: Migrate enrichment/extract_questions.py</name>
  <files>enrichment/extract_questions.py</files>
  <read_first>
    - enrichment/extract_questions.py (full contents; 1 Gemini touchpoint — uses config.gemini_call per scope map)
    - tests/unit/test_extract_questions.py (understand mocks; D-06 says migrate patch targets as part of Wave 4, but if current test breaks, fix in this commit)
    - enrichment/fetch_zhihu.py (Wave 2 reference — same small-file pattern)
  </read_first>
  <action>
    Single Gemini touchpoint; simplest migration in this wave.

    Step A — Imports:
    ```python
    from lib import INGESTION_LLM, generate_sync
    ```
    (Or `from lib import ENRICHMENT_LLM, generate_sync` if the file is specifically for enrichment — both constants default to `gemini-2.5-flash-lite` so the choice is semantic. Use `ENRICHMENT_LLM` for semantic clarity.)

    Step B — Swap the one `config.gemini_call(...)` call for `generate_sync(ENRICHMENT_LLM, ...)`. Preserve grounding kwargs (per Phase 4 D-12: Gemini 2.5 Flash Lite + grounding).

    Step C — If test at tests/unit/test_extract_questions.py patches `config.gemini_call` or `google.genai.Client`: run it — if it fails, update the patch target to `lib.llm_client.generate_sync`. Fix in the same commit.

    Commit: `refactor(07-03): migrate enrichment/extract_questions.py to lib/`
  </action>
  <verify>
    <automated>venv/Scripts/python -m pytest tests/unit/test_extract_questions.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from lib import" enrichment/extract_questions.py` matches
    - `grep "config.gemini_call\|from config import gemini_call" enrichment/extract_questions.py` returns ZERO matches
    - `grep -E '"gemini-(2\.5|3\.1)' enrichment/extract_questions.py` returns ZERO matches
    - `venv/Scripts/python -m pytest tests/unit/test_extract_questions.py -v` exits 0
  </acceptance_criteria>
  <done>extract_questions.py migrated, extract_questions tests green.</done>
</task>

<task type="auto">
  <name>Task 3.4: Migrate init_cognee.py + setup_cognee.py</name>
  <files>init_cognee.py, setup_cognee.py</files>
  <read_first>
    - init_cognee.py (full contents; small setup script)
    - setup_cognee.py (full contents; small setup script)
    - cognee_wrapper.py (just migrated — init pattern reference)
  </read_first>
  <action>
    Two small scripts; one commit each (D-03 per-file commits).

    Commit 3.4a — init_cognee.py:
    - Add `from lib import current_key`
    - Replace any `llm_config.llm_api_key = os.environ["GEMINI_API_KEY"]` or `= GEMINI_API_KEY` with `= current_key()`
    - Remove module-level `GEMINI_API_KEY = os.environ.get(...)` if present
    - Preserve all `os.environ["LLM_PROVIDER"] = "gemini"` / `os.environ["LLM_MODEL"] = ...` setup assignments (Cognee handshake, not API auth)
    - Commit: `refactor(07-03): migrate init_cognee.py to lib/`

    Commit 3.4b — setup_cognee.py:
    - Same treatment.
    - Commit: `refactor(07-03): migrate setup_cognee.py to lib/`

    Run smoke: `venv/Scripts/python -c "import init_cognee, setup_cognee; print('ok')"` — both must import without error.
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import init_cognee; print('ok')" && venv/Scripts/python -c "import setup_cognee; print('ok')" && venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from lib import" init_cognee.py` matches
    - `grep "from lib import" setup_cognee.py` matches
    - `grep "current_key" init_cognee.py` matches (at least in the llm_api_key assignment if one exists)
    - `grep "current_key" setup_cognee.py` matches (at least in the llm_api_key assignment if one exists)
    - `grep 'os.environ\["GEMINI_API_KEY"\]\|os.environ.get("GEMINI_API_KEY")' init_cognee.py setup_cognee.py` returns ZERO matches
    - `grep 'os.environ\["LLM_PROVIDER"\]\|os.environ\["LLM_MODEL"\]' init_cognee.py setup_cognee.py` still matches (Cognee handshake preserved)
    - `venv/Scripts/python -c "import init_cognee, setup_cognee; print('ok')"` succeeds
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>Both Cognee setup scripts migrated; Cognee handshake env mutations preserved.</done>
</task>

<task type="checkpoint:wave3-complete" gate="blocking">
  <name>Task 3.5: CHECKPOINT — Wave 3 complete; Cognee bridge rotation loop live end-to-end</name>
  <files>.planning/phases/07-model-key-management/07-03-SUMMARY.md</files>
  <action>Execute the <how-to-verify> steps below in order (integration test → full suite → optional live smoke → write SUMMARY → present wave-complete prompt to user and pause for resume-signal). No source-code changes this task.</action>
  <what-built>
    Wave 3 migrated 5 P1 files (cognee_wrapper, cognee_batch_processor, extract_questions, init_cognee, setup_cognee) to lib/. The Cognee bridge observer chain (rotate_key → on_rotate listener → propagate_key_to_cognee → Cognee @lru_cache'd singleton) is now exercised by real production code. The integration test from Wave 0 (tests/integration/test_cognee_rotation.py) covers the chain; the live smoke (python kg_synthesize.py "what do I know about agents" hybrid) exercises it against a real KG.
  </what-built>
  <how-to-verify>
    Step A — Integration test: `venv/Scripts/python -m pytest tests/integration/test_cognee_rotation.py -v` — MUST exit 0.

    Step B — Full test suite: `venv/Scripts/python -m pytest tests/ -v` — MUST exit 0.

    Step C — (OPTIONAL if live env available) Manual smoke: `venv/Scripts/python kg_synthesize.py "what do I know about agents" hybrid`. Expect log line `Recalled previous context` from cognee_wrapper.recall_previous_context + a synthesis result. If the smoke requires live Gemini + real Cognee DB and the environment isn't set up, note as deferred to remote PC (per 07-VALIDATION.md Manual-Only Verifications row "End-to-end kg_synthesize + Cognee rotation").

    Step D — Write `.planning/phases/07-model-key-management/07-03-SUMMARY.md` capturing:
    - Commits landed (5 expected: cognee_wrapper, cognee_batch_processor, extract_questions, init_cognee, setup_cognee)
    - Integration test result
    - Whether live kg_synthesize smoke ran (and if not, why deferred)
    - Any friction discovered in the bridge chain (listener didn't fire, cache not cleared, etc.)

    Then present the following to the user for sign-off:
    - "Wave 3 migration complete. 5 P1 files in lib/. Cognee rotation loop validated via integration test (optionally live smoke)."
    - "Proceed to Wave 4 (batch scripts, skill_runner, verify_gate tests, SKILL.md updates, deploy docs)?"
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 4, or describe issues (e.g., listener didn't fire, specific test failure, live smoke surfaced drift)</resume-signal>
  <verify>
    <automated>venv/Scripts/python -m pytest tests/integration/test_cognee_rotation.py tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/phases/07-model-key-management/07-03-SUMMARY.md` exists
    - `grep "Integration test" .planning/phases/07-model-key-management/07-03-SUMMARY.md` matches
    - `venv/Scripts/python -m pytest tests/integration/test_cognee_rotation.py -v` exits 0
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
    - User has acknowledged wave completion (checkpoint gate resumed with "approved" or equivalent)
  </acceptance_criteria>
  <done>Wave 3 SUMMARY written, Cognee rotation loop confirmed live, user has acknowledged wave completion before Wave 4 begins.</done>
</task>

</tasks>

<verification>
Wave 3 gate: 5 P1 files migrated in separate commits; Cognee rotation loop exercised end-to-end via integration test and (if possible) live kg_synthesize smoke. Checkpoint task blocks Wave 4 start until user approves (MED 2).

Maps to 07-VALIDATION.md rows: "Wave 3 parity: Cognee rotation end-to-end", "End-to-end kg_synthesize + Cognee rotation (manual)".
</verification>

<success_criteria>
- [ ] 5 P1 files migrated in 5 commits
- [ ] cognee_wrapper.py Phase 4 function signatures + fire-and-forget semantics preserved
- [ ] cognee_batch_processor.py atomic-write + idempotency patterns preserved
- [ ] Cognee handshake env mutations (LLM_PROVIDER/LLM_MODEL) preserved in init_cognee.py + setup_cognee.py
- [ ] `tests/integration/test_cognee_rotation.py` green (from Wave 0 fixtures)
- [ ] Full pytest suite green
- [ ] 07-03-SUMMARY.md written
- [ ] Checkpoint task (Task 3.5) reached and user approved before Wave 4 starts
</success_criteria>

<output>
After completion, create `.planning/phases/07-model-key-management/07-03-SUMMARY.md` recording:
- Commit SHAs (5 total)
- Integration test result
- Whether live kg_synthesize.py smoke ran (or why deferred)
- Any test patch target updates required by D-06 migration inside this wave
- Deferred drift discovered (to pick up in Wave 4 P2 cleanup)
- Checkpoint resumption signal received (value of <resume-signal> the user typed)
</output>
</content>
</invoke>