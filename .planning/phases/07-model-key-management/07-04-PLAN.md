---
phase: 07-model-key-management
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - batch_classify_kol.py
  - batch_ingest_from_spider.py
  - batchkol_topic.py
  - _reclassify.py
  - skill_runner.py
  - tests/verify_gate_a.py
  - tests/verify_gate_b.py
  - tests/verify_gate_c.py
  - skills/omnigraph_ingest/SKILL.md
  - skills/omnigraph_query/SKILL.md
  - skills/omnigraph_architect/SKILL.md
  - Deploy.md
  - .env.template
  - CLAUDE.md
autonomous: true
decisions_addressed: [D-03, D-04, D-06, D-07]
requirements: [D-03, D-04, D-06, D-07]
must_haves:
  truths:
    - "All batch scripts + _reclassify.py + skill_runner.py use lib/ for model + key + rate limit concerns (zero direct GEMINI_API_KEY reads, zero hardcoded model strings except skill_runner's _GEMINI_MODEL test-harness literal per 07-RESEARCH Open Question #4)"
    - "tests/verify_gate_a.py / verify_gate_b.py / verify_gate_c.py — patch targets updated to lib.llm_client level per D-06 where needed; old per-call-site patches migrated"
    - "All 3 Hermes skills (omnigraph_ingest, omnigraph_query, omnigraph_architect) have SKILL.md frontmatter declaring OMNIGRAPH_GEMINI_KEY as required_environment_variables with GEMINI_API_KEY documented as fallback in help text (D-07)"
    - "All 3 Hermes skills declare OpenClaw metadata block with primaryEnv: OMNIGRAPH_GEMINI_KEY and skillKey: omnigraph-vault (D-07)"
    - "Deploy.md contains OMNIGRAPH_* environment variable table covering OMNIGRAPH_GEMINI_KEY, OMNIGRAPH_GEMINI_KEYS, OMNIGRAPH_MODEL_*, OMNIGRAPH_RPM_*"
    - ".env.template contains OMNIGRAPH_GEMINI_KEY=, OMNIGRAPH_GEMINI_KEYS=, and notes GEMINI_API_KEY as fallback"
    - "CLAUDE.md Environment Variables section adds a paragraph describing OMNIGRAPH_GEMINI_KEY / _KEYS / _MODEL_* / _RPM_* with fallback behavior"
    - "No production file outside lib/ contains a 'gemini-*' hardcoded model string (skill_runner exception documented)"
    - "No production file outside lib/api_keys.py contains an os.environ read for GEMINI_API_KEY / GEMINI_API_KEY_BACKUP / OMNIGRAPH_GEMINI_KEY / OMNIGRAPH_GEMINI_KEYS"
    - "D-11 config.py shims may remain indefinitely (acceptable per D-11); Wave 4 does NOT sweep them unless explicitly requested"
    - "D-06 migration sweep complete for any remaining per-call-site patches that weren't fixed during Waves 1-3"
    - "Full test suite green; skill_runner suite green; deploy story is one coherent read (Deploy.md + .env.template + SKILL.md all agree)"
  artifacts:
    - path: "batch_classify_kol.py"
      provides: "KOL batch classification via lib/"
      contains: "from lib import"
    - path: "batch_ingest_from_spider.py"
      provides: "DB-first batch ingest via lib/"
      contains: "from lib import"
    - path: "batchkol_topic.py"
      provides: "Topic-filtered batch via lib/"
      contains: "from lib import"
    - path: "_reclassify.py"
      provides: "Re-classification helper via lib/"
      contains: "from lib import"
    - path: "skill_runner.py"
      provides: "Test harness — tests at lib level per D-06; keeps _GEMINI_MODEL literal for test independence per 07-RESEARCH Open Question #4"
      contains: "from lib import"
    - path: "tests/verify_gate_a.py"
      provides: "Gate A Cognee remember() verification via lib mocks"
      contains: "lib.llm_client"
    - path: "tests/verify_gate_b.py"
      provides: "Gate B Cognee recall+search verification via lib mocks"
      contains: "lib.llm_client"
    - path: "tests/verify_gate_c.py"
      provides: "Gate C entity disambiguation verification via lib mocks"
      contains: "lib.llm_client"
    - path: "skills/omnigraph_ingest/SKILL.md"
      provides: "Dual-host metadata: Hermes required_environment_variables + OpenClaw primaryEnv for OMNIGRAPH_GEMINI_KEY"
      contains: "OMNIGRAPH_GEMINI_KEY"
    - path: "skills/omnigraph_query/SKILL.md"
      provides: "Dual-host metadata for omnigraph_query"
      contains: "OMNIGRAPH_GEMINI_KEY"
    - path: "skills/omnigraph_architect/SKILL.md"
      provides: "Dual-host metadata for omnigraph_architect"
      contains: "OMNIGRAPH_GEMINI_KEY"
    - path: "Deploy.md"
      provides: "OMNIGRAPH_* env var table for deploy story"
      contains: "OMNIGRAPH_GEMINI_KEY"
    - path: ".env.template"
      provides: "Example env file with OMNIGRAPH_GEMINI_KEY + OMNIGRAPH_GEMINI_KEYS + fallback note"
      contains: "OMNIGRAPH_GEMINI_KEY"
    - path: "CLAUDE.md"
      provides: "Project-level env var convention paragraph"
      contains: "OMNIGRAPH_GEMINI_KEY"
  key_links:
    - from: "skills/*/SKILL.md"
      to: "lib.api_keys precedence chain"
      via: "Hermes/OpenClaw declares OMNIGRAPH_GEMINI_KEY; lib.api_keys.load_keys reads it first"
      pattern: "OMNIGRAPH_GEMINI_KEY"
    - from: "Deploy.md"
      to: "~/.hermes/.env env var documentation"
      via: "deploy-time env table"
      pattern: "OMNIGRAPH_GEMINI_KEY"
---

<objective>
Finish the migration: P2 batch scripts (split into 4 separate tasks per D-03 per-file commits — MED 3 resolution), verify_gate tests, skill_runner test harness, 3 Hermes skill SKILL.md frontmatters (D-07), and the deploy story (Deploy.md + .env.template + CLAUDE.md).

Purpose: After this wave, the codebase is internally consistent (all callers use lib/), test fixtures follow D-06 (mock at lib.llm_client level), and the deploy story is a single coherent read — a user can set `OMNIGRAPH_GEMINI_KEY` in their env and every script, skill, and test picks it up automatically.

Output: 14 files modified across 4 categories (batch scripts as 4 separate tasks, skill_runner, verify_gate tests, skills+docs), one commit per file (per D-03). Deploy story complete. D-11 config.py shims left in place indefinitely (acceptable per D-11; sweeping them is out of scope for this wave unless flagged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-model-key-management/07-CONTEXT.md
@.planning/phases/07-model-key-management/07-RESEARCH.md
@.planning/phases/07-model-key-management/07-03-PLAN.md
@.planning/phases/07-model-key-management/07-03-SUMMARY.md
@ingest_wechat.py
@lib/__init__.py
@lib/api_keys.py
@batch_classify_kol.py
@batch_ingest_from_spider.py
@batchkol_topic.py
@_reclassify.py
@skill_runner.py
@tests/verify_gate_a.py
@tests/verify_gate_b.py
@tests/verify_gate_c.py
@tests/conftest.py
@skills/omnigraph_ingest/SKILL.md
@skills/omnigraph_query/SKILL.md
@skills/omnigraph_architect/SKILL.md
@Deploy.md
@CLAUDE.md

<interfaces>
<!-- Hermes SKILL.md frontmatter pattern (D-07 target) — per 07-REQUIREMENTS.md §2.1 and §4.5 -->

```yaml
  ---
name: omnigraph_ingest
description: |
  <existing description — preserve>
compatibility: |
  <existing text — preserve>

# --- Hermes ---
required_environment_variables:
  - name: OMNIGRAPH_GEMINI_KEY
    prompt: "Gemini API key for OmniGraph-Vault (get from https://aistudio.google.com/apikey)"
    help: "Required for LLM, embedding, and vision calls. Falls back to GEMINI_API_KEY if unset; for multi-account rotation set OMNIGRAPH_GEMINI_KEYS (comma-separated)."
    required_for: full functionality

# --- OpenClaw ---
metadata:
  openclaw:
    skillKey: omnigraph-vault
    primaryEnv: OMNIGRAPH_GEMINI_KEY
    os: ["darwin", "linux", "win32"]
    requires:
      bins: ["bash", "python"]
      config: ["OMNIGRAPH_GEMINI_KEY"]
  ---
```

<!-- .env.template target -->

```
# Primary Gemini API key (scoped to OmniGraph-Vault)
OMNIGRAPH_GEMINI_KEY=

# Optional: comma-separated pool for multi-account rotation.
# Only useful across different Google accounts / GCP projects
# (Gemini quotas are per-project, not per-key).
OMNIGRAPH_GEMINI_KEYS=

# Legacy fallback — still works if OMNIGRAPH_GEMINI_KEY is unset.
GEMINI_API_KEY=

# Optional: override default models (greppable constants in lib/models.py)
# OMNIGRAPH_MODEL_INGESTION_LLM=gemini-2.5-flash-lite
# OMNIGRAPH_MODEL_VISION_LLM=gemini-2.5-flash-lite
# OMNIGRAPH_MODEL_SYNTHESIS_LLM=gemini-2.5-flash-lite
# OMNIGRAPH_MODEL_EMBEDDING=gemini-embedding-2
# OMNIGRAPH_MODEL_GITHUB_INGEST=gemini-3.1-flash-lite-preview

# Optional: override per-model RPM caps on paid tier
# OMNIGRAPH_RPM_GEMINI_2_5_FLASH_LITE=300
```
</interfaces>

<batch_migration_pattern>
<!-- Shared pattern used by Tasks 4.1a / 4.1b / 4.1c / 4.1d (MED 3 split — was one monolithic Task 4.1) -->

For each file, apply:

Step A — Imports (tailor per file):
```python
from lib import INGESTION_LLM, current_key, generate_sync, get_limiter
```

Step B — Swap `config.gemini_call(...)` → `generate_sync(INGESTION_LLM, ...)`.

Step C — Direct `genai.Client(api_key=...)` calls:
```python
# BEFORE:
client = genai.Client(api_key=os.environ["GEMINI_API_KEY"])
response = client.models.generate_content(model="gemini-2.5-flash-lite", contents=prompt)
# AFTER (sync, preferred):
response_text = generate_sync(INGESTION_LLM, prompt)
# OR (if the call uses unusual kwargs not supported by generate_sync — rare):
from google import genai
client = genai.Client(api_key=current_key())
```

Step D — Hardcoded model strings → `INGESTION_LLM` (or the appropriate model constant if the file uses a different one; grep the current literal to confirm).

Step E — Remove any top-level `GEMINI_API_KEY = os.environ.get(...)` module-level constants.

Step F — Run `venv/Scripts/python -c "import <file>; print('ok')"`.

Step G — After the commit, run `venv/Scripts/python -m pytest tests/ -v` — must stay green (D-03 per-file commit gate).
</batch_migration_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1a: Migrate batch_classify_kol.py</name>
  <files>batch_classify_kol.py</files>
  <read_first>
    - batch_classify_kol.py (full; 3 touchpoints)
    - ingest_wechat.py (Wave 1 reference pattern)
    - enrichment/extract_questions.py (Wave 3 reference for small sync migrations)
  </read_first>
  <action>
    Apply <batch_migration_pattern> above.

    Commit: `refactor(07-04): migrate batch_classify_kol.py to lib/`

    After commit: `venv/Scripts/python -m pytest tests/ -v` MUST pass (D-03 per-commit gate; MED 3 explicit).
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import batch_classify_kol; print('ok')" && venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from lib import" batch_classify_kol.py` matches
    - `grep "config.gemini_call\|from config import gemini_call" batch_classify_kol.py` returns ZERO matches
    - `grep -E '"gemini-(2\.5|3\.1|embedding)' batch_classify_kol.py` returns ZERO matches
    - `grep 'os.environ\["GEMINI_API_KEY"\]\|os.environ.get("GEMINI_API_KEY")' batch_classify_kol.py` returns ZERO matches
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>batch_classify_kol.py migrated in 1 commit, tests green.</done>
</task>

<task type="auto">
  <name>Task 4.1b: Migrate batch_ingest_from_spider.py — preserve --from-db --topic-filter CLI (Phase 5 D-11)</name>
  <files>batch_ingest_from_spider.py</files>
  <read_first>
    - batch_ingest_from_spider.py (full; 4 touchpoints)
    - ingest_wechat.py (Wave 1 reference pattern)
  </read_first>
  <action>
    Apply <batch_migration_pattern>. CRITICAL PRESERVATION: the `--from-db --topic-filter` argparse CLI shape is referenced by Phase 5 D-11 — do not alter the argparse block.

    Commit: `refactor(07-04): migrate batch_ingest_from_spider.py to lib/ (CLI preserved)`

    After commit: `venv/Scripts/python -m pytest tests/ -v` MUST pass (D-03).
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import batch_ingest_from_spider; print('ok')" && venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from lib import" batch_ingest_from_spider.py` matches
    - `grep "config.gemini_call\|from config import gemini_call" batch_ingest_from_spider.py` returns ZERO matches
    - `grep -E '"gemini-(2\.5|3\.1|embedding)' batch_ingest_from_spider.py` returns ZERO matches
    - `grep 'os.environ\["GEMINI_API_KEY"\]\|os.environ.get("GEMINI_API_KEY")' batch_ingest_from_spider.py` returns ZERO matches
    - `grep -- "--topic-filter" batch_ingest_from_spider.py` still matches (Phase 5 CLI preserved)
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>batch_ingest_from_spider.py migrated in 1 commit, CLI preserved, tests green.</done>
</task>

<task type="auto">
  <name>Task 4.1c: Migrate batchkol_topic.py</name>
  <files>batchkol_topic.py</files>
  <read_first>
    - batchkol_topic.py (full; 3 touchpoints)
    - ingest_wechat.py (Wave 1 reference pattern)
  </read_first>
  <action>
    Apply <batch_migration_pattern>.

    Commit: `refactor(07-04): migrate batchkol_topic.py to lib/`

    After commit: `venv/Scripts/python -m pytest tests/ -v` MUST pass (D-03).
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import batchkol_topic; print('ok')" && venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from lib import" batchkol_topic.py` matches
    - `grep "config.gemini_call\|from config import gemini_call" batchkol_topic.py` returns ZERO matches
    - `grep -E '"gemini-(2\.5|3\.1|embedding)' batchkol_topic.py` returns ZERO matches
    - `grep 'os.environ\["GEMINI_API_KEY"\]\|os.environ.get("GEMINI_API_KEY")' batchkol_topic.py` returns ZERO matches
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>batchkol_topic.py migrated in 1 commit, tests green.</done>
</task>

<task type="auto">
  <name>Task 4.1d: Migrate _reclassify.py</name>
  <files>_reclassify.py</files>
  <read_first>
    - _reclassify.py (full; 3 touchpoints)
    - ingest_wechat.py (Wave 1 reference pattern)
  </read_first>
  <action>
    Apply <batch_migration_pattern>.

    Commit: `refactor(07-04): migrate _reclassify.py to lib/`

    After commit: `venv/Scripts/python -m pytest tests/ -v` MUST pass (D-03).
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import _reclassify; print('ok')" && venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from lib import" _reclassify.py` matches
    - `grep "config.gemini_call\|from config import gemini_call" _reclassify.py` returns ZERO matches
    - `grep -E '"gemini-(2\.5|3\.1|embedding)' _reclassify.py` returns ZERO matches
    - `grep 'os.environ\["GEMINI_API_KEY"\]\|os.environ.get("GEMINI_API_KEY")' _reclassify.py` returns ZERO matches
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>_reclassify.py migrated in 1 commit, tests green.</done>
</task>

<task type="auto">
  <name>Task 4.2: Migrate skill_runner.py (test harness — keep _GEMINI_MODEL literal for independence)</name>
  <files>skill_runner.py</files>
  <read_first>
    - skill_runner.py (full contents; 3 touchpoints; line 151 has `_GEMINI_MODEL = "gemini-3.1-flash-lite-preview"` per 07-RESEARCH Open Question #4)
    - .planning/phases/07-model-key-management/07-RESEARCH.md § Open Questions #4 (rationale for keeping literal)
  </read_first>
  <action>
    skill_runner.py is the test harness. Per 07-RESEARCH Open Question #4, its `_GEMINI_MODEL` constant stays as a STRING LITERAL to preserve test independence from production model changes. Update the literal value to `"gemini-2.5-flash-lite"` (canonical GA model), add a comment explaining the choice.

    Step A — Line 151 (approximately):
    ```python
    # BEFORE:
    _GEMINI_MODEL = "gemini-3.1-flash-lite-preview"
    # AFTER:
    # Test harness keeps this as a string literal (NOT from lib.models) to stay independent
    # of production model changes — updating INGESTION_LLM should not break skill tests.
    # Per 07-RESEARCH § Open Questions #4.
    _GEMINI_MODEL = "gemini-2.5-flash-lite"
    ```

    Step B — Replace any direct `os.environ["GEMINI_API_KEY"]` / `os.environ.get("GEMINI_API_KEY")` with `current_key()`:
    ```python
    from lib import current_key
    client = genai.Client(api_key=current_key())
    ```
    This IS allowed here because skill_runner uses lib for the KEY but keeps the MODEL literal. Key rotation is a runtime concern; model identity is a test-harness concern.

    Step C — If `skill_runner.py` uses any other hardcoded model strings (e.g., for embedding in integration tests), also leave them as literals and add the same rationale comment.

    Step D — Run skill tests: `venv/Scripts/python skill_runner.py skills/omnigraph_ingest --test-file tests/skills/test_omnigraph_ingest.json` (if test file exists) — must pass. OR run the generic suite mode if it exists.

    Step E — Run pytest: `venv/Scripts/python -m pytest tests/ -v`.

    Commit: `refactor(07-04): migrate skill_runner.py key handling (model literal kept per Open Q #4)`
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import skill_runner; print('ok')" && venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from lib import" skill_runner.py` matches AND includes `current_key`
    - `grep "_GEMINI_MODEL" skill_runner.py` matches (literal preserved per Open Q #4)
    - `grep '"gemini-2.5-flash-lite"' skill_runner.py` matches (canonical value, replacing 3.1-preview)
    - `grep "Open Questions #4\|test harness\|test independence" skill_runner.py` matches (rationale comment in code)
    - `grep 'os.environ\["GEMINI_API_KEY"\]\|os.environ.get("GEMINI_API_KEY")' skill_runner.py` returns ZERO matches
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
  </acceptance_criteria>
  <done>skill_runner.py key-handling migrated; model literal retained with rationale.</done>
</task>

<task type="auto">
  <name>Task 4.3: Migrate verify_gate tests — lib-level mocking per D-06</name>
  <files>tests/verify_gate_a.py, tests/verify_gate_b.py, tests/verify_gate_c.py</files>
  <read_first>
    - tests/verify_gate_a.py, tests/verify_gate_b.py, tests/verify_gate_c.py (full contents — manual verification scripts that hit real APIs)
    - tests/conftest.py (mock_lib_llm fixture from Wave 0)
    - .planning/phases/07-model-key-management/07-CONTEXT.md § D-06
  </read_first>
  <action>
    These are manual-run verification scripts (not pytest tests). They test Cognee remember/recall/disambiguate end-to-end against the real Gemini API.

    Step A — Read each file. Identify the 4-6 Gemini touchpoints per file.

    Step B — For each file:
    - Add: `from lib import current_key`
    - Replace any `api_key=os.environ["GEMINI_API_KEY"]` with `api_key=current_key()`
    - Replace any hardcoded `"gemini-*"` model strings with constants imported from `lib.models` (these ARE production scripts, not test harness — D-06 applies)
    - If the file calls `config.gemini_call(...)`, swap to `lib.generate_sync(...)`

    Step C — Since these are verification scripts that exercise the REAL APIs, no test mocking is needed for the scripts themselves. D-06 applies to pytest tests (which use the `mock_lib_llm` fixture from conftest); these scripts bypass mocks by design.

    Step D — Run each script manually (or note deferral if Gemini/Cognee not available):
    ```
    venv/Scripts/python tests/verify_gate_a.py
    venv/Scripts/python tests/verify_gate_b.py
    venv/Scripts/python tests/verify_gate_c.py
    ```
    Each exits 0 on success. If live API calls are unavailable in the executor's environment, just confirm `venv/Scripts/python -c "import tests.verify_gate_a; print('ok')"` succeeds and defer live runs to the remote Hermes PC.

    Step E — Commit each file separately per D-03:
    - `refactor(07-04): migrate tests/verify_gate_a.py to lib/`
    - `refactor(07-04): migrate tests/verify_gate_b.py to lib/`
    - `refactor(07-04): migrate tests/verify_gate_c.py to lib/`
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import tests.verify_gate_a; import tests.verify_gate_b; import tests.verify_gate_c; print('ok')" 2>&1 || venv/Scripts/python -c "exec(open('tests/verify_gate_a.py').read().split('if __name__')[0]); print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "from lib import" tests/verify_gate_a.py tests/verify_gate_b.py tests/verify_gate_c.py` returns 3 matches
    - `grep -El '"gemini-(2\.5|3\.1|embedding)' tests/verify_gate_a.py tests/verify_gate_b.py tests/verify_gate_c.py` returns ZERO matches
    - `grep -l 'os.environ\["GEMINI_API_KEY"\]\|os.environ.get("GEMINI_API_KEY")' tests/verify_gate_a.py tests/verify_gate_b.py tests/verify_gate_c.py` returns ZERO matches
    - `grep -l "config.gemini_call\|from config import gemini_call" tests/verify_gate_a.py tests/verify_gate_b.py tests/verify_gate_c.py` returns ZERO matches
    - Each file imports cleanly (parse check)
  </acceptance_criteria>
  <done>3 verify_gate scripts migrated; live-run validation deferred to remote where needed.</done>
</task>

<task type="auto">
  <name>Task 4.4: Update SKILL.md frontmatter for all 3 Hermes skills (D-07)</name>
  <files>skills/omnigraph_ingest/SKILL.md, skills/omnigraph_query/SKILL.md, skills/omnigraph_architect/SKILL.md</files>
  <read_first>
    - skills/omnigraph_ingest/SKILL.md (existing frontmatter — describes GEMINI_API_KEY in compatibility + openclaw.requires.config)
    - skills/omnigraph_query/SKILL.md (same pattern)
    - skills/omnigraph_architect/SKILL.md (same pattern)
    - .planning/phases/07-model-key-management/07-CONTEXT.md § D-07 (scope of changes)
    - .planning/phases/07-model-key-management/07-REQUIREMENTS.md § 2.1 Hermes required_environment_variables shape + § 4.5 dual-host example
  </read_first>
  <action>
    Each of 3 SKILL.md files gets the SAME treatment. One commit covers all three (they're tightly coupled; the dual-host metadata block is identical in shape across them).

    For EACH skill's SKILL.md frontmatter (between `---` lines at the top):

    Step A — PRESERVE everything already there: `name`, `description`, `compatibility`. Do NOT rewrite the description. Do NOT touch the skill body below the frontmatter.

    Step B — ADD a Hermes `required_environment_variables` block (between compatibility and metadata):
    ```yaml
    required_environment_variables:
      - name: OMNIGRAPH_GEMINI_KEY
        prompt: "Gemini API key for OmniGraph-Vault (get from https://aistudio.google.com/apikey)"
        help: "Required for LLM, embedding, and vision calls. Falls back to GEMINI_API_KEY if unset; for multi-account rotation set OMNIGRAPH_GEMINI_KEYS (comma-separated)."
        required_for: full functionality
    ```

    Step C — UPDATE the existing OpenClaw metadata block:
    ```yaml
    # BEFORE:
    metadata:
      openclaw:
        os: ["darwin", "linux", "win32"]
        requires:
          bins: ["bash", "python"]
          config: ["GEMINI_API_KEY"]
    # AFTER:
    metadata:
      openclaw:
        skillKey: omnigraph-vault
        primaryEnv: OMNIGRAPH_GEMINI_KEY
        os: ["darwin", "linux", "win32"]
        requires:
          bins: ["bash", "python"]
          config: ["OMNIGRAPH_GEMINI_KEY"]
    ```
    Key changes: added `skillKey`, added `primaryEnv`, changed `requires.config` from `GEMINI_API_KEY` → `OMNIGRAPH_GEMINI_KEY`.

    Step D — UPDATE the `compatibility:` block to reference the new preferred var:
    ```yaml
    # BEFORE:
    compatibility: |
      Requires: GEMINI_API_KEY in ~/.hermes/.env, Python venv at $OMNIGRAPH_ROOT/venv.
      ...
    # AFTER:
    compatibility: |
      Requires: OMNIGRAPH_GEMINI_KEY (preferred) or GEMINI_API_KEY (fallback) in ~/.hermes/.env;
      Python venv at $OMNIGRAPH_ROOT/venv.
      ...
    ```
    Preserve any other text in compatibility (APIFY_TOKEN for ingest, image server note for query, rules_engine.json for architect).

    Step E — Validate YAML syntax:
    ```
    venv/Scripts/python -c "import yaml; yaml.safe_load(open('skills/omnigraph_ingest/SKILL.md').read().split('---')[1])"
    ```
    Repeat for each SKILL.md. All three must parse.

    Step F — If a deployed Hermes PC is accessible: run `hermes skills list omnigraph_ingest` and confirm `OMNIGRAPH_GEMINI_KEY` appears as a required env var. Defer to remote deploy-time validation if local Hermes not configured.

    Commit: `feat(07-04): dual-host SKILL.md frontmatter for 3 skills (D-07)`
  </action>
  <verify>
    <automated>venv/Scripts/python -c "import yaml; [yaml.safe_load(open(f).read().split('---')[1]) for f in ['skills/omnigraph_ingest/SKILL.md','skills/omnigraph_query/SKILL.md','skills/omnigraph_architect/SKILL.md']]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "OMNIGRAPH_GEMINI_KEY" skills/omnigraph_ingest/SKILL.md` returns >= 2 matches (one in required_environment_variables, one in openclaw.requires.config; or more if also in compatibility)
    - `grep "OMNIGRAPH_GEMINI_KEY" skills/omnigraph_query/SKILL.md` returns >= 2 matches
    - `grep "OMNIGRAPH_GEMINI_KEY" skills/omnigraph_architect/SKILL.md` returns >= 2 matches
    - `grep "primaryEnv: OMNIGRAPH_GEMINI_KEY" skills/omnigraph_ingest/SKILL.md skills/omnigraph_query/SKILL.md skills/omnigraph_architect/SKILL.md` returns 3 matches
    - `grep "skillKey: omnigraph-vault" skills/omnigraph_ingest/SKILL.md skills/omnigraph_query/SKILL.md skills/omnigraph_architect/SKILL.md` returns 3 matches
    - `grep "required_environment_variables" skills/omnigraph_ingest/SKILL.md skills/omnigraph_query/SKILL.md skills/omnigraph_architect/SKILL.md` returns 3 matches
    - `grep "GEMINI_API_KEY.*fallback\|fallback.*GEMINI_API_KEY" skills/omnigraph_ingest/SKILL.md skills/omnigraph_query/SKILL.md skills/omnigraph_architect/SKILL.md` returns >= 3 matches (fallback documented)
    - YAML parses for all 3 SKILL.md frontmatters
  </acceptance_criteria>
  <done>3 skill frontmatters updated with dual-host metadata + fallback documentation.</done>
</task>

<task type="auto">
  <name>Task 4.5: Create .env.template + update Deploy.md + CLAUDE.md env var paragraph</name>
  <files>.env.template, Deploy.md, CLAUDE.md</files>
  <read_first>
    - Deploy.md (full contents — understand current deploy story, know where env vars are documented today)
    - CLAUDE.md (the project-level one; locate the "Environment Variables" section if it exists, or the ingest/config section where env vars are first mentioned)
    - .planning/phases/07-model-key-management/07-CONTEXT.md § Claude's Discretion (Deploy.md depth = full table; CLAUDE.md = small paragraph recommended); § D-10 (embedding-2 default)
    - Check if .env.template already exists: `ls .env.template 2>/dev/null`
  </read_first>
  <action>
    Deploy story — three files, one commit.

    Step A — Create (or overwrite if tiny/stub) `.env.template` at repo root. Content (note D-10: OMNIGRAPH_MODEL_EMBEDDING default value is now gemini-embedding-2):
    ```
    # OmniGraph-Vault environment template.
    # Copy to ~/.hermes/.env and fill in real values.

    # =========================
    # Gemini API — required
    # =========================

    # Primary key, scoped to OmniGraph-Vault.
    OMNIGRAPH_GEMINI_KEY=

    # Optional: comma-separated pool for multi-account rotation.
    # Only useful across different Google accounts / GCP projects —
    # Gemini quotas are per-project, not per-key.
    OMNIGRAPH_GEMINI_KEYS=

    # Legacy fallback — still works if OMNIGRAPH_GEMINI_KEY is unset.
    # For new deployments, prefer OMNIGRAPH_GEMINI_KEY above.
    GEMINI_API_KEY=

    # =========================
    # Optional model overrides (hybrid pattern, see lib/models.py)
    # =========================
    # OMNIGRAPH_MODEL_INGESTION_LLM=gemini-2.5-flash-lite
    # OMNIGRAPH_MODEL_VISION_LLM=gemini-2.5-flash-lite
    # OMNIGRAPH_MODEL_SYNTHESIS_LLM=gemini-2.5-flash-lite
    # OMNIGRAPH_MODEL_ENRICHMENT_LLM=gemini-2.5-flash-lite
    # OMNIGRAPH_MODEL_EMBEDDING=gemini-embedding-2          # D-10: production default
    # OMNIGRAPH_MODEL_GITHUB_INGEST=gemini-3.1-flash-lite-preview

    # =========================
    # Optional: per-model RPM caps on paid tier
    # Default values from lib/models.py RATE_LIMITS_RPM (free-tier limits).
    # =========================
    # OMNIGRAPH_RPM_GEMINI_2_5_FLASH_LITE=300
    # OMNIGRAPH_RPM_GEMINI_EMBEDDING_2=1500

    # =========================
    # Scraping (optional)
    # =========================
    APIFY_TOKEN=
    FIRECRAWL_API_KEY=
    CDP_URL=http://localhost:9223
    ```

    Step B — Update `Deploy.md`. Add (do NOT remove existing content) a new section titled `## Environment Variables (Phase 7)` near the start of deploy instructions. Content (D-10 reflects gemini-embedding-2 default):
    ```markdown
    ## Environment Variables (Phase 7)

    OmniGraph-Vault uses scoped `OMNIGRAPH_*` env vars. All are read from `~/.hermes/.env`.

    ### Required

    | Var | Purpose | Fallback |
    |---|---|---|
    | `OMNIGRAPH_GEMINI_KEY` | Primary Gemini API key. | `GEMINI_API_KEY` (legacy) |

    ### Optional — multi-account rotation

    | Var | Purpose |
    |---|---|
    | `OMNIGRAPH_GEMINI_KEYS` | Comma-separated pool. Only useful across different Google accounts / GCP projects (quotas are per-project). |

    Legacy `GEMINI_API_KEY_BACKUP` is automatically folded into the pool if set (no deprecation window needed — your existing key keeps working).

    ### Optional — model overrides (hybrid pattern)

    Defaults live in `lib/models.py`. Override any single model via env without code changes:

    | Var | Default |
    |---|---|
    | `OMNIGRAPH_MODEL_INGESTION_LLM` | `gemini-2.5-flash-lite` |
    | `OMNIGRAPH_MODEL_VISION_LLM` | `gemini-2.5-flash-lite` |
    | `OMNIGRAPH_MODEL_SYNTHESIS_LLM` | `gemini-2.5-flash-lite` |
    | `OMNIGRAPH_MODEL_ENRICHMENT_LLM` | `gemini-2.5-flash-lite` |
    | `OMNIGRAPH_MODEL_EMBEDDING` | `gemini-embedding-2` (D-10 — matches deployed lightrag_embedding default) |
    | `OMNIGRAPH_MODEL_GITHUB_INGEST` | `gemini-3.1-flash-lite-preview` (preserved per phase D-05) |

    ### Optional — RPM overrides (paid tier)

    Free-tier defaults in `lib/models.py::RATE_LIMITS_RPM`. Override per model:

    ```
    OMNIGRAPH_RPM_GEMINI_2_5_FLASH_LITE=300   # Tier 1 cap
    OMNIGRAPH_RPM_GEMINI_EMBEDDING_2=1500
    ```

    See `.env.template` for the full template.
    ```

    Do NOT delete any existing Deploy.md content — APPEND / INSERT. Preserve existing deploy steps verbatim.

    Step C — Update `CLAUDE.md` (project root). Find the "Environment Variables" section (under "Architecture" in the current file, with the table starting "| Variable | Required | Used For |"). Add ONE paragraph AFTER that table, before the next `##` section:

    ```markdown
    **Phase 7 scoped env vars** (added 2026-04-28): the preferred Gemini key variable is `OMNIGRAPH_GEMINI_KEY` (namespaced, won't collide with Hermes's own LLM vars). `GEMINI_API_KEY` remains as a fallback for local dev. For multi-account rotation set `OMNIGRAPH_GEMINI_KEYS` (comma-separated; only useful across different Google accounts / GCP projects). Model names and per-model RPM caps are overridable via `OMNIGRAPH_MODEL_*` and `OMNIGRAPH_RPM_*` env vars — defaults live in `lib/models.py` (embedding default is `gemini-embedding-2` per D-10, matching production). Full deploy table in `Deploy.md`.
    ```

    Step D — Commit: `docs(07-04): deploy story — .env.template, Deploy.md, CLAUDE.md for Phase 7 env vars`
  </action>
  <verify>
    <automated>ls .env.template && grep "OMNIGRAPH_GEMINI_KEY" Deploy.md && grep "OMNIGRAPH_GEMINI_KEY" CLAUDE.md && echo "all present"</automated>
  </verify>
  <acceptance_criteria>
    - `ls .env.template` exists
    - `grep "OMNIGRAPH_GEMINI_KEY=" .env.template` matches
    - `grep "OMNIGRAPH_GEMINI_KEYS=" .env.template` matches
    - `grep "GEMINI_API_KEY=" .env.template` matches (fallback documented)
    - `grep "OMNIGRAPH_MODEL_INGESTION_LLM" .env.template` matches
    - `grep "OMNIGRAPH_MODEL_EMBEDDING=gemini-embedding-2" .env.template` matches (D-10)
    - `grep "OMNIGRAPH_RPM_" .env.template` matches
    - `grep "OMNIGRAPH_GEMINI_KEY" Deploy.md` returns >= 2 matches (in the table + surrounding text)
    - `grep "OMNIGRAPH_MODEL_GITHUB_INGEST" Deploy.md` matches AND the line mentions `gemini-3.1-flash-lite-preview` AND `D-05`
    - `grep "gemini-embedding-2" Deploy.md` matches (D-10 default documented)
    - `grep "OMNIGRAPH_GEMINI_KEY" CLAUDE.md` matches (new paragraph landed)
    - `grep "lib/models.py" CLAUDE.md` matches (references the registry)
    - Deploy.md still contains its pre-existing deploy steps (no regression) — `wc -l Deploy.md` is larger than before, not smaller
  </acceptance_criteria>
  <done>Deploy story complete: template + Deploy.md table + CLAUDE.md paragraph all point to the same env var convention.</done>
</task>

<task type="auto">
  <name>Task 4.6: Final phase gate — grep clean-room + full test suite + phase SUMMARY</name>
  <files>.planning/phases/07-model-key-management/07-04-SUMMARY.md</files>
  <read_first>
    - All commits landed in Waves 0-4 (use `git log --oneline` to review)
    - .planning/phases/07-model-key-management/07-VALIDATION.md (confirm every "pending" row is now "green")
  </read_first>
  <action>
    End-of-phase validation. No new code.

    Step A — Clean-room grep verification. Run these and ALL must pass (zero matches where expected). NOTE (LOW 2): the `skill_runner.py` filter does NOT use a `^` anchor — ripgrep may report matches with a leading `./` or other prefix, so we drop the anchor to avoid false negatives.

    ```
    # No hardcoded model strings outside lib/ and skill_runner.py (Open Q #4 exception).
    # LOW 2 fix: filter by basename presence, NOT ^skill_runner.py (anchor may miss ./-prefixed paths).
    grep -rnE '"gemini-(2\.5|3\.1|embedding)[^"]*"' --include="*.py" \
      --exclude-dir=venv --exclude-dir=.venv --exclude-dir=__pycache__ --exclude-dir=.planning \
      | grep -v "^lib/" | grep -v "skill_runner.py"
    # Expected: ZERO matches (or only in skill_runner.py _GEMINI_MODEL per Open Q #4; filtered out by the anchor-free grep -v)

    # No direct GEMINI_API_KEY reads outside lib/api_keys.py.
    # Keep the ^ anchor here — the lib/api_keys.py path is always repo-relative.
    grep -rnE 'os\.environ\["GEMINI_API_KEY"\]|os\.environ\.get\("GEMINI_API_KEY"\)' --include="*.py" \
      --exclude-dir=venv --exclude-dir=.venv --exclude-dir=__pycache__ --exclude-dir=.planning \
      | grep -v "^lib/api_keys.py"
    # Expected: ZERO matches

    # config.gemini_call references — D-11 shim may still exist inside config.py itself, which is acceptable.
    grep -rn "config\.gemini_call\|from config import gemini_call" --include="*.py" \
      --exclude-dir=venv --exclude-dir=.venv --exclude-dir=__pycache__ --exclude-dir=.planning
    # Expected: if config.py kept the D-11 shim body, grep matches only the definition inside config.py itself;
    # no external callers.
    ```

    Step B — Full test suite: `venv/Scripts/python -m pytest tests/ -v` — exits 0.

    Step C — Skill suite (if test files exist): `venv/Scripts/python skill_runner.py skills/ --test-all` — exits 0. If the `--test-all` flag is not supported, run each skill's test file individually.

    Step D — Update 07-VALIDATION.md: flip every `⬜ pending` row to `✅ green` (for items verified in this wave) and set `wave_0_complete: true` (if not already set in Wave 0) + add a `wave_4_complete: true` field to the frontmatter. Also flip `nyquist_compliant: true` if all rows are now green.

    Step E — Write `.planning/phases/07-model-key-management/07-04-SUMMARY.md` with:
    - Commits per wave (use `git log --grep="07-0[0-4]" --oneline`)
    - Grep-clean-room results (all 3 queries above)
    - Any files that legitimately still contain a hardcoded model string or env read (with the exception documented)
    - Confirmation that D-11 shims in config.py remain intact (acceptable per D-11; not swept in this wave)
    - List of remaining open items for future phases (e.g., "Cognee's own LLM calls still bypass lib.generate; out of scope per phase boundary")

    Step F — Final phase-level SUMMARY commit (ok if done as part of the deploy story commit; D-03 allows docs commits to be grouped): `docs(07): close Phase 7 — model & key management complete`
  </action>
  <verify>
    <automated>venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - Grep command A (model strings) returns ZERO lines — and the filter uses `grep -v "skill_runner.py"` (no `^` anchor, per LOW 2)
    - Grep command B (env reads) returns ZERO lines
    - `venv/Scripts/python -m pytest tests/ -v` exits 0
    - `ls .planning/phases/07-model-key-management/07-04-SUMMARY.md` exists
    - `grep "Grep-clean-room" .planning/phases/07-model-key-management/07-04-SUMMARY.md` matches (the three queries + results are documented)
    - `grep "wave_4_complete: true\|nyquist_compliant: true" .planning/phases/07-model-key-management/07-VALIDATION.md` matches
    - `grep "✅ green" .planning/phases/07-model-key-management/07-VALIDATION.md` matches 10+ times (previously ⬜ pending rows flipped)
    - `grep "D-11 shims" .planning/phases/07-model-key-management/07-04-SUMMARY.md` matches (explicit note about shim retention)
  </acceptance_criteria>
  <done>Phase 7 closed. Codebase is internally consistent. Deploy story complete. Validation doc reflects all green. D-11 shim retention documented.</done>
</task>

</tasks>

<verification>
Wave 4 gate: full grep-clean-room pass + full test suite green + skill suite green + 07-VALIDATION.md all green. Tasks 4.1a-4.1d enforce D-03 per-commit pytest gate (MED 3 resolution). Task 4.6 grep uses anchor-free skill_runner.py filter (LOW 2 fix).

Maps to 07-VALIDATION.md rows: "D-07 (SKILL.md frontmatter updated)", "Wave 4 parity: all skill tests green".

Phase 7 deliverables recap (all boxes checkable after Wave 4):
- [ ] `lib/` package (Wave 0)
- [ ] Reference migration complete (Wave 1)
- [ ] P0 files + config.py (Wave 2)
- [ ] P1 Cognee bridge end-to-end (Wave 3)
- [ ] P2 batch + tests + deploy story (Wave 4)
- [ ] All D-01..D-11 decisions implemented
</verification>

<success_criteria>
- [ ] 4 batch scripts split into 4 separate commits (Tasks 4.1a-4.1d; MED 3 resolution) with `pytest tests/ -v` green between each
- [ ] skill_runner migrated (Task 4.2) — _GEMINI_MODEL literal preserved per Open Q #4
- [ ] 3 verify_gate scripts migrated (Task 4.3)
- [ ] 3 SKILL.md frontmatters updated with Hermes required_environment_variables + OpenClaw primaryEnv (1 commit from Task 4.4, covering D-07)
- [ ] .env.template + Deploy.md + CLAUDE.md updated (1 commit from Task 4.5) — D-10 embedding-2 reflected in defaults
- [ ] Grep-clean-room: zero hardcoded model strings outside lib/ (skill_runner exception via anchor-free filter per LOW 2), zero direct GEMINI_API_KEY reads outside lib/api_keys.py
- [ ] 07-VALIDATION.md frontmatter: nyquist_compliant: true + wave_0_complete: true + wave_4_complete: true
- [ ] All previously ⬜ pending rows in 07-VALIDATION.md flipped to ✅ green
- [ ] Full pytest suite green
- [ ] skill_runner test suite green (or deferred to remote with explicit note)
- [ ] D-11 config.py shims documented as retained (not swept) in 07-04-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/07-model-key-management/07-04-SUMMARY.md` recording:
- All Wave 4 commit SHAs (Tasks 4.1a-4.1d contribute 4 commits individually — MED 3)
- Grep-clean-room results (3 queries and their outputs; Task 4.6 filter uses anchor-free skill_runner.py exclusion per LOW 2)
- Confirmation of SKILL.md YAML parsing for all 3 skills
- Any Hermes/OpenClaw live deploy validation (hermes skills list) — or deferred to remote with reason
- Final phase-level stats: total commits across waves 0-4, total files modified, test suite size before/after
- Explicit note: "D-11 shims in config.py (INGEST_LLM_MODEL / IMAGE_DESCRIPTION_MODEL / ENRICHMENT_LLM_MODEL / gemini_call) retained — acceptable per D-11; future sweep optional."
- Next step pointer: Phase 5 Wave 0 can now begin replanning (its 05-00-embedding-migration-and-consolidation-PLAN.md updates to reference lib/ instead of creating a new shared module — per D-01/D-09). Per D-10, the "embedding switch" subtask can be dropped entirely — the default is already `gemini-embedding-2`.
</output>
</content>
</invoke>