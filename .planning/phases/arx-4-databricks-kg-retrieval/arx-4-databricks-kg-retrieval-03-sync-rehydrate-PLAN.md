---
phase: arx-4-databricks-kg-retrieval
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified: []
autonomous: false
requirements: [ARX4-64]
user_setup: []

must_haves:
  truths:
    - "Before the sync, Aliyun on-disk vdb is asserted FRESH + aligned (vdb_relationships.json > 49 bytes with real rows; vdb_chunks data_len ≈ Qdrant chunks; graphml nodes ≈ vdb_entities data_len) — the stale state is NEVER transplanted again."
    - "The aligned Aliyun lightrag_storage snapshot is copied to the UC Volume via the existing sync_to_databricks.sh (not a stale archive — the real regenerated vdb_*.json)."
    - "Databricks re-hydrates the new snapshot (the startup_adapter idempotency short-circuit is defeated so /tmp re-copies the fresh data)."
    - "The deployed backend log shows >0 vector chunks on a query AND does NOT show 'falling back to WEIGHT method'."
  artifacts:
    - path: "databricks-deploy/_aliyun_pull/lightrag_storage/vdb_relationships.json"
      provides: "The regenerated (non-placeholder) relationships vdb pulled from Aliyun into local staging by sync_to_databricks.sh Step 3 — proof the real file (not vdb_archive) is what ships"
      contains: "matrix"
  key_links:
    - from: "Aliyun lightrag_storage/vdb_*.json (regenerated by Plan 01)"
      to: "UC Volume dbfs:/Volumes/mdlg_ai_shared/kb_v2/omnigraph_vault/lightrag_storage"
      via: "scripts/sync_to_databricks.sh Step 3 tar→scp + Step 6 fs cp -r --overwrite"
      pattern: "fs cp -r --overwrite"
    - from: "UC Volume lightrag_storage"
      to: "/tmp/omnigraph_vault/lightrag_storage on the deployed app"
      via: "startup_adapter.hydrate_lightrag_storage_from_volume (idempotency short-circuit at :83 — must be cleared)"
      pattern: "already_hydrated"
    - from: "deployed LightRAG chunks_vdb"
      to: "vector-similarity retrieval (not WEIGHT fallback)"
      via: "aligned vdb_chunks.json matrix"
      pattern: "vector chunks"
---

<objective>
Close ISSUES #64: the deployed Databricks app reads on-disk `vdb_*.json` (NanoVectorDBStorage — no Qdrant on Databricks), `sync_to_databricks.sh` Step 3 copies Aliyun's on-disk `vdb_*.json` to the UC Volume, but Aliyun's on-disk vdb was stale/broken (relationships = 49-byte placeholder, chunks = June 6 / 3294 rows vs graphml 32056 nodes). Plan 01 has now regenerated Aliyun's on-disk vdb from its fresh Qdrant (aligned to the fresh graphml). This plan transplants that NOW-ALIGNED snapshot to Databricks and proves vector-similarity retrieval is restored.

This is a SERIAL chain that REQUIRES Plan 01 complete (the regen can't have happened, and the pre-sync freshness gate would fail, if the converter were still OOM-dead). Wave 2.

Purpose: stop the deployed app serving a misaligned snapshot (`0 vector chunks` → WEIGHT fallback on every query) by syncing the aligned snapshot + forcing a re-hydrate, then verifying on the deployed log.

Output: UC Volume carries the aligned snapshot, the app re-hydrated it, and the deployed log shows `>0 vector chunks` with no WEIGHT-fallback WARNING.

ZERO new features — this uses the existing validated sync script + existing hydration path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/arx-4-databricks-kg-retrieval/arx-4-databricks-kg-retrieval-CONTEXT.md
@.planning/phases/arx-4-databricks-kg-retrieval/arx-4-databricks-kg-retrieval-01-SUMMARY.md
</context>

<tasks>

<task type="auto" gate="aliyun-read-only">
  <name>Task 1: [PRE-SYNC GATE — read-only] Assert Aliyun on-disk vdb is fresh + internally aligned BEFORE any transplant</name>
  <files>(read-only probes — no files modified; produces gate evidence for SUMMARY)</files>
  <read_first>
    - .planning/phases/arx-4-databricks-kg-retrieval/arx-4-databricks-kg-retrieval-CONTEXT.md (the #64 "Pre-sync gate (NEW, mandatory)" paragraph + the "vdb_archive_*.json" specific — confirm the sync ships the REAL file not the archive)
    - .planning/phases/arx-4-databricks-kg-retrieval/arx-4-databricks-kg-retrieval-01-SUMMARY.md (Plan 01's load_storage row-count proof — this gate re-confirms it independently)
    - scripts/qdrant_to_nanovdb.py (NAMESPACE_TO_QDRANT_COLLECTION — to map vdb files to Qdrant counts)
    - MEMORY: aliyun_vitaclaw_ssh, aliyun_ssh_manual_trigger_env, graphml_qdrant_cross_version_divergence (the divergence detector)
  </read_first>
  <action>
    **CHANNEL: ALIYUN READ-ONLY. The executor runs these probes directly via Bash `ssh aliyun-vitaclaw` (read-only diagnostics are always executor-run per CONTEXT channel-discipline).** This is the mandatory pre-sync gate from CONTEXT — its entire purpose is to NOT transplant the stale state again. If ANY assertion fails, STOP and surface (do not run the sync); the likely cause would be Plan 01 not actually completing.

    Probes (all read-only):
    1. Live Qdrant counts (the alignment target):
       `ssh aliyun-vitaclaw "set -a; source /root/.hermes/.env; set +a; curl -s localhost:6333/collections/lightrag_vdb_chunks_gemini_embedding_2_3072d | python3 -c 'import sys,json;print(\"chunks\",json.load(sys.stdin)[\"result\"][\"points_count\"])'"` and the same for `..._relationships_...` and `..._entities_...`.
    2. On-disk vdb row counts via load_storage (the synced artifact):
       `ssh aliyun-vitaclaw "set -a; source /root/.hermes/.env; set +a; cd /root/OmniGraph-Vault && venv-aim1/bin/python -c 'from nano_vectordb.dbs import load_storage as L; import os; base=\"/root/.hermes/omonigraph-vault/lightrag_storage\"; [print(n, len(L(os.path.join(base,n))[\"data\"]), L(os.path.join(base,n))[\"matrix\"].shape) for n in (\"vdb_chunks.json\",\"vdb_relationships.json\",\"vdb_entities.json\")]'"`
    3. graphml node count (for the entities alignment check):
       `ssh aliyun-vitaclaw "set -a; source /root/.hermes/.env; set +a; cd /root/OmniGraph-Vault && venv-aim1/bin/python -c 'import networkx as nx; g=nx.read_graphml(\"/root/.hermes/omonigraph-vault/lightrag_storage/graph_chunk_entity_relation.graphml\"); print(\"graphml nodes\", g.number_of_nodes(), \"edges\", g.number_of_edges())'"`
    4. Confirm the REAL vdb_relationships.json (not the archive) is what will sync — check size + that vdb_archive_relationships.json (if present) is NOT what the sync tars:
       `ssh aliyun-vitaclaw "ls -la /root/.hermes/omonigraph-vault/lightrag_storage/vdb_relationships.json /root/.hermes/omonigraph-vault/lightrag_storage/vdb_archive_*.json 2>/dev/null"`

    Assert (record each pass/fail in SUMMARY):
    - vdb_relationships.json size > 1000000 bytes (NOT 49-byte placeholder).
    - on-disk vdb_chunks data_len within ±5% of Qdrant chunks points_count (≈3851).
    - on-disk vdb_relationships data_len within ±5% of Qdrant relationships points_count (≈82582).
    - graphml node count and vdb_entities data_len are the same order (entities vdb ≈ graphml nodes; note entities 59191 vs graphml 32056 — they need not be equal since graphml nodes include both entity+chunk-anchor nodes, but vdb_entities must be NON-empty and recent; the hard gate is relationships+chunks freshness — flag any gross mismatch for judgment).
    - All matrices are shape (N, 3072).
  </action>
  <verify>
    <automated>ssh aliyun-vitaclaw "test $(stat -c%s /root/.hermes/omonigraph-vault/lightrag_storage/vdb_relationships.json) -gt 1000000 && echo GATE_REL_REAL"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records the Qdrant points_count for all 3 collections AND the on-disk load_storage data_len + matrix shape for all 3 vdb files.
    - vdb_relationships.json size > 1000000 bytes (asserted via `ls -la` / `stat -c%s`).
    - vdb_chunks data_len is within ±5% of Qdrant chunks points_count; vdb_relationships data_len within ±5% of Qdrant relationships points_count — both PASS, stated explicitly.
    - All three matrices report shape `(N, 3072)`.
    - If any assertion FAILS, the SUMMARY records HALT (sync not run) and the task is marked blocked pending Plan 01.
  </acceptance_criteria>
  <done>Aliyun on-disk vdb is proven fresh + aligned to live Qdrant (relationships not a placeholder, chunks/relationships within ±5% of Qdrant counts, all 3072-dim) — the green light to transplant. The stale-snapshot trap (#44/#64 root) cannot recur.</done>
</task>

<task type="auto" gate="aliyun-write-op-and-databricks-deploy">
  <name>Task 2: [ALIYUN WRITE-OP (tar in /tmp) + DATABRICKS DEPLOY] Run sync_to_databricks.sh to push the aligned snapshot to the UC Volume + redeploy the app</name>
  <files>databricks-deploy/_aliyun_pull/ (local staging, gitignored); UC Volume dbfs:/Volumes/mdlg_ai_shared/kb_v2/omnigraph_vault/lightrag_storage (remote); Aliyun /tmp (transient tar)</files>
  <read_first>
    - scripts/sync_to_databricks.sh (the WHOLE 10-step script — esp. Step 3 :110-132 tar lightrag_storage from Aliyun, Step 6 :164-170 fs cp -r --overwrite to UC Volume, Step 9a-9c :191-243 stop/start/redeploy)
    - scripts/sync_to_databricks.md (companion runbook)
    - .planning/phases/arx-4-databricks-kg-retrieval/arx-4-databricks-kg-retrieval-CONTEXT.md (#64 sync section — fs cp -r --overwrite MERGES not replaces; the Step 9c redeploy is mandatory because stop+start wipes the artifact)
    - MEMORY: databricks_apps_stop_start_wipes_deployment, claude_databricks_deployment_autonomous, databricks_sync_full_false_positive (verify the UC Volume actually received the new bytes)
  </read_first>
  <action>
    **CHANNEL: MIXED. The Aliyun side is a WRITE-OP (the script tars into Aliyun /tmp, transient ~2.6GB) — explicitly authorized as a write-op; the executor runs the script. The Databricks side (fs cp, apps stop/start/deploy) is Claude-owned CLI per Principle #7.** Run the script from Git Bash on the local machine (the script header :20-21 says Git Bash OK; it self-wraps Databricks paths with MSYS_NO_PATHCONV=1). Background it (Step 3 is ~25-50min at corp 0.77 MB/s) and use `--yes` since stdin won't be a tty in a background run:

    `bash scripts/sync_to_databricks.sh --yes 2>&1 | tee .scratch/arx4-sync.log`

    CRITICAL caveat — `fs cp -r --overwrite` MERGES, it does not replace (CONTEXT + memory). The OLD stale vdb files on the UC Volume have the SAME filenames as the new ones, so --overwrite replaces them file-for-file — correct. But if the OLD volume had EXTRA files (e.g. a stale `vdb_archive_*.json` leftover) they linger. After Step 6, VERIFY the UC Volume vdb_relationships.json size matches local staging (defeats the `databricks_sync_full_false_positive` trap):
    - `MSYS_NO_PATHCONV=1 databricks --profile dev fs ls dbfs:/Volumes/mdlg_ai_shared/kb_v2/omnigraph_vault/lightrag_storage --output json` → confirm vdb_relationships.json file_size > 1000000 (NOT 49).
    - Compare to `ls -la databricks-deploy/_aliyun_pull/lightrag_storage/vdb_relationships.json` (the local staging Step 3 pulled) — sizes should match.

    Let the script's Step 9a-9c run (stop → start → wait-for-pending-clear → redeploy). The redeploy is what re-mounts the app with the new artifact. Capture the final `apps get` JSON.

    NOTE: this task does NOT yet prove the deployed RETRIEVAL is fixed — it proves the data is ON the volume + the app redeployed. The re-hydrate + retrieval proof is Task 3 (because of the startup_adapter idempotency short-circuit).
  </action>
  <verify>
    <automated>MSYS_NO_PATHCONV=1 databricks --profile dev fs ls dbfs:/Volumes/mdlg_ai_shared/kb_v2/omnigraph_vault/lightrag_storage --output json | grep -A2 vdb_relationships</automated>
  </verify>
  <acceptance_criteria>
    - `.scratch/arx4-sync.log` ends with the script's Step 10 "Sync complete" banner (script exited 0, no STEP N FAILED).
    - `databricks --profile dev fs ls dbfs:/Volumes/mdlg_ai_shared/kb_v2/omnigraph_vault/lightrag_storage --output json` shows `vdb_relationships.json` with file_size > 1000000 bytes (the real file, not the 49-byte placeholder) — recorded in SUMMARY.
    - The local staging `databricks-deploy/_aliyun_pull/lightrag_storage/vdb_relationships.json` exists and its size matches the UC Volume file_size (sync was not a false-positive no-op).
    - `databricks --profile dev apps get omnigraph-kb -o json` final state shows the app deployed (not UNAVAILABLE) post Step 9c redeploy.
  </acceptance_criteria>
  <done>The aligned Aliyun snapshot (real vdb_relationships.json > 1MB, fresh vdb_chunks) is on the UC Volume (size-verified, not a false-positive sync) and the app has been redeployed from the workspace artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [DATABRICKS DEPLOY] Defeat the startup_adapter idempotency short-circuit, force re-hydrate, and verify >0 vector chunks / no WEIGHT fallback on the deployed log</name>
  <files>(no repo files — deployed-app ops + log verification; produces evidence for SUMMARY)</files>
  <read_first>
    - databricks-deploy/startup_adapter.py (:56-120 hydrate_lightrag_storage_from_volume — esp. the idempotency short-circuit at :83 `if dst.exists() and any(dst.iterdir()): return skipped already_hydrated`. The deployed /tmp likely STILL holds the OLD snapshot → a plain redeploy will SKIP re-copy and serve stale data.)
    - .planning/phases/arx-4-databricks-kg-retrieval/arx-4-databricks-kg-retrieval-CONTEXT.md (#64 re-hydrate note — "a re-hydrate after sync may need the /tmp cache cleared or the app restarted to re-copy"; the pass-bar)
    - MEMORY: databricks_apps_tmpfs_coldstart (/tmp is tmpfs → a FULL cold restart wipes /tmp, forcing re-hydrate), databricks-kg-weight-fallback-residue (the exact log symptom to confirm gone)
  </read_first>
  <action>
    **CHANNEL: DATABRICKS DEPLOY (Claude-owned CLI, PowerShell/Git Bash per Principle #7).** The startup_adapter (`:83`) short-circuits re-copy when `/tmp/omnigraph_vault/lightrag_storage` already has files. After Task 2's redeploy the container MAY have reused a warm /tmp holding the OLD snapshot. Force a clean re-hydrate:

    Because /tmp is tmpfs on Databricks Apps (memory `databricks_apps_tmpfs_coldstart`), a FULL stop→start→redeploy cycle gives a fresh container with empty /tmp, which makes startup_adapter take the copy path (not the already_hydrated skip). Task 2's script already did one stop/start/redeploy — but to be certain the NEW volume data (not a warm-container leftover) is what hydrated, do a deterministic verification:

    1. Confirm the deployed app's hydrate log shows a COPY (not a skip) of the new data. Fetch the startup log via the project's `make logs` / `tail_app_logs.py` (memory `databricks_apps_logs_websocket` — `databricks apps logs` does not exist). Grep for `startup_adapter: copied via` (expect `fuse` or `sdk`, with a bytes= count reflecting the new ~2.6GB) — NOT `skip already_hydrated`. If you see `skip already_hydrated`, the container reused warm /tmp → trigger another full stop/start/redeploy (the tmpfs is wiped on the new container) and re-check.
    2. Once a fresh `copied via ... bytes=<large>` is confirmed, exercise ONE deployed kg-retrieval query. Entra-SSO blocks Playwright (per arx-2-finish VERIFICATION), so this is a HUMAN-VERIFY checkpoint: the user runs the query in their browser (logged into the app via SSO) and the executor reads the backend log.
    3. Grep the backend query log for the pass-bar strings.

    The query to run (browser console, logged in — same shape as the sync script Step 10 smoke 4, but the log is what matters):
    ```
    fetch(APP_URL + '/api/synthesize', {method:'POST', headers:{'Content-Type':'application/json'},
      body:JSON.stringify({query:'What is LightRAG?', mode:'long_form'})}).then(r=>r.json()).then(console.log)
    ```
    Then the executor fetches the backend log and greps for `vector chunks` and `falling back to WEIGHT method`.
  </action>
  <what-built>Task 1+2 regenerated Aliyun's on-disk vdb (Plan 01), pre-gated its freshness, synced it to the UC Volume, and redeployed the app. This checkpoint forces a clean re-hydrate (defeating the startup_adapter :83 idempotency skip via tmpfs cold restart) and verifies the deployed retrieval now uses vector similarity instead of the WEIGHT fallback.</what-built>
  <how-to-verify>
    1. Executor confirms (via make logs) the startup log shows `startup_adapter: copied via {fuse|sdk} ... bytes=<~2.6GB-scale>` (NOT `skip already_hydrated`). If skipped, executor triggers another full `databricks apps stop/start/deploy` cycle (PowerShell) — tmpfs wipe forces re-copy — and re-checks.
    2. User (logged into the Databricks app via Entra SSO) opens the browser console and runs the `/api/synthesize` long_form fetch above (or any kg query). Wait ~60-90s for completion.
    3. Executor fetches the backend log and confirms the pass-bar.
  </how-to-verify>
  <verify>
    <automated>echo "MANUAL — see how-to-verify: executor greps deployed backend log for 'vector chunks' >0 AND absence of 'falling back to WEIGHT method' after the human-run query"</automated>
  </verify>
  <acceptance_criteria>
    - Deployed startup log contains `startup_adapter: copied via` with a large `bytes=` (fresh hydrate of the new snapshot) — NOT `skip already_hydrated` on the run that serves the verified query.
    - Deployed query backend log contains `vector chunks` with a count > 0 (e.g. `Raw search results: N vector chunks`, N>0).
    - Deployed query backend log does NOT contain `falling back to WEIGHT method` (the #64 symptom is gone).
    - The query still returns a real cited report (sources>0) — i.e. fixing the vector path did not break retrieval.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the pasted backend-log excerpt (the `vector chunks` count line + confirmation the WEIGHT-fallback line is absent), or describe what still shows WEIGHT fallback.</resume-signal>
</task>

</tasks>

<verification>
- UC Volume: `fs ls .../lightrag_storage --output json` shows vdb_relationships.json > 1MB (size-matched to local staging).
- Deployed startup log: `startup_adapter: copied via ...` (fresh hydrate, not skip) on the serving run.
- Deployed query log: `vector chunks` count > 0 AND `falling back to WEIGHT method` ABSENT AND sources>0.
</verification>

<success_criteria>
- Pre-sync gate proved Aliyun on-disk vdb fresh+aligned (no stale transplant).
- Aligned snapshot on UC Volume (size-verified) + app redeployed.
- Forced re-hydrate confirmed (copied, not skipped); deployed log shows >0 vector chunks, no WEIGHT fallback. #64 closed.
</success_criteria>

<output>
After completion, create `.planning/phases/arx-4-databricks-kg-retrieval/arx-4-databricks-kg-retrieval-03-SUMMARY.md` citing: the pre-sync gate numbers (Qdrant counts vs on-disk data_len), the UC Volume vdb_relationships.json file_size, the `startup_adapter: copied via` log line, and the before/after `vector chunks` + WEIGHT-fallback log excerpt.
</output>
