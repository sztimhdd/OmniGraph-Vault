Test Cases:
https://mp.weixin.qq.com/s/Y_uRMYBmdLWUPnz_ac7jWA
https://mp.weixin.qq.com/s/8SGRMIyspvUcLMcmeDa2Mw
https://mp.weixin.qq.com/s/4bE4AZPAAYVdtQIlf9hP9A
https://mp.weixin.qq.com/s/qzacaj9XHfq9etTOBt8r5Q
https://mp.weixin.qq.com/s/oGXo8psXgP6A24mmKbTGIw

MCP:
 npx @playwright/mcp@latest --cdp-endpoint=http://127.0.0.1:9222 --host=0.0.0.0 --port=8931 --allowed-hosts=* --allowed-origins=*
npm error config prefix cannot be changed from project config: C:\Users\Administrator\.npmrc.
Listening on http://localhost:8931
Put this in your client config:
{
  "mcpServers": {
    "playwright": {
      "url": "http://localhost:8931/mcp"
    }
  }
}


═══════════════════════════════════════════════════════════════════════════════
PHASE 1: BUG FIXES & GATE 6 VALIDATION
OmniGraph-Vault Manual Testing Checkpoint for Hermes Agent
═══════════════════════════════════════════════════════════════════════════════

PROJECT CONTEXT
───────────────────────────────────────────────────────────────────────────────
OmniGraph-Vault is a personal knowledge graph for Hermes Agent & OpenClaw AI assistants.

Architecture:
  • Ingestion: WeChat articles/PDFs → LightRAG knowledge graph + Cognee memory
  • Query/Synthesis: Natural language questions → LightRAG retrieval + Cognee context recall
  • Skills Interface: Two OpenClaw/Hermes skills expose ingest & query functions

Tech Stack:
  • LightRAG: Knowledge graph construction/querying
  • Cognee: Stateful memory layer (entity canonicalization, context recall)
  • Gemini 2.5 Flash/Pro: LLM + vision API
  • Apify + Playwright CDP: Web scraping
  • Local HTTP image server (port 8765): Serves extracted article images
  • Runtime data: ~/.hermes/omonigraph-vault/ (note: "omonigraph" not "omnigraph" — preserve typo)


WAVE 1 INFRASTRUCTURE FIXES COMPLETED
───────────────────────────────────────────────────────────────────────────────
Phase 1 Plan 01-01 has been executed. All 4 infrastructure bugs are now fixed.

INFRA-01: Centralized path constants in config.py
  • Added ENTITY_BUFFER_DIR = ~/.hermes/omonigraph-vault/entity_buffer
  • Added CANONICAL_MAP_FILE = ~/.hermes/omonigraph-vault/canonical_map.json
  • All scripts now import from config.py instead of hardcoding paths

INFRA-02: Removed all hardcoded /home/sztimhdd/ paths (13 instances across 12 files)
  • All scripts now use config.py constants or os.path.expanduser()
  • Verified: grep -rn "/home/sztimhdd/" --include="*.py" . returns 0 matches

INFRA-03: Fixed NameError in kg_synthesize.py
  • Added missing "import json" statement
  • Fixed: canonical_map = json.load(...) no longer throws NameError

INFRA-04: Changed default query mode from "naive" to "hybrid"
  • kg_synthesize.py now defaults to mode="hybrid" for better accuracy
  • Better for cross-article synthesis queries

Additional fixes:
  • Fixed bare except clauses with proper Exception logging
  • Fixed ingest_pdf() variable references (full_content→full_text, url→file_path)
  • Added image download success/failure counter with 50% threshold warning
  • Removed venv path injections from 8 scripts


WAVE 2 VALIDATION: WHERE WE ARE NOW
───────────────────────────────────────────────────────────────────────────────
Phase 1 Plan 01-02 — Automated tests COMPLETE:

  ✓ Task 1: Pre-completed work verified (SKILL.md Case 5 & 9th test case intact from v1.1)
  ✓ Task 2: skill_runner.py passes all 9/9 LLM routing tests (GATE6-05 PASS)
  ⏸️  Task 3: CHECKPOINT — Manual end-to-end pipeline validation (requires human action)

You are HERE. Hermes must complete 4 manual validation steps to prove the fixed
pipeline works end-to-end on this machine.


STEP 1: INGEST 3 WECHAT ARTICLES (GATE6-01, GATE6-04)
───────────────────────────────────────────────────────────────────────────────
What this validates:
  • GATE6-01: Real WeChat article ingestion works without crashes
  • GATE6-04: ingest_wechat.py uses config-based paths (no path errors)

Ingestion pipeline flow:
  1. Fetches HTML via Apify (primary) or CDP/Playwright (fallback)
  2. Parses to markdown via BeautifulSoup + html2text
  3. Downloads images to ~/.hermes/omonigraph-vault/images/{article_hash}/
  4. Sends images to Gemini Vision for descriptions
  5. Extracts named entities via Gemini Flash → entity_buffer/{hash}_entities.json
  6. Inserts entities and content into LightRAG knowledge graph (lightrag_storage/)

Instructions:
  Pick 3 WeChat article URLs that discuss related topics (LightRAG, Cognee, AI agents, LLMs, etc.)

  For each:
  python ingest_wechat.py "https://mp.weixin.qq.com/s/<article-id-1>"
  python ingest_wechat.py "https://mp.weixin.qq.com/s/<article-id-2>"
  python ingest_wechat.py "https://mp.weixin.qq.com/s/<article-id-3>"

✅ SUCCESS CRITERIA for each run:
  • Exit code 0 (no exceptions)
  • Output contains "Successfully Ingested!"
  • No NameError (would indicate INFRA-03 not applied)
  • No path error like "No such file or directory" (would indicate INFRA-01/02 not applied)
  • Images and entity buffer files created in ~/.hermes/omonigraph-vault/

Expected warnings (not failures):
  • Image download rate < 50%: Warning printed (feature, not bug)
  • Apify token missing: Falls back to CDP silently (expected)

Failure indicators (STOP and report):
  • NameError when importing json or kg_synthesize
  • Path error containing "/home/sztimhdd/"
  • Exception/traceback thrown


STEP 2: RUN ENTITY CANONICALIZATION BATCH (GATE6-02)
───────────────────────────────────────────────────────────────────────────────
What this validates:
  • GATE6-02: Cognee batch processor creates canonical_map.json successfully
  • Entity disambiguation ran across all 3 ingested articles

Batch processor flow:
  1. Polls entity_buffer/ for new *_entities.json files (created by Step 1)
  2. For each file: disambiguates entity names (LightRAG vs Cognee vs embeddings)
  3. Builds canonical mapping: canonical_name → [variants, aliases]
  4. Writes canonical_map.json atomically (write to .tmp, then rename for safety)
  5. Marks processed files with .processed marker (idempotency — safe to re-run)

Instructions:
  python cognee_batch_processor.py

✅ SUCCESS CRITERIA:
  • Exit code 0
  • File created: ~/.hermes/omonigraph-vault/canonical_map.json
  • File is valid JSON (can be parsed without error)
  • File size > 100 bytes (contains actual entity mappings, not empty)

Failure indicators:
  • canonical_map.json not created
  • JSON parse error when reading file
  • Exception in process


STEP 3: CROSS-ARTICLE SYNTHESIS QUERY (GATE6-03)
───────────────────────────────────────────────────────────────────────────────
What this validates:
  • GATE6-03: kg_synthesize.py produces substantive, multi-source synthesis
  • Pipeline correctly loads canonical_map.json (INFRA-03 fix working)
  • LightRAG hybrid mode retrieves relevant entities from all 3 articles
  • Cognee context recall works (memory layer functional)

Synthesis pipeline flow:
  1. Loads canonical_map.json to normalize entity names in query
  2. Queries LightRAG with mode="hybrid" for retrieval-augmented generation
  3. Calls cognee_wrapper.recall_previous_context() to load past synthesis memory
  4. Combines LightRAG results + Cognee context into Gemini prompt
  5. Generates markdown synthesis response
  6. Calls cognee_wrapper.remember_synthesis() to store response in memory
  7. Writes to ~/.hermes/omonigraph-vault/synthesis_output.md

Instructions:
  python kg_synthesize.py "What are the key concepts and tools discussed across the articles I ingested?" hybrid

✅ SUCCESS CRITERIA:
  • Exit code 0
  • Output is substantive (≥200 words) — not generic or "I don't know"
  • Output references SPECIFIC ENTITIES/CONCEPTS from AT LEAST 2 of the 3 articles
    Good examples:
      "Article 1 discusses LightRAG for entity extraction, while Article 2 covers Cognee's disambiguation..."
      "Both the WeChat article on AI agents and the tutorial on LLMs mention context window optimization..."
    NOT OK:
      Generic "There are many tools for knowledge graphs..."
      References only 1 article
      No specific entity names from articles
  • Output is markdown formatted with clear headers/structure
  • File ~/.hermes/omonigraph-vault/synthesis_output.md exists with output

Failure indicators:
  • NameError when importing json (INFRA-03 not applied)
  • Path error like "/home/sztimhdd/" (INFRA-01/02 not applied)
  • Output references 0 or 1 articles only (synthesis not using LightRAG results)
  • Output is generic with no entity names from articles


STEP 4: VERIFY NO HARDCODED PATHS (AUTOMATED ✓)
───────────────────────────────────────────────────────────────────────────────
What this validates:
  • INFRA-02: All hardcoded /home/sztimhdd/ paths removed
  • (Already verified in this session — included for completeness)

Command to verify:
  grep -rn "/home/sztimhdd/" --include="*.py" .

✅ SUCCESS CRITERIA:
  • Command returns 0 matches (no output)
  • Exit code 0


═══════════════════════════════════════════════════════════════════════════════
HOW TO SUBMIT RESULTS
═══════════════════════════════════════════════════════════════════════════════

After completing all 4 steps, reply to the checkpoint with:

---

User completed Task 3 manual steps:

✓ Step 1: 3 articles ingested successfully
  - Article 1: [title/topic, e.g. "LightRAG Architecture Tutorial"]
  - Article 2: [title/topic, e.g. "Building AI Agents with Cognee"]
  - Article 3: [title/topic, e.g. "LLM Best Practices for Knowledge Graphs"]

✓ Step 2: canonical_map.json created
  Location: ~/.hermes/omonigraph-vault/canonical_map.json
  Size: [e.g. "2543 bytes"]

✓ Step 3: Cross-article synthesis references multi-article context
  Entities mentioned: [list specific concepts/entities from 2+ articles, e.g. "LightRAG (Article 1), Cognee (Article 2), entity linking (both articles)"]
  Output length: [e.g. "287 words"]
  Location: ~/.hermes/omonigraph-vault/synthesis_output.md

✓ Step 4: No hardcoded paths found
  grep result: 0 matches

---

This tells Claude exactly what passed and allows them to finalize Phase 1.


═══════════════════════════════════════════════════════════════════════════════
TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

Q: Step 1 fails with "No such file or directory ~/.hermes/omonigraph-vault/"
A: Create it:
   mkdir -p ~/.hermes/omonigraph-vault/{entity_buffer,images,lightrag_storage}

Q: Step 1 fails with "GEMINI_API_KEY not set"
A: Set it in ~/.hermes/.env:
   GEMINI_API_KEY=<your-api-key>

Q: Step 2 produces empty canonical_map.json
A: Step 1 may not have completed. Check entity_buffer:
   ls -la ~/.hermes/omonigraph-vault/entity_buffer/
   If empty, fix Step 1 first.

Q: Step 3 output references only 1 article or is generic
A: LightRAG retrieval not pulling from all 3 articles.
   Verify Step 1 ingested all 3 (should see "Successfully Ingested!" 3 times).
   Try more specific query: "Compare entity handling approaches across the articles."

Q: Step 3 NameError on json.load()
A: INFRA-03 fix not applied. Check:
   grep "import json" kg_synthesize.py
   Should show import statement. If missing, INFRA-03 fix incomplete.

Q: Step 4 finds hardcoded paths
A: Should not happen if Wave 1 completed. Contact Claude to debug.


═══════════════════════════════════════════════════════════════════════════════
ENVIRONMENT SETUP (if needed)
═══════════════════════════════════════════════════════════════════════════════

Activate venv and install dependencies:

Windows (PowerShell/CMD):
  python -m venv venv
  venv\Scripts\activate
  pip install -r requirements.txt

macOS/Linux (Bash):
  python -m venv venv
  source venv/bin/activate
  pip install -r requirements.txt

Verify:
  python -c "import lightrag; print('LightRAG OK')"
  python -c "import cognee; print('Cognee OK')"

Start image server (required for rendering, port 8765):
  cd ~/.hermes/omonigraph-vault
  python -m http.server 8765 --directory images &


═══════════════════════════════════════════════════════════════════════════════
GATE 6 REQUIREMENTS TRACEABILITY
═══════════════════════════════════════════════════════════════════════════════

Phase requirements (from REQUIREMENTS.md):

  GATE6-01: Pipeline handles real WeChat article ingestion
            → Validated by Step 1 ✓

  GATE6-02: Entity canonicalization batch produces canonical_map.json
            → Validated by Step 2 ✓

  GATE6-03: Cross-article synthesis uses multi-article context
            → Validated by Step 3 ✓

  GATE6-04: ingest_wechat.py works with fixed paths (no hardcoded strings)
            → Validated by Step 1 ✓

  GATE6-05: skill_runner.py LLM routing tests pass (9/9)
            → Already verified ✓ (Task 2 complete)

All 5 gates must pass to unlock Phase 2.

═══════════════════════════════════════════════════════════════════════════════
